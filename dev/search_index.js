var documenterSearchIndex = {"docs":
[{"category":"section","location":"guide/embeddings/#Embeddings","page":"Embeddings","text":"UniLM.jl supports text embeddings across multiple providers. The default is OpenAI's text-embedding-3-small (1536 dimensions), but you can target Ollama, Gemini, Mistral, or any OpenAI-compatible server via the service parameter.","title":"Embeddings"},{"category":"section","location":"guide/embeddings/#Basic-Usage","page":"Embeddings","text":"using UniLM\n\n# Single text\nemb = Embeddings(\"Julia is a high-performance programming language\")\nprintln(\"Model: \", emb.model)\nprintln(\"Embedding dimensions: \", length(emb.embeddings))\nprintln(\"Pre-allocated (all zeros): \", all(x -> x == 0.0, emb.embeddings))\nModel: text-embedding-3-small\nEmbedding dimensions: 1536\nPre-allocated (all zeros): true\n\nAfter calling the API, the embeddings are filled in-place:\n\nemb = Embeddings(\"Julia is a high-performance programming language for technical computing.\")\nembeddingrequest!(emb)\nprintln(\"First 5 dimensions:\")\nfor v in emb.embeddings[1:5]\n    println(\"  \", round(v, digits=6))\nend\nprintln(\"L2 norm: \", round(sqrt(sum(x^2 for x in emb.embeddings)), digits=4))\nFirst 5 dimensions:\n  -0.039459\n  -0.009293\n  0.001677\n  -0.028122\n  0.063354\nL2 norm: 0.9997","title":"Basic Usage"},{"category":"section","location":"guide/embeddings/#Batch-Embeddings","page":"Embeddings","text":"Embed multiple texts in a single API call:\n\ntexts = [\n    \"Julia is fast\",\n    \"Python is popular\",\n    \"Rust is safe\"\n]\n\nemb = Embeddings(texts)\nprintln(\"Model: \", emb.model)\nprintln(\"Number of texts: \", length(emb.input))\nprintln(\"Embeddings per text: \", length(emb.embeddings[1]), \" dimensions\")\nModel: text-embedding-3-small\nNumber of texts: 3\nEmbeddings per text: 1536 dimensions\n\nembeddingrequest!(emb)\nprintln(\"Embedding dimensions per text: \", length(emb.embeddings[1]))\nEmbedding dimensions per text: 1536","title":"Batch Embeddings"},{"category":"section","location":"guide/embeddings/#Computing-Similarity","page":"Embeddings","text":"A common use case is computing cosine similarity between embeddings:\n\nusing LinearAlgebra\n\nemb = Embeddings([\"Julia\", \"Python\", \"Rust\", \"Fortran\"])\nembeddingrequest!(emb)\n\nsim = dot(emb.embeddings[1], emb.embeddings[4]) /\n      (norm(emb.embeddings[1]) * norm(emb.embeddings[4]))\nprintln(\"Cosine similarity (Julia vs Fortran): \", round(sim, digits=4))\nCosine similarity (Julia vs Fortran): 0.311","title":"Computing Similarity"},{"category":"section","location":"guide/embeddings/#Available-Models","page":"Embeddings","text":"Model Dimensions Notes\ntext-embedding-3-small 1536 Default, good balance\ntext-embedding-3-large 3072 Higher quality, more dimensions\n\nnote: Note\nThe buffers start at something(dimensions, 1536) zeros per input and are resized to whatever length the model returns, so text-embedding-3-large (3072 dimensions) needs no adjustment.","title":"Available Models"},{"category":"section","location":"guide/embeddings/#Using-Other-Providers","page":"Embeddings","text":"Pass service and model to embed with a different backend:\n\n# Ollama (local)\nemb = Embeddings(\"test\"; service=OllamaEndpoint(), model=\"nomic-embed-text\")\nembeddingrequest!(emb)\n\n# Gemini (OpenAI-compatible endpoint; the native generateContent API has no embeddings)\nemb = Embeddings(\"test\"; service=GEMINIOpenAIServiceEndpoint, model=\"gemini-embedding-001\")\nembeddingrequest!(emb)\n\nnote: Note\nDifferent providers return different embedding dimensions. The buffers are pre-allocated for 1536 (OpenAI's text-embedding-3-small) and resized to the length the provider returns.\n\nnote: Gemini embedding models\ngemini-embedding-001, the default for GEMINIOpenAIServiceEndpoint, shuts down on May 14, 2028. Its successor gemini-embedding-2 is generally available and works through the same OpenAI-compatible endpoint: Embeddings(\"test\"; service=GEMINIOpenAIServiceEndpoint, model=\"gemini-embedding-2\"). The OpenAI-compatible Gemini embeddings endpoint returns no usage, so estimated_cost on its results is 0.0; the gemini-embedding-2 price row applies only to results that carry usage.","title":"Using Other Providers"},{"category":"section","location":"guide/embeddings/#In-Place-Design","page":"Embeddings","text":"The Embeddings struct pre-allocates the embedding vectors at construction time. embeddingrequest! fills them in-place — no allocation on the hot path when the model returns the pre-allocated length. This is idiomatic Julia for performance-sensitive workloads. The result aliases the request (result.embeddings === emb), so a second call on the same Embeddings overwrites the first one's vectors: use one Embeddings per concurrent call, and copy vectors you keep before reusing a request.\n\nemb = Embeddings(\"test\")\nprintln(\"Pre-allocated length: \", length(emb.embeddings))\nprintln(\"All zeros before API call: \", all(x -> x == 0.0, emb.embeddings))\nPre-allocated length: 1536\nAll zeros before API call: true","title":"In-Place Design"},{"category":"section","location":"guide/embeddings/#Retry-Behaviour","page":"Embeddings","text":"embeddingrequest! returns an EmbeddingSuccess/EmbeddingFailure/EmbeddingCallError and fills emb.embeddings in place (use embedding_vectors(result) for the vectors). It automatically retries transient HTTP statuses (408, 429, 500, 502, 503, 504, 529) with exponential backoff and jitter — a Retry-After header is a floor under the jittered wait — bounded by the resolved RequestConfig (max_attempts, default 3; total_deadline, default 900 s). Pass config=RequestConfig(max_attempts=1) to disable retries; timeouts surface as EmbeddingCallError with the UniLMTimeout in .cause, and a cancelled call (cancel=, or an ambient with_cancel token) with a UniLMCancelled there. embedding_vectors throws LLMResultError on a failure result. encoding_format accepts only \"float\" (the vectors are stored as Float64); anything else throws ArgumentError at construction.","title":"Retry Behaviour"},{"category":"section","location":"guide/embeddings/#API-Reference","page":"Embeddings","text":"See the Embeddings API page for full type documentation.","title":"API Reference"},{"category":"section","location":"api/results/#Result-Types","page":"Result Types","text":"Abstract and concrete types for handling API call outcomes. All result types are subtypes of LLMRequestResponse.","title":"Result Types"},{"category":"section","location":"api/results/#Abstract-Type","page":"Result Types","text":"","title":"Abstract Type"},{"category":"section","location":"api/results/#Chat-Completions-Results","page":"Result Types","text":"","title":"Chat Completions Results"},{"category":"section","location":"api/results/#Pattern-Matching","page":"Result Types","text":"result = chatrequest!(chat)\n\nif result isa LLMSuccess\n    println(result.message.content)\n    println(result.message.finish_reason)  # \"stop\"\nelseif result isa LLMFailure\n    @warn \"HTTP $(result.status): $(result.response)\"\nelseif result isa LLMCallError\n    @error \"Call error: $(result.error)\"\nend","title":"Pattern Matching"},{"category":"section","location":"api/results/#Result-Helpers","page":"Result Types","text":"Predicates and an accessor for consuming any result — success or failure:\n\ntext returns the assistant reply on a LLMSuccess (possibly nothing for a tool-calls-only turn) and throws an LLMResultError on a failure. Guard with issuccess / isfailure, or pattern-match the result type first:\n\nresult = chatrequest!(chat)\nif issuccess(result)\n    println(text(result))\nelse\n    @warn \"call did not succeed\"\nend","title":"Result Helpers"},{"category":"section","location":"api/results/#Responses-API-Results","page":"Result Types","text":"See also the Responses API reference.\n\nResponseSuccess\nResponseFailure\nResponseCallError","title":"Responses API Results"},{"category":"section","location":"api/results/#Pattern-Matching-2","page":"Result Types","text":"result = respond(\"Tell me a joke\")\n\nif result isa ResponseSuccess\n    println(output_text(result))\n    println(result.response.status)  # \"completed\"\nelseif result isa ResponseFailure\n    @warn \"HTTP $(result.status)\"\nelseif result isa ResponseCallError\n    @error result.error\nend","title":"Pattern Matching"},{"category":"section","location":"api/results/#Image-Generation-Results","page":"Result Types","text":"See also the Images API reference.\n\nImageSuccess\nImageFailure\nImageCallError","title":"Image Generation Results"},{"category":"section","location":"api/results/#Pattern-Matching-3","page":"Result Types","text":"result = generate_image(\"A robot writing Julia code\")\n\nif result isa ImageSuccess\n    save_image(image_data(result)[1], \"robot.png\")\nelseif result isa ImageFailure\n    @warn \"HTTP $(result.status): $(result.response)\"\nelseif result isa ImageCallError\n    @error result.error\nend","title":"Pattern Matching"},{"category":"section","location":"api/results/#Type-Hierarchy","page":"Result Types","text":"All result types share the abstract parent LLMRequestResponse:\n\nLLMRequestResponse   (abstract parent of every result type below)\n│\n├─ Chat Completions   LLMSuccess · LLMFailure · LLMCallError\n├─ Responses API      ResponseSuccess · ResponseFailure · ResponseCallError\n├─ Embeddings         EmbeddingSuccess · EmbeddingFailure · EmbeddingCallError\n├─ Image Generation   ImageSuccess · ImageFailure · ImageCallError\n├─ FIM Completion     FIMSuccess · FIMFailure · FIMCallError\n├─ Files              FileSuccess · FileListSuccess · FileContentSuccess · FileDeleteSuccess · FileFailure · FileCallError\n├─ Vector Stores      VectorStoreSuccess · VectorStoreListSuccess · VectorStoreFileSuccess · VectorStoreBatchSuccess · VectorStoreDeleteSuccess · VectorStoreFailure · VectorStoreCallError\n├─ Conversations      ConversationSuccess · ConversationItemSuccess · ConversationItemListSuccess · ConversationDeleteSuccess · ConversationFailure · ConversationCallError\n├─ Moderations        ModerationSuccess · ModerationFailure · ModerationCallError\n├─ Audio              SpeechSuccess · TranscriptionSuccess · AudioFailure · AudioCallError\n├─ Batch              BatchSuccess · BatchListSuccess · BatchFailure · BatchCallError\n├─ Fine-tuning        FineTuningSuccess · FineTuningListSuccess · FineTuningFailure · FineTuningCallError\n├─ Containers         ContainerSuccess · ContainerListSuccess · ContainerDeleteSuccess · ContainerFailure · ContainerCallError\n├─ Uploads            UploadSuccess · UploadPartSuccess · UploadFailure · UploadCallError\n├─ Realtime           RealtimeSecretSuccess · RealtimeFailure · RealtimeCallError\n└─ System One         SystemOneSuccess · TypeSafeModelsSuccess · SystemOneFailure · SystemOneCallError\n\nEvery *Success wraps a parsed response object; every *Failure carries the HTTP status and body; every *CallError carries the rendered error text and, in cause, the exception behind it when there is one — a UniLMTimeout for a timeout, a UniLMCancelled for a cancellation. Pattern-match on the family you called (see each API-reference page for the concrete fields).","title":"Type Hierarchy"},{"category":"type","location":"api/results/#UniLM.LLMRequestResponse","page":"Result Types","text":"LLMRequestResponse\n\nAbstract supertype for all API call results. Pattern-match on subtypes to handle outcomes:\n\nLLMSuccess — successful response\nLLMFailure — HTTP-level failure (non-200 status)\nLLMCallError — exception during the call (network error, etc.)\nResponseSuccess — successful Responses API result\nResponseFailure — Responses API HTTP failure\nResponseCallError — Responses API exception\n\n\n\n\n\n","title":"UniLM.LLMRequestResponse"},{"category":"type","location":"api/results/#UniLM.LLMSuccess","page":"Result Types","text":"LLMSuccess(; message, self, usage=nothing, sse_dropped=0)\n\nSuccessful Chat Completions API response.\n\nFields\n\nmessage::Message: The assistant's reply message.\nself::Chat: The updated Chat object (with the new message appended if history=true).\nusage::Union{TokenUsage, Nothing}: Token usage statistics from the API.\nsse_dropped::Int: Undecodable SSE data: payloads dropped while assembling this streamed turn — 0 for a non-streamed call, and for a clean stream. Non-zero means the turn was built from an incomplete wire.\n\n\n\n\n\n","title":"UniLM.LLMSuccess"},{"category":"type","location":"api/results/#UniLM.LLMFailure","page":"Result Types","text":"LLMFailure(; response, status, self, request_id=nothing, sse_dropped=0)\n\nHTTP-level failure from the Chat Completions API. The server returned a non-200 status.\n\nFields\n\nresponse::String: The raw response body.\nstatus::Int: The HTTP status code.\nself::Chat: The Chat object (unchanged).\nrequest_id::Union{String, Nothing}: The HTTP request ID from headers, if available.\nsse_dropped::Int: Undecodable SSE data: payloads dropped during a streamed attempt — 0 for a non-streamed call. On a truncated stream (HTTP 200, no terminal event) this is often the reason no message could be built.\n\n\n\n\n\n","title":"UniLM.LLMFailure"},{"category":"type","location":"api/results/#UniLM.LLMCallError","page":"Result Types","text":"LLMCallError(; error, status=nothing, self, request_id=nothing, cause=nothing)\n\nException-level error during a Chat Completions API call (network failure, JSON parse error, timeout, etc.).\n\nFields\n\nerror::String: The stringified exception.\nstatus::Union{Int,Nothing}: HTTP status if available (nothing for timeouts — no fabricated statuses).\nself::Chat: The Chat object (unchanged).\nrequest_id::Union{String, Nothing}: The HTTP request ID from headers, if available.\ncause::Union{Nothing,Exception}: The underlying typed exception when one exists (e.g. a UniLMTimeout carrying phase/elapsed/limit).\n\n\n\n\n\n","title":"UniLM.LLMCallError"},{"category":"function","location":"api/results/#UniLM.issuccess","page":"Result Types","text":"issuccess(r::LLMRequestResponse) -> Bool\n\ntrue when r is a success result (any *Success type), false for every failure or call-error result. The generic method returns false; each concrete *Success result type gets its own true method (registered once every result type across the APIs is defined — see the bottom of UniLM.jl).\n\nresult = chatrequest!(chat)\nissuccess(result) ? println(text(result)) : @warn \"call did not succeed\"\n\n\n\n\n\n","title":"UniLM.issuccess"},{"category":"function","location":"api/results/#UniLM.isfailure","page":"Result Types","text":"isfailure(r::LLMRequestResponse) -> Bool\n\nNegation of issuccess: true for any failure or call-error result.\n\n\n\n\n\n","title":"UniLM.isfailure"},{"category":"function","location":"api/results/#UniLM.text","page":"Result Types","text":"text(r::LLMSuccess) -> Union{String,Nothing}\n\nThe assistant reply text — r.message.content. Can be nothing when the reply carries only tool calls (no text). On a LLMFailure or LLMCallError, text throws an LLMResultError; guard with issuccess / isfailure, or pattern-match the result type first.\n\n\n\n\n\n","title":"UniLM.text"},{"category":"type","location":"api/results/#UniLM.LLMResultError","page":"Result Types","text":"LLMResultError <: Exception\n\nThrown by the result accessors — text, output_text, embedding_vectors, image_data and fim_text — when they are called on a non-success result. Carries the offending result. showerror prints only the status and a short (≤200-char) excerpt of the response body or error message — never the conversation, the service endpoint, or the API key.\n\n\n\n\n\n","title":"UniLM.LLMResultError"},{"category":"section","location":"api/fine_tuning/#fine_tuning_api","page":"Fine-tuning","text":"OpenAI restricts new jobs to eligible existing customers. Job creation ends January 6, 2027; inference remains available until the base model retires. See OpenAI's availability schedule.\n\nCreate, retrieve, cancel, and list fine-tuning jobs, and list a job's events and checkpoints. Training and validation data is uploaded via the Files API with purpose=\"fine-tune\". OpenAI only.","title":"Fine-tuning API"},{"category":"section","location":"api/fine_tuning/#Objects","page":"Fine-tuning","text":"","title":"Objects"},{"category":"section","location":"api/fine_tuning/#Result-Types","page":"Fine-tuning","text":"","title":"Result Types"},{"category":"section","location":"api/fine_tuning/#Request-Functions","page":"Fine-tuning","text":"","title":"Request Functions"},{"category":"section","location":"api/fine_tuning/#Usage","page":"Fine-tuning","text":"# Upload training data (purpose=\"fine-tune\"), then create a job\ntrain = upload_file(\"train.jsonl\", \"fine-tune\")\njob = create_fine_tuning_job(model=\"gpt-4o-mini\", training_file=\"file-abc123\")\njob isa FineTuningSuccess && println(\"Job: \", job.response.id)\n\n# Retrieve status, list events / checkpoints, list all jobs, cancel\nretrieve_fine_tuning_job(\"ftjob-abc123\")\nlist_fine_tuning_events(\"ftjob-abc123\")\nlist_fine_tuning_checkpoints(\"ftjob-abc123\")\nlist_fine_tuning_jobs(limit=10)\ncancel_fine_tuning_job(\"ftjob-abc123\")","title":"Usage"},{"category":"type","location":"api/fine_tuning/#UniLM.FineTuningJob","page":"Fine-tuning","text":"FineTuningJob\n\nA fine-tuning job: id, status, model, and fine_tuned_model (the resulting model name once training completes); raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.FineTuningJob"},{"category":"type","location":"api/fine_tuning/#UniLM.FineTuningList","page":"Fine-tuning","text":"FineTuningList\n\nA page of records from list_fine_tuning_jobs, list_fine_tuning_events, or list_fine_tuning_checkpoints; data holds the raw JSON entries and has_more signals that further pages are available.\n\n\n\n\n\n","title":"UniLM.FineTuningList"},{"category":"type","location":"api/fine_tuning/#UniLM.FineTuningSuccess","page":"Fine-tuning","text":"Successful create/retrieve/cancel result wrapping a FineTuningJob.\n\n\n\n\n\n","title":"UniLM.FineTuningSuccess"},{"category":"type","location":"api/fine_tuning/#UniLM.FineTuningListSuccess","page":"Fine-tuning","text":"Successful list result wrapping a FineTuningList.\n\n\n\n\n\n","title":"UniLM.FineTuningListSuccess"},{"category":"type","location":"api/fine_tuning/#UniLM.FineTuningFailure","page":"Fine-tuning","text":"Fine-tuning API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.FineTuningFailure"},{"category":"type","location":"api/fine_tuning/#UniLM.FineTuningCallError","page":"Fine-tuning","text":"Fine-tuning API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout.\n\n\n\n\n\n","title":"UniLM.FineTuningCallError"},{"category":"function","location":"api/fine_tuning/#UniLM.create_fine_tuning_job","page":"Fine-tuning","text":"create_fine_tuning_job(; model, training_file, validation_file=nothing, method=nothing,\n                       suffix=nothing, metadata=nothing, service=OPENAIServiceEndpoint)\n\nCreate a fine-tuning job. training_file is a file id from upload_file(path, \"fine-tune\"). method is e.g. Dict(\"type\"=>\"supervised\", \"supervised\"=>Dict(\"hyperparameters\"=>...)).\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.create_fine_tuning_job"},{"category":"function","location":"api/fine_tuning/#UniLM.retrieve_fine_tuning_job","page":"Fine-tuning","text":"retrieve_fine_tuning_job(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.retrieve_fine_tuning_job"},{"category":"function","location":"api/fine_tuning/#UniLM.cancel_fine_tuning_job","page":"Fine-tuning","text":"cancel_fine_tuning_job(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.cancel_fine_tuning_job"},{"category":"function","location":"api/fine_tuning/#UniLM.list_fine_tuning_jobs","page":"Fine-tuning","text":"list_fine_tuning_jobs(; limit=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_fine_tuning_jobs"},{"category":"function","location":"api/fine_tuning/#UniLM.list_fine_tuning_events","page":"Fine-tuning","text":"list_fine_tuning_events(id; limit=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nList a job's events, a page at a time: limit sets the page size and after the last event id of the previous page (see has_more on the result).\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_fine_tuning_events"},{"category":"function","location":"api/fine_tuning/#UniLM.list_fine_tuning_checkpoints","page":"Fine-tuning","text":"list_fine_tuning_checkpoints(id; limit=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nList a job's checkpoints, a page at a time: limit sets the page size and after the last checkpoint id of the previous page (see has_more on the result).\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_fine_tuning_checkpoints"},{"category":"section","location":"guide/mcp/#mcp_guide","page":"MCP (Model Context Protocol)","text":"UniLM.jl provides native MCP support — both as a client (connect to MCP servers) and a server (build your own). MCP tools integrate seamlessly with tool_loop! and tool_loop via the CallableTool bridge.\n\nProtocol: JSON-RPC 2.0 over stdio or Streamable HTTP. The client and the server negotiate MCP revisions 2025-11-25 (preferred), 2025-06-18 and 2025-03-26. The stateless 2026-07-28 revision is not supported yet, so a peer that speaks only 2026-07-28 cannot interoperate: mcp_connect fails naming the revision such a server returned, serve answers such a client's initialize with 2025-11-25, and over HTTP a request whose MCP-Protocol-Version header names an unsupported revision gets 400. Zero external dependencies.\n\n","title":"Model Context Protocol (MCP)"},{"category":"section","location":"guide/mcp/#MCP-Client","page":"MCP (Model Context Protocol)","text":"","title":"MCP Client"},{"category":"section","location":"guide/mcp/#Transports","page":"MCP (Model Context Protocol)","text":"Two transport types are available:\n\nStdioTransport — launches a subprocess, communicates via stdin/stdout (newline-delimited JSON-RPC)\nHTTPTransport — communicates via POST requests with Mcp-Session-Id session management\n\n# Transport types are constructed but not connected until mcp_connect\nt1 = StdioTransport(`echo hello`)\nprintln(\"Stdio transport for: \", t1.command)\n\nt2 = HTTPTransport(\"https://mcp.example.com/mcp\";\n    headers=[\"Authorization\" => \"Bearer token\"])\nprintln(\"HTTP transport for: \", t2.url)\nStdio transport for: `echo hello`\nHTTP transport for: https://mcp.example.com/mcp","title":"Transports"},{"category":"section","location":"guide/mcp/#Connecting-to-an-MCP-Server","page":"MCP (Model Context Protocol)","text":"Use mcp_connect with a Cmd (stdio), URL string (HTTP), or transport object:\n\n# Stdio — launches subprocess (stderr=devnull or a log-file path silences its stderr)\nsession = mcp_connect(`npx -y @modelcontextprotocol/server-filesystem /tmp`)\n\n# HTTP\nsession = mcp_connect(\"https://mcp.example.com/mcp\";\n    headers=[\"Authorization\" => \"Bearer token\"])\n\n# Custom transport\nsession = mcp_connect(StdioTransport(`my-server`))\n\nThe do-block form automatically disconnects when done:\n\nmcp_connect(`npx server`) do session\n    tools = mcp_tools(session)\n    # ... use tools ...\nend  # session is disconnected here\n\nnote: A session runs one call at a time, first come first served\nEvery call on an MCPSession — its liveness check, id allocation and request/response exchange — holds the session, and waiting callers are served in arrival order. A call's per-call bound (timeout, default mcp_request_timeout) also covers its wait: a call that cannot acquire the session in time throws MCPTimeoutError with phase :queue without touching it. Once a call holds the session, its exchange gets the full bound, measured from that moment, so a waiter's clock never tears down the exchange in progress. mcp_disconnect! waits its turn too, so a disconnect racing a call in flight waits for that exchange to finish instead of tearing the transport down under its reader. For real parallelism, open one session per concurrent worker — see Concurrency, Tasks and Cancellation.\n\nA call on a session that is closed — by mcp_disconnect!, by a stdio timeout or crash, or because its transport is not connected — throws the typed MCPSessionClosedError, whose cause is :disconnected, :timeout or :crash (see Timeouts for auto_respawn).","title":"Connecting to an MCP Server"},{"category":"section","location":"guide/mcp/#Discovering-Tools,-Resources,-and-Prompts","page":"MCP (Model Context Protocol)","text":"After connecting, the session auto-populates tool/resource/prompt caches. You can also refresh them manually:\n\ntools    = list_tools!(session)     # -> Vector{MCPToolInfo}\nresources = list_resources!(session) # -> Vector{MCPResourceInfo}\nprompts  = list_prompts!(session)   # -> Vector{MCPPromptInfo}\n\nnote: Server notifications\nMCP servers may send notifications interleaved with responses; the client skips them transparently, answers server ping requests, and answers any other server request with -32601. It reads the server's frames only during an exchange — a stdio notification sent between calls is read with the next call, and over HTTP there is no standing listener, so only notifications carried in a response body (before or after the response itself) are seen. When a notifications/tools/list_changed is read, the session's cached tool list is marked stale — check session.tools_stale and call list_tools! to refresh. A stdio line that is not JSON (a stray log line on the server's stdout) is skipped with a warning.\n\n# MCPToolInfo fields\ninfo = MCPToolInfo(Dict{String,Any}(\n    \"name\" => \"read_file\",\n    \"description\" => \"Read a file from disk\",\n    \"inputSchema\" => Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"path\" => Dict{String,Any}(\"type\" => \"string\", \"description\" => \"File path\")\n        ),\n        \"required\" => [\"path\"]\n    )\n))\nprintln(\"Tool: \", info.name)\nprintln(\"Description: \", info.description)\nprintln(\"Schema: \", JSON.json(info.input_schema, 2))\nTool: read_file\nDescription: Read a file from disk\nSchema: {\n  \"properties\": {\n    \"path\": {\n      \"description\": \"File path\",\n      \"type\": \"string\"\n    }\n  },\n  \"required\": [\n    \"path\"\n  ],\n  \"type\": \"object\"\n}","title":"Discovering Tools, Resources, and Prompts"},{"category":"section","location":"guide/mcp/#Calling-Tools-Directly","page":"MCP (Model Context Protocol)","text":"result = call_tool(session, \"read_file\", Dict{String,Any}(\"path\" => \"/tmp/data.txt\"))\ncontent = read_resource(session, \"config://app\")\nmessages = get_prompt(session, \"review\", Dict{String,Any}(\"code\" => \"x + 1\"))\nping(session)","title":"Calling Tools Directly"},{"category":"section","location":"guide/mcp/#mcp_timeouts","page":"MCP (Model Context Protocol)","text":"Every mcp_connect handshake and every call_tool / list_tools! exchange runs under a bound — set per session at connect time, or overridden for one call:\n\nsession = mcp_connect(`npx server`;\n    config=RequestConfig(current_config(); mcp_connect_timeout=10.0, mcp_request_timeout=30.0),\n    auto_respawn=true)\n\ncall_tool(session, \"read_file\", Dict{String,Any}(\"path\" => \"/tmp/data.txt\"); timeout=5.0)\n\nA stdio request timeout closes the session: a read blocked on an unresponsive server can be released only by killing the server. The watchdog closes the pipe, so the typed MCPTimeoutError surfaces at about the bound, and then tears the server's process group down (stdin EOF, SIGTERM, then SIGKILL); a reply that raced the teardown is discarded. With auto_respawn=true the next call transparently respawns the server (same command, fresh handshake; in-memory server state is lost and tools are refetched); without it, the next call raises MCPSessionClosedError naming the opt-in. An HTTP request timeout does not close the session — each exchange is an independent POST — and the timed-out request is cancelled on the server with a best-effort notifications/cancelled.\n\nauto_respawn covers hangs and crashes alike: a server that dies abruptly — killed, crashed, or its stdio pipe broken — closes the session too, surfacing the typed MCPCrashError (carrying the exit code or signal when known) on the call in flight, and the next call respawns it when auto_respawn=true. Stdio servers still running when Julia exits are torn down by an exit hook, and when a server's process-group leader exits (an npx wrapper, say) the rest of its group is killed at once.\n\nA CancelToken reaches MCP calls over HTTP: a cancel ends the call at once with UniLMCancelled, sends a best-effort notifications/cancelled, and leaves the session open. A stdio exchange does not observe the token; it is bounded by mcp_request_timeout alone. Teardown — mcp_disconnect!'s DELETE, a cancellation notice — is sent even inside a cancelled with_cancel scope.","title":"Timeouts"},{"category":"section","location":"guide/mcp/#Bridging-to-tool_loop!-(Chat-Completions)","page":"MCP (Model Context Protocol)","text":"mcp_tools converts MCP tools into Vector{CallableTool{Tool}} for use with tool_loop!. Tool names are advertised provider-safe: OpenAI and Anthropic accept only ^[a-zA-Z0-9_-]{1,128}$, so any other character of an MCP tool name (the dots in admin.tools.list, say) becomes _, cut to 128 characters — the bridged callable still calls the tool by its MCP name. Two tools that map to the same name raise an ArgumentError.\n\nsession = mcp_connect(`npx server`)\ntools = mcp_tools(session)\n\nchat = Chat(model=\"gpt-5.2\", tools=tools)\npush!(chat, Message(Val(:system), \"You are a helpful assistant.\"))\npush!(chat, Message(Val(:user), \"List files in /tmp\"))\nresult = tool_loop!(chat; tools)\n\nmcp_disconnect!(session)","title":"Bridging to tool_loop! (Chat Completions)"},{"category":"section","location":"guide/mcp/#Bridging-to-tool_loop-(Responses-API)","page":"MCP (Model Context Protocol)","text":"mcp_tools_respond converts MCP tools into Vector{CallableTool{FunctionTool}} for use with tool_loop:\n\nsession = mcp_connect(\"https://mcp.example.com/mcp\")\ntools = mcp_tools_respond(session)\nresult = tool_loop(\"List files in /tmp\"; tools=tools)\nmcp_disconnect!(session)\n\n","title":"Bridging to tool_loop (Responses API)"},{"category":"section","location":"guide/mcp/#MCP-Server","page":"MCP (Model Context Protocol)","text":"","title":"MCP Server"},{"category":"section","location":"guide/mcp/#Creating-a-Server","page":"MCP (Model Context Protocol)","text":"server = MCPServer(\"calc\", \"1.0.0\"; description=\"A calculator server\")\nprintln(\"Server: \", server.name, \" v\", server.version)\nServer: calc v1.0.0","title":"Creating a Server"},{"category":"section","location":"guide/mcp/#Registering-Tools","page":"MCP (Model Context Protocol)","text":"Register tools with explicit JSON Schema or auto-inferred schema:\n\n# Explicit schema\nregister_tool!(server, \"add\", \"Add two numbers\",\n    Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"a\" => Dict{String,Any}(\"type\" => \"number\"),\n            \"b\" => Dict{String,Any}(\"type\" => \"number\")\n        ),\n        \"required\" => [\"a\", \"b\"]\n    ),\n    args -> string(args[\"a\"] + args[\"b\"]))\n\nprintln(\"Registered tools: \", collect(keys(server.tools)))\nRegistered tools: [\"add\"]\n\nWith the inferred form, the handler's positional parameters are the schema: each tools/call binds the arguments object to them by name. A parameter whose type admits nothing (Union{T,Nothing}) is optional; every other one is required, and a value must have its parameter's JSON type (a string for String, an integral number for an integer, a number for a float, a boolean for Bool; a Symbol binds from a string, and a typed Vector or Dict element-wise). A call that violates this never reaches the handler: the client gets a tool result with isError: true naming the argument, so the model can correct its call. A handler that takes one Dict (the explicit-schema convention) or varargs has no names to bind and is rejected with an ArgumentError.\n\n# Auto-inferred schema from function signature: `name` is a required string\nregister_tool!(server, \"greet\", \"Greet someone\", (name::String) -> \"Hello, $name!\")\n\nprintln(\"Tools now: \", collect(keys(server.tools)))\nprintln(\"greet schema: \", JSON.json(server.tools[\"greet\"].input_schema))\nTools now: [\"add\", \"greet\"]\ngreet schema: {\"properties\":{\"name\":{\"type\":\"string\"}},\"required\":[\"name\"],\"type\":\"object\"}\n\nYou can also register existing CallableTool instances:\n\n# From Chat Completions tool\nregister_tool!(server, my_callable_gpt_tool)\n\n# From Responses API tool\nregister_tool!(server, my_callable_function_tool)","title":"Registering Tools"},{"category":"section","location":"guide/mcp/#Registering-Resources","page":"MCP (Model Context Protocol)","text":"# Static resource\nregister_resource!(server, \"config://app\", \"App Config\",\n    () -> \"{\\\"debug\\\": true}\";\n    mime_type=\"application/json\",\n    description=\"Application configuration\")\n\nprintln(\"Resources: \", collect(keys(server.resources)))\nResources: [\"config://app\"]\n\n# URI-templated resource\nregister_resource_template!(server, \"file://{path}\", \"File Reader\",\n    (params::Dict{String,String}) -> \"Contents of $(params[\"path\"])\";\n    description=\"Read files by path\")\n\nprintln(\"Templates: \", length(server.resource_templates))\nTemplates: 1","title":"Registering Resources"},{"category":"section","location":"guide/mcp/#Registering-Prompts","page":"MCP (Model Context Protocol)","text":"register_prompt!(server, \"review\", (args::Dict{String,Any}) ->\n    [Dict{String,Any}(\"role\" => \"user\",\n        \"content\" => Dict{String,Any}(\"type\" => \"text\",\n            \"text\" => \"Review this code:\\n$(args[\"code\"])\"))];\n    description=\"Code review prompt\",\n    arguments=[Dict{String,Any}(\"name\" => \"code\", \"required\" => true)])\n\nprintln(\"Prompts: \", collect(keys(server.prompts)))\nPrompts: [\"review\"]","title":"Registering Prompts"},{"category":"section","location":"guide/mcp/#Macros","page":"MCP (Model Context Protocol)","text":"The @mcp_tool, @mcp_resource, and @mcp_prompt macros provide a more ergonomic registration API with automatic JSON Schema generation from Julia type annotations. The examples below are executed at doc-build time and call the registered handlers directly to prove the wiring works.","title":"Macros"},{"category":"section","location":"guide/mcp/#@mcp_tool-—-typed-args-become-JSON-Schema","page":"MCP (Model Context Protocol)","text":"A named typed function becomes a tool whose inputSchema is inferred from the argument types (typed args are required, untyped ones optional). The generated handler binds the incoming arguments object to your function's parameters by name, with the same type checks as the inferred-schema register_tool!; a missing or wrong-typed argument is answered with an isError: true tool result naming it. The tool is registered without a description.\n\nnote: Contract\n@mcp_tool requires a named function (function name(args…)). An anonymous function(x) … end now raises a clear error instead of registering a tool named after its first argument.\n\nsrv = MCPServer(\"calc\", \"1.0.0\")\n\n@mcp_tool srv function add(a::Float64, b::Float64)::String\n    string(a + b)\nend\n\n# Generated schema, inferred from the signature:\nprintln(\"schema: \", JSON.json(srv.tools[\"add\"].input_schema))\n# Call the registered handler with raw JSON-shaped args:\nprintln(\"add(2, 3) = \", srv.tools[\"add\"].handler(Dict{String,Any}(\"a\" => 2.0, \"b\" => 3.0)))\nschema: {\"properties\":{\"a\":{\"type\":\"number\"},\"b\":{\"type\":\"number\"}},\"required\":[\"a\",\"b\"],\"type\":\"object\"}\nadd(2, 3) = 5.0","title":"@mcp_tool — typed args become JSON Schema"},{"category":"section","location":"guide/mcp/#@mcp_resource-—-static-and-URI-templated","page":"MCP (Model Context Protocol)","text":"A function() registers a static resource; a URI with {param} placeholders registers a template whose handler arguments are bound from the matched path params.\n\nnote: Contract\nFor template resources, each handler argument name must match a URI {param} name — it is bound from the matched params (e.g. {id} ⇒ function(id::String)).\n\n@mcp_resource srv \"config://app\" function()\n    \"{\\\"debug\\\": true}\"\nend\n\n@mcp_resource srv \"note://{id}\" function(id::String)\n    \"Note #$id\"\nend\n\n# Read the templated resource via JSON-RPC; the {id} param is bound into `id`:\nreq = Dict{String,Any}(\"jsonrpc\" => \"2.0\", \"id\" => 1, \"method\" => \"resources/read\",\n    \"params\" => Dict{String,Any}(\"uri\" => \"note://42\"))\nresp = UniLM._dispatch_mcp(srv, req)\nprintln(\"note://42 -> \", resp[\"result\"][\"contents\"][1][\"text\"])\nnote://42 -> Note #42","title":"@mcp_resource — static and URI-templated"},{"category":"section","location":"guide/mcp/#@mcp_prompt-—-args-bound-from-the-request","page":"MCP (Model Context Protocol)","text":"The anonymous function(arg::String) … end form registers a prompt; declared args are bound from the prompts/get request arguments.\n\n@mcp_prompt srv \"review\" function(code::String)\n    [Dict(\"role\" => \"user\",\n        \"content\" => Dict(\"type\" => \"text\", \"text\" => \"Review: $code\"))]\nend\n\n# Dispatch prompts/get; `code` is bound from the request arguments:\ngreq = Dict{String,Any}(\"jsonrpc\" => \"2.0\", \"id\" => 2, \"method\" => \"prompts/get\",\n    \"params\" => Dict{String,Any}(\"name\" => \"review\",\n        \"arguments\" => Dict{String,Any}(\"code\" => \"x + 1\")))\ngresp = UniLM._dispatch_mcp(srv, greq)\nprintln(\"review -> \", gresp[\"result\"][\"messages\"][1][\"content\"][\"text\"])\nreview -> Review: x + 1","title":"@mcp_prompt — args bound from the request"},{"category":"section","location":"guide/mcp/#Serving","page":"MCP (Model Context Protocol)","text":"Start the server with serve:\n\nserve(server)                              # stdio (default) — for Claude Desktop/CLI\nserve(server; transport=:http, port=3000)  # HTTP on port 3000 — blocks until closed\n\nThe HTTP transport blocks until the server is closed (like HTTP.serve). To keep working in the same session, pass block=false and close the returned server yourself:\n\nhandle = serve(server; transport=:http, port=3000, block=false)\n# ... interact with the running server ...\nclose(handle)\n\nThe HTTP transport validates the Origin header (a DNS-rebinding defense the MCP spec requires): requests without an Origin header (curl, SDK clients) and requests from localhost origins always pass; any other browser origin gets 403 unless you allowlist it:\n\nserve(server; transport=:http, port=3000,\n      allowed_origins=[\"https://app.example.com\"])\n\nHandler concurrency. Over HTTP, tools/call, resources/read and prompts/get handlers run concurrently — one task per request, on the default thread pool — so handlers must be thread-safe (protocol requests such as initialize, the lists and ping are answered inline and stay prompt while handlers are busy). Over stdio, handlers run one at a time, and while serving on the process stdout it is pointed at stderr, so a handler that prints cannot corrupt the protocol stream. Registration is synchronized: tools, resources and prompts may be registered while the server is serving.\n\nWhat reaches the client. A tool handler's exception is a tool result, not a protocol error: the client receives isError: true with the text Error: <showerror text>, which lets a model correct itself — raise with a message you are willing to show it. An exception from a resource or prompt handler, or from dispatch itself, is answered with a generic JSON-RPC -32603 \"Internal error\" and logged on the server, so no exception text (file paths, argument values) reaches the peer. An InterruptException is never converted: it propagates. Frames (stdio) and request bodies (HTTP) are capped at 16 MiB: stdio answers an oversized frame with -32600, HTTP with 413 Payload Too Large. initialize answers the revision the client requested when it is supported, otherwise 2025-11-25; a JSON-RPC response sent to the server is accepted without an answer (HTTP 202).\n\n","title":"Serving"},{"category":"section","location":"guide/mcp/#MCP-Tool-in-Responses-API","page":"MCP (Model Context Protocol)","text":"Separately from the client/server above, OpenAI's Responses API has a built-in MCPTool type for server-side MCP integration. This tells the model to connect to an external MCP server during response generation:\n\ntool = mcp_tool(\"my-server\", \"https://mcp.example.com/sse\";\n    require_approval=\"never\",\n    allowed_tools=[\"read_file\", \"list_dir\"])\nprintln(\"Type: \", typeof(tool))\nprintln(\"Label: \", tool.server_label)\nprintln(\"URL: \", tool.server_url)\nprintln(\"JSON: \", JSON.json(JSON.lower(tool)))\nType: MCPTool\nLabel: my-server\nURL: https://mcp.example.com/sse\nJSON: {\"allowed_tools\":[\"read_file\",\"list_dir\"],\"require_approval\":\"never\",\"server_label\":\"my-server\",\"server_url\":\"https://mcp.example.com/sse\",\"type\":\"mcp\"}\n\nThis is distinct from the UniLM.jl MCP client — MCPTool delegates tool execution to OpenAI's servers, while mcp_connect runs tools locally.\n\n","title":"MCP Tool in Responses API"},{"category":"section","location":"guide/mcp/#See-Also","page":"MCP (Model Context Protocol)","text":"Tool Calling Guide — function tools and automated tool loop\nMCP API Reference — full type and function reference\nTimeouts & Retries — MCP bounds, the :queue phase and auto_respawn\nConcurrency, Tasks and Cancellation — sessions under fan-out, server handler concurrency\nCallableTool, tool_loop!, tool_loop — tool loop integration","title":"See Also"},{"category":"section","location":"api/embeddings/#embeddings_api","page":"Embeddings","text":"Types and functions for the Embeddings API.","title":"Embeddings API"},{"category":"section","location":"api/embeddings/#Embeddings-Object","page":"Embeddings","text":"","title":"Embeddings Object"},{"category":"section","location":"api/embeddings/#Construction","page":"Embeddings","text":"using UniLM\n\n# Single input\nemb = Embeddings(\"Julia is a great language\")\nprintln(\"Model: \", emb.model)\nprintln(\"Embedding dims: \", length(emb.embeddings))\n\n# Batch input\nbatch = Embeddings([\"Hello\", \"World\", \"Julia\"])\nprintln(\"Batch size: \", length(batch.input))\nprintln(\"Each embedding dims: \", length(batch.embeddings[1]))\nModel: text-embedding-3-small\nEmbedding dims: 1536\nBatch size: 3\nEach embedding dims: 1536","title":"Construction"},{"category":"section","location":"api/embeddings/#Request-Function","page":"Embeddings","text":"","title":"Request Function"},{"category":"section","location":"api/embeddings/#Result-Types","page":"Embeddings","text":"","title":"Result Types"},{"category":"section","location":"api/embeddings/#Model-Constants","page":"Embeddings","text":"println(\"Default embedding model: \", UniLM.GPTTextEmbedding3Small)\nDefault embedding model: text-embedding-3-small","title":"Model Constants"},{"category":"section","location":"api/embeddings/#Usage-Example","page":"Embeddings","text":"julia> emb = Embeddings(\"Julia is a high-performance programming language for technical computing.\")\n\njulia> embeddingrequest!(emb)\n\njulia> emb.embeddings[1:5]  # first 5 dimensions\n5-element Vector{Float64}:\n  -0.039474\n  -0.009283\n  0.001706\n  -0.028087\n  0.063363\n\njulia> sqrt(sum(x^2 for x in emb.embeddings))  # L2 norm ≈ 1.0\n1.0","title":"Usage Example"},{"category":"type","location":"api/embeddings/#UniLM.Embeddings","page":"Embeddings","text":"Embeddings(input::String; service=OPENAIServiceEndpoint, model=\"\", dimensions=nothing,\n           encoding_format=nothing, user=nothing)\nEmbeddings(input::Vector{String}; service=OPENAIServiceEndpoint, model=\"\", dimensions=nothing,\n           encoding_format=nothing, user=nothing)\n\nCreate an embedding request for one or more texts. model=\"\" resolves to the service's default embedding model — OpenAI's text-embedding-3-small (1536 dimensions), gemini-embedding-001 for GEMINIOpenAIServiceEndpoint — and a service without one (generic endpoints, DeepSeek) needs an explicit model (ArgumentError otherwise). Works with any provider via the service parameter — Ollama, Gemini, Mistral, or any OpenAI-compatible server.\n\nThe embeddings field is pre-allocated (something(dimensions, 1536) zeros per input), filled in place by embeddingrequest!, and resized to the length the model returns. The result aliases this struct, so use one Embeddings per concurrent call.\n\nFields\n\nservice::ServiceEndpointSpec: LLM provider (default: OPENAIServiceEndpoint).\nmodel::String: The embedding model name.\ninput::Union{String,Vector{String}}: Text(s) to embed.\nembeddings::Union{Vector{Float64},Vector{Vector{Float64}}}: Pre-allocated embedding vector(s).\nuser::Union{String,Nothing}: Optional end-user identifier.\ndimensions::Union{Int,Nothing}: Requested vector size, where the model supports it.\nencoding_format::Union{String,Nothing}: \"float\" or unset; any other format (e.g. \"base64\") throws ArgumentError, since the result stores Float64 vectors.\n\nExample\n\nemb = Embeddings(\"Julia is a great language\")\nembeddingrequest!(emb)\nemb.embeddings  # => Float64[...] (1536 dims)\n\n# With Ollama\nemb = Embeddings(\"test\"; service=OllamaEndpoint(), model=\"nomic-embed-text\")\n\n\n\n\n\n","title":"UniLM.Embeddings"},{"category":"function","location":"api/embeddings/#UniLM.embeddingrequest!","page":"Embeddings","text":"embeddingrequest!(emb::Embeddings; config=nothing, cancel=nothing) -> LLMRequestResponse\n\nSend an Embeddings API request for the input in emb. Returns EmbeddingSuccess, EmbeddingFailure (non-2xx), or EmbeddingCallError (network/parse/timeout). The resulting vectors are filled into emb.embeddings in place and are also reachable via embedding_vectors(result).\n\nTransient statuses (408/429/500/502/503/504/529) are retried with backoff and jitter under the resolved RequestConfig (config === nothing resolves the ambient configuration). Timeouts surface as EmbeddingCallError with status = nothing and the UniLMTimeout in cause.\n\ncancel::Union{Nothing,CancelToken} (nothing: the ambient token of with_cancel): a cancel ends the call with EmbeddingCallError(status=nothing, cause=UniLMCancelled(:token, …)), never retried; a pre-cancelled token sends nothing. A TCP connect or TLS handshake already in progress finishes (or reaches connect_timeout) before the cancel takes effect.\n\nThrows ArgumentError before any network I/O when emb.service is an endpoint type that declares its capabilities and does not list :embeddings.\n\n\n\n\n\n","title":"UniLM.embeddingrequest!"},{"category":"type","location":"api/embeddings/#UniLM.EmbeddingSuccess","page":"Embeddings","text":"EmbeddingSuccess(; embeddings, usage=nothing, raw)\n\nSuccessful Embeddings API response. Vectors are in embeddings.embeddings (also filled in place on the request struct); embedding_vectors(r) returns them.\n\n\n\n\n\n","title":"UniLM.EmbeddingSuccess"},{"category":"type","location":"api/embeddings/#UniLM.EmbeddingFailure","page":"Embeddings","text":"EmbeddingFailure(; response, status)\n\nHTTP-level failure from the Embeddings API (non-2xx).\n\n\n\n\n\n","title":"UniLM.EmbeddingFailure"},{"category":"type","location":"api/embeddings/#UniLM.EmbeddingCallError","page":"Embeddings","text":"EmbeddingCallError(; error, status=nothing, cause=nothing)\n\nException-level error during an Embeddings API call (network, parse, timeout, etc.). cause holds the underlying typed exception when one exists (e.g. a UniLMTimeout); status stays nothing for timeouts — no fabricated HTTP statuses.\n\n\n\n\n\n","title":"UniLM.EmbeddingCallError"},{"category":"function","location":"api/embeddings/#UniLM.embedding_vectors","page":"Embeddings","text":"embedding_vectors(r::EmbeddingSuccess)\n\nReturn the embedding vector(s): Vector{Float64} (single input) or Vector{Vector{Float64}} (batch). On an EmbeddingFailure or EmbeddingCallError it throws an LLMResultError; guard with issuccess.\n\n\n\n\n\n","title":"UniLM.embedding_vectors"},{"category":"section","location":"api/images/#Image-Generation-API","page":"Images","text":"Types and functions for the Image Generation API (/v1/images/generations).","title":"Image Generation API"},{"category":"section","location":"api/images/#Request-Type","page":"Images","text":"","title":"Request Type"},{"category":"section","location":"api/images/#Construction","page":"Images","text":"using UniLM\nusing JSON\n\n# Minimal request. `model` is a sentinel: it stays \"\" on the struct and resolves\n# only when the request is serialized, so read the wire body to see what will be\n# sent rather than the field.\nig = ImageGeneration(prompt=\"A watercolor painting of a sunset\")\nprintln(\"Model field: \", repr(ig.model))\nprintln(\"Model on the wire: \", JSON.parse(JSON.json(ig))[\"model\"])\nprintln(\"Prompt: \", ig.prompt)\n\n# Full options\nig2 = ImageGeneration(\n    prompt=\"A minimalist logo for a Julia package\",\n    size=\"1024x1024\",\n    quality=\"high\",\n    background=\"transparent\",\n    output_format=\"png\",\n    n=2\n)\nprintln(\"\\nFull options:\")\nprintln(\"  Size: \", ig2.size)\nprintln(\"  Quality: \", ig2.quality)\nprintln(\"  Background: \", ig2.background)\nprintln(\"  Format: \", ig2.output_format)\nprintln(\"  Count: \", ig2.n)\nModel field: \"\"\nModel on the wire: gpt-image-2\nPrompt: A watercolor painting of a sunset\n\nFull options:\n  Size: 1024x1024\n  Quality: high\n  Background: transparent\n  Format: png\n  Count: 2","title":"Construction"},{"category":"section","location":"api/images/#JSON-Serialization","page":"Images","text":"ig = ImageGeneration(prompt=\"A cute robot\", quality=\"high\", size=\"1024x1024\")\nprintln(JSON.json(ig))\n{\"model\":\"gpt-image-2\",\"prompt\":\"A cute robot\",\"quality\":\"high\",\"size\":\"1024x1024\"}","title":"JSON Serialization"},{"category":"section","location":"api/images/#Response-Types","page":"Images","text":"","title":"Response Types"},{"category":"section","location":"api/images/#Result-Types","page":"Images","text":"","title":"Result Types"},{"category":"section","location":"api/images/#Request-Function","page":"Images","text":"","title":"Request Function"},{"category":"section","location":"api/images/#Usage-Examples","page":"Images","text":"julia> result = generate_image(\n           \"A watercolor painting of a friendly robot reading a Julia programming book\",\n           size=\"1024x1024\", quality=\"medium\"\n       )\n\njulia> result isa ImageSuccess\ntrue\n\njulia> length(image_data(result))\n1\n\njulia> save_image(image_data(result)[1], \"robot_julia.png\")\n\"robot_julia.png\"","title":"Usage Examples"},{"category":"section","location":"api/images/#Accessor-Functions","page":"Images","text":"","title":"Accessor Functions"},{"category":"section","location":"api/images/#Saving-Images","page":"Images","text":"result = generate_image(\"A sunset over mountains\", n=3)\nif result isa ImageSuccess\n    for (i, img) in enumerate(image_data(result))\n        save_image(img, \"sunset_$i.png\")\n    end\nend","title":"Saving Images"},{"category":"section","location":"api/images/#Parameters-Reference","page":"Images","text":"Parameter Type Default Description\nmodel String \"\" Sentinel; resolves on serialization to \"gpt-image-2\" for OpenAI\nprompt String (required) Text description of the image\nn Int 1 Number of images (1–10)\nsize String \"auto\" \"1024x1024\", \"1536x1024\", etc.; on gpt-image-2 and later any WIDTHxHEIGHT with multiples of 16, up to 3840x2160\nquality String \"auto\" \"low\", \"medium\", \"high\"; \"xhigh\" and \"max\" on gpt-image-2.5-flare / gpt-image-2.5-sunburst\nbackground String \"auto\" \"transparent\", \"opaque\"\noutput_format String \"png\" \"png\", \"webp\", \"jpeg\"\noutput_compression Int — 0–100, for webp/jpeg\nuser String — End-user identifier\nmoderation String \"auto\" \"auto\", \"low\"\n\nDefaults shown as quoted strings other than model are the API's defaults for an omitted field; the struct stores nothing and omits them from the request body. input_fidelity is an edit parameter: set it on ImageEdit — ImageGeneration does not accept it.","title":"Parameters Reference"},{"category":"type","location":"api/images/#UniLM.ImageGeneration","page":"Images","text":"ImageGeneration(; prompt, model=\"\", kwargs...)\n\nConfiguration struct for an OpenAI Image Generation API request.\n\nKey Fields\n\nmodel::String: Model to use. The default is the sentinel \"\", which resolves at serialization time to the service's default image model (\"gpt-image-2\" for OpenAI) — so the field READS BACK as \"\" until you set it explicitly, and a service with no default image model throws ArgumentError when the request is serialized. Inspect JSON.json(ig) to see the model that will go on the wire.\nprompt::String: A text description of the desired image\nn::Union{Int,Nothing}: Number of images to generate (1–10)\nsize::Union{String,Nothing}: Size (\"1024x1024\", \"1536x1024\", \"1024x1536\", \"auto\"). On gpt-image-2 and later, any WIDTHxHEIGHT with both edges multiples of 16, up to 3840x2160 (aspect ratio between 1:3 and 3:1)\nquality::Union{String,Nothing}: Quality level (\"low\", \"medium\", \"high\", \"auto\"); gpt-image-2.5-flare and gpt-image-2.5-sunburst also accept \"xhigh\" and \"max\"\nbackground::Union{String,Nothing}: Background (\"transparent\", \"opaque\", \"auto\")\noutput_format::Union{String,Nothing}: File format (\"png\", \"webp\", \"jpeg\")\noutput_compression::Union{Int,Nothing}: Compression (0–100, for \"webp\" and \"jpeg\")\nuser::Union{String,Nothing}: End-user identifier\nmoderation::Union{String,Nothing}: Content-moderation strictness (\"auto\", \"low\")\n\ninput_fidelity is an image-edit parameter: set it on ImageEdit, not here.\n\nExamples\n\n# Simple prompt\nImageGeneration(prompt=\"A watercolor painting of a mountain sunset\")\n\n# With all options\nImageGeneration(\n    prompt=\"A minimalist logo for a Julia package\",\n    size=\"1024x1024\",\n    quality=\"high\",\n    background=\"transparent\",\n    output_format=\"png\"\n)\n\n\n\n\n\n","title":"UniLM.ImageGeneration"},{"category":"type","location":"api/images/#UniLM.ImageEdit","page":"Images","text":"ImageEdit(; image, prompt, mask=nothing, model=\"\", n=nothing, size=nothing,\n          quality=nothing, input_fidelity=nothing, background=nothing, output_format=nothing)\n\nAn image-edit request. image is a path (or vector of paths); mask is an optional path. Sent as multipart/form-data to /v1/images/edits.\n\n\n\n\n\n","title":"UniLM.ImageEdit"},{"category":"type","location":"api/images/#UniLM.ImageObject","page":"Images","text":"ImageObject\n\nA single generated image from the API response.\n\nFields\n\nb64_json::Union{String,Nothing}: Base64-encoded image data\nrevised_prompt::Union{String,Nothing}: The prompt as revised by the model\nurl::Union{String,Nothing}: Where to download the image, when the service delivered it by URL instead of inline\n\n\n\n\n\n","title":"UniLM.ImageObject"},{"category":"type","location":"api/images/#UniLM.ImageResponse","page":"Images","text":"ImageResponse\n\nParsed response from the Image Generation API.\n\nAccessors\n\nimage_data(r) — the image payloads: base64 data, or the URL of a URL-delivered image\nr.created, r.data, r.usage — basic fields\nr.raw — the complete raw JSON dict\n\nFields\n\ncreated::Int64: Timestamp when the response was created\ndata::Vector{ImageObject}: Generated images\nusage::Union{Dict{String,Any},Nothing}: Token usage information\nraw::Dict{String,Any}: Complete raw JSON response\n\n\n\n\n\n","title":"UniLM.ImageResponse"},{"category":"type","location":"api/images/#UniLM.ImageSuccess","page":"Images","text":"ImageSuccess <: LLMRequestResponse\n\nSuccessful image generation. Access the parsed response via .response.\n\nExamples\n\nresult = generate_image(\"A cute robot\")\nif result isa ImageSuccess\n    imgs = image_data(result)   # Vector{String} of base64 images\n    save_image(imgs[1], \"robot.png\")\nend\n\n\n\n\n\n","title":"UniLM.ImageSuccess"},{"category":"type","location":"api/images/#UniLM.ImageFailure","page":"Images","text":"ImageFailure <: LLMRequestResponse\n\nHTTP-level failure from the Image Generation API: the raw response body, the status code, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.ImageFailure"},{"category":"type","location":"api/images/#UniLM.ImageCallError","page":"Images","text":"ImageCallError <: LLMRequestResponse\n\nException-level error during an Image Generation API call (network, timeout, or a 200 that could not be parsed). cause is the underlying exception — a UniLMTimeout for a timeout — and request_id is set when a reply arrived but could not be decoded.\n\n\n\n\n\n","title":"UniLM.ImageCallError"},{"category":"function","location":"api/images/#UniLM.generate_image","page":"Images","text":"generate_image(ig::ImageGeneration; config=nothing, cancel=nothing)\n\nSend a request to the OpenAI Image Generation API.\n\nReturns ImageSuccess, ImageFailure, or ImageCallError.\n\nThrows ArgumentError before any network I/O when ig.service is an endpoint type that declares its capabilities and does not list :images.\n\nExamples\n\nig = ImageGeneration(prompt=\"A cute robot learning Julia\", quality=\"high\")\nresult = generate_image(ig)\nif result isa ImageSuccess\n    println(\"Generated $(length(result.response.data)) image(s)\")\n    save_image(image_data(result)[1], \"robot.png\")\nend\n\nPass config::Union{Nothing,RequestConfig} to override the timeout/retry budget for this call.\n\nPass cancel::Union{Nothing,CancelToken} (default: the ambient with_cancel token) to make the call cancellable: once the token is cancelled it returns an ImageCallError whose cause is a UniLMCancelled — without sending anything when the token was already cancelled, at once when mid-request (never retried).\n\n\n\n\n\ngenerate_image(prompt::String; kwargs...)\n\nConvenience method: create an ImageGeneration from a prompt + keyword arguments and send it. config and cancel go to the request, as for generate_image(ig).\n\nExamples\n\n# Simple generation\nresult = generate_image(\"A watercolor painting of a Julia butterfly\")\n\n# With options\nresult = generate_image(\n    \"A minimalist logo\",\n    size=\"1024x1024\",\n    quality=\"high\",\n    background=\"transparent\"\n)\n\n# Save to file\nif result isa ImageSuccess\n    save_image(image_data(result)[1], \"logo.png\")\nend\n\n\n\n\n\n","title":"UniLM.generate_image"},{"category":"function","location":"api/images/#UniLM.edit_image","page":"Images","text":"edit_image(e::ImageEdit; config=nothing, cancel=nothing) -> LLMRequestResponse\nedit_image(image, prompt; mask=nothing, service=OPENAIServiceEndpoint, config=nothing, cancel=nothing, kwargs...)\n\nEdit/extend image(s) under a text prompt. Returns the same ImageSuccess/ImageFailure/ ImageCallError shapes as generate_image.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout/retry budget for this call, and cancel::Union{Nothing,CancelToken} to make it cancellable exactly as for generate_image: a cancelled call returns an ImageCallError whose cause is a UniLMCancelled.\n\n\n\n\n\n","title":"UniLM.edit_image"},{"category":"function","location":"api/images/#UniLM.image_data","page":"Images","text":"image_data(r::ImageResponse)::Vector{String}\nimage_data(r::ImageSuccess)::Vector{String}\n\nThe image payloads of a response, one per generated image: the base64 data, or — for an image the service delivered by URL — its url (download it; only base64 data can go to save_image). An entry carrying neither is skipped.\n\nOn an ImageFailure or ImageCallError this throws an LLMResultError, as text does: a call that did not succeed has no images, and an empty list would read as \"zero images generated\".\n\nExamples\n\nresult = generate_image(\"A sunset over mountains\")\nimgs = image_data(result)       # Vector{String}\nlength(imgs)                     # number of images generated\n\n\n\n\n\n","title":"UniLM.image_data"},{"category":"function","location":"api/images/#UniLM.save_image","page":"Images","text":"save_image(img_b64::String, filepath::String)\n\nDecode a base64-encoded image and save it to a file.\n\nDecoding happens before filepath is opened, so a malformed payload raises without touching whatever already lives there — a failed save costs nothing.\n\nExamples\n\nresult = generate_image(\"A watercolor landscape\")\nif result isa ImageSuccess\n    save_image(image_data(result)[1], \"landscape.png\")\nend\n\n\n\n\n\n","title":"UniLM.save_image"},{"category":"section","location":"api/realtime/#realtime_api","page":"Realtime","text":"Low-latency speech and text over a WebSocket. This client covers the WebSocket event transport and ephemeral client-secret minting; audio is exchanged as base64 PCM inside events. WebRTC media capture and SIP telephony are out of scope. OpenAI only.","title":"Realtime API"},{"category":"section","location":"api/realtime/#Bounds","page":"Realtime","text":"realtime_connect takes a config::Union{Nothing,RequestConfig}, resolves it the usual four ways, and captures it on the session's config field:\n\nThe open phase is bounded by connect_timeout — a peer that accepts the TCP connection but never completes the upgrade throws UniLMTimeout(:connect, …) rather than blocking. An open that fails for another reason throws that error, and an upgrade that completes only after the timeout is closed without running the handler. Only OPENAIServiceEndpoint has a Realtime WebSocket: any other service throws ArgumentError before any I/O, so another endpoint's credentials are never sent to api.openai.com.\nrealtime_receive is bounded by the session's stream_idle_timeout and throws UniLMTimeout(:stream_idle, …) on a breach. Unblocking a parked read means closing the socket, and HTTP.jl's WebSocket close allows the peer up to ~5 s to acknowledge, so the breach surfaces within [limit, limit + ~5 s].\nThe session's lifetime is deliberately unbounded. Once your handler runs, how long it stays connected is your decision — a Realtime session is meant to sit idle waiting for input.\n\nRealtime throws rather than returning a typed result value, and realtime_connect makes a single attempt; max_attempts does not apply. Neither the WebSocket open nor realtime_receive observes a CancelToken. mint_realtime_secret is an ordinary HTTP verb that returns typed results: a 200 without a non-empty secret is a RealtimeCallError, and a RealtimeCallError carries its exception in cause and the provider's request_id when one was sent. A minted client secret is a live credential, so RealtimeSecretSuccess redacts it when displayed — read .value programmatically. See Timeouts & Retries.","title":"Bounds"},{"category":"section","location":"api/realtime/#Session-and-Result-Types","page":"Realtime","text":"","title":"Session and Result Types"},{"category":"section","location":"api/realtime/#Transport-Functions","page":"Realtime","text":"","title":"Transport Functions"},{"category":"section","location":"api/realtime/#Event-Builders","page":"Realtime","text":"","title":"Event Builders"},{"category":"section","location":"api/realtime/#Client-Secret","page":"Realtime","text":"","title":"Client Secret"},{"category":"section","location":"api/realtime/#Usage","page":"Realtime","text":"# Mint an ephemeral client secret for a client-side (browser) connection\nsecret = mint_realtime_secret()\nsecret isa RealtimeSecretSuccess && println(\"client secret: \", secret.value)\n\n# Open a WebSocket session, configure it, stream audio, and read events\nrealtime_connect(model=\"gpt-realtime-2\") do session\n    realtime_send(session, session_update(Dict(\"modalities\" => [\"text\", \"audio\"])))\n    realtime_send(session, input_audio_append(audio_b64))   # audio_b64 :: base64 PCM\n    realtime_send(session, response_create())\n    event = realtime_receive(session)                        # blocks for the next server event\n    println(event[\"type\"])\nend","title":"Usage"},{"category":"type","location":"api/realtime/#UniLM.RealtimeSession","page":"Realtime","text":"A live Realtime WebSocket session. Created by realtime_connect, which resolves a RequestConfig and captures it on config — realtime_receive reads its stream_idle_timeout from there. The two-argument constructor inherits the ambient config.\n\n\n\n\n\n","title":"UniLM.RealtimeSession"},{"category":"type","location":"api/realtime/#UniLM.RealtimeSecretSuccess","page":"Realtime","text":"Successful mint_realtime_secret result; value is the ephemeral client secret and raw the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.RealtimeSecretSuccess"},{"category":"type","location":"api/realtime/#UniLM.RealtimeFailure","page":"Realtime","text":"Realtime API error result: HTTP status and the raw response body.\n\n\n\n\n\n","title":"UniLM.RealtimeFailure"},{"category":"type","location":"api/realtime/#UniLM.RealtimeCallError","page":"Realtime","text":"Realtime API call that produced no usable reply (transport failure, timeout, cancel, or a 200 without a secret); cause is the underlying exception — a UniLMTimeout for a timeout, a UniLMCancelled for a cancel — and request_id is the id the service sent with a reply that could not be used.\n\n\n\n\n\n","title":"UniLM.RealtimeCallError"},{"category":"function","location":"api/realtime/#UniLM.realtime_connect","page":"Realtime","text":"realtime_connect(handler; model=\"gpt-realtime-2\", service=OPENAIServiceEndpoint, config=nothing)\n\nOpen a Realtime WebSocket and run handler(session::RealtimeSession). Inside the handler use realtime_send to send event dicts and realtime_receive to read server events. The socket closes when handler returns.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this session. ONLY the open phase is bounded, by connect_timeout: a peer that accepts the TCP connection but never completes the upgrade throws UniLMTimeout(:connect, …) instead of blocking forever. Once handler is running the session's lifetime is the caller's to decide, so no bound applies to it. The resolved config travels on the session, so realtime_receive inherits stream_idle_timeout. handler runs on an internal task (that is what makes the open phase separable); dynamically scoped values propagate into it. An upgrade that completes only after the caller received the timeout is closed without calling handler.\n\nThrows ArgumentError before any I/O for a service other than OPENAIServiceEndpoint: the Realtime WebSocket is served by api.openai.com, and another endpoint's credentials must not be sent there.\n\n\n\n\n\n","title":"UniLM.realtime_connect"},{"category":"function","location":"api/realtime/#UniLM.realtime_send","page":"Realtime","text":"realtime_send(session, event::AbstractDict)\n\n\n\n\n\n","title":"UniLM.realtime_send"},{"category":"function","location":"api/realtime/#UniLM.realtime_receive","page":"Realtime","text":"realtime_receive(session) -> Dict\n\nBlock for the next server event, bounded by the session's stream_idle_timeout. A parked read cannot be polled out of, so a breach closes the socket — the universal unblocker — and throws UniLMTimeout(:stream_idle, …).\n\n\n\n\n\n","title":"UniLM.realtime_receive"},{"category":"function","location":"api/realtime/#UniLM.realtime_event","page":"Realtime","text":"realtime_event(type; kwargs...) — a generic client event dict.\n\n\n\n\n\n","title":"UniLM.realtime_event"},{"category":"function","location":"api/realtime/#UniLM.session_update","page":"Realtime","text":"session_update(session) — a `session.update` event.\n\n\n\n\n\n","title":"UniLM.session_update"},{"category":"function","location":"api/realtime/#UniLM.input_audio_append","page":"Realtime","text":"input_audio_append(audio_b64) — append base64 PCM to the input audio buffer.\n\n\n\n\n\n","title":"UniLM.input_audio_append"},{"category":"function","location":"api/realtime/#UniLM.response_create","page":"Realtime","text":"response_create(; response=nothing) — request a model response.\n\n\n\n\n\n","title":"UniLM.response_create"},{"category":"function","location":"api/realtime/#UniLM.mint_realtime_secret","page":"Realtime","text":"mint_realtime_secret(; session=nothing, service=OPENAIServiceEndpoint)\n\nCreate an ephemeral client secret for client-side Realtime connections (POST /v1/realtime/client_secrets). session is an optional session-config dict. Returns RealtimeSecretSuccess (.value), RealtimeFailure, or RealtimeCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.mint_realtime_secret"},{"category":"section","location":"guide/chat_completions/#chat_guide","page":"Chat Completions","text":"The Chat Completions API is the standard way to talk to LLM providers. UniLM.jl wraps it in a type-safe, stateful Chat object that tracks conversation history automatically — and works with every supported backend (OpenAI, DeepSeek, Ollama, Gemini, Mistral, and more).","title":"Chat Completions"},{"category":"section","location":"guide/chat_completions/#Creating-a-Chat","page":"Chat Completions","text":"chat = Chat(\n    model=\"gpt-5.4-mini\",   # model name\n    temperature=0.7,        # sampling temperature\n)\nprintln(\"Model: \", chat.model)\nprintln(\"Messages: \", length(chat))\nModel: gpt-5.4-mini\nMessages: 0\n\nAll parameters are optional with sensible defaults. See Chat for the full list.","title":"Creating a Chat"},{"category":"section","location":"guide/chat_completions/#Building-Conversations","page":"Chat Completions","text":"Messages are added with push!, which enforces the conversation structure: a push! (or pop!) that would produce an invalid sequence throws instead. The checks live in push! and pop!; chat[i] = msg and direct edits to chat.messages bypass them.\n\n# System message must come first\npush!(chat, Message(Val(:system), \"You are a helpful Julia programming tutor.\"))\n\n# Then user messages\npush!(chat, Message(Val(:user), \"What are parametric types?\"))\n\nprintln(\"Conversation length: \", length(chat))\nprintln(\"First message role: \", chat[1].role)\nprintln(\"Last message role: \", chat[end].role)\nConversation length: 2\nFirst message role: system\nLast message role: user\n\nThe convenience Val(:system) and Val(:user) constructors keep things concise. You can also use the keyword constructor:\n\nchat2 = Chat()\npush!(chat2, Message(role=\"system\", content=\"Be helpful\"))\npush!(chat2, Message(role=\"user\", content=\"Tell me more\"))\nprintln(\"chat2 length: \", length(chat2))\nchat2 length: 2","title":"Building Conversations"},{"category":"section","location":"guide/chat_completions/#Conversation-Rules","page":"Chat Completions","text":"The first message must have role system\nMessages must alternate roles (no two consecutive messages from the same role; consecutive tool results are the exception)\nAt least content, tool_calls, or refusal_message must be non-nothing\nAttempting to violate these rules throws InvalidConversationError — the invalid message is never added\nA Message role must be \"system\", \"user\", \"assistant\" or \"tool\" (anything else throws ArgumentError)\nWith history=true, sending a conversation that ends with an assistant message throws InvalidConversationError before any request: the reply could not be appended to it\n\n# Demonstrate validation — an invalid mutation throws and leaves the chat unchanged\nchat3 = Chat()\npush!(chat3, Message(Val(:system), \"sys\"))\npush!(chat3, Message(Val(:user), \"hello\"))\ntry\n    push!(chat3, Message(Val(:user), \"hello again\"))  # same role — rejected\ncatch e\n    println(\"Rejected: \", e isa InvalidConversationError)\nend\nprintln(\"Length after rejected push: \", length(chat3), \" (still 2 — the invalid message was not added)\")\nRejected: true\nLength after rejected push: 2 (still 2 — the invalid message was not added)","title":"Conversation Rules"},{"category":"section","location":"guide/chat_completions/#Sending-Requests","page":"Chat Completions","text":"result = chatrequest!(chat)\n\nThe ! suffix is a Julia convention — chatrequest! mutates chat by appending the assistant's response to the message history (when history=true).","title":"Sending Requests"},{"category":"section","location":"guide/chat_completions/#Result-Handling","page":"Chat Completions","text":"result = chatrequest!(chat)\nif result isa LLMSuccess\n    println(result.message.content)\n    println(\"\\nFinish reason: \", result.message.finish_reason)\n    println(\"Conversation length: \", length(chat))\nelse\n    println(\"Request failed — see result for details\")\nend\nIn Julia, **parametric types** are types that take one or more **type parameters**. They let you define a family of related types with shared structure but different element types or settings.\n\n## Basic idea\n\nFor example:\n\n```julia\nVector{Int}\nVector{Float64}\n```\n\nThese are both parametric types. `Vector` is really an alias for `Array{T,1}`, where `T` is a type parameter.\n\nSo:\n\n- `Vector{Int}` = a vector whose elements are `Int`\n- `Vector{String}` = a vector whose elements are `String`\n\n## Why they’re useful\n\nParametric types let Julia:\n\n- enforce that data has a specific element type\n- write generic code that works for many types\n- optimize performance, since the compiler knows the types\n\n## Example: a custom parametric type\n\n```julia\nstruct Point{T}\n    x::T\n    y::T\nend\n```\n\nThis defines a family of types:\n\n- `Point{Int}`\n- `Point{Float64}`\n- `Point{String}`\n\nExamples:\n\n```julia\np1 = Point{Int}(1, 2)\np2 = Point{Float64}(1.5, 2.5)\n```\n\nHere `T` is the parameter, and both fields use that same type.\n\n## Type parameter can vary\n\nYou can also allow different field types:\n\n```julia\nstruct PairBox{A,B}\n    first::A\n    second::B\nend\n```\n\nThen:\n\n```julia\nPairBox{Int,String}(1, \"hello\")\n```\n\n## Important concept: parametric types are concrete only when parameters are filled in\n\n- `Point` by itself is not a concrete type\n- `Point{Int}` is a concrete type\n\nThis matters for performance and dispatch.\n\n## In short\n\nA parametric type is a type definition with placeholders for types, like:\n\n```julia\nstruct Box{T}\n    value::T\nend\n```\n\nIt creates specialized versions depending on what `T` is.\n\nIf you want, I can also explain:\n- **how parametric types differ from abstract types**\n- **how Julia uses them for multiple dispatch**\n- **invariance of parametric types**\n\nFinish reason: stop\nConversation length: 3","title":"Result Handling"},{"category":"section","location":"guide/chat_completions/#One-Shot-Requests-via-Keywords","page":"Chat Completions","text":"Skip the Chat object entirely for simple one-off requests:\n\nresult = chatrequest!(\n    systemprompt=\"You are a calculator. Respond only with the number.\",\n    userprompt=\"What is 42 * 17?\",\n    model=\"gpt-5.4-mini\",\n    temperature=0.0\n)\nif result isa LLMSuccess\n    println(result.message.content)\nelse\n    println(\"Request failed — see result for details\")\nend\n714","title":"One-Shot Requests via Keywords"},{"category":"section","location":"guide/chat_completions/#Multi-Turn-Conversations","page":"Chat Completions","text":"Because chatrequest! appends the response, you can keep chatting:\n\nchat = Chat(model=\"gpt-5.4-mini\")\npush!(chat, Message(Val(:system), \"You are a concise Julia programming tutor.\"))\npush!(chat, Message(Val(:user), \"What is multiple dispatch? Answer in 2-3 sentences.\"))\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(result.message.content)\nelse\n    println(\"Request failed — see result for details\")\nend\nMultiple dispatch is a way for a language to choose which function method to run based on the types of *all* its arguments, not just one. In Julia, this lets you write several methods with the same function name, and Julia picks the most specific one that matches the argument types at runtime.\n\n# `chatrequest!` appends the assistant reply on success, which is what makes the next\n# user turn valid. If the previous call appended none (e.g. it failed), pushing another\n# user message would throw InvalidConversationError — so guard the follow-up turn.\nif !isempty(chat) && chat[end].role == RoleAssistant\n    push!(chat, Message(Val(:user), \"Give a short Julia code example of it.\"))\n    result = chatrequest!(chat)\n    if result isa LLMSuccess\n        println(result.message.content)\n        println(\"\\nConversation length: \", length(chat))\n    else\n        println(\"Request failed — see result for details\")\n    end\nelse\n    println(\"No assistant reply to build on — skipping the follow-up turn.\")\nend\n```julia\ngreet(x::String) = \"Hello, $x!\"\ngreet(x::Int)    = \"Hello, number $x!\"\n\ngreet(\"Julia\")   # \"Hello, Julia!\"\ngreet(42)        # \"Hello, number 42!\"\n```\n\nThis shows multiple methods for the same function name, and Julia chooses the one matching the argument type.\n\nConversation length: 5","title":"Multi-Turn Conversations"},{"category":"section","location":"guide/chat_completions/#Checking-Conversation-Validity","page":"Chat Completions","text":"fresh = Chat(model=\"gpt-5.4-mini\")\npush!(fresh, Message(Val(:system), \"You are a concise Julia programming tutor.\"))\npush!(fresh, Message(Val(:user), \"What is multiple dispatch?\"))\nprintln(\"Is chat valid? \", issendvalid(fresh))  # true — system + user\n\nempty_chat = Chat()\nprintln(\"Is empty chat valid? \", issendvalid(empty_chat))  # false\nIs chat valid? true\nIs empty chat valid? false\n\nThis checks:\n\nAt least 2 messages\nFirst message is system\nLast message is user\nNo consecutive same-role messages","title":"Checking Conversation Validity"},{"category":"section","location":"guide/chat_completions/#Models","page":"Chat Completions","text":"UniLM.jl works with any model name string. Common choices:\n\nModel Usage\n\"gpt-5.6-sol\" Default; Chat tools require no reasoning; promotional price guaranteed through November 21, 2026\n\"gpt-6-astra\" Most capable; Responses-only tools, no none/minimal effort, $10/$50 per M tokens\n\"gpt-6-sol\" Built for complex coding and agentic workflows, $2/$10 per M tokens\n\"gpt-6-luna\" Fast and cheap, $0.10/$0.50\n\"gpt-5.6-luna\" Fast and cheap\n\"gpt-5.4-mini\" Mini model; reasoning effort \"none\" by default\n\"gpt-4.1-mini\" Balanced performance\n\"o4-mini\" Fast reasoning; shuts down October 23, 2026 (replacement gpt-5.6-terra)\n\nAll eight are keys in DEFAULT_PRICING, so cost accounting works out of the box. Any other model name is accepted — it is just a string on the wire — but an unpriced one estimates at 0.0 (with a one-time warning) — see Unpriced models cost $0 before relying on estimated_cost.","title":"Models"},{"category":"section","location":"guide/chat_completions/#Using-Other-Providers","page":"Chat Completions","text":"Pass a service to target any supported backend:\n\n# DeepSeek (default model: deepseek-flash)\nchat = Chat(service=DeepSeekEndpoint())\n\n# Ollama (local)\nchat = Chat(service=OllamaEndpoint(), model=\"llama3.1\")\n\n# Mistral\nchat = Chat(service=MistralEndpoint(), model=\"mistral-large-latest\")\n\nSee the Multi-Backend Guide for the full list of providers and configuration.","title":"Using Other Providers"},{"category":"section","location":"guide/chat_completions/#JSON-Serialization","page":"Chat Completions","text":"The Chat object serializes cleanly to JSON for the API:\n\nprintln(JSON.json(chat))\n{\"messages\":[{\"content\":\"You are a concise Julia programming tutor.\",\"role\":\"system\"},{\"content\":\"What is multiple dispatch? Answer in 2-3 sentences.\",\"role\":\"user\"},{\"content\":\"Multiple dispatch is a way for a language to choose which function method to run based on the types of *all* its arguments, not just one. In Julia, this lets you write several methods with the same function name, and Julia picks the most specific one that matches the argument types at runtime.\",\"role\":\"assistant\"},{\"content\":\"Give a short Julia code example of it.\",\"role\":\"user\"},{\"content\":\"```julia\\ngreet(x::String) = \\\"Hello, $x!\\\"\\ngreet(x::Int)    = \\\"Hello, number $x!\\\"\\n\\ngreet(\\\"Julia\\\")   # \\\"Hello, Julia!\\\"\\ngreet(42)        # \\\"Hello, number 42!\\\"\\n```\\n\\nThis shows multiple methods for the same function name, and Julia chooses the one matching the argument type.\",\"role\":\"assistant\"}],\"model\":\"gpt-5.4-mini\"}","title":"JSON Serialization"},{"category":"section","location":"guide/chat_completions/#Retry-Behaviour","page":"Chat Completions","text":"chatrequest! automatically retries transient HTTP statuses (408, 429, 500, 502, 503, 504, 529) with exponential backoff and jitter; a Retry-After header is a floor under the jittered wait, so concurrent callers that received the same header do not retry in lockstep. Attempts and total time are bounded by the resolved RequestConfig (max_attempts, default 3; total_deadline, default 900 s). Pass config=RequestConfig(max_attempts=1) to disable retries for a call, or set scoped/process-wide defaults with with_request_config / set_default_config!. Timeouts surface as LLMCallError with status = nothing and the UniLMTimeout (phase, elapsed, limit) in .cause; a cancelled call (see Concurrency, Tasks and Cancellation) carries a UniLMCancelled there instead.\n\nLocal validation is not a result: an option the provider or model cannot express, a service that does not declare :chat, or a conversation the reply could not be appended to throws (ArgumentError / InvalidConversationError) before any request is sent.","title":"Retry Behaviour"},{"category":"section","location":"guide/chat_completions/#Parameter-Validation","page":"Chat Completions","text":"The Chat constructor validates parameter ranges at construction time:\n\nParameter Valid Range\ntemperature 0.0–2.0\ntop_p 0.0–1.0\nn 1 (a result carries a single choice)\nmax_tokens, max_completion_tokens ≥ 1\npresence_penalty -2.0–2.0\nfrequency_penalty -2.0–2.0\ntop_logprobs 0–20\nlogit_bias values -100–100 (any Real)\nreasoning_effort \"none\", \"minimal\", \"low\", \"medium\", \"high\", \"xhigh\", \"max\"\n\nOut-of-range values throw ArgumentError. Additionally, temperature and top_p are mutually exclusive, and an empty tools vector is stored as nothing. Provider- and model-specific limits (for example GPT-5.6 Chat tools requiring reasoning_effort=\"none\", or Claude's temperature range) are checked when the request is encoded, before any network I/O.","title":"Parameter Validation"},{"category":"section","location":"guide/chat_completions/#See-Also","page":"Chat Completions","text":"Chat — full type reference\nMessage — message type reference\nTool Calling — function calling with Chat Completions\nStreaming — real-time streaming\nStructured Output — JSON-constrained generation","title":"See Also"},{"category":"section","location":"guide/concurrency/#concurrency_guide","page":"Concurrency, Tasks and Cancellation","text":"UniLM calls are ordinary blocking Julia calls: run many at once by putting them on tasks. This guide covers which objects a concurrent call may share, the fan-out patterns, streaming into a Channel, cooperative cancellation, the thread layout the package assumes, and what the HTTP, tool-loop and MCP layers do under load. The time bounds themselves are in Timeouts & Retries.","title":"Concurrency, Tasks and Cancellation"},{"category":"section","location":"guide/concurrency/#What-a-concurrent-call-may-share","page":"Concurrency, Tasks and Cancellation","text":"Object Mutated by its verb? Rule\nChat yes — the reply is appended to messages, the cost accrues one Chat per in-flight call; fan out with fork\nEmbeddings yes — the vectors are written into emb.embeddings, and the result aliases them one Embeddings per in-flight call\nRespond, ImageGeneration, ImageEdit, FIMCompletion, SystemOneRequest no share freely\nMCPSession its exchanges are serialized one session per parallel worker (see MCP)\nRequestConfig, CancelToken immutable / atomic share freely\n\nA Chat is a conversation, not a request: chatrequest! pushes the reply onto chat.messages and adds the call's estimated cost to its cumulative-cost Ref, both without a lock. Two calls on one Chat interleave into a corrupted conversation or a lost cost update, and a push! while its streaming task runs does the same. fork is the fan-out: fork(chat) deep-copies every field except service (the endpoint is shared), so forks share nothing mutable:\n\nbase = Chat(model=\"gpt-5.4-mini\", stop=[\"END\"])\npush!(base, Message(Val(:system), \"You are terse.\"))\nforks = fork(base, 3)\npush!(forks[1], Message(Val(:user), \"first branch\"))\npush!(forks[1].stop, \"HALT\")\n(length(base), length(forks[1]), length(forks[2]), base.stop)\n(1, 2, 1, [\"END\"])\n\nEach fork gets its own cost accumulator, but it starts at the parent's running total, not at zero: summing cumulative_cost over a parent and its forks counts the parent's spend once more per fork. To account per fork, subtract the parent's total at fork time from cumulative_cost(fork), or sum estimated_cost over the fork's own results.\n\nembeddingrequest! fills its Embeddings in place and returns a result whose embeddings field is that same object, so a second call on it overwrites what the first returned. Build one Embeddings per concurrent call, and copy the vectors before reusing a request.","title":"What a concurrent call may share"},{"category":"section","location":"guide/concurrency/#Fan-out-patterns","page":"Concurrency, Tasks and Cancellation","text":"The request verbs return failures as values (*Failure, *CallError). What they throw is a local validation error raised before any I/O (ArgumentError, InvalidConversationError) or an InterruptException, so fetch on a task running one returns its typed result and raises TaskFailedException only for those. The Task a streaming call returns throws only for an interrupt: its validation ran before the task was spawned. The MCP client, the Realtime WebSocket, nl_dispatch and @branch are throw-based instead; their exceptions reach fetch the same way.\n\nThreads.@spawn + fetch — one task per call, on the default thread pool:\n\nprompts = [\"Summarize A\", \"Summarize B\", \"Summarize C\"]\ntasks = map(prompts) do p\n    Threads.@spawn respond(p; model=\"gpt-5.4-mini\")\nend\nresults = fetch.(tasks)                 # typed results, in submission order\nok = filter(r -> r isa ResponseSuccess, results)\n\nasyncmap(...; ntasks) — a concurrency cap for a large batch. Its tasks run on the calling thread, which suits I/O-bound calls; keep heavy post-processing out of them (see Thread layout):\n\nresults = asyncmap(prompts; ntasks=8) do p\n    respond(p; model=\"gpt-5.4-mini\")\nend\n\n@sync — wait for a group of tasks that write into preallocated slots:\n\nreplies = Vector{LLMRequestResponse}(undef, 4)\n@sync for (i, f) in enumerate(fork(chat, 4))\n    push!(f, Message(Val(:user), \"Continue with ending $i.\"))\n    Threads.@spawn replies[i] = chatrequest!(f)\nend\n\nA with_request_config or with_cancel scope opened around any of these reaches the spawned tasks: both are scoped values, which Julia propagates into Threads.@spawn and @async tasks.","title":"Fan-out patterns"},{"category":"section","location":"guide/concurrency/#concurrency_streaming","page":"Concurrency, Tasks and Cancellation","text":"A streaming callback runs on the stream's own task. Hand each delta to a consumer through a Channel so the callback stays short:\n\nchat = Chat(model=\"gpt-5.4-mini\", stream=true)\npush!(chat, Message(Val(:system), \"You are a storyteller.\"))\npush!(chat, Message(Val(:user), \"Tell a long story.\"))\n\ndeltas = Channel{String}(64)\nconsumer = Threads.@spawn for piece in deltas\n    print(piece)                        # slow work here is fine\nend\nbind(deltas, consumer)                  # a consumer that dies closes the channel\n\ntask = chatrequest!(chat; callback = (chunk, close) -> chunk isa String && put!(deltas, chunk))\nresult = fetch(task)                    # LLMSuccess, or a typed failure\nclose(deltas)\nwait(consumer)\n\nBackpressure is TCP's. When the channel is full, put! blocks the callback, the stream task stops reading, and TCP flow control slows the sender. Nothing is buffered beyond the channel and the socket.\nA slow consumer does not kill a healthy stream. stream_idle_timeout bounds the gap between bytes arriving off the socket; time the stream task spends inside your callback (or on_tool_call) is not counted. The flip side: no UniLM bound covers a callback that never returns. bind(deltas, consumer) closes the channel when the consumer fails, so the blocked put! throws and the stream ends with an LLMCallError whose cause is that exception.\nStopping early: cancel the call's token from any task (next section), or set close[] = true in the callback. Either stops the stream at once — or, while the stream task is inside your callback, as soon as that callback returns.","title":"Streaming into a Channel"},{"category":"section","location":"guide/concurrency/#concurrency_cancellation","page":"Concurrency, Tasks and Cancellation","text":"A CancelToken is a cooperative, level-triggered flag: cancel! it from any task, and every operation that observes it stops.\n\ntok = CancelToken()\ncancel!(tok)                            # stays cancelled: level-triggered\nchat = Chat(service=GenericOpenAIEndpoint(\"http://127.0.0.1:9\", \"\"), model=\"any\")\npush!(chat, Message(Val(:system), \"You are terse.\"))\npush!(chat, Message(Val(:user), \"Hello\"))\nr = chatrequest!(chat; cancel=tok)      # sends nothing: the token is already cancelled\n(typeof(r), r.status, r.cause.source)\n(LLMCallError, nothing, :token)\n\nHow an operation gets its token. Pass cancel=tok, or run the code inside with_cancel(tok) do … end, which makes tok the ambient token for everything in the block, tasks spawned inside it included; an explicit cancel wins. The cancel keyword is on chatrequest!, respond, embeddingrequest!, tool_loop! / tool_loop, generate_image, edit_image, fim_complete, prefix_complete, ask, list_models, nl_dispatch, @branch, poll_batch and poll_file_batch. Every other HTTP verb — the platform and lifecycle calls — observes the ambient token.\nWhat a cancelled call returns. The verb's usual call-error result with status = nothing and cause::UniLMCancelled (UniLMCancelled): never retried, nothing committed to a Chat, no terminal callback. cause.source is :token for a cancel! and :callback for a stream stopped with close[] = true; cause.elapsed is the time since the call started. The cancel takes effect wherever it lands — before connecting, during the response-header wait, mid-stream, or during a retry backoff — unless the provider's completion marker already arrived: then the turn is committed, the final-message callback runs, and usage may be missing. On a Chat stream that marker is the chunk carrying the finish reason, which on the OpenAI wire precedes the usage chunk and [DONE]; on a Responses stream it is the terminal event, whose response (usage included) is the result. nl_dispatch and @branch throw SystemOneError wrapping the SystemOneCallError, as for any failed call. A cancelled tool loop returns completed=false with the cancelled turn's call error as response.\nLevel-triggered means one token per unit of work. A cancelled token stays cancelled, and every later operation that sees it stops before any network I/O. Create a fresh token for each request, batch or user action you may want to abandon.\nLimits. A TCP connect or TLS handshake already in progress cannot be interrupted (HTTP.jl 2.7.1): a cancel during one takes effect when the connect completes or reaches connect_timeout. A callback that is running finishes first. The Realtime WebSocket does not observe the token, and neither does an MCP stdio exchange (bounded by mcp_request_timeout); an MCP call over HTTP does (see MCP).\n\ntok = CancelToken()\nbatch = Threads.@spawn with_cancel(tok) do\n    asyncmap(p -> respond(p; model=\"gpt-5.4-mini\"), prompts; ntasks=8)\nend\n# … the user pressed \"stop\":\ncancel!(tok)\nresults = fetch(batch)   # finished calls keep their results; the rest carry UniLMCancelled\n\nThe Julia 1.14 development branch adds task cancellation built on scoped, level-triggered cancellation tokens (Base.CancellationTokenSource, carried in the scoped value Base.CANCEL_TOKEN, with a cancel keyword on blocking operations). UniLM's token, ambient scope and cancel keyword have the same shape so a later release can bridge to it. No bridge exists yet, and UniLM is not tested on that branch.","title":"Cancellation"},{"category":"section","location":"guide/concurrency/#concurrency_threads","page":"Concurrency, Tasks and Cancellation","text":"Since Julia 1.12 a plain julia starts one default thread and one interactive thread. The main task (and so the REPL) runs on thread 1 in the interactive pool, and so do @async and asyncmap tasks started from it; Threads.@spawn lands on the default pool. libuv's event loop, which fires every Timer in the process, shares thread 1 with the main task.\n\nKeep CPU-heavy work off thread 1. A task on thread 1 that computes without yielding holds up the event loop, and with it every timer: on Julia 1.13 a 0.1 s sleep or Timer on a default-pool thread fired only when a 2 s non-yielding loop on thread 1 ended. UniLM's timeout watchdogs, stream idle checks and retry backoffs are timers, so they fire late too. Move heavy work to Threads.@spawn tasks and feed them through a Channel.\nKeep streaming callbacks short. A callback runs on its stream's task, on the default pool — a single thread unless you start Julia with more — so a CPU-heavy callback holds up its own stream and every other spawned task waiting for that thread. Hand the work to a consumer (see Streaming into a Channel).\nStart with more default threads for parallel work: julia -t auto or julia -t 8,1 (8 default, 1 interactive).\nEverything works on a single thread (julia -t 1,0): calls, streams, timeouts and cancellation are cooperative, so concurrency comes from tasks yielding on I/O. Parallel CPU work does not.","title":"Thread layout"},{"category":"section","location":"guide/concurrency/#HTTP-under-fan-out","page":"Concurrency, Tasks and Cancellation","text":"Each stream has its own HTTP/1.1 connection. Streams pin HTTP/1.1, so a consumer applying backpressure to one stream cannot starve its siblings through a shared HTTP/2 flow-control window. HTTP.jl 2.7.1's default pool sets no per-host connection cap, so N concurrent streams hold N connections.\nNon-streaming calls may share one HTTP/2 connection. They keep protocol negotiation, and concurrent requests to one https host can multiplex over one connection.\nRate limits do not retry in lockstep. A Retry-After header is a floor under the jittered backoff, not a replacement for it: each call waits at least what the header asked for, plus a random share of at most half the budget left after it, so a rate-limited batch spreads its retries instead of waking at one instant. A wait that does not fit the remaining total_deadline returns the last real response (the 429) at once.","title":"HTTP under fan-out"},{"category":"section","location":"guide/concurrency/#Tool-loops","page":"Concurrency, Tasks and Cancellation","text":"A tool loop runs one turn's tool calls one at a time in the calling task by default. tool_concurrency = n runs up to n of a turn's calls at once on spawned tasks: the dispatcher (or each CallableTool callable) must then be thread-safe. Results are sent back in call order once every call of the turn has finished, so the next request does not depend on completion order. An InterruptException from any dispatch propagates once the interrupted turn is removed from the chat, and a cancelled token stops the hand-out of further calls.\n\nresult = tool_loop!(chat; tools=my_callable_tools, tool_concurrency=4)","title":"Tool loops"},{"category":"section","location":"guide/concurrency/#concurrency_mcp","page":"Concurrency, Tasks and Cancellation","text":"A session serializes its exchanges, first come first served. Calls on one MCPSession run one at a time, in arrival order. A call's timeout (default mcp_request_timeout) bounds its wait for the session as well: a call that cannot acquire it in time throws MCPTimeoutError with phase :queue and never touches the session. Once a call holds the session its exchange gets its full bound, so waiting callers never cut it short.\nFor parallelism, open one session per worker.\nCancellation depends on the transport. Over HTTP a cancel ends the call at once: it throws UniLMCancelled, sends the server a best-effort notifications/cancelled, and the session stays open. A stdio exchange cannot be interrupted; it runs to completion under its mcp_request_timeout.\nServer handlers: serve over HTTP runs tools/call, resources/read and prompts/get handlers concurrently, one task per request on the default pool, so handlers must be thread-safe (protocol requests such as initialize and ping are answered inline and stay prompt while handlers are busy). Over stdio, handlers run one at a time. Registering tools, resources or prompts while the server is serving is synchronized.","title":"MCP"},{"category":"section","location":"guide/concurrency/#Known-limits","page":"Concurrency, Tasks and Cancellation","text":"A process that blocks thread 1 without yielding delays every timer in the process, UniLM's timeouts and backoffs included, until it yields.\nStreaming callbacks run on the stream's task: a callback that blocks holds that stream (and nothing else) until it returns, and no bound covers it.\nA cancel cannot interrupt a TCP connect or TLS handshake in progress (it waits for the connect or for connect_timeout), a running callback, a Realtime WebSocket, or an MCP stdio exchange.","title":"Known limits"},{"category":"section","location":"guide/concurrency/#See-also","page":"Concurrency, Tasks and Cancellation","text":"Timeouts & Retries — every bound, and the typed failures.\nStreaming — callbacks, stopping a stream, streamed tool calls.\nTool Calling — the tool-loop contract.\nMCP — sessions, transports and serving.","title":"See also"},{"category":"section","location":"guide/custom_backends/#custom_backends_guide","page":"Custom Backends","text":"UniLM's provider surface is a typed, open extension point: you add a backend by defining a ServiceEndpoint subtype and the handful of methods that route and — when the wire differs — translate its requests. The request driver, retries, cost accounting, tool loop, and streaming are not provider-specific; they call a small wire seam through multiple dispatch, so a new backend plugs in without touching that machinery.\n\nThe functions you add methods to — get_url, auth_header, default_model, the chat seam encode_request / decode_response / handle_sse_event! and the agentic seam encode_agentic / decode_agentic / decode_agentic_stream — and the stream-state types StreamState / AgenticStreamState are UniLM's public extension API: declared public, not exported, so qualify them (UniLM.get_url). Each is documented in the Extension API reference.\n\nThere are two cases, depending on whether your provider speaks the OpenAI wire.","title":"Custom Backends"},{"category":"section","location":"guide/custom_backends/#Case-1-—-an-OpenAI-compatible-provider","page":"Custom Backends","text":"If the provider implements OpenAI's /v1/chat/completions (and, optionally, the /v1/responses and /v1/embeddings surfaces), subtype OpenAIWireEndpoint. Subtypes inherit the OpenAI request/response encoding and the SSE stream handling for free; you define only routing (get_url) and authentication (auth_header).\n\nusing UniLM\n\n# A fixed-URL, OpenAI-compatible provider.\nstruct AcmeEndpoint <: OpenAIWireEndpoint\n    api_key::String\nend\n\n# Routing: where a chat-completions request POSTs to.\nUniLM.get_url(::AcmeEndpoint, ::Chat) = \"https://api.acme.ai/v1/chat/completions\"\n\n# Authentication: the headers attached to every request.\nUniLM.auth_header(e::AcmeEndpoint) =\n    [\"Authorization\" => \"Bearer $(e.api_key)\", \"Content-Type\" => \"application/json\"]\n\nchat = Chat(service=AcmeEndpoint(ENV[\"ACME_API_KEY\"]), model=\"acme-large\")\npush!(chat, Message(Val(:system), \"You are helpful.\"))\npush!(chat, Message(Val(:user), \"Hi!\"))\nresult = chatrequest!(chat)   # encode_request / decode_response / SSE are all inherited\n\nEmbeddings route the same way, through get_url(::MyEndpoint, ::Embeddings), and respond through get_url(::MyEndpoint, ::Respond). The Responses lifecycle operations (get_response, cancel_response, compact_response, …) build their URLs from one base instead, the internal hook UniLM._api_base_url(service) (the Responses URL is that base plus \"/v1/responses\"). Defining it is the one step here outside the public extension API — an underscore name that may change without notice. GenericOpenAIEndpoint is the built-in reference for this shape:\n\nUniLM._api_base_url(s::MyEndpoint) = rstrip(s.base_url, '/')   # internal hook\nUniLM.get_url(s::MyEndpoint, ::Chat)       = UniLM._api_base_url(s) * \"/v1/chat/completions\"\nUniLM.get_url(s::MyEndpoint, ::Embeddings) = UniLM._api_base_url(s) * \"/v1/embeddings\"\n\nFor a provider that is a plain OpenAI endpoint at a custom URL you need no new type at all — GenericOpenAIEndpoint(base_url, api_key) already does exactly this. Define a new OpenAIWireEndpoint subtype when you want a named type, provider-specific defaults, or custom routing/auth:\n\ndefault_model — without a method for your endpoint, model= is required: Chat(service=AcmeEndpoint(key)) throws ArgumentError: model must be specified when using AcmeEndpoint at construction. UniLM.default_model(::AcmeEndpoint) = \"acme-large\" makes it optional.\nprovider_capabilities — an endpoint that defines no method is dispatched unvalidated by the four primary verbs and rejected by the platform verbs (see Provider Capabilities); declaring a set opts into validation against it.\nStreamed usage — the built-in OpenAI and DeepSeek endpoints ask a stream to report token usage (stream_options.include_usage); other OpenAI-wire endpoints do not, since some servers reject the field. Set stream_options=Dict(\"include_usage\" => true) on a streamed Chat when your server supports it, or streamed turns carry no usage.\n\nSingleton endpoints (no fields) dispatch on the type — get_url(::Type{MyEndpoint}, ::Chat); field-bearing endpoints dispatch on the instance, as above.","title":"Case 1 — an OpenAI-compatible provider"},{"category":"section","location":"guide/custom_backends/#Case-2-—-a-provider-with-a-different-wire","page":"Custom Backends","text":"If the provider speaks a genuinely different wire (its own request shape, response shape, or SSE dialect), subtype ServiceEndpoint directly and additionally implement the three chat wire-seam methods. They translate between the neutral Chat/Message types and the provider's wire:\n\nMethod Signature Return contract\nencode_request encode_request(service, chat::Chat) String — the request body to POST\ndecode_response decode_response(service, resp::HTTP.Response) (; message::Message, usage::Union{TokenUsage,Nothing}) — the parsed 200 response\nhandle_sse_event! handle_sse_event!(service, event, payload, state::StreamState) Symbol — :continue | :done | :error; mutates state in place\n\nhandle_sse_event! is called once per streamed SSE event (event/payload are strings): it appends text and tool-call deltas onto state, and returns :done at the provider's end-of-stream sentinel, :error on an in-band error event, or :continue otherwise.\n\nThe in-repo reference implementation is the Anthropic backend (src/anthropic.jl): ANTHROPICServiceEndpoint subtypes ServiceEndpoint and implements all three methods against Anthropic's native Messages API — including capturing provider-native content blocks for verbatim multi-turn round-trip. Read it as a complete worked example rather than copying it here. The native Gemini backend (src/gemini.jl) is a second example, with a different end-of-stream rule (EOF, not a sentinel).\n\nTo also serve the agentic respond verb on a native wire, override the agentic seam the same way — encode_agentic / decode_agentic / decode_agentic_stream (the stream handler mutates an AgenticStreamState) — and route it with the internal hook UniLM._agentic_url(service), which respond and the lifecycle operations read (an underscore name outside the public extension API); the Gemini Interactions backend (src/interactions.jl) is the reference.\n\nBoth seams split validation from failure the same way. An encoder runs before any network I/O and what it throws propagates from the verb, so throw ArgumentError for an option your wire cannot express. An exception thrown by a decoder becomes the verb's typed call-error result (LLMCallError / ResponseCallError) with the exception in cause, and one thrown by a stream handler drops that payload, counted in the result's sse_dropped.","title":"Case 2 — a provider with a different wire"},{"category":"section","location":"guide/custom_backends/#Fail-loud-contract","page":"Custom Backends","text":"The wire seam has no permissive fallback. A bare ServiceEndpoint subtype that omits a seam method fails with a MethodError at call time, rather than silently emitting OpenAI-shaped requests to a provider that does not speak that wire. Subtyping OpenAIWireEndpoint is the explicit opt-in to the OpenAI wire; subtyping ServiceEndpoint is the commitment to implement the seam yourself.\n\nusing UniLM\n# The OpenAI-wire opt-in is a real supertype relationship:\nprintln(OPENAIServiceEndpoint  <: OpenAIWireEndpoint)   # true  — inherits the wire\nprintln(ANTHROPICServiceEndpoint <: OpenAIWireEndpoint) # false — native wire, own seam\ntrue\nfalse","title":"Fail-loud contract"},{"category":"section","location":"guide/custom_backends/#See-Also","page":"Custom Backends","text":"OpenAIWireEndpoint, ServiceEndpoint — the two extension supertypes\nExtension API — the public functions and types a backend implements\nMulti-Backend Support — using the built-in backends\nGenericOpenAIEndpoint — the built-in configurable OpenAI-compatible endpoint","title":"See Also"},{"category":"section","location":"api/batch/#batch_api","page":"Batch","text":"Create, retrieve, cancel, and list async bulk jobs, and poll them to completion. The Batch API processes large request sets asynchronously at roughly half the cost: the input is a JSONL file uploaded via the Files API with purpose=\"batch\", and output is fetched with file_content using batch.output_file_id. OpenAI only.","title":"Batch API"},{"category":"section","location":"api/batch/#Parsed-Objects","page":"Batch","text":"","title":"Parsed Objects"},{"category":"section","location":"api/batch/#Result-Types","page":"Batch","text":"","title":"Result Types"},{"category":"section","location":"api/batch/#Request-Functions","page":"Batch","text":"","title":"Request Functions"},{"category":"section","location":"api/batch/#Usage","page":"Batch","text":"# Upload a JSONL request file, then create a batch job\nupload = upload_file(\"requests.jsonl\", \"batch\")\nbatch = create_batch(upload.response.id, \"/v1/chat/completions\")\n\n# Poll until terminal (completed / failed / cancelled / expired)\ndone = poll_batch(batch.response.id)\nif done isa BatchSuccess && done.response.status == \"completed\"\n    output = file_content(done.response.output_file_id)\n    output isa FileContentSuccess && save_file_content(output, \"results.jsonl\")\nend\n\n# List jobs, or cancel one\nlist_batches(limit=10)\ncancel_batch(batch.response.id)","title":"Usage"},{"category":"type","location":"api/batch/#UniLM.BatchObject","page":"Batch","text":"BatchObject\n\nA batch job from the Batch API: id, status, endpoint, input_file_id, output_file_id, error_file_id, and request_counts; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.BatchObject"},{"category":"type","location":"api/batch/#UniLM.BatchList","page":"Batch","text":"BatchList\n\nA page of BatchObjects from list_batches; has_more signals that further pages are available.\n\n\n\n\n\n","title":"UniLM.BatchList"},{"category":"type","location":"api/batch/#UniLM.BatchSuccess","page":"Batch","text":"Successful create/retrieve/cancel result wrapping a BatchObject.\n\n\n\n\n\n","title":"UniLM.BatchSuccess"},{"category":"type","location":"api/batch/#UniLM.BatchListSuccess","page":"Batch","text":"Successful list_batches result wrapping a BatchList.\n\n\n\n\n\n","title":"UniLM.BatchListSuccess"},{"category":"type","location":"api/batch/#UniLM.BatchFailure","page":"Batch","text":"Batch API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.BatchFailure"},{"category":"type","location":"api/batch/#UniLM.BatchCallError","page":"Batch","text":"Batch API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout. last_observed is set only when poll_batch runs out of time or is cancelled: the last BatchObject it saw, if any.\n\n\n\n\n\n","title":"UniLM.BatchCallError"},{"category":"function","location":"api/batch/#UniLM.create_batch","page":"Batch","text":"create_batch(input_file_id, endpoint; completion_window=\"24h\", metadata=nothing, service=OPENAIServiceEndpoint)\n\nCreate a batch job. endpoint is e.g. \"/v1/chat/completions\", \"/v1/responses\", or \"/v1/embeddings\". input_file_id comes from upload_file(path, \"batch\").\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.create_batch"},{"category":"function","location":"api/batch/#UniLM.retrieve_batch","page":"Batch","text":"retrieve_batch(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.retrieve_batch"},{"category":"function","location":"api/batch/#UniLM.cancel_batch","page":"Batch","text":"cancel_batch(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.cancel_batch"},{"category":"function","location":"api/batch/#UniLM.list_batches","page":"Batch","text":"list_batches(; limit=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_batches"},{"category":"function","location":"api/batch/#UniLM.poll_batch","page":"Batch","text":"poll_batch(id; interval=10.0, timeout=86400.0, service=OPENAIServiceEndpoint, cancel=nothing)\n\nPoll a batch until it reaches a terminal status (completed/failed/cancelled/expired). timeout bounds the wall-clock time of the whole poll (Inf waits indefinitely); interval is the pause between GETs. Both must be positive, else ArgumentError.\n\nA transient failure — a status in the retryable set (408/429/500/502/503/504/529) or a GET that timed out — is polled through; any other failure is returned as it came. When the time runs out the result is a BatchCallError with cause = UniLMTimeout(:deadline, …) and last_observed set to the last BatchObject seen (nothing if no GET succeeded).\n\nPass cancel::Union{Nothing,CancelToken} (default: the ambient with_cancel token) to make the poll cancellable: a cancel ends it at once, mid-GET or mid-pause, with a BatchCallError whose cause is a UniLMCancelled and last_observed as above; a token cancelled before the poll sends nothing.\n\nPass config::Union{Nothing,RequestConfig} to bound each GET (a single attempt; its total_deadline is capped at the time left).\n\n\n\n\n\n","title":"UniLM.poll_batch"},{"category":"section","location":"guide/system_one/#system_one_guide","page":"Typed Judgments with Jev","text":"","title":"Typed Judgments with Jev (TypeSafe System One)"},{"category":"section","location":"guide/system_one/#What-System-One-is","page":"Typed Judgments with Jev","text":"A System One model reads a piece of state and answers the questions you enumerated with a typed value and a probability distribution over the outcomes you named — never with generated text. Jev is TypeSafe's flagship System One model and the first one of its kind. The division of labour is the point: your code owns the control flow, the deterministic rules and the side effects, and the model is called only where the system needs a narrow semantic decision over unstructured input. Compared with an LLM there is no reasoning trace to read, no prose to parse, no tool loop to drive and no system prompt to jailbreak — one request, one round trip, a fixed set of answers. The probabilities are trained to be calibrated across groups of predictions, which is what makes confidence-gating meaningful: the answer says what, the distribution says whether to act on it. Calibration across a population is not a guarantee about any single answer, so treat a high confidence as \"the distribution is peaked\", not as \"this is correct\".\n\nThis is a first-class surface in UniLM alongside the LLM providers because the two are complements, not alternatives. Route, verify, guard and rank with Jev; generate with an LLM. A typical pipeline classifies the request with a choice, sends only the requests that need prose to respond, and screens the draft with a noul before it reaches a user.\n\nSee the System One concept page for the model class, and How to build with TypeSafe for the workflow it implies.\n\nBecause the answer is already a branch decision, it also drives Julia's control flow directly: Multiple Dispatch on Natural Language turns a meaning into a method signature, so a Jev answer selects which method runs.","title":"What System One is"},{"category":"section","location":"guide/system_one/#Setup","page":"Typed Judgments with Jev","text":"Set the API key:\n\nexport TYPESAFE_API_KEY=\"...\"\n\nTwo optional variables change where the call goes and what it names:\n\nVariable Default Meaning\nTYPESAFE_API_KEY — Required. A missing key is a SystemOneCallError, not a thrown KeyError.\nTYPESAFE_BASE_URL https://api.typesafe.ai API root override, for a proxy or a mock server.\nTYPESAFE_DEFAULT_MODEL jev-latest The model ask names when a call does not.\n\nAll three are read at call time, so exporting them after using UniLM still works.\n\njev-latest is an alias that moves when a new version ships. That is what you want while you are building. Once you have tuned a confidence threshold against a specific version, pin the versioned id — ask(...; model=\"jev-1.13.0\") or TYPESAFE_DEFAULT_MODEL=jev-1.13.0 — and move to the next one on your own schedule (Models).\n\nTYPESAFEServiceEndpoint is not a chat backend. It declares only :system_one and :models, so the verbs reject it up front with an ArgumentError rather than posting a request the service would not answer: chatrequest!, respond, embeddingrequest! and the other platform verbs. Constructing a Chat or an Embeddings that names the endpoint is allowed; only sending one is refused. Omitting model= is refused too, because this endpoint has no chat default to resolve. See Provider Capabilities.","title":"Setup"},{"category":"section","location":"guide/system_one/#First-request","page":"Typed Judgments with Jev","text":"One call carries the state once and every question you want answered about it. Answers come back keyed by the names you chose:\n\nusing UniLM\n\nr = ask(\n    \"Help! My payouts have been failing for 3 days and nobody has replied to my emails.\",\n    \"department\" => choice(\"Which team should handle this ticket?\", (\n        billing   = \"Payments, invoicing, payouts, refunds\",\n        technical = \"Bugs, outages, integrations\",\n        sales     = \"Pricing, upgrades, new accounts\")),\n    \"urgency\" => score(\"How urgent is this ticket?\",\n        [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"]),\n    \"is_frustrated\" => noul(\"Is the customer frustrated?\";\n        yes = \"The customer expresses frustration or impatience\",\n        no  = \"The customer is neutral or satisfied\"))\n\nif r isa SystemOneSuccess\n    d = r[\"department\"]\n    println(\"department:    \", d.choice, \"  (confidence \", d.confidence, \")\")\n    # => department:    billing  (confidence 1.0)\n    println(\"urgency:       \", r[\"urgency\"].score, \" of \", length(r[\"urgency\"].legend) - 1)\n    # => urgency:       1.98 of 2\n    println(\"is_frustrated: \", r[\"is_frustrated\"].noul)\n    # => is_frustrated: 0.98\n    println(\"answered by:   \", r.response.model)\n    # => answered by:   jev-1.13.0\nelse\n    println(\"Request failed — \", r)   # a SystemOneFailure or a SystemOneCallError — `ask` never throws\nend\ndepartment:    billing  (confidence 1.0)\nurgency:       1.98 of 2\nis_frustrated: 0.98\nanswered by:   jev-1.13.0\n\nThe # => lines are the output of that exact request in one recorded run against the live service, and every number the service reports is rounded to two decimals. The output rendered under the block is the docs build's own call: a fresh answer, or the typed error when the build ran without a key. The rest of what came back with the recorded run:\n\nr[\"department\"].probabilities[\"billing\"]     # => 1.0\nr[\"department\"].probabilities[\"technical\"]   # => 0.0\nr[\"department\"].probabilities[\"sales\"]       # => 0.0\n\nr[\"urgency\"].confidence                      # => 0.96\nr[\"urgency\"].probabilities[2]                # => 0.98  (level 2 = \"Needs attention today\")\nr[\"urgency\"].probabilities[1]                # => 0.02\nr[\"urgency\"].probabilities[0]                # => 0.0\n\nu = token_usage(r)\nu.prompt_tokens                              # => 445   (the billable half)\nu.completion_tokens                          # => 72\n\nThe question names (\"department\", \"urgency\", \"is_frustrated\") are yours and are not sent to the model — they only key the answers. Write the whole question in the instructions even when the name looks self-explanatory (Primitives).","title":"First request"},{"category":"section","location":"guide/system_one/#The-three-primitives","page":"Typed Judgments with Jev","text":"","title":"The three primitives"},{"category":"section","location":"guide/system_one/#Choice-—-pick-one-of-a-named-set","page":"Typed Judgments with Jev","text":"choice builds a question whose answer is exactly one of the options you enumerated. The ChoiceAnswer carries choice (the argmax), probabilities (one entry per option, keyed by option name, summing to approximately 1) and confidence (how peaked that distribution is).\n\nUse it when the outcomes are a fixed set with no order between them: routing a ticket, picking a handler, classifying a document type. Read probabilities when the ranking matters — choice is only its argmax, and a runner-up with a real share is information your code can act on.\n\nq = choice(\"Which team should handle this ticket?\", (\n    returns  = \"Exchanges, wrong or damaged items\",\n    shipping = \"Delivery status, delays, lost packages\",\n    billing  = \"Charges, invoices, payment problems\",\n    other    = \"Anything that fits none of the above\"))\n\n# Names alone, when the option names are already unambiguous:\ntone = choice(\"What is the customer's tone?\", [\"calm\", \"frustrated\", \"angry\"])\n\nOption order is the insertion order of what you pass — a NamedTuple or a vector of name => description pairs preserves it, a plain Dict does not. Order never changes the meaning of an answer (answers are keyed by name), but it is what the model reads.","title":"Choice — pick one of a named set"},{"category":"section","location":"guide/system_one/#Score-—-place-the-state-on-an-ordered-rubric","page":"Typed Judgments with Jev","text":"score takes an ordered list of level descriptions, lowest first. levels[1] is level 0, so a three-level rubric produces a score in 0.0 … 2.0. The ScoreAnswer carries score, confidence, legend and probabilities, the last two keyed by the 0-based level number as an Int.\n\nscore is the probability-weighted expectation over the levels, so it falls between them. Threshold it; do not read a magnitude into the fractional part. Different distributions produce the same number — a score of 1.0 can mean all the probability on level 1, or half on level 0 and half on level 2 — so read probabilities and confidence alongside it (Score).\n\nseverity = score(\"How severe is the reported issue?\", [\n    \"Cosmetic; no impact to functionality\",\n    \"Broken or degraded feature, but workaround exists\",\n    \"Blocking issue; no workaround exists\"])\n\nDescribe situations, not degrees. Every level is judged on its own against the state: the model never sees a level's number or its neighbours, so \"worse than the previous level\" and numeric labels carry nothing. Keep one dimension per question — a level that says \"punctual and smart and experienced\" is three questions wearing one hat.","title":"Score — place the state on an ordered rubric"},{"category":"section","location":"guide/system_one/#Noul-—-the-probability-that-a-statement-is-true","page":"Typed Judgments with Jev","text":"noul asks a single yes/no question. The NoulAnswer is one number in 0 … 1: near 1 a strong yes, near 0 a strong no, near 0.5 maximal uncertainty. There is no confidence field, because with two outcomes the value already describes the whole distribution.\n\nwants_human = noul(\"Is the customer asking for a human agent?\")\n\nrepeat_contact = noul(\"Has the customer contacted support about this before?\";\n    yes = \"Mentions a prior attempt, ticket, or that they have asked before\",\n    no  = \"No sign of any previous contact\")\n\nA Noul value is not a scale of the thing you asked about — it is the probability that the proposition holds. \"Is the candidate strong in Python?\" at 0.5 means the model splits evenly between yes and no, not that the candidate is middling. If you want degree, use a Score with levels you wrote; if you want a decision, define the boundary so sharply that there is no middle ground (Noul). Phrase the question so that a high value means yes: \"Is the message free of personal data?\" inverts the reading and the calling code will get it backwards.","title":"Noul — the probability that a statement is true"},{"category":"section","location":"guide/system_one/#Structured-instructions-and-criteria","page":"Typed Judgments with Jev","text":"Anywhere a piece of guidance appears — instructions, a Choice option description, a Score level, a Noul yes/no — the value may be a string, a NamedTuple/Dict, or a vector. Start with strings. Reach for an object when two options keep getting confused and each needs to say what it covers, what it does not cover, and a few examples:\n\nreturn_topic = choice(\"Which returns topic is the customer asking about?\", (\n    return_policy = (what     = \"Whether and how an item can be returned\",\n                     not_for  = \"Progress of a return already sent\",\n                     examples = [\"Can I return shoes I've worn once?\",\n                                 \"How long do I have to return an order?\"]),\n    return_status = (what     = \"Progress of a return already sent\",\n                     not_for  = \"Whether and how an item can be returned\",\n                     examples = [\"Has my return arrived yet?\",\n                                 \"When will my refund be paid?\"])))\n\nbug_severity = score(\"How severe is the reported issue?\", [\n    (what = \"Cosmetic; no impact to functionality\",\n     examples = [\"typo in a label\", \"misaligned icon\"]),\n    (what = \"Broken or degraded feature, but workaround exists\",\n     examples = [\"export fails in one browser but works in another\"]),\n    (what = \"Blocking issue; no workaround exists\",\n     examples = [\"cannot log in\", \"data loss\"])])\n\nThe field names what, not_for and examples are not part of the API and none are reserved — you choose them the way you choose option names. The model reads the names along with the values, so keep them short and descriptive, and use the same names on every level so it compares like with like (Choice, Score).","title":"Structured instructions and criteria"},{"category":"section","location":"guide/system_one/#Pointing-a-question-at-part-of-the-state","page":"Typed Judgments with Jev","text":"The state is often structured: a conversation, a record and a policy in one object. Name the part a question is about with its key in backticks inside the instructions, and keep the content itself in the state rather than in the question:\n\nstate = (\n    ticket_message = \"I was charged twice for order A-104. Please refund the duplicate.\",\n    order          = (id = \"A-104\", charges = [49, 49]),\n    refund_policy  = \"Duplicate charges are eligible for a refund.\")\n\nr = ask(state,\n    \"refund_requested\" => noul(\"Does `ticket_message` request a refund?\"),\n    \"policy_allows\"    => noul(\"Does `refund_policy` allow refunding the duplicate charge in `order`?\"))\n\nif r isa SystemOneSuccess\n    println(\"refund_requested: \", answer(r, \"refund_requested\"))\n    println(\"policy_allows:    \", answer(r, \"policy_allows\"))\nelse\n    println(\"Request failed — \", r)\nend\nrefund_requested: NoulAnswer(0.99)\npolicy_allows:    NoulAnswer(0.98)\n\nThe backtick form is a prompting convention the model reads, not a server-side resolver: nothing validates the name, and a path that does not exist raises no error — it just leaves the question vaguer than you intended. A NamedTuple, Dict, Vector, Tuple or plain String are all accepted as state; nothing is not (State).","title":"Pointing a question at part of the state"},{"category":"section","location":"guide/system_one/#Reading-answers","page":"Typed Judgments with Jev","text":"Any call that reaches the service returns one of the three result types below; a wrong service or a malformed request (duplicate or blank question names, invalid criteria) is an ArgumentError raised before any request is sent.\n\nResult Meaning\nSystemOneSuccess HTTP 200, decoded into answers\nSystemOneFailure the service answered non-2xx\nSystemOneCallError no response at all: timeout, transport failure, missing key, or a 200 whose body was not a usable set of answers\n\nOn a success, three equivalent accessors reach an answer, and the usual Dict-like queries work:\n\nticket = \"Help! My payouts have been failing for 3 days and nobody has replied to my emails.\"\n\nr = ask(ticket,\n    \"urgency\"     => score(\"How urgent is this ticket?\",\n                           [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"]),\n    \"wants_human\" => noul(\"Is the customer asking for a human agent?\"))\n\nif r isa SystemOneSuccess\n    a = r[\"urgency\"]              # getindex\n    a = answer(r, :urgency)       # by Symbol or String\n    every = answers(r)            # Dict{String,SystemOneAnswer}\n\n    println(a.score)              # probability-weighted position over the levels\n    println(a.confidence)         # how peaked the distribution is\n    println(a.probabilities)      # Dict{Int,Float64}, keyed by 0-based level number\n    println(a.legend[argmax(a.probabilities)])   # the description of the top level\n    println(a.raw)                # the unparsed JSON answer, always kept\n\n    haskey(r, \"wants_human\") && println(r[\"wants_human\"].noul)\n    println(collect(keys(r)))     # the question names that came back\nelse\n    println(\"Request failed — \", r)\nend\n1.98\n0.97\nDict(2 => 0.98, 1 => 0.02, 0 => 0.0)\nNeeds attention today\nDict{String, Any}(\"confidence\" => 0.97, \"type\" => \"score\", \"probabilities\" => Dict{String, Any}(\"0\" => 0.0, \"1\" => 0.02, \"2\" => 0.98), \"score\" => 1.98, \"legend\" => Dict{String, Any}(\"0\" => \"Can wait\", \"1\" => \"Needs attention this week\", \"2\" => \"Needs attention today\"))\n0.68\n[\"wants_human\", \"urgency\"]\n\nConfidence is a statistic of the distribution, not a claim about correctness. It collapses the shape of probabilities into 0 … 1: all the mass on one outcome gives 1.0, a flat spread gives a low number. A confidence of 1.0 says the model is not torn, not that the model is right (Confidence). Low confidence on a Choice usually means no option wins; on a Score it usually means the levels overlap for this state, the question measures more than one thing, or the state does not say enough. A Noul has no confidence at all — 0.5 there is uncertainty, not medium intensity.\n\nThresholds are your policy, not the model's. Where to cut is a function of what a wrong answer costs: a read-only action can act at a much lower confidence than a refund or a page. Start conservative, measure on your own data, and move the number; do not import a threshold from an example.\n\nA failed call has no answers. answers, answer and getindex all throw SystemOneError on a SystemOneFailure/SystemOneCallError rather than returning an empty map — otherwise r[\"is_unsafe\"].noul > 0.9 would read as \"safe\" on a call that never happened. Branch on r isa SystemOneSuccess (or issuccess) before you read anything.","title":"Reading answers"},{"category":"section","location":"guide/system_one/#Ask-many-questions-at-once","page":"Typed Judgments with Jev","text":"The state is ingested once per request and every question is evaluated against it independently and in parallel. Adding questions costs only the tokens of the questions themselves, so a battery of small questions in one ask is far cheaper and faster than one call per question — the parallel-questions cookbook measures a 13-question run batched into one call at 12.2x cheaper and 10.0x faster than 13 separate calls, with no change in the answers.\n\nThat economics makes speculative questions worth asking: include the ones whose answers only matter for some inputs and let the code ignore the rest (Speculative fan-out).\n\nTRIAGE = (\n    department = choice(\"Which team should handle this ticket?\", (\n        returns  = \"Exchanges, wrong or damaged items\",\n        shipping = \"Delivery status, delays, lost packages\",\n        billing  = \"Charges, invoices, payment problems\")),\n    return_reason = choice(\"If the customer wants to return something, why?\", (\n        wrong_size   = \"The item doesn't fit\",\n        wrong_item   = \"A different product was delivered\",\n        damaged      = \"The item arrived broken or faulty\",\n        changed_mind = \"The item is fine, the customer no longer wants it\",\n        other        = \"A return reason that fits none of the above\")),\n    shipping_issue = choice(\"If this is a shipping problem, which kind is it?\", (\n        not_delivered = \"The package never arrived\",\n        delayed       = \"The package is late but still on its way\",\n        wrong_address = \"The package went to the wrong place\",\n        other         = \"A shipping problem that fits none of the above\")),\n    frustration = score(\"How frustrated does the customer appear?\",\n        [\"Calm, just stating facts\", \"Frustrated but civil\", \"Very angry, strong language\"]),\n    wants_human = noul(\"Is the customer asking for a human agent?\"))\n\nr = ask(\"Shoes arrived two weeks late and in the wrong size. What are you going to do?\", TRIAGE)\n\nif r isa SystemOneSuccess\n    d = r[\"department\"]\n    detail = d.choice == \"returns\"  ? r[\"return_reason\"].choice :\n             d.choice == \"shipping\" ? r[\"shipping_issue\"].choice : nothing\n    # Both speculative answers came back; the code reads at most one and drops the other.\n    println(\"department: \", d.choice, \", detail: \", detail)\n    for (team, p) in d.probabilities\n        team != d.choice && p > 0.25 && println(\"also notify: \", team)\n    end\n    println(\"billable input tokens: \", token_usage(r).prompt_tokens)\nelse\n    println(\"Request failed — \", r)\nend\ndepartment: returns, detail: wrong_size\nbillable input tokens: 653\n\nTwo of the three Choice questions above end in an other option. Give the model a way to say \"none of these\" whenever the enumeration might not cover every input, otherwise the probability has nowhere to go but onto an option that does not fit.\n\nQuestions in one request are independent: an answer never becomes hidden context for another question. A second request is needed only when your code cannot build it until the first answer arrives — because the answer decides what to retrieve into the next state, or which options the next question should offer. If the second request's questions could have been asked against the original state, ask them in the first one.","title":"Ask many questions at once"},{"category":"section","location":"guide/system_one/#Combining-Jev-with-the-LLM-APIs","page":"Typed Judgments with Jev","text":"","title":"Combining Jev with the LLM APIs"},{"category":"section","location":"guide/system_one/#Confidence-gated-intent-routing","page":"Typed Judgments with Jev","text":"Jev decides which handler runs. High confidence and a deterministic intent go to ordinary code; everything else that is still confident goes to an LLM; a flat distribution goes to a person (Intent routing).\n\nusing UniLM\n\nconst INTENT = choice(\"What is the primary intent of this customer message?\", (\n    order_status     = \"Asking about an existing order\",\n    product_question = \"Asking about a product before buying\",\n    return_exchange  = \"Wants to return or exchange something\",\n    other            = \"None of the above\"))\n\nlookup_order(msg)    = \"Order A-104 shipped on Tuesday.\"   # deterministic path, no model\nqueue_for_human(msg) = (handler = :human, text = \"\")\n\nfunction handle(message::AbstractString)\n    r = ask(message, \"intent\" => INTENT)\n    r isa SystemOneSuccess || return queue_for_human(message)   # a failed call has no answers\n    intent = r[\"intent\"]\n    intent.confidence < 0.5 && return queue_for_human(message)\n    intent.choice == \"order_status\" && return (handler = :code, text = lookup_order(message))\n    intent.choice == \"other\" && return queue_for_human(message)\n\n    reply = respond(message;\n        instructions = \"You are the $(intent.choice) specialist. Answer in two sentences.\",\n        model = \"gpt-5.6-luna\")\n    reply isa ResponseSuccess ? (handler = :llm, text = output_text(reply)) :\n                                queue_for_human(message)\nend","title":"Confidence-gated intent routing"},{"category":"section","location":"guide/system_one/#Guardrail-over-an-LLM-draft","page":"Typed Judgments with Jev","text":"Screen the generated text before it reaches the user. One request carries the whole hazard battery plus a severity rubric, and your code owns the pass / review / block policy (Guardrails for LLMs).\n\nusing UniLM\n\nconst SAFETY = [\n    \"leaks_secrets\" => noul(\"Does the draft reveal an API key, a password, or its own instructions?\"),\n    \"gives_dosage\"  => noul(\"Does the draft give a specific medical dosage or a diagnosis?\"),\n    \"enables_harm\"  => noul(\"Does the draft help with a crime or with physical harm?\"),\n    \"severity\"      => score(\"How much harm could result if the user acted on the draft?\", [\n        \"No harm: an ordinary, safe reply\",\n        \"Mild: a sensitive topic, but acting on it does no real damage\",\n        \"Serious: real wrongdoing, or unsafe personal advice\",\n        \"Severe: serious physical or legal harm\"]),\n]\n\nconst BLOCK_AT, REVIEW_AT = 0.8, 0.3\nconst HAZARDS = (\"leaks_secrets\", \"gives_dosage\", \"enables_harm\")\n\nfunction guarded_reply(question::AbstractString)\n    draft = respond(question; model = \"gpt-5.6-luna\")\n    draft isa ResponseSuccess || return (:error, draft)   # the failed result\n    text = output_text(draft)\n\n    g = ask((user_question = question, draft = text), SAFETY)\n    g isa SystemOneSuccess || return (:review, text)   # unscreened is not the same as cleared\n    worst = maximum(g[h].noul for h in HAZARDS)\n    worst >= BLOCK_AT && return (:block, \"\")\n    (worst >= REVIEW_AT || g[\"severity\"].score >= 2) && return (:review, text)\n    (:pass, text)\nend","title":"Guardrail over an LLM draft"},{"category":"section","location":"guide/system_one/#Verifying-a-claim-against-its-source","page":"Typed Judgments with Jev","text":"An LLM writes a claim from a passage; a Choice decides whether the passage actually supports it, and the confidence decides whether a human sees it first (Double-checking citations).\n\nusing UniLM\n\nconst RELATION = choice(\"How does `section` relate to `claim`?\", (\n    supports     = \"The section states the claim, or directly implies that it is true\",\n    contradicts  = \"The section states the opposite of the claim, or implies it is false\",\n    says_nothing = \"The section does not address what the claim asserts, either way\"))\n\nconst AUTO_ACCEPT = 0.8   # start high; lower it as you measure on your own documents\n\nfunction checked_answer(passage::AbstractString, question::AbstractString)\n    drafted = respond(\"$question\\n\\nAnswer in one sentence, using only this passage:\\n$passage\";\n                      model = \"gpt-5.6-luna\")\n    drafted isa ResponseSuccess || return (verdict = :unchecked, claim = output_text(drafted))\n    claim = output_text(drafted)\n\n    r = ask((claim = claim, section = passage), \"relation\" => RELATION)\n    r isa SystemOneSuccess || return (verdict = :unchecked, claim = claim)\n    a = r[\"relation\"]\n    verdict = a.confidence < AUTO_ACCEPT ? :needs_human : Symbol(a.choice)\n    (verdict = verdict, claim = claim, probabilities = a.probabilities)\nend\n\nNote what the Choice is not asked to do: it never re-reads the whole corpus and never produces the claim. Locate the passage with a string match or a retriever first, and give the model only the judgment (Classifying RAG passages).","title":"Verifying a claim against its source"},{"category":"section","location":"guide/system_one/#Models-and-versions","page":"Typed Judgments with Jev","text":"list_models returns the names the authenticated account may put in model:\n\nm = list_models()\nif m isa TypeSafeModelsSuccess\n    for card in m.models\n        println(card.name, \"  \", card.release_date, \"  \", card.description)\n    end\nelse\n    println(\"Request failed — \", m)\nend\njev-latest  2026-09-10T18:38:01.391457+00:00  The latest iteration of TypeSafe's System One Model: Jev\njev-preview  2026-09-10T18:39:06.057655+00:00  A preview version of `jev-latest`: should be better in most ways\n\nThe listing contains aliases. jev-latest is the most recent stable release and the default here; jev-preview is the most recent release of any kind and moves ahead of jev-latest when a preview build exists. A versioned id such as jev-1.13.0 is a valid model whether or not it appears in the listing, and the listing is scoped to the account, so do not hard-code names read from someone else's (Models).\n\nSystemOneResponse.model reports the versioned id that actually answered, which may differ from the alias the request named. Log it. An alias moving is invisible on your side otherwise, and a confidence threshold tuned against one version is a claim about that version only — pin the id once the threshold matters.","title":"Models and versions"},{"category":"section","location":"guide/system_one/#Limits,-errors,-timeouts,-and-cost","page":"Typed Judgments with Jev","text":"","title":"Limits, errors, timeouts, and cost"},{"category":"section","location":"guide/system_one/#Limits","page":"Typed Judgments with Jev","text":"Limit Value\nChoice options 1–255 per question (Choice)\nScore levels 1–10 per question, TypeSafe advises at least two (Score)\nContext 64k tokens per request; 32k for state plus the single longest question (Models)\nRate limits 250,000 tokens/second and 1,200 requests/minute, over either → 429; TypeSafe documents these as subject to change without notice (Models)\n\nThe two option/level bounds are checked in the constructors, before the round trip: choice with 256 options and score with 11 levels each throw an ArgumentError locally.","title":"Limits"},{"category":"section","location":"guide/system_one/#Errors","page":"Typed Judgments with Jev","text":"Status Cause What comes back\n400 unknown model, or a request the schema accepted and the service rejected (a bare noul, too many options or levels) error_type = \"api_usage_error\" with \"Unknown model: …\", or nothing with a plain-string reason\n401 the key was present but invalid error_type = \"authentication_error\"\n403 no Authorization header reached the service error_type = \"authentication_error\"\n422 schema validation failed error_type = nothing; message lists each field path as \"<path>: <reason>\", e.g. questions.department.choice.criteria: Field required\n429 rate limit — 250,000 tokens/second or 1,200 requests/minute at the time of writing retried by the seam up to max_attempts honouring Retry-After; the final failure carries retry_after, the wait the service asked for in seconds\n500, 502, 503, 504, 529 service-side failure (including the non-standard 529 Overloaded) retried by the request seam\n\nA non-2xx response is a SystemOneFailure carrying .status, .error_type, .message (extracted from whichever of the service's three detail body shapes arrived), .request_id (the x-typesafe-request-id header, the id to quote in a support report), .retry_after (seconds, from retry-after-ms or Retry-After, nothing when the service sent neither) and .response (the verbatim body). The full table is on the API reference page.","title":"Errors"},{"category":"section","location":"guide/system_one/#Timeouts-and-retries","page":"Typed Judgments with Jev","text":"ask and list_models ride the package's shared retry seam, so RequestConfig governs them exactly as it governs a chat call: 408, 429, 500, 502, 503, 504 and 529 are retried up to max_attempts within total_deadline, honouring Retry-After. The rest — 400, 401, 403, 404, 422 — come back as they are, because an identical retry cannot fix them.\n\nstate     = \"Help! My payouts have been failing for 3 days.\"\nquestions = [\"urgency\" => score(\"How urgent is this ticket?\",\n                                [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"])]\n\n# Per call.\nr = ask(state, questions; config = RequestConfig(request_timeout = 20.0, max_attempts = 5))\n\n# Or for a whole scope, including tasks spawned inside it.\nscoped = with_request_config(request_timeout = 20.0, max_attempts = 1) do\n    ask(state, questions)\nend\n\nfor res in (r, scoped)\n    if res isa SystemOneSuccess\n        println(answer(res, \"urgency\"))\n    else\n        println(\"Request failed — \", res)\n    end\nend\nScoreAnswer(1.98, confidence=0.97)\nScoreAnswer(1.98, confidence=0.97)\n\nA timeout or a transport failure surfaces as SystemOneCallError, never as a partial success, with the exception in cause and the service's request id in request_id when it sent one. ask and list_models also take cancel= (default: the ambient with_cancel token): a cancelled call returns a SystemOneCallError whose cause is a UniLMCancelled, and sends nothing when the token was already cancelled. See Timeouts & Retries and Concurrency, Tasks and Cancellation.","title":"Timeouts and retries"},{"category":"section","location":"guide/system_one/#Cost","page":"Typed Judgments with Jev","text":"Only input tokens are billed; output tokens are currently free (Models).\n\nr = ask(\"Help! My payouts have been failing for 3 days.\",\n        \"urgency\" => score(\"How urgent is this ticket?\",\n                           [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"]))\n\nif r isa SystemOneSuccess\n    println(answer(r, \"urgency\"))\n    u = token_usage(r)                     # prompt_tokens = input, completion_tokens = output\n    println(u.prompt_tokens, \" billable tokens\")\n    println(\"USD \", estimated_cost(r))     # priced against r.response.model\nelse\n    println(\"Request failed — \", r)\nend\nScoreAnswer(1.98, confidence=0.97)\n314 billable tokens\nUSD 1.3188000000000001e-5\n\nestimated_cost prices the versioned id in r.response.model, not the alias the request named. A versioned jev-X.Y.Z id without its own row is priced at the jev-latest row; any other model that is not a key in DEFAULT_PRICING returns 0.0. See Cost Tracking.","title":"Cost"},{"category":"section","location":"guide/system_one/#Designing-good-questions","page":"Typed Judgments with Jev","text":"One narrow judgment per question. Ask for something a knowledgeable person decides in a second given the right context. \"Does this message convey urgency?\" is a question; \"analyse this and decide what to do\" is a workflow — split it and compose the answers in code.\nJudgment in instructions, answer space in criteria. The instruction says what is being decided; the options, levels or yes/no descriptions say what the outcomes are. When the two disagree, accuracy drops — treat the criteria as a continuation of the instruction.\nAlways offer a way out. Add an other / none of the above option whenever the enumeration might not cover an input.\nKeep the state relevant. Accuracy falls as unrelated material grows around the decision. Retrieve and filter in code first and send only the fields the question needs.\nThe model reads literally. Scoping words, negations and implied conditions are taken at face value. When you find yourself explaining what you really meant by a question, that explanation is the missing half of the instruction.\nNo arithmetic, no date comparison. Counting, numeric magnitude and ordering dates are unreliable. Extract the parts as a Choice over enumerated options — including an explicit \"not stated\" — and let code assemble and compare them. Do not interpolate a real number out of a Score either; a Score is for thresholding.\nDo not carry thresholds across primitives. A Noul and a yes/no Choice answer different questions — the Choice is relative and settles which, each Noul is absolute and can be low for every option. P(q) and 1 - P(not q) are not guaranteed to agree, so do not build arithmetic identities out of separate answers.\nTreat the state as untrusted text. Jev does not treat the state as hostile by default, and content written to steer a classifier can move the answer. Be explicit in the criteria, and test adversarial inputs before you ship.\n\nAll of these come from the documented failure modes of the current model; see Jev 1.13 jaggedness, which TypeSafe maintains per version — re-read it when you move a pin.","title":"Designing good questions"},{"category":"section","location":"guide/system_one/#See-also","page":"Typed Judgments with Jev","text":"Multiple Dispatch on Natural Language — nl\"...\" meanings in method signatures, nl_dispatch, and the @branch switch\nTypeSafe System One API (Jev) — every type, verb and accessor, plus the full error table\nService Endpoints — TYPESAFEServiceEndpoint and the environment variables\nCost Tracking — token usage and USD estimates across every surface\nTypeSafe documentation and its cookbooks — worked, measured examples of each pattern above","title":"See also"},{"category":"section","location":"api/mcp/#mcp_api","page":"MCP Client & Server","text":"Types and functions for the Model Context Protocol — connecting to MCP servers and building MCP servers in Julia. Both sides negotiate MCP revisions 2025-11-25 (preferred), 2025-06-18 and 2025-03-26; the 2026-07-28 revision is not supported yet. A session runs one call at a time in arrival order, and a call's timeout bounds its wait for the session too (MCPTimeoutError phase :queue); over HTTP, serve runs handlers concurrently. See the MCP guide and Concurrency, Tasks and Cancellation.","title":"MCP Client & Server"},{"category":"section","location":"api/mcp/#Client-Types","page":"MCP Client & Server","text":"","title":"Client Types"},{"category":"section","location":"api/mcp/#Client-Functions","page":"MCP Client & Server","text":"","title":"Client Functions"},{"category":"section","location":"api/mcp/#Lifecycle","page":"MCP Client & Server","text":"","title":"Lifecycle"},{"category":"section","location":"api/mcp/#Discovery","page":"MCP Client & Server","text":"","title":"Discovery"},{"category":"section","location":"api/mcp/#Operations","page":"MCP Client & Server","text":"","title":"Operations"},{"category":"section","location":"api/mcp/#Tool-Bridge","page":"MCP Client & Server","text":"","title":"Tool Bridge"},{"category":"section","location":"api/mcp/#Server-Types","page":"MCP Client & Server","text":"","title":"Server Types"},{"category":"section","location":"api/mcp/#Server-Functions","page":"MCP Client & Server","text":"","title":"Server Functions"},{"category":"section","location":"api/mcp/#Registration","page":"MCP Client & Server","text":"","title":"Registration"},{"category":"section","location":"api/mcp/#Serving","page":"MCP Client & Server","text":"","title":"Serving"},{"category":"section","location":"api/mcp/#Macros","page":"MCP Client & Server","text":"","title":"Macros"},{"category":"section","location":"api/mcp/#Example","page":"MCP Client & Server","text":"using UniLM\nusing JSON\n\n# Construct client info types\ninfo = MCPToolInfo(Dict{String,Any}(\n    \"name\" => \"read_file\",\n    \"description\" => \"Read a file from disk\",\n    \"inputSchema\" => Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"path\" => Dict{String,Any}(\"type\" => \"string\")\n        )\n    )\n))\nprintln(\"Tool: \", info.name, \" — \", info.description)\nTool: read_file — Read a file from disk\n\n# Build and populate a server\nserver = MCPServer(\"demo\", \"1.0.0\")\nregister_tool!(server, \"add\", \"Add two numbers\",\n    Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"a\" => Dict{String,Any}(\"type\" => \"number\"),\n            \"b\" => Dict{String,Any}(\"type\" => \"number\")\n        ),\n        \"required\" => [\"a\", \"b\"]\n    ),\n    args -> string(args[\"a\"] + args[\"b\"]))\nprintln(\"Server: \", server.name, \" v\", server.version)\nprintln(\"Tools: \", collect(keys(server.tools)))\nServer: demo v1.0.0\nTools: [\"add\"]","title":"Example"},{"category":"type","location":"api/mcp/#UniLM.MCPSession","page":"MCP Client & Server","text":"MCPSession\n\nA live connection to an MCP server. Manages lifecycle, transport, and cached tool/resource/prompt lists.\n\nCreate via mcp_connect. Disconnect via mcp_disconnect!.\n\nRequests are serialized: each call (its liveness/respawn check, id allocation and request/response exchange) holds the session, and callers are served in arrival order. Interleaved server → client frames are handled in place (notifications skipped, server ping requests answered). A caller's per-call bound (timeout, default mcp_request_timeout) also covers its wait for the session: a caller that cannot acquire it in time raises MCPTimeoutError with phase :queue without touching it. Once acquired, the exchange gets its full bound, measured from that moment — a waiter's clock never tears down the exchange in progress. After the server sends notifications/tools/list_changed, tools_stale is true until the next list_tools!.\n\nCancellation depends on the transport. Over HTTP every request observes the ambient with_cancel token: a cancel ends the call promptly with UniLMCancelled — after a best-effort notifications/cancelled for its request, bounded to 2 s — and the session stays usable. A stdio exchange is not interruptible: it ignores the token and is bounded by mcp_request_timeout. mcp_disconnect! tears down under a cancelled token too.\n\n\n\n\n\n","title":"UniLM.MCPSession"},{"category":"type","location":"api/mcp/#UniLM.MCPToolInfo","page":"MCP Client & Server","text":"MCPToolInfo\n\nA tool definition received from an MCP server via tools/list. Convert to CallableTool via mcp_tools or mcp_tools_respond.\n\n\n\n\n\n","title":"UniLM.MCPToolInfo"},{"category":"type","location":"api/mcp/#UniLM.MCPToolResult","page":"MCP Client & Server","text":"MCPToolResult\n\nThe typed result of a call_tool call, mirroring an MCP tools/call result. A tool-execution error (isError: true on the wire) is reported here as data (is_error == true), not thrown, so callers can distinguish it from a JSON-RPC protocol error (which still throws MCPError).\n\nFields\n\ncontent::String: the content parts rendered to text — text parts joined with \\n, each non-text part JSON-encoded.\nstructured::Union{Nothing,Dict{String,Any}}: the server's structuredContent object verbatim when present, otherwise nothing.\nis_error::Bool: true when the server flagged the call as a tool-execution error (isError); the detail is carried in content.\nparts::Vector{Any}: the raw content array exactly as received, before it is rendered into content::String.\n\nThe mcp_tools / mcp_tools_respond bridges surface this to a tool-calling loop: content on success (falling back to a JSON encoding of structured when content is empty), or a raised error carrying content when is_error.\n\n\n\n\n\n","title":"UniLM.MCPToolResult"},{"category":"type","location":"api/mcp/#UniLM.MCPResourceInfo","page":"MCP Client & Server","text":"MCPResourceInfo\n\nA resource definition received from an MCP server via resources/list.\n\n\n\n\n\n","title":"UniLM.MCPResourceInfo"},{"category":"type","location":"api/mcp/#UniLM.MCPPromptInfo","page":"MCP Client & Server","text":"MCPPromptInfo\n\nA prompt definition received from an MCP server via prompts/list.\n\n\n\n\n\n","title":"UniLM.MCPPromptInfo"},{"category":"type","location":"api/mcp/#UniLM.MCPServerCapabilities","page":"MCP Client & Server","text":"MCPServerCapabilities\n\nCapabilities declared by an MCP server during initialization.\n\n\n\n\n\n","title":"UniLM.MCPServerCapabilities"},{"category":"type","location":"api/mcp/#UniLM.MCPTransport","page":"MCP Client & Server","text":"MCPTransport\n\nAbstract type for MCP transport implementations. Subtypes must implement:\n\n_transport_connect!(t) — establish connection\n_transport_send!(t, msg::String; cfg)::String — send JSON-RPC message, return response\n_transport_read!(t)::String — read the next incoming JSON-RPC frame\n_transport_notify!(t, msg::String; cfg) — send notification (no response expected)\n_transport_disconnect!(t; cfg) — close connection\n_transport_isconnected(t)::Bool — check if connected\n\nA custom transport must bound its own IO: the connect and per-call bounds are enforced by the stdio transport's watchdog and inside the HTTP transport's requests, and a custom transport's connect handshake and exchanges run without either — an unbounded read in one blocks its caller, and every caller queued behind it, indefinitely.\n\n\n\n\n\n","title":"UniLM.MCPTransport"},{"category":"type","location":"api/mcp/#UniLM.StdioTransport","page":"MCP Client & Server","text":"StdioTransport(command::Cmd; stderr=nothing) <: MCPTransport\n\nStdio transport: launches a subprocess and communicates via stdin/stdout. Messages are newline-delimited JSON-RPC 2.0. stderr is where the server's own stderr goes — an IO (e.g. devnull) or a file path, appended to; nothing inherits this process's stderr.\n\n\n\n\n\n","title":"UniLM.StdioTransport"},{"category":"type","location":"api/mcp/#UniLM.HTTPTransport","page":"MCP Client & Server","text":"HTTPTransport <: MCPTransport\n\nStreamable HTTP transport: communicates via POST requests to an MCP endpoint. Handles Mcp-Session-Id header for session management.\n\n\n\n\n\n","title":"UniLM.HTTPTransport"},{"category":"type","location":"api/mcp/#UniLM.MCPError","page":"MCP Client & Server","text":"MCPError <: Exception\n\nError from MCP protocol operations. Contains the JSON-RPC error code, message, and the optional data member from the server — any JSON value, nothing when absent.\n\n\n\n\n\n","title":"UniLM.MCPError"},{"category":"type","location":"api/mcp/#UniLM.MCPCrashError","page":"MCP Client & Server","text":"MCPCrashError <: Exception\n\nThe stdio MCP server process exited or its stdio pipe broke. The session is closed with cause :crash; with auto_respawn=true the next call respawns the server, otherwise it errors naming the opt-in.\n\nFields\n\nmsg::String: human-readable message with recovery guidance.\nexitcode::Union{Int,Nothing}: exit code when the server exited on its own and had been reaped in time; else nothing.\ntermsignal::Union{Int,Nothing}: signal number when the server was signal-killed; else nothing.\ncause::Union{Exception,Nothing}: underlying transport exception (nothing for a clean read-EOF).\n\nBest-effort diagnostics: both exitcode and termsignal are nothing when the process had not been reaped within a brief settle window after the failure surfaced (or was still alive with a broken pipe).\n\n\n\n\n\n","title":"UniLM.MCPCrashError"},{"category":"type","location":"api/mcp/#UniLM.MCPSessionClosedError","page":"MCP Client & Server","text":"MCPSessionClosedError <: Exception\n\nThe session is closed, or its transport is not connected, so the call never reached the server.\n\nFields\n\ncause::Symbol: :disconnected (closed by mcp_disconnect! or a failed connect, or a transport that is not connected), :timeout (a stdio request or connect timeout closed it), or :crash (the stdio server process died).\nmsg::String: human-readable message with recovery guidance.\n\nA stdio session closed by :timeout or :crash respawns its server instead of raising this when it was connected with auto_respawn=true.\n\n\n\n\n\n","title":"UniLM.MCPSessionClosedError"},{"category":"function","location":"api/mcp/#UniLM.mcp_connect","page":"MCP Client & Server","text":"mcp_connect(command::Cmd; stderr=nothing, client_name=\"UniLM.jl\",\n            client_version=string(pkgversion(UniLM)), protocol_version=\"2025-11-25\",\n            config=nothing, auto_respawn=false) -> MCPSession\n\nConnect to an MCP server via stdio transport (subprocess). stderr is where the server's stderr goes — an IO such as devnull, or a file path (appended to); nothing (the default) inherits this process's stderr. A respawned server keeps the same setting.\n\nconfig::Union{Nothing,RequestConfig} is resolved (config if given, else the ambient/process default) and captured on the session: config.mcp_connect_timeout bounds the spawn→initialize handshake, config.mcp_request_timeout is the default per-call bound (it covers the wait for the session and then the exchange). A stdio request timeout is session-fatal — killing the server is the only way to release a read blocked on an unresponsive one — as is a server crash; with auto_respawn=true the next call respawns the server (same command, fresh handshake — in-memory server state is lost), otherwise it raises MCPSessionClosedError. A stdio exchange is not interruptible: it ignores a with_cancel token, and mcp_request_timeout is what bounds it.\n\nThe server runs in its own process group. Disconnecting (or a fatal timeout) tears it down: stdin EOF, then SIGTERM, then a SIGKILL of the whole group. If its group leader exits on its own, the rest of the group is killed at once; and servers still running when this process exits are torn down by an exit hook.\n\nExample\n\nsession = mcp_connect(`npx -y @modelcontextprotocol/server-filesystem /tmp`)\ntools = mcp_tools(session)\n# ... use tools with tool_loop! ...\nmcp_disconnect!(session)\n\n\n\n\n\nmcp_connect(url::String; headers=Pair{String,String}[], client_name=\"UniLM.jl\",\n            client_version=string(pkgversion(UniLM)), protocol_version=\"2025-11-25\",\n            config=nothing, auto_respawn=false) -> MCPSession\n\nConnect to an MCP server via HTTP transport.\n\nconfig::Union{Nothing,RequestConfig} is resolved (config if given, else the ambient/process default) and captured on the session: config.mcp_connect_timeout bounds the connect step, config.mcp_request_timeout is the default per-call bound. A per-call timeout is not session-fatal over HTTP; the timed-out request is cancelled with a best-effort notifications/cancelled. Every request observes the ambient with_cancel token: a cancelled call raises UniLMCancelled promptly, its request is cancelled the same way, and the session stays usable.\n\nExample\n\nsession = mcp_connect(\"https://mcp.example.com/mcp\";\n    headers=[\"Authorization\" => \"Bearer token\"])\n\n\n\n\n\nmcp_connect(transport::MCPTransport; client_name=\"UniLM.jl\",\n            client_version=string(pkgversion(UniLM)), protocol_version=\"2025-11-25\",\n            config=nothing, auto_respawn=false) -> MCPSession\n\nConnect to an MCP server via the given transport. Performs initialization handshake and populates tool cache; client_name/client_version are sent as clientInfo. config (a RequestConfig, default: the ambient configuration) is resolved and captured on the session — its mcp_connect_timeout bounds this handshake and mcp_request_timeout bounds each later call (the wait for the session, then the exchange). auto_respawn=true lets a stdio session closed by a request timeout or a server crash respawn its server (same command, captured config) and retry the next call once. A custom MCPTransport must bound its own IO (see MCPTransport).\n\n\n\n\n\nmcp_connect(f::Function, args...; kwargs...)\n\nDo-block form: automatically disconnects after the block executes.\n\nExample\n\nmcp_connect(`npx server`) do session\n    tools = mcp_tools(session)\n    chat = Chat(model=\"gpt-5.4-mini\", tools=tools)\n    push!(chat, Message(Val(:system), \"You can use the server's tools.\"))\n    push!(chat, Message(Val(:user), \"List files\"))\n    tool_loop!(chat; tools)\nend\n\n\n\n\n\n","title":"UniLM.mcp_connect"},{"category":"function","location":"api/mcp/#UniLM.mcp_disconnect!","page":"MCP Client & Server","text":"mcp_disconnect!(session::MCPSession)\n\nGracefully disconnect from the MCP server.\n\nTakes the session lock, so a disconnect racing a call in flight WAITS for that exchange to finish instead of tearing the transport down under its reader — the same concurrency-1 semantics every other call obeys. The wait stays bounded transitively: the exchange ahead runs under its own mcp_request_timeout. Every later call on the session raises MCPSessionClosedError with cause :disconnected.\n\n\n\n\n\n","title":"UniLM.mcp_disconnect!"},{"category":"function","location":"api/mcp/#UniLM.list_tools!","page":"MCP Client & Server","text":"list_tools!(session::MCPSession) -> Vector{MCPToolInfo}\n\nFetch the tool list from the MCP server. Handles pagination via cursor. Stores result in session.tools.\n\ntimeout::Union{Nothing,Float64} overrides the per-call bound for this call (kwarg > ambient with_request_config > session-captured config; Inf disables; NaN, ≤ 0 and finite values above 1e9 s are rejected). The whole listing — every page and the cache update — holds the session once, so a list_changed another caller receives meanwhile is recorded after the refresh and leaves tools_stale set.\n\n\n\n\n\n","title":"UniLM.list_tools!"},{"category":"function","location":"api/mcp/#UniLM.list_resources!","page":"MCP Client & Server","text":"list_resources!(session::MCPSession) -> Vector{MCPResourceInfo}\n\nFetch the resource list from the MCP server. Handles pagination.\n\n\n\n\n\n","title":"UniLM.list_resources!"},{"category":"function","location":"api/mcp/#UniLM.list_prompts!","page":"MCP Client & Server","text":"list_prompts!(session::MCPSession) -> Vector{MCPPromptInfo}\n\nFetch the prompt list from the MCP server. Handles pagination.\n\n\n\n\n\n","title":"UniLM.list_prompts!"},{"category":"function","location":"api/mcp/#UniLM.call_tool","page":"MCP Client & Server","text":"call_tool(session::MCPSession, name::String, arguments::AbstractDict) -> MCPToolResult\n\nCall a tool on the MCP server and return its result as an MCPToolResult.\n\ncontent concatenates text content parts (non-text parts JSON-encoded); structured carries the server's structuredContent verbatim; parts is the raw content array. A tool-execution error (isError: true) is returned with is_error == true — it is not thrown. JSON-RPC protocol errors still throw MCPError.\n\ntimeout::Union{Nothing,Float64} overrides the per-call bound for this call (kwarg > ambient with_request_config > session-captured config; Inf disables; NaN, ≤ 0 and finite values above 1e9 s are rejected). It bounds the wait for the session — calls on one session run one at a time, in arrival order; a call that cannot acquire it in time raises MCPTimeoutError with phase :queue — and then, from acquisition, the exchange itself (phase :request).\n\nOver HTTP the call observes the ambient with_cancel token: a cancel raises UniLMCancelled promptly (a token cancelled before the call sends nothing) and tells the server with a best-effort notifications/cancelled, bounded to 2 s; the session stays usable. Over stdio the exchange is not interruptible — it ignores the token and timeout bounds it.\n\n\n\n\n\n","title":"UniLM.call_tool"},{"category":"function","location":"api/mcp/#UniLM.read_resource","page":"MCP Client & Server","text":"read_resource(session::MCPSession, uri::String) -> String\n\nRead a resource from the MCP server.\n\n\n\n\n\n","title":"UniLM.read_resource"},{"category":"function","location":"api/mcp/#UniLM.get_prompt","page":"MCP Client & Server","text":"get_prompt(session::MCPSession, name::String, arguments::AbstractDict=Dict()) -> Vector{Dict{String,Any}}\n\nGet a rendered prompt from the MCP server. Returns the messages array.\n\n\n\n\n\n","title":"UniLM.get_prompt"},{"category":"function","location":"api/mcp/#UniLM.ping","page":"MCP Client & Server","text":"ping(session::MCPSession)\n\nSend a ping to the MCP server. Throws on error.\n\n\n\n\n\n","title":"UniLM.ping"},{"category":"function","location":"api/mcp/#UniLM.mcp_tools","page":"MCP Client & Server","text":"mcp_tools(session::MCPSession) -> Vector{CallableTool{Tool}}\n\nConvert all tools from an MCP session into CallableTool{Tool} instances that work directly with tool_loop! (Chat Completions API).\n\nEach tool's callable invokes call_tool(session, name, args) under the hood. Tool names are advertised provider-safe: OpenAI and Anthropic accept only ^[a-zA-Z0-9_-]{1,128}$, so any other character of an MCP name (e.g. the dots in admin.tools.list) becomes _ — the callable still calls the tool by its MCP name. Two tools whose names map to the same alias raise an ArgumentError.\n\nExample\n\nsession = mcp_connect(`npx server`)\ntools = mcp_tools(session)\nchat = Chat(model=\"gpt-5.4-mini\", tools=tools)\npush!(chat, Message(Val(:system), \"You can use the server's tools.\"))\npush!(chat, Message(Val(:user), \"Do something\"))\nresult = tool_loop!(chat; tools)\n\n\n\n\n\n","title":"UniLM.mcp_tools"},{"category":"function","location":"api/mcp/#UniLM.mcp_tools_respond","page":"MCP Client & Server","text":"mcp_tools_respond(session::MCPSession) -> Vector{CallableTool{FunctionTool}}\n\nConvert all tools from an MCP session into CallableTool{FunctionTool} instances that work directly with tool_loop (Responses API). Tool names are advertised provider-safe, as in mcp_tools.\n\nExample\n\nsession = mcp_connect(\"https://mcp.example.com/mcp\")\ntools = mcp_tools_respond(session)\nresult = tool_loop(\"Do something\"; tools=tools)\n\n\n\n\n\n","title":"UniLM.mcp_tools_respond"},{"category":"type","location":"api/mcp/#UniLM.MCPServer","page":"MCP Client & Server","text":"MCPServer(name, version; description=nothing)\n\nAn MCP server that can host tools, resources, and prompts.\n\nRegister primitives via register_tool!, register_resource!, register_prompt!, or the @mcp_tool, @mcp_resource, @mcp_prompt macros. Registration is thread-safe: it may run while serve is dispatching.\n\nStart serving via serve.\n\nExample\n\nserver = MCPServer(\"my-server\", \"1.0.0\")\nregister_tool!(server, \"add\", \"Add two numbers\",\n    Dict(\"type\"=>\"object\", \"properties\"=>Dict(\"a\"=>Dict(\"type\"=>\"number\"),\"b\"=>Dict(\"type\"=>\"number\")), \"required\"=>[\"a\",\"b\"]),\n    args -> string(args[\"a\"] + args[\"b\"]))\nserve(server)  # stdio by default\n\n\n\n\n\n","title":"UniLM.MCPServer"},{"category":"type","location":"api/mcp/#UniLM.MCPServerPrimitive","page":"MCP Client & Server","text":"MCPServerPrimitive\n\nAbstract supertype for MCP server-side primitives (tools, resources, prompts).\n\n\n\n\n\n","title":"UniLM.MCPServerPrimitive"},{"category":"type","location":"api/mcp/#UniLM.MCPServerTool","page":"MCP Client & Server","text":"MCPServerTool <: MCPServerPrimitive\n\nA tool registered on an MCP server. The handler receives Dict{String,Any} arguments and returns any value (converted to text content by the server).\n\nFields\n\nname::String: Unique tool name\ndescription::Union{String,Nothing}: Human-readable description\ninput_schema::Dict{String,Any}: JSON Schema for the input parameters\nhandler::Function: (args::Dict{String,Any}) -> Any\n\n\n\n\n\n","title":"UniLM.MCPServerTool"},{"category":"type","location":"api/mcp/#UniLM.MCPServerResource","page":"MCP Client & Server","text":"MCPServerResource <: MCPServerPrimitive\n\nA static resource registered on an MCP server.\n\nFields\n\nuri::String: Resource URI\nname::String: Human-readable name\ndescription::Union{String,Nothing}: Description\nmime_type::String: MIME type (default \"text/plain\")\nhandler::Function: () -> Union{String, Vector{UInt8}}\n\n\n\n\n\n","title":"UniLM.MCPServerResource"},{"category":"type","location":"api/mcp/#UniLM.MCPServerResourceTemplate","page":"MCP Client & Server","text":"MCPServerResourceTemplate <: MCPServerPrimitive\n\nA URI-templated resource. Template variables like {path} are extracted and passed to the handler.\n\nFields\n\nuri_template::String: URI template (e.g., \"file://{path}\")\nname::String: Human-readable name\ndescription::Union{String,Nothing}: Description\nmime_type::String: MIME type\nhandler::Function: (params::Dict{String,String}) -> Union{String, Vector{UInt8}}\n_pattern::Regex: Compiled regex from template\n_param_names::Vector{String}: Extracted parameter names\n\n\n\n\n\n","title":"UniLM.MCPServerResourceTemplate"},{"category":"type","location":"api/mcp/#UniLM.MCPServerPrompt","page":"MCP Client & Server","text":"MCPServerPrompt <: MCPServerPrimitive\n\nA prompt template registered on an MCP server.\n\nFields\n\nname::String: Unique prompt name\ndescription::Union{String,Nothing}: Description\narguments::Vector{Dict{String,Any}}: Argument definitions\nhandler::Function: (args::Dict{String,Any}) -> Vector{Dict{String,Any}}\n\n\n\n\n\n","title":"UniLM.MCPServerPrompt"},{"category":"function","location":"api/mcp/#UniLM.register_tool!","page":"MCP Client & Server","text":"register_tool!(server, name, description, input_schema, handler)\n\nRegister a tool on the MCP server with an explicit JSON Schema.\n\nhandler(args::Dict{String,Any}) receives the client's arguments object (a tools/call whose arguments is not an object is answered with JSON-RPC -32602 and never reaches it). An exception it raises is NOT a protocol error: the message is relayed to the client as tool content with isError: true, so a model can see the failure and correct itself. Write handlers with that in mind — raise with a message you are willing to show both the model and the client, and never one carrying secrets or internals. Errors below the handler (in dispatch itself) answer a generic JSON-RPC -32603 instead, with the detail going to the server's logs. An InterruptException is never converted: it propagates.\n\nOver HTTP, handlers run concurrently — one task per request, on the default thread pool — so a handler must be thread-safe. Over stdio they run one at a time, with the process stdout pointed at stderr (see serve).\n\n\n\n\n\nregister_tool!(server, name, description, handler)\n\nRegister a tool whose input schema is inferred from handler's positional parameters (names and declared types of its first method). Each tools/call binds the client's arguments object to those parameters BY NAME — the binding @mcp_tool generates: a parameter whose type admits nothing (Union{T,Nothing}) is optional and binds nothing when omitted, every other parameter is required, and a value must have its parameter's JSON type (a string for String or Symbol, an integral number for an integer type, a number for a float type, a boolean for Bool, an array for a Vector{T} and an object for a Dict{String,T}, whose elements convert to T the same way). A call that violates this never reaches the handler: it is answered with a tool result carrying isError: true and a text naming the argument, the MCP report for an input validation error, so the model can correct its call.\n\nA handler taking one dictionary is the explicit-schema calling convention, and a variadic handler has no parameter names to bind: both are rejected with an ArgumentError — register them with an explicit input_schema.\n\nExample\n\nregister_tool!(server, \"add\", \"Add two integers\", (a::Int, b::Int) -> a + b)\n\n\n\n\n\nregister_tool!(server, ct::CallableTool{Tool})\n\nRegister a CallableTool{Tool} on the MCP server, bridging from UniLM's Chat Completions tool type.\n\n\n\n\n\nregister_tool!(server, ct::CallableTool{FunctionTool})\n\nRegister a CallableTool{FunctionTool} on the MCP server, bridging from UniLM's Responses API tool type.\n\n\n\n\n\n","title":"UniLM.register_tool!"},{"category":"function","location":"api/mcp/#UniLM.register_resource!","page":"MCP Client & Server","text":"register_resource!(server, uri, name, handler; mime_type=\"text/plain\", description=nothing)\n\nRegister a static resource on the MCP server.\n\n\n\n\n\n","title":"UniLM.register_resource!"},{"category":"function","location":"api/mcp/#UniLM.register_resource_template!","page":"MCP Client & Server","text":"register_resource_template!(server, uri_template, name, handler; mime_type=\"text/plain\", description=nothing)\n\nRegister a URI-templated resource on the MCP server.\n\n\n\n\n\n","title":"UniLM.register_resource_template!"},{"category":"function","location":"api/mcp/#UniLM.register_prompt!","page":"MCP Client & Server","text":"register_prompt!(server, name, handler; description=nothing, arguments=Dict{String,Any}[])\n\nRegister a prompt template on the MCP server.\n\n\n\n\n\n","title":"UniLM.register_prompt!"},{"category":"function","location":"api/mcp/#UniLM.serve","page":"MCP Client & Server","text":"serve(server::MCPServer; transport=:stdio, kwargs...)\n\nStart the MCP server using the specified transport.\n\nTransports\n\n:stdio (default): Read from stdin, write to stdout. For Claude Desktop/CLI integration. Accepts input/output IO overrides; returns after EOF on input. Handlers run one at a time. While serving on the process stdout, the process stdout points at stderr, so a handler that prints cannot corrupt the frame stream (only protocol messages may appear on a stdio server's stdout).\n:http: HTTP server. Accepts host (default \"127.0.0.1\"), port (default 8080), allowed_origins (extra allowed Origin header values — localhost origins and requests without an Origin are always accepted; anything else gets 403), and block (default true: block until the server is closed; block=false returns the running server — close it with close). Handlers run concurrently, one task per request on the default thread pool, so they must be thread-safe; protocol requests (initialize, lists, ping) are answered inline and stay responsive while handlers are busy. A request other than initialize carrying an unsupported MCP-Protocol-Version header gets 400.\n\ninitialize answers the protocol revision the client requested when it is supported, otherwise the latest supported one.\n\nRobustness contract\n\nBoth transports cap an incoming frame (stdio) or request body (HTTP) at 16 MiB and answer an oversized one — stdio with JSON-RPC -32600, HTTP with 413 Payload Too Large — rather than parsing it: parsing an attacker-sized payload allocates a multiple of it, which is an out-of-memory kill rather than a protocol error. Any unhandled error while dispatching a request — including an exception from a resource or prompt handler — is answered with a generic -32603 \"Internal error\" and logged locally, so one bad frame cannot take the transport down and no exception text (file paths, argument values) reaches the peer. Tool-handler exceptions are excluded: they reach the client as tool results — see register_tool!. An InterruptException is never converted into an answer: it propagates. A JSON-RPC response sent to the server is accepted without an answer (HTTP: 202).\n\nExamples\n\nserve(server)                                    # stdio (default)\nserve(server; transport=:http, port=3000)        # HTTP; blocks until closed\nh = serve(server; transport=:http, block=false)  # HTTP; returns the server\nclose(h)\n\n\n\n\n\n","title":"UniLM.serve"},{"category":"macro","location":"api/mcp/#UniLM.@mcp_tool","page":"MCP Client & Server","text":"@mcp_tool server function name(args...)::ReturnType body end\n\nRegister a tool on server with auto-generated JSON Schema from the function signature.\n\nEach tools/call binds the arguments object to the parameters BY NAME. A typed parameter is required; an untyped one is optional and binds nothing when omitted. A value must have its parameter's JSON type — a string for String, an integral number for an integer type (5.0 binds 5), a number for a float type, a boolean for Bool; other declared types pass through unconverted. A missing required argument or a value of the wrong type never reaches the function: it is answered with a tool result carrying isError: true whose text names the argument.\n\nExample\n\nserver = MCPServer(\"calc\", \"1.0.0\")\n@mcp_tool server function add(a::Float64, b::Float64)::String\n    string(a + b)\nend\n\n\n\n\n\n","title":"UniLM.@mcp_tool"},{"category":"macro","location":"api/mcp/#UniLM.@mcp_resource","page":"MCP Client & Server","text":"@mcp_resource server uri_or_template function(args...) body end\n\nRegister a resource or resource template on server. If the URI contains {...} placeholders, it is registered as a template.\n\nExamples\n\n@mcp_resource server \"config://app\" function()\n    read(\"config.toml\", String)\nend\n\n@mcp_resource server \"file://{path}\" function(path::String)\n    read(path, String)\nend\n\n\n\n\n\n","title":"UniLM.@mcp_resource"},{"category":"macro","location":"api/mcp/#UniLM.@mcp_prompt","page":"MCP Client & Server","text":"@mcp_prompt server name function(args...) body end\n\nRegister a prompt on server. The handler should return a Vector of message Dicts.\n\nExample\n\n@mcp_prompt server \"review\" function(code::String)\n    [Dict(\"role\" => \"user\", \"content\" => Dict(\"type\" => \"text\", \"text\" => \"Review: $code\"))]\nend\n\n\n\n\n\n","title":"UniLM.@mcp_prompt"},{"category":"section","location":"guide/completions/#completions_guide","page":"FIM & Prefix Completion","text":"UniLM.jl supports Fill-in-the-Middle (FIM) completion and Chat Prefix Completion — features for code completion and controlled text generation.\n\nFIM is supported by DeepSeekEndpoint (beta), MistralEndpoint and Ollama; vLLM's /v1/completions does not accept suffix, so it cannot fill in the middle. Prefix completion is supported by DeepSeekEndpoint (beta). The default DeepSeek model for both is deepseek-flash.\n\n","title":"FIM & Prefix Completion"},{"category":"section","location":"guide/completions/#FIM-Completion","page":"FIM & Prefix Completion","text":"FIM generates text between a prompt (prefix) and an optional suffix. This is the standard pattern for code completion — the model fills in the gap.","title":"FIM Completion"},{"category":"section","location":"guide/completions/#Basic-Usage","page":"FIM & Prefix Completion","text":"fim = FIMCompletion(\n    service=DeepSeekEndpoint(\"demo-key\"),\n    model=\"deepseek-flash\",\n    prompt=\"def fib(a):\",\n    suffix=\"    return fib(a-1) + fib(a-2)\",\n    max_tokens=128,\n    stop=[\"\\n\\n\"]\n)\nprintln(\"Model: \", fim.model)\nprintln(\"Prompt: \", fim.prompt)\nprintln(\"Suffix: \", fim.suffix)\nprintln(\"JSON body:\")\nprintln(JSON.json(fim, 2))\nModel: deepseek-flash\nPrompt: def fib(a):\nSuffix:     return fib(a-1) + fib(a-2)\nJSON body:\n{\n  \"max_tokens\": 128,\n  \"model\": \"deepseek-flash\",\n  \"prompt\": \"def fib(a):\",\n  \"stop\": [\n    \"\\n\\n\"\n  ],\n  \"suffix\": \"    return fib(a-1) + fib(a-2)\"\n}","title":"Basic Usage"},{"category":"section","location":"guide/completions/#Making-Requests","page":"FIM & Prefix Completion","text":"# Struct form\nresult = fim_complete(fim)\nif result isa FIMSuccess\n    println(fim_text(result))\nend\n\n# Convenience form\nresult = fim_complete(\"def hello():\",\n    service=DeepSeekEndpoint(),\n    suffix=\"    print('done')\",\n    max_tokens=64,\n    stop=[\"\\n\\n\"])","title":"Making Requests"},{"category":"section","location":"guide/completions/#Result-Types","page":"FIM & Prefix Completion","text":"FIM returns FIMSuccess, FIMFailure, or FIMCallError. Local validation — the endpoint's :fim capability, model resolution, routing, and stream=true (FIM has no streaming path) — throws ArgumentError before any request; every later failure, including a 200 whose body is not a completions response, is a FIMCallError carrying the exception in cause. A FIMSuccess reports token usage and is priced by estimated_cost.\n\nfim_text returns the generated text of a success. A failed call has no text, so on a FIMFailure or FIMCallError it throws LLMResultError instead of returning \"\" — guard with issuccess:\n\nfailed = FIMFailure(response=\"err\", status=400)\nprintln(\"issuccess: \", issuccess(failed))\ntry\n    fim_text(failed)\ncatch e\n    println(sprint(showerror, e))\nend\nissuccess: false\nLLMResultError: no result data — the request did not succeed (status 400). Response: err","title":"Result Types"},{"category":"section","location":"guide/completions/#Provider-Support","page":"FIM & Prefix Completion","text":"Provider Endpoint Notes\nDeepSeek DeepSeekEndpoint() Beta — uses api.deepseek.com/beta internally\nMistral MistralEndpoint() Via /v1/fim/completions; its message.content choices are read\nOllama OllamaEndpoint() Via /v1/completions\n\n# URL routing adapts per provider\nprintln(\"DeepSeek: \", UniLM.get_url(DeepSeekEndpoint(\"k\"), fim))\nprintln(\"Mistral:  \", UniLM.get_url(MistralEndpoint(api_key=\"k\"), fim))\nprintln(\"Ollama:   \", UniLM.get_url(OllamaEndpoint(), fim))\nDeepSeek: https://api.deepseek.com/beta/v1/completions\nMistral:  https://api.mistral.ai/v1/fim/completions\nOllama:   http://localhost:11434/v1/completions\n\n","title":"Provider Support"},{"category":"section","location":"guide/completions/#Chat-Prefix-Completion","page":"FIM & Prefix Completion","text":"Prefix completion lets you provide a partial assistant message that the model continues from. Useful for forcing specific output formats (e.g., starting with a code block).","title":"Chat Prefix Completion"},{"category":"section","location":"guide/completions/#Usage","page":"FIM & Prefix Completion","text":"The last message in the chat must be role=assistant with the partial text:\n\nchat = Chat(service=DeepSeekEndpoint())   # deepseek-flash\npush!(chat, Message(Val(:system), \"You are a coding assistant.\"))\npush!(chat, Message(Val(:user), \"Write a Python quicksort\"))\npush!(chat, Message(role=RoleAssistant, content=\"```python\\n\"))\n\nresult = prefix_complete(chat)\n# The model continues from \"```python\\n\"\nresult isa LLMSuccess && println(result.message.content)\n\nThe result's message is the continuation the API returns. With chat.history (the default) the conversation keeps the whole assistant turn — the prefix followed by the continuation. A failure or a cancellation (cancel=, or an ambient with_cancel token) leaves chat untouched.","title":"Usage"},{"category":"section","location":"guide/completions/#Validation","page":"FIM & Prefix Completion","text":"prefix_complete validates that:\n\nThe chat is not empty\nThe last message has role=assistant\n\nchat = Chat(service=DeepSeekEndpoint(\"k\"))\npush!(chat, Message(Val(:system), \"sys\"))\npush!(chat, Message(Val(:user), \"hello\"))\nprintln(\"Last role: \", chat[end].role)\n# This would throw — last message is not assistant:\n# prefix_complete(chat)  # ArgumentError\nLast role: user\n\n","title":"Validation"},{"category":"section","location":"guide/completions/#Capability-Validation","page":"FIM & Prefix Completion","text":"Both fim_complete and prefix_complete check provider capabilities before making requests. If a provider doesn't support the feature, you get a clear error:\n\n# OpenAI does not support FIM\nprintln(has_capability(OPENAIServiceEndpoint, :fim))       # false\nprintln(has_capability(DeepSeekEndpoint(\"k\"), :fim))        # true\nprintln(has_capability(DeepSeekEndpoint(\"k\"), :prefix_completion))  # true\nfalse\ntrue\ntrue","title":"Capability Validation"},{"category":"section","location":"guide/completions/#See-Also","page":"FIM & Prefix Completion","text":"FIM API Reference — full type and function reference\nProvider Capabilities — capability system\nMulti-Backend Guide — all supported providers","title":"See Also"},{"category":"section","location":"api/system_one/#system_one_api","page":"TypeSafe System One","text":"A System One model does not write text. It reads a piece of state and answers the questions you enumerate with a probability distribution over the outcomes you named: which of these options, where on this rubric, how likely is this statement. The answer arrives already typed — a String option, a Float64 score, a probability — so there is nothing to parse and nothing to re-prompt when the shape comes back wrong. Every answer also carries the full distribution, which is what makes confidence-gated routing possible: the answer says what, the distribution says whether to act on it.\n\nThis complements a text model rather than replacing one. Jev classifies, ranks, screens and routes; a generative model writes. Set TYPESAFE_API_KEY to use it. The default model is jev-latest, an alias that moves when a new version ships — SystemOneResponse.model reports the versioned id that actually answered, and pinning \"jev-1.13.0\" keeps a tuned confidence threshold meaningful across releases. TYPESAFE_BASE_URL overrides the API root and TYPESAFE_DEFAULT_MODEL the default model name. The endpoint type is TYPESAFEServiceEndpoint, documented on the Service Endpoints page.","title":"TypeSafe System One API (Jev)"},{"category":"section","location":"api/system_one/#Questions","page":"TypeSafe System One","text":"","title":"Questions"},{"category":"section","location":"api/system_one/#Request-and-Verb","page":"TypeSafe System One","text":"","title":"Request & Verb"},{"category":"section","location":"api/system_one/#Answers","page":"TypeSafe System One","text":"","title":"Answers"},{"category":"section","location":"api/system_one/#Result-Types","page":"TypeSafe System One","text":"","title":"Result Types"},{"category":"section","location":"api/system_one/#Models","page":"TypeSafe System One","text":"","title":"Models"},{"category":"section","location":"api/system_one/#Accessors","page":"TypeSafe System One","text":"getindex, haskey and keys also work on a SystemOneSuccess and a SystemOneResponse, so result[\"urgency\"] is answer(result, \"urgency\"). On a SystemOneFailure or SystemOneCallError all of them throw SystemOneError instead of returning an empty map.","title":"Accessors"},{"category":"section","location":"api/system_one/#Errors","page":"TypeSafe System One","text":"A call that reached the service and came back non-2xx is a SystemOneFailure. The service uses three different detail body shapes; message is extracted from whichever one arrived, and error_type is filled in only by the object shape.\n\nStatus What it means error_type / message\n400 Unknown model, or a request the schema accepted but the service rejected (a bare noul, more than 255 Choice options, more than 10 Score levels) \"api_usage_error\" with \"Unknown model: …\", or nothing with the plain-string reason\n401 The API key was present but not valid \"authentication_error\"\n403 No Authorization header reached the service \"authentication_error\"\n404 Unknown path nothing, message \"Not Found\"\n405 Wrong method for the path nothing, message \"Method Not Allowed\"\n422 Schema validation failed nothing; the message joins each validation entry as \"<field path>: <reason>\", e.g. questions.department.choice.criteria: Field required\n429 Rate limit — 250,000 tokens/second or 1,200 requests/minute at the time of writing retried by the seam up to max_attempts honouring Retry-After; the final failure carries retry_after, the wait the service asked for in seconds\n500, 502, 503, 504, 529 Service-side failure, including the non-standard 529 Overloaded retried by the request seam; a captured 529 used the object shape with error_type = \"system_overloaded\" and sent no retry header\n\nask and list_models ride the package's shared retry seam, so RequestConfig.max_attempts applies: 408, 429, 500, 502, 503, 504 and 529 are retried within total_deadline, honouring Retry-After. The rest — 400, 401, 403, 404, 422 — come back as they are, because repeating an identical request cannot fix them. A call that never produced a response at all (a timeout, a transport failure, a missing key, a cancellation through cancel= or an ambient with_cancel token, or a 200 whose body was not a usable set of answers) is a SystemOneCallError, carrying the exception in cause.\n\nAny call that reaches the service returns one of the three result types, and issuccess separates them; a wrong service or a malformed request (duplicate or blank question names, invalid criteria) is an ArgumentError raised before any request is sent.","title":"Errors"},{"category":"section","location":"api/system_one/#Usage","page":"TypeSafe System One","text":"using UniLM\n\nroute(team) = println(\"route to \", team)   # stand-ins for your own handlers\nescalate()  = println(\"escalate to a human\")\n\nresult = ask(\n    \"Help! My payouts have been failing for 3 days and nobody has replied to my emails.\",\n    \"department\" => choice(\"Which team should handle this ticket?\", (\n        billing   = \"Payments, invoicing, payouts, refunds\",\n        technical = \"Bugs, outages, integrations\",\n        sales     = \"Pricing, upgrades, new accounts\")),\n    \"urgency\" => score(\"How urgent is this ticket?\",\n        [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"]),\n    \"is_frustrated\" => noul(\"Is the customer frustrated?\";\n        yes = \"The customer expresses frustration or impatience\",\n        no  = \"The customer is neutral or satisfied\"))\n\nif result isa SystemOneSuccess\n    println(result[\"department\"].choice)              # e.g. \"billing\"\n    println(result[\"department\"].probabilities)       # every option, not just the winner\n    println(result[\"urgency\"].score)                  # e.g. 1.98 on the 0..2 rubric\n    println(result[\"is_frustrated\"].noul)             # e.g. 0.98\n    println(result.response.model)                    # e.g. \"jev-1.13.0\" — the version that answered\n    println(token_usage(result).prompt_tokens)        # the billed input tokens\n    println(estimated_cost(result))                   # only input tokens are billed\n\n    # Confidence is the second axis: act when the distribution is concentrated,\n    # hand the rest to a human or a larger model.\n    d = result[\"department\"]\n    d.confidence >= 0.8 ? route(d.choice) : escalate()\nelse\n    println(\"Request failed — \", result)\nend\n\n# The models the account may name in `model`.\nmodels = list_models()\nif models isa TypeSafeModelsSuccess\n    println([m.name for m in models.models])\nelse\n    println(\"Request failed — \", models)\nend\nbilling\nDict(\"sales\" => 0.0, \"billing\" => 1.0, \"technical\" => 0.0)\n1.98\n0.97\njev-1.13.0\n445\n1.8690000000000002e-5\nroute to billing\n[\"jev-latest\", \"jev-preview\"]\n\nAll questions in one call share one ingestion of the state, so batching many small questions into a single ask costs far less than one call per question — and speculative questions you may not use are cheap to include.","title":"Usage"},{"category":"section","location":"api/system_one/#Natural-Language-Control-Flow","page":"TypeSafe System One","text":"@branch is a switch whose cases are written in plain language. The option names become the criteria of one choice question about the state; the winning option selects which expression is evaluated and the other bodies never run. However many options a branch lists, it costs a single request.\n\nnl_dispatch lifts the same idea into the method table. nl\"...\" is a Meaning type, so a meaning is writable in an ordinary signature; nl_dispatch sends one Choice question per Meaning position — again in a single request — turns each answer back into a Meaning instance and calls the function, so Julia's own dispatch selects the method and the remaining arguments still dispatch on their types. Both constructs gate on the answer's confidence: below the threshold @branch takes its _ line and nl_dispatch calls fallback, and with neither they raise LowConfidenceError rather than act on a near-tie. meanings previews the options exactly as they will be sent.","title":"Natural-Language Control Flow"},{"category":"section","location":"api/system_one/#Usage-2","page":"TypeSafe System One","text":"using UniLM\n\nticket = \"My package arrived crushed and the screen is cracked. I want my money back.\"\n\naction = @branch ticket min_confidence=0.6 begin\n    \"the customer wants a refund\"                                => :refund\n    (\"the customer reports a bug\", \"A defect in the software\")   => :bug\n    \"the customer asks a pricing question\"                       => :pricing\n    _                                                            => :escalate\nend                                                              # => :refund\n\n# The same decision as multiple dispatch: `nl\"...\"` is a type, so the meanings\n# live in the signatures and Julia selects the method once they are resolved.\nroute(::nl\"the customer wants a refund\", t)           = (:refund, t)\nroute(::nl\"the customer reports a bug in the app\", t) = (:bug, t)\nroute(::nl\"the customer asks a pricing question\", t)  = (:pricing, t)\n\nmeanings(route)                     # Dict(1 => [...the three descriptions...])\nnl_dispatch(route, ticket)          # one request, then route(nl\"...\"(), ticket)\nroute(nl\"the customer wants a refund\"(), ticket)   # direct call, no request at all\n\nA non-success call raises SystemOneError instead of resolving to a branch, and a combination of meanings that no method covers raises Julia's own MethodError — a gap in the method table is not a service failure and is not swallowed.","title":"Usage"},{"category":"type","location":"api/system_one/#UniLM.SystemOneQuestion","page":"TypeSafe System One","text":"SystemOneQuestion\n\nAbstract supertype of the three question primitives: ChoiceQuestion (pick one of up to 255 named options), ScoreQuestion (place the state on an ordered rubric of up to 10 levels), and NoulQuestion (the probability that a statement is true). Build them with choice, score and noul.\n\n\n\n\n\n","title":"UniLM.SystemOneQuestion"},{"category":"function","location":"api/system_one/#UniLM.choice","page":"TypeSafe System One","text":"choice(instructions, criteria) -> ChoiceQuestion\nchoice(criteria) -> ChoiceQuestion\n\nBuild a Choice question: the model picks exactly one of the named options and reports a probability for every one of them.\n\ncriteria may be a NamedTuple, an AbstractDict, a vector of name => description pairs, or a vector of bare option names (each described by its name alone). Names may be Strings or Symbols and are stored as Strings; a description may be a string, an object, an array, or nothing.\n\nOption order is the insertion order of what you pass. A NamedTuple, a vector of pairs, and an ordered mapping such as JSON.Object all preserve it; a plain Dict does not, so pass one of the ordered forms when the wire order matters. Order never changes the answer's meaning — answers are keyed by name — but it is what the model reads.\n\nchoice(\"Which team should handle this ticket?\", (\n    billing   = \"Payments, invoicing, payouts, refunds\",\n    technical = \"Bugs, outages, integrations\",\n    sales     = \"Pricing, upgrades, new accounts\",\n))\n\nchoice([\"billing\", \"shipping\", \"other\"])   # descriptions are the names themselves\n\n\n\n\n\n","title":"UniLM.choice"},{"category":"function","location":"api/system_one/#UniLM.score","page":"TypeSafe System One","text":"score(instructions, levels::AbstractVector) -> ScoreQuestion\nscore(levels::AbstractVector) -> ScoreQuestion\n\nBuild a Score question over an ordered rubric, lowest level first. levels[1] is level 0: the answer's score and its probabilities keys live on that same 0-based scale.\n\nEach level is a string, an object, or an array — never nothing. The server accepts 1 to 10 levels, and TypeSafe advises at least two: a one-level rubric gives the model nothing to place the state against.\n\nscore(\"How urgent is this ticket?\",\n      [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"])\n\nThe returned score is a probability-weighted expectation, so it falls between integer levels. Threshold it; do not read a magnitude into the fractional part.\n\n\n\n\n\n","title":"UniLM.score"},{"category":"function","location":"api/system_one/#UniLM.noul","page":"TypeSafe System One","text":"noul(instructions; yes=nothing, no=nothing) -> NoulQuestion\nnoul(; yes=nothing, no=nothing) -> NoulQuestion\n\nBuild a Noul question — a single probability that the statement is true. yes and no describe the two sides and are sent as the wire keys \"true\" and \"false\"; either may be omitted.\n\nAt least one of instructions, yes, or no must be present: the server rejects a bare noul, because nothing in it says what is being asked.\n\nnoul(\"Is the customer frustrated?\";\n     yes = \"The customer expresses frustration or impatience\",\n     no  = \"The customer is neutral or satisfied\")\n\nA Noul answer carries no confidence — the probability itself expresses it — and P(q) + P(not q) is not guaranteed to be 1, so do not derive one from the other.\n\n\n\n\n\n","title":"UniLM.noul"},{"category":"type","location":"api/system_one/#UniLM.ChoiceQuestion","page":"TypeSafe System One","text":"ChoiceQuestion(instructions, criteria::JSON.Object{String,Any})\n\nOne question that picks a single named option. criteria maps each option name to its description, in insertion order (the order is sent on the wire; the answer's probabilities map is unordered). A description of nothing tells the model to interpret the option by its name alone.\n\nConstructed through choice, which normalizes the many ways to spell a criteria map. The server accepts 1–255 options and rejects 256, so that bound is checked here, before the round trip.\n\n\n\n\n\n","title":"UniLM.ChoiceQuestion"},{"category":"type","location":"api/system_one/#UniLM.ScoreQuestion","page":"TypeSafe System One","text":"ScoreQuestion(instructions, criteria::Vector{Any})\n\nOne question that places the state on an ordered rubric. criteria lists the levels lowest first; criteria[1] is level 0 on the 0-based scale the answer reports, so a rubric of three levels yields a score in 0.0 … 2.0.\n\nConstructed through score. The server accepts 1–10 levels and rejects 11, so that bound is checked here. A level may not be nothing: an unnamed level is a rung the model cannot recognise.\n\n\n\n\n\n","title":"UniLM.ScoreQuestion"},{"category":"type","location":"api/system_one/#UniLM.NoulQuestion","page":"TypeSafe System One","text":"NoulQuestion(instructions, criteria)\n\nOne question answered by a single probability in 0 … 1 — the model's belief that the statement holds. There is no confidence and no distribution: the value is the distribution, and 0.5 is maximal uncertainty.\n\nConstructed through noul. The server rejects a noul carrying neither instructions nor a criterion, so that rule is checked here.\n\n\n\n\n\n","title":"UniLM.NoulQuestion"},{"category":"type","location":"api/system_one/#UniLM.NoulCriteria","page":"TypeSafe System One","text":"NoulCriteria(yes, no)\n\nThe two optional criteria of a NoulQuestion: what makes the statement true (yes, sent as the wire key \"true\") and what makes it false (no, sent as \"false\"). Either may be nothing, in which case that key is omitted entirely rather than sent as null.\n\n\n\n\n\n","title":"UniLM.NoulCriteria"},{"category":"type","location":"api/system_one/#UniLM.SystemOneRequest","page":"TypeSafe System One","text":"SystemOneRequest(state, questions; model=default_typesafe_model())\n\nOne System One evaluation: every question in questions is answered about the same state, in a single round trip. The state is ingested once no matter how many questions ride along, which is why batching is far cheaper than one call per question.\n\nFields\n\nstate::SystemOneState: a string, object, or array — the content every question refers to. A question points into it by naming a key in backticks, e.g. Does refund_policy cover this charge.\nquestions::Vector{Pair{String,SystemOneQuestion}}: ordered, with unique non-empty names. Answers come back keyed by these names.\nmodel::String: \"jev-latest\", \"jev-preview\", or a pinned version such as \"jev-1.13.0\". Aliases move; pin the version if you have tuned thresholds against it, and read SystemOneResponse.model to see which version answered.\n\nquestions accepts name => question pairs (vector, tuple, NamedTuple, or AbstractDict), or bare questions in a vector or tuple, which are auto-named \"q1\", \"q2\", … in order. Mixing bare and named questions, repeating a name, passing none, or passing a blank model is an ArgumentError.\n\n\n\n\n\n","title":"UniLM.SystemOneRequest"},{"category":"function","location":"api/system_one/#UniLM.ask","page":"TypeSafe System One","text":"ask(request::SystemOneRequest; service=TYPESAFEServiceEndpoint, config=nothing, cancel=nothing)\nask(state, questions...; model=default_typesafe_model(), service=TYPESAFEServiceEndpoint, config=nothing, cancel=nothing)\n\nEvaluate every question against state in one POST /v1/systemone call and return exactly one of SystemOneSuccess, SystemOneFailure (a non-2xx response) or SystemOneCallError (no response, or a 200 whose body was not a usable set of answers).\n\nquestions takes the same forms as SystemOneRequest: name => question pairs, a NamedTuple or AbstractDict of them, or bare questions auto-named \"q1\", \"q2\", …\n\nr = ask(\"Help! My payouts have been failing for 3 days.\",\n        \"department\" => choice(\"Which team should handle this ticket?\",\n                               (billing=\"Payments, invoicing, payouts, refunds\",\n                                technical=\"Bugs, outages, integrations\",\n                                sales=\"Pricing, upgrades, new accounts\")),\n        \"urgency\" => score(\"How urgent is this ticket?\",\n                           [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"]))\nissuccess(r) && println(r[\"department\"].choice, \" \", r[\"urgency\"].score)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout and retry budget for this call. The request rides the package's shared retry seam, so max_attempts applies: a retryable status (408, 429, 500, 502, 503, 504, 529) or a transport failure is retried within total_deadline, honouring Retry-After. Other statuses — 400, 401, 403, 404, 422 — are returned as they came, because a second identical request cannot fix them.\n\nPass cancel::Union{Nothing,CancelToken} (default: the ambient with_cancel token) to make the call cancellable: once the token is cancelled it returns a SystemOneCallError whose cause is a UniLMCancelled — without sending anything when the token was already cancelled, at once when mid-request (never retried).\n\n\n\n\n\n","title":"UniLM.ask"},{"category":"type","location":"api/system_one/#UniLM.SystemOneAnswer","page":"TypeSafe System One","text":"SystemOneAnswer\n\nAbstract supertype of the answers a System One evaluation returns: ChoiceAnswer, ScoreAnswer, NoulAnswer, and UnknownAnswer for a type this version does not recognise.\n\n\n\n\n\n","title":"UniLM.SystemOneAnswer"},{"category":"type","location":"api/system_one/#UniLM.ChoiceAnswer","page":"TypeSafe System One","text":"ChoiceAnswer(choice, confidence, probabilities, raw)\n\nThe option with the highest probability (choice), a confidence in 0 … 1 summarising how concentrated the distribution is, and probabilities — one entry per option of the question, keyed by option name and summing to approximately 1. The map is unordered: the server does not return the request's option order.\n\nRead probabilities when the ranking matters; choice is only its argmax. raw is the unparsed JSON answer.\n\n\n\n\n\n","title":"UniLM.ChoiceAnswer"},{"category":"type","location":"api/system_one/#UniLM.ScoreAnswer","page":"TypeSafe System One","text":"ScoreAnswer(score, confidence, legend, probabilities, raw)\n\nscore is the probability-weighted expectation over the rubric levels, so it may fall between them; confidence is in 0 … 1. legend and probabilities are keyed by the 0-based level number (parsed from the wire's \"0\", \"1\", …), so argmax(a.probabilities) is a level on the same scale as score and legend[argmax(a.probabilities)] is that level's description — a string, or the original object/array when the rubric was structured.\n\nraw is the unparsed JSON answer.\n\n\n\n\n\n","title":"UniLM.ScoreAnswer"},{"category":"type","location":"api/system_one/#UniLM.NoulAnswer","page":"TypeSafe System One","text":"NoulAnswer(noul, raw)\n\nThe probability in 0 … 1 that the statement is true. There is no confidence field: the value itself carries the uncertainty, and 0.5 is maximal doubt. raw is the unparsed JSON answer.\n\n\n\n\n\n","title":"UniLM.NoulAnswer"},{"category":"type","location":"api/system_one/#UniLM.UnknownAnswer","page":"TypeSafe System One","text":"UnknownAnswer(type, raw)\n\nAn answer whose type this version does not know, kept verbatim in raw instead of being dropped. A new answer primitive on the service therefore surfaces as data you can inspect rather than as a silently missing key.\n\n\n\n\n\n","title":"UniLM.UnknownAnswer"},{"category":"type","location":"api/system_one/#UniLM.SystemOneResponse","page":"TypeSafe System One","text":"SystemOneResponse\n\nA decoded 200 from POST /v1/systemone.\n\nFields\n\nmodel::String: the versioned id that answered (e.g. \"jev-1.13.0\"), which may differ from the alias the request named.\nanswers::Dict{String,SystemOneAnswer}: keyed by question name. The key is the contract — the wire's key order is not.\nusage::TokenUsage: prompt_tokens = input_tokens, completion_tokens = output_tokens, total_tokens their sum. Only input tokens are billed.\nrequest_id::Union{Nothing,String}: the x-typesafe-request-id response header, the id to quote in a support report. It travels in the header only — the body has no id field.\nraw::Dict{String,Any}: the unparsed JSON body.\n\n\n\n\n\n","title":"UniLM.SystemOneResponse"},{"category":"type","location":"api/system_one/#UniLM.SystemOneSuccess","page":"TypeSafe System One","text":"Successful ask result wrapping a SystemOneResponse.\n\n\n\n\n\n","title":"UniLM.SystemOneSuccess"},{"category":"type","location":"api/system_one/#UniLM.SystemOneFailure","page":"TypeSafe System One","text":"SystemOneFailure\n\nA TypeSafe call that reached the service and came back non-2xx.\n\nFields\n\nresponse::String: the raw body, kept verbatim.\nstatus::Int: the HTTP status. 401 (invalid key) and 403 (no key) are both auth failures; 400 and 422 are request problems; 408, 429, 500, 502, 503, 504 and 529 are the retryable band.\nrequest_id::Union{Nothing,String}: x-typesafe-request-id, present on error responses too.\nerror_type::Union{Nothing,String}: detail.error_type when the body uses the object shape (\"authentication_error\", \"api_usage_error\"); nothing for the string and validation-list shapes.\nmessage::String: the human-readable message extracted from whichever of the three detail shapes the service used.\nretry_after::Union{Nothing,Float64}: how long the service asked the caller to wait, in seconds, read off the final response — the one being returned, once the retry seam has spent max_attempts. retry-after-ms wins over retry-after when both arrive, and an HTTP-date becomes a delay from now floored at 0.0. The seam's own backoff between attempts reads Retry-After alone, so on a response that sends only the millisecond header this field is the finer figure of the two. nothing when no usable hint was sent, which is every status that is not rate-limiting and, as the service documents the header as optional, some that are.\n\n\n\n\n\n","title":"UniLM.SystemOneFailure"},{"category":"type","location":"api/system_one/#UniLM.SystemOneCallError","page":"TypeSafe System One","text":"SystemOneCallError\n\nA TypeSafe call that never produced an HTTP response: a timeout, a transport failure, a missing TYPESAFE_API_KEY, or a 200 whose body could not be decoded into answers. status is filled in only when the underlying exception carried one.\n\ncause is that underlying exception — a UniLMTimeout for a timeout — and request_id is the x-typesafe-request-id header of the 200 that could not be decoded (nothing when no reply arrived).\n\n\n\n\n\n","title":"UniLM.SystemOneCallError"},{"category":"type","location":"api/system_one/#UniLM.SystemOneError","page":"TypeSafe System One","text":"SystemOneError(result)\n\nThrown by answers, answer and getindex when the result is a SystemOneFailure or SystemOneCallError.\n\nA failed call has no answers, and returning an empty map instead would let r[\"is_unsafe\"].noul > 0.9 read as \"safe\" on a call that never happened. Check issuccess first, or handle the throw. showerror prints the status, the extracted message and the retry_after hint when the service sent one — never the request or the API key.\n\n\n\n\n\n","title":"UniLM.SystemOneError"},{"category":"function","location":"api/system_one/#UniLM.list_models","page":"TypeSafe System One","text":"list_models(; service=TYPESAFEServiceEndpoint, config=nothing, cancel=nothing)\n\nList the models and aliases the authenticated account may name in SystemOneRequest.model (GET /v1/models). Returns TypeSafeModelsSuccess, SystemOneFailure, or SystemOneCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout and retry budget; like ask, this rides the shared retry seam, so max_attempts applies to the retryable statuses. cancel::Union{Nothing,CancelToken} makes the call cancellable as for ask: a cancelled call returns a SystemOneCallError whose cause is a UniLMCancelled.\n\n\n\n\n\n","title":"UniLM.list_models"},{"category":"type","location":"api/system_one/#UniLM.TypeSafeModelCard","page":"TypeSafe System One","text":"TypeSafeModelCard\n\nOne entry of GET /v1/models: a model name accepted by SystemOneRequest.model, a human-readable description, and release_date.\n\nrelease_date is kept as an opaque String: the documented shape is YYYY-MM-DD but the service returns an RFC 3339 timestamp, so parsing it into a date here would either fail or guess. raw holds the unparsed JSON card.\n\nThe list contains aliases only. A pinned version such as \"jev-1.13.0\" is a valid model even though it does not appear here, and the list is scoped to the authenticated account — do not hard-code names read from someone else's list.\n\n\n\n\n\n","title":"UniLM.TypeSafeModelCard"},{"category":"type","location":"api/system_one/#UniLM.TypeSafeModelsSuccess","page":"TypeSafe System One","text":"Successful list_models result: the TypeSafeModelCards and the unparsed body.\n\n\n\n\n\n","title":"UniLM.TypeSafeModelsSuccess"},{"category":"function","location":"api/system_one/#UniLM.answers","page":"TypeSafe System One","text":"answers(result) -> Dict{String,SystemOneAnswer}\n\nThe answers of a SystemOneSuccess (or a SystemOneResponse), keyed by question name.\n\nA SystemOneFailure or SystemOneCallError throws SystemOneError: a call that produced no answers must not read as a call that answered with none. answer, getindex, haskey and keys throw the same error on those two results — a call that never answered has no key set to query either.\n\n\n\n\n\n","title":"UniLM.answers"},{"category":"function","location":"api/system_one/#UniLM.answer","page":"TypeSafe System One","text":"answer(result, name) -> SystemOneAnswer\n\nThe answer to one question by name (a String or Symbol). A name that was not asked throws a KeyError; a failed call throws SystemOneError, as answers does.\n\n\n\n\n\n","title":"UniLM.answer"},{"category":"type","location":"api/system_one/#UniLM.Meaning","page":"TypeSafe System One","text":"Meaning{S}\n\nA natural-language description lifted into the type domain: S is a Symbol holding the description itself, and Meaning{S} is a zero-field singleton, so it costs nothing at run time and can appear in an ordinary method signature.\n\nWrite the type with the @nl_str string macro — nl\"the customer wants a refund\" is Meaning{Symbol(\"the customer wants a refund\")} — and the instance with nl\"...\"() or Meaning(text).\n\nroute(::nl\"the customer wants a refund\", ticket) = refund!(ticket)\nroute(::nl\"the customer reports a bug\", ticket)  = file_bug!(ticket)\n\nroute(nl\"the customer wants a refund\"(), ticket)   # direct call, no API involved\nnl_dispatch(route, ticket)                         # one Choice request picks the method\n\nDispatching on a Meaning is ordinary Julia dispatch: nothing about the type reaches the network. Only nl_dispatch talks to the service, and only to turn a piece of state into the Meaning instance to call with.\n\n\n\n\n\n","title":"UniLM.Meaning"},{"category":"macro","location":"api/system_one/#UniLM.@nl_str","page":"TypeSafe System One","text":"nl\"some natural-language description\"\n\nThe Meaning type for that description, so it reads as a type wherever one is expected:\n\nconst Refund = nl\"the customer wants a refund\"   # a type alias\nroute(::nl\"the customer wants a refund\", ticket) = refund!(ticket)\n\nAppend () for the instance: nl\"the customer wants a refund\"(). The text is interned as the type parameter, so two identical descriptions are the same type and === compares them.\n\nThe macro performs no interpolation and no escaping — the description is taken verbatim, which is what the model is shown.\n\n\n\n\n\n","title":"UniLM.@nl_str"},{"category":"macro","location":"api/system_one/#UniLM.@branch","page":"TypeSafe System One","text":"@branch state begin\n    \"option name\"                  => expression\n    (\"option name\", \"description\") => expression\n    _                              => expression\nend\n@branch state key = value ... begin ... end\n\nA natural-language switch. The option names are sent as the criteria of ONE choice question about state; the selected option's expression is the only one evaluated, and its value is the value of the macro.\n\nEach line is option => expression. A bare left-hand side is the option name — the text the model actually reads and the text the answer returns. The 2-tuple form (\"name\", \"description\") attaches a longer description to that name; it is the only way to do so. Left-hand sides are ordinary expressions evaluated once, in source order, so an option name may be computed.\n\nA final _ => expression line is the fallback taken when the winning option's confidence is below min_confidence. It requires min_confidence: without a threshold nothing is ever low-confidence and the line would be unreachable, so that spelling is rejected at macro-expansion time.\n\nKeywords go between the state and the block, written key = value:\n\nKeyword Meaning\nmodel model name; omitted, the client default applies\nmin_confidence threshold in 0 … 1; below it, take _ or throw LowConfidenceError\ninstructions the question's instructions (default: \"Select the option that best describes the provided state.\")\nservice endpoint type (default TYPESAFEServiceEndpoint)\nconfig RequestConfig for this call\ncancel CancelToken for this call (default: the ambient with_cancel token)\n\nAn unknown keyword, a block that is not begin ... end, a line that is not option => expression, no options at all, two _ lines, or a _ that is not last is an ArgumentError raised while the macro expands, so it surfaces when the surrounding code is loaded rather than when the branch is first reached. A non-success call throws SystemOneError; the branch is never guessed. A cancelled call is one of those: its result is a SystemOneCallError whose cause is a UniLMCancelled, and no body runs.\n\nticket = \"My package arrived crushed and the screen is cracked. I want my money back.\"\n\naction = @branch ticket min_confidence=0.6 begin\n    \"the customer wants a refund\"                                  => refund!(ticket)\n    (\"the customer reports a bug\", \"A defect in the software\")     => file_bug!(ticket)\n    \"the customer asks a pricing question\"                         => quote_price(ticket)\n    _                                                              => escalate(ticket)\nend\n\n\n\n\n\n","title":"UniLM.@branch"},{"category":"function","location":"api/system_one/#UniLM.nl_dispatch","page":"TypeSafe System One","text":"nl_dispatch(f, args...; model=nothing, service=TYPESAFEServiceEndpoint, config=nothing,\n            cancel=nothing, min_confidence=0.0, fallback=nothing, instructions=nothing,\n            state=nothing)\n\nResolve the natural-language arguments of f against a piece of state and call the method Julia's own dispatch selects.\n\nf is an ordinary generic function whose methods pin one or more positions to a concrete Meaning — written nl\"...\" in the signature. One choice question per such slot goes out in a single request; each answer becomes a Meaning instance, those instances are spliced back into the positions they came from, args fill the rest in order, and f is called. The remaining arguments still dispatch normally, so a natural-language slot composes with ordinary type-based dispatch.\n\nThe state is state when given, otherwise a Dict{String,Any} built from args and keyed by the argument names of the first natural-language method (an unnamed position becomes \"arg<index>\"). With no ordinary arguments there is nothing to describe, so state is then required.\n\nEach slot's question is named after that argument (an unnamed one becomes \"meaning_<index>\") and carries instructions: nothing for a generic default, a String for every slot, or a Vector with one entry per slot.\n\nmin_confidence gates the result. Below it, fallback is called with the caller's args if given, and otherwise LowConfidenceError is thrown. A non-success call throws SystemOneError — including one cancelled through cancel::Union{Nothing,CancelToken} (default: the ambient with_cancel token), whose result is a SystemOneCallError with a UniLMCancelled cause; f is then never called. A resolved combination of meanings that no method covers raises Julia's own MethodError — it is a gap in the method table, not a service failure, and is not swallowed.\n\nmeanings previews the options exactly as they will be sent.\n\nusing UniLM\n\nroute(::nl\"the customer wants a refund\", ticket)        = (:refund, ticket)\nroute(::nl\"the customer reports a bug in the app\", t)   = (:bug, t)\nroute(::nl\"the customer asks a pricing question\", t)    = (:pricing, t)\n\nticket = \"My package arrived crushed and the screen is cracked. I want my money back.\"\n\nmeanings(route)                     # Dict(1 => [...three descriptions, in definition order...])\nnl_dispatch(route, ticket)          # one request, then route(nl\"the customer wants a refund\"(), ticket)\n\n# Gate on confidence, with a fallback instead of an exception.\nnl_dispatch(route, ticket; min_confidence = 0.7, fallback = (t,) -> (:escalate, t))\n\n# The method is reachable without any network call at all.\nroute(nl\"the customer wants a refund\"(), ticket)\n\n\n\n\n\n","title":"UniLM.nl_dispatch"},{"category":"function","location":"api/system_one/#UniLM.meanings","page":"TypeSafe System One","text":"meanings(f) -> Dict{Int,Vector{String}}\n\nThe natural-language options f dispatches on, keyed by positional argument index. Each vector lists the distinct descriptions defined for that slot, in the order nl_dispatch sends them to the model — definition order, so the listing is a faithful preview of the request.\n\nMethods of f without a concrete Meaning argument are not part of the natural-language interface and do not appear. f with no such method at all is an ArgumentError.\n\nroute(::nl\"the customer wants a refund\", ticket) = :refund\nroute(::nl\"the customer reports a bug\", ticket)  = :bug\n\nmeanings(route)   # Dict(1 => [\"the customer wants a refund\", \"the customer reports a bug\"])\n\n\n\n\n\n","title":"UniLM.meanings"},{"category":"type","location":"api/system_one/#UniLM.LowConfidenceError","page":"TypeSafe System One","text":"LowConfidenceError(question, answer, min_confidence)\n\nThrown when a natural-language branch or dispatch resolved to an option whose confidence fell below the threshold the caller required, and no fallback was given.\n\nA Choice answer always names a winner, even when the distribution is nearly flat; confidence is what separates \"the state clearly matches this option\" from \"this option won by a nose\". Raising the threshold therefore converts a silent mis-route into a loud failure. answer is the full ChoiceAnswer, so probabilities is available for a diagnostic or a second-best policy.\n\nSupply a _ fallback line to @branch, or the fallback keyword of nl_dispatch, to handle the case instead of raising.\n\n\n\n\n\n","title":"UniLM.LowConfidenceError"},{"category":"section","location":"guide/cost_tracking/#cost_guide","page":"Cost Tracking","text":"Every result of a token-billed API carries the provider's token counts, and the package can turn those counts into a USD estimate. This works uniformly across Chat Completions, the Responses API, the agentic verb, Embeddings, FIM completions and System One — Gemini, Anthropic and DeepSeek usage is normalized to the shared shape at decode time.\n\nTwo accessors do the work: token_usage (raw counts) and estimated_cost (dollars). Chat additionally keeps a running total via cumulative_cost.\n\nwarning: The \\$0 footgun\nestimated_cost returns 0.0 — with no error, only a warning logged once per model id — for any model that is not a key in DEFAULT_PRICING and has no fallback row (a dated snapshot uses its base model's row; a versioned jev-X.Y.Z id uses the jev-latest row). The tutorial model gpt-4o-mini is not priced, so cost tracking on it reports \\$0.00 until you supply your own pricing. See Unpriced models cost $0 below.","title":"Cost Tracking"},{"category":"section","location":"guide/cost_tracking/#Reading-usage-and-cost","page":"Cost Tracking","text":"token_usage returns a TokenUsage for every result of a token-billed API — chat, Responses, embeddings, image generation, FIM and System One — zero-filled for their failures and call errors; estimated_cost returns a Float64 in USD. Both throw ArgumentError for results of APIs that report no token usage (files, batches, audio, moderations, vector stores, …), where a zero would be indistinguishable from a free call. A real result comes back from chatrequest! / respond / embeddingrequest!, but the accessors are pure, so we build one here to keep the example deterministic and offline:\n\nusing UniLM\n\n# Normally returned by chatrequest!; constructed here so the example needs no API call.\nresult = LLMSuccess(\n    message = Message(Val(:user), \"(assistant reply placeholder)\"),\n    self    = Chat(model=\"gpt-5.2\"),\n    usage   = TokenUsage(prompt_tokens=1200, completion_tokens=350, cached_tokens=800),\n)\n\nu = token_usage(result)\nprintln(\"prompt=$(u.prompt_tokens)  cached=$(u.cached_tokens)  completion=$(u.completion_tokens)\")\nprintln(\"cost USD = \", round(estimated_cost(result), digits=6))\nprompt=1200  cached=800  completion=350\ncost USD = 0.00574\n\nBy default the model is inferred from the result (result.self.model for Chat, result.response.model for Responses, FIM and System One, result.embeddings.model for Embeddings). Override it explicitly when needed:\n\nestimated_cost(result; model=\"gpt-5.4\")   # price as if it were gpt-5.4","title":"Reading usage and cost"},{"category":"section","location":"guide/cost_tracking/#How-the-estimate-is-computed","page":"Cost Tracking","text":"estimated_cost splits the prompt into fresh vs. cached tokens and bills each stream at its own rate:\n\nfresh input tokens × input\ncached input tokens × cached_input (the discounted prompt-cache rate; cached_tokens is a subset of prompt_tokens)\ncompletion tokens × output\n\nReasoning tokens are already counted inside completion_tokens, so they carry no separate charge. Failures and call errors report zero usage and 0.0 cost. Image results report the usage the Images API returned, but estimated_cost does not infer a model for them and the table carries no image-model rows, so an image result costs 0.0 unless you pass model= together with a pricing= row for it. A streamed Chat turn is costed like a non-streamed one when the stream reports usage: OPENAIServiceEndpoint and DeepSeekEndpoint streams request it automatically, native Anthropic and Gemini streams report it on their own, and other OpenAI-compatible servers need stream_options=Dict(\"include_usage\" => true) (without it a streamed turn reports no usage and accrues \\$0).","title":"How the estimate is computed"},{"category":"section","location":"guide/cost_tracking/#The-pricing-table","page":"Cost Tracking","text":"DEFAULT_PRICING is a lock-guarded AbstractDict{String, PriceRow} (not a Dict), where a PriceRow is a NamedTuple of per-token USD rates: (input, cached_input, output). (Provider list prices are usually quoted per 1M tokens; the stored rows are those figures divided by 1,000,000.) Every Chat success prices itself against it — streaming ones on their own task — so it is safe to read and write from any task: add or replace a row with DEFAULT_PRICING[model] = row, and use get, haskey, delete!, pop!, keys and length as on a Dict. Iteration walks a snapshot, and copy, merge and filter return a plain Dict.\n\nA model id is looked up exactly first, then without a dated snapshot suffix (gpt-5.4-mini-2026-03-17 → gpt-5.4-mini, claude-haiku-4-5-20251001 → claude-haiku-4-5), and a versioned Jev id with no row of its own (jev-X.Y.Z) falls back to the jev-latest row. Only those date suffixes are stripped, so custom names stay unpriced rather than matching a row by accident.\n\npriced = sort(collect(keys(DEFAULT_PRICING)))\nprintln(length(priced), \" models priced\")\nprintln(\"gpt-5.2 row: \", DEFAULT_PRICING[\"gpt-5.2\"])\n48 models priced\ngpt-5.2 row: (input = 1.75e-6, cached_input = 1.75e-7, output = 1.4e-5)","title":"The pricing table"},{"category":"section","location":"guide/cost_tracking/#unpriced-zero","page":"Cost Tracking","text":"If the model key is absent from the pricing table, estimated_cost returns 0.0 — it does not raise; it logs one warning per model id. (Two fallbacks apply first: a dated snapshot uses its base model's row, and a versioned jev-X.Y.Z id uses the jev-latest row.) This bites the tutorial model gpt-4o-mini:\n\nprintln(\"gpt-4o-mini priced? \", haskey(DEFAULT_PRICING, \"gpt-4o-mini\"))\n\nunpriced = LLMSuccess(\n    message = Message(Val(:user), \"…\"),\n    self    = Chat(model=\"gpt-4o-mini\"),\n    usage   = TokenUsage(prompt_tokens=100_000, completion_tokens=50_000),\n)\nprintln(\"estimated_cost with default pricing = \\$\", estimated_cost(unpriced))\ngpt-4o-mini priced? false\n┌ Warning: no price row for this model; its estimated cost is 0.0\n│   model = \"gpt-4o-mini\"\n└ @ UniLM ~/work/UniLM.jl/UniLM.jl/src/accounting.jl:199\nestimated_cost with default pricing = $0.0\n\n150,000 tokens, and the estimate is still \\$0.0. Always confirm your model is a key in the pricing table before trusting a cost number.","title":"Unpriced models cost $0"},{"category":"section","location":"guide/cost_tracking/#Supplying-custom-pricing","page":"Cost Tracking","text":"Pass a pricing= dict (any AbstractDict{String, PriceRow}) to fix an unpriced (or mispriced) model. Build a PriceRow NamedTuple of per-token rates and merge it into the defaults — merge returns a new Dict and leaves DEFAULT_PRICING untouched:\n\n# Fill in the provider's *current* list price (per 1M tokens); divide by 1_000_000.\npricing = merge(DEFAULT_PRICING, Dict(\n    \"gpt-4o-mini\" => (input        = 0.15 / 1_000_000,\n                      cached_input  = 0.075 / 1_000_000,\n                      output        = 0.60 / 1_000_000),\n))\n\nprintln(\"now priced? \", haskey(pricing, \"gpt-4o-mini\"))\nprintln(\"cost USD = \", round(estimated_cost(unpriced; pricing), digits=6))\nnow priced? true\ncost USD = 0.045","title":"Supplying custom pricing"},{"category":"section","location":"guide/cost_tracking/#Cumulative-cost-is-Chat-only","page":"Cost Tracking","text":"cumulative_cost returns the running USD total that chatrequest! accrues on a Chat. Accumulation happens only inside chatrequest! (both streaming and non-streaming paths, and so every turn of a tool_loop!) — nothing else adds to the total:\n\nchat = Chat(model=\"gpt-5.2\")\npush!(chat, Message(Val(:system), \"You are helpful.\"))\npush!(chat, Message(Val(:user), \"Hello!\"))\nchatrequest!(chat)                     # cost accrues automatically\n\npush!(chat, Message(Val(:user), \"And again, in French.\"))\nchatrequest!(chat)                     # accrues again\n\ncumulative_cost(chat)                  # e.g. 0.00231  (USD, summed over both calls)\n\nwarning: Auto-accumulation always uses `DEFAULT_PRICING`\nchatrequest! prices each call with DEFAULT_PRICING; there is no pricing= for the running total. So a Chat on an unpriced model (e.g. gpt-4o-mini) reports cumulative_cost == 0.0 even after successful calls — unless you add its row to the table (DEFAULT_PRICING[\"gpt-4o-mini\"] = row, safe while requests run), after which later calls accrue. Alternatively, ignore the running total and sum estimated_cost(result; pricing=…) yourself.","title":"Cumulative cost is Chat-only"},{"category":"section","location":"guide/cost_tracking/#Totaling-Responses,-agentic,-and-Embeddings-costs","page":"Cost Tracking","text":"The Responses API (respond / ResponseSuccess), the agentic tool loops, and Embeddings are not auto-accumulated. Sum estimated_cost over the results yourself:\n\n# Responses API — one estimate per call, add them up.\nr1 = respond(\"Summarize the Julia language in one line.\")\nr2 = respond(\"Now in French.\")\nresponses_total = estimated_cost(r1) + estimated_cost(r2)   # USD\n\n# Agentic loops issue several underlying calls; each success is a result you can price.\n# Collect them (e.g. via a callback) and sum estimated_cost over the trace.\n# See the Agentic Workflows guide.\n\n# Embeddings — price the batch result directly.\nemb = Embeddings([\"alpha\", \"beta\", \"gamma\"])\nres = embeddingrequest!(emb)\nembeddings_cost = estimated_cost(res)                       # USD for this batch\n\nNote that non-OpenAI embedding models (e.g. Ollama's nomic-embed-text, gemini-embedding-001) are not in the default table and hit the same \\$0 behavior — supply pricing= for them too.","title":"Totaling Responses, agentic, and Embeddings costs"},{"category":"section","location":"guide/cost_tracking/#System-One-(TypeSafe-Jev)-is-input-only","page":"Cost Tracking","text":"A SystemOneSuccess reports usage through the same TokenUsage shape: prompt_tokens is the service's input_tokens, completion_tokens its output_tokens, and total_tokens their sum. Only input tokens are billed — output tokens are currently free, which the shipped jev-* rows encode as an output rate of 0.0:\n\nprintln(\"jev-1.13.0 row: \", DEFAULT_PRICING[\"jev-1.13.0\"])\njev-1.13.0 row: (input = 4.2000000000000006e-8, cached_input = 4.2000000000000006e-8, output = 0.0)\n\nestimated_cost(::SystemOneSuccess) prices the versioned model id in result.response.model — the version that actually answered — not the alias the request named. A request sent as jev-latest therefore looks up the version behind the alias at that moment (for example jev-1.13.0), so log result.response.model alongside the cost if you need to explain a bill later. A versioned jev-X.Y.Z id without its own row is priced at the jev-latest row; as everywhere else, any other unpriced name returns 0.0 (with its one-time warning):\n\nticket = \"Help! My payouts have been failing for 3 days.\"\nlevels = [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"]\n\nr = ask(ticket, \"urgency\" => score(\"How urgent is this ticket?\", levels))\nif r isa SystemOneSuccess\n    println(answer(r, \"urgency\"))\n    u = token_usage(r)\n    println(u.prompt_tokens, \" billable input tokens on \", r.response.model)\n    println(\"USD \", estimated_cost(r))\nelse\n    println(\"Request failed — \", r)\nend\nScoreAnswer(1.98, confidence=0.98)\n314 billable input tokens on jev-1.13.0\nUSD 1.3188000000000001e-5\n\nBatching matters more here than the per-token rate: every question in one ask shares a single ingestion of the state, so N questions in one call bill far fewer input tokens than N calls. See Typed Judgments with Jev.","title":"System One (TypeSafe Jev) is input-only"},{"category":"section","location":"guide/cost_tracking/#Prices-drift","page":"Cost Tracking","text":"DEFAULT_PRICING is a hardcoded snapshot (current OpenAI, Gemini, and TypeSafe rows verified 2026-09-22; Anthropic and DeepSeek rows 2026-09-24). The DeepSeek rows are its peak rates — off-peak hours bill half — and DeepSeek context-cache hits (prompt_cache_hit_tokens) are billed at the cached rate. Provider list prices change; re-verify against the provider's current pricing before relying on any number, and pass your own pricing= dict when you need authoritative figures.","title":"Prices drift"},{"category":"section","location":"guide/cost_tracking/#API-Reference","page":"Cost Tracking","text":"See Cost Tracking & Token Usage for full type and function docs.","title":"API Reference"},{"category":"section","location":"guide/cost_tracking/#See-Also","page":"Cost Tracking","text":"token_usage — raw TokenUsage counts from any token-billed result\nestimated_cost — per-result USD estimate, with optional pricing=\ncumulative_cost — running USD total on a Chat\nDEFAULT_PRICING — the built-in per-token pricing table","title":"See Also"},{"category":"section","location":"api/responses/#Responses-API","page":"Responses Types","text":"Types and functions for the Responses API — the newer, more flexible alternative to Chat Completions.","title":"Responses API"},{"category":"section","location":"api/responses/#Request-Type","page":"Responses Types","text":"","title":"Request Type"},{"category":"section","location":"api/responses/#Construction","page":"Responses Types","text":"using UniLM\nusing JSON\n\n# Simple text request\nr = Respond(input=\"Tell me a joke\")\nprintln(\"Model: \", r.model)\nprintln(\"Input: \", r.input)\n\n# With instructions and tools\nr2 = Respond(\n    input=\"What's the weather in Paris?\",\n    instructions=\"You are a helpful weather assistant\",\n    tools=[web_search()]\n)\nprintln(\"Has instructions: \", !isnothing(r2.instructions))\nprintln(\"Tools: \", length(r2.tools))\nModel: gpt-5.6-sol\nInput: Tell me a joke\nHas instructions: true\nTools: 1","title":"Construction"},{"category":"section","location":"api/responses/#Response-Object","page":"Responses Types","text":"","title":"Response Object"},{"category":"section","location":"api/responses/#Result-Types","page":"Responses Types","text":"","title":"Result Types"},{"category":"section","location":"api/responses/#Accessor-Functions","page":"Responses Types","text":"","title":"Accessor Functions"},{"category":"section","location":"api/responses/#Request-Functions","page":"Responses Types","text":"","title":"Request Functions"},{"category":"section","location":"api/responses/#Input-Helpers","page":"Responses Types","text":"","title":"Input Helpers"},{"category":"section","location":"api/responses/#Multimodal-Input","page":"Responses Types","text":"# Text-only input\nmsg = InputMessage(role=\"user\", content=\"What is Julia?\")\nprintln(\"Role: \", msg.role)\n\n# Multimodal input\nparts = [\n    input_text(\"Describe this image:\"),\n    input_image(\"https://example.com/photo.jpg\", detail=\"high\")\n]\nprintln(\"Parts: \", length(parts))\nprintln(\"Part types: \", [p[:type] for p in parts])\nRole: user\nParts: 2\nPart types: [\"input_text\", \"input_image\"]","title":"Multimodal Input"},{"category":"section","location":"api/responses/#Tool-Types","page":"Responses Types","text":"","title":"Tool Types"},{"category":"section","location":"api/responses/#Tool-Constructors","page":"Responses Types","text":"# Function tool\nft = function_tool(\"calculate\", \"Evaluate a math expression\",\n    parameters=Dict(\"type\" => \"object\", \"properties\" => Dict(\n        \"expr\" => Dict(\"type\" => \"string\")\n    ))\n)\nprintln(\"Function tool: \", ft.name)\n\n# Web search\nws = web_search(context_size=\"high\")\nprintln(\"Web search context: \", ws.search_context_size)\nFunction tool: calculate\nWeb search context: high","title":"Tool Constructors"},{"category":"section","location":"api/responses/#Tool-Choice","page":"Responses Types","text":"","title":"Tool Choice"},{"category":"section","location":"api/responses/#Tool-Results-and-Hosted-(Gemini)-Tools","page":"Responses Types","text":"","title":"Tool Results & Hosted (Gemini) Tools"},{"category":"section","location":"api/responses/#Text-Format","page":"Responses Types","text":"tf = text_format()\nprintln(\"Default format: \", tf.format.type)\n\njf = json_object_format()\nprintln(\"JSON format: \", jf.format.type)\nDefault format: text\nJSON format: json_object","title":"Text Format"},{"category":"section","location":"api/responses/#Reasoning,-Prompt-Caching-and-Moderation","page":"Responses Types","text":"reasoning = UniLM.Reasoning(effort=\"high\")\nprintln(\"Effort: \", reasoning.effort)\nEffort: high","title":"Reasoning, Prompt Caching & Moderation"},{"category":"type","location":"api/responses/#UniLM.Respond","page":"Responses Types","text":"Respond(; model=\"\", input, kwargs...)\n\nConfiguration struct for an OpenAI Responses API request.\n\nKey Fields\n\nmodel::String: Model to use; \"\" (the default) resolves to the service's default model at construction (\"gpt-5.6-sol\" for OpenAI)\ninput::Any: A String or Vector{InputMessage} — the prompt input\ninstructions::String: System-level instructions\ntools::Vector: Available tools (FunctionTool, WebSearchTool, FileSearchTool, …); a Chat Tool is converted to the equivalent FunctionTool\nprevious_response_id::String: Chain to a previous response for multi-turn; cannot be combined with conversation\nbackground::Bool: Run in the background; requires a stored response (store=false throws ArgumentError)\nreasoning::Reasoning: Reasoning config for O-series models\ntext::TextConfig: Output format (text, json, json_schema)\ntemperature::Float64: Sampling temperature (0.0–2.0), mutually exclusive with top_p\ntop_p::Float64: Nucleus sampling (0.0–1.0), mutually exclusive with temperature\nmax_output_tokens::Int64: Max tokens in the response\nstream::Bool: Enable streaming\ntruncation::String: \"auto\" or \"disabled\"\nstore::Bool: Whether to store the response for later retrieval\nmetadata::AbstractDict: Arbitrary metadata to attach\nuser::String: End-user identifier\nprompt::AbstractDict: Reusable prompt template reference; the reusable prompts API (v1/prompts) shuts down on November 30, 2026\nmoderation::ModerationConfig: Moderated completions — see ModerationConfig\n\nExamples\n\n# Simple text\nRespond(input=\"Tell me a joke\")\n\n# With instructions\nRespond(input=\"Translate to French: Hello\", instructions=\"You are a translator\")\n\n# Multi-turn via chaining\nRespond(input=\"Tell me more\", previous_response_id=\"resp_abc123\")\n\n# With tools\nRespond(\n    input=\"What's the weather in NYC?\",\n    tools=ResponseTool[function_tool(\"get_weather\", \"Get weather\", parameters=Dict(\"type\"=>\"object\", \"properties\"=>Dict(\"location\"=>Dict(\"type\"=>\"string\"))))]\n)\n\n# Reasoning\nRespond(input=\"Solve this math problem...\", model=\"gpt-5.4-mini\", reasoning=Reasoning(effort=\"high\"))\n\n\n\n\n\n","title":"UniLM.Respond"},{"category":"type","location":"api/responses/#UniLM.ResponseObject","page":"Responses Types","text":"ResponseObject\n\nParsed response from the Responses API.\n\nAccessors\n\noutput_text(r) — extract concatenated text output\nfunction_calls(r) — extract function call outputs\nr.id, r.status, r.model — basic metadata\nr.output — full output array (raw dicts)\nr.usage — token usage info\nr.raw — the complete raw JSON dict\n\n\n\n\n\n","title":"UniLM.ResponseObject"},{"category":"type","location":"api/responses/#UniLM.ResponseSuccess","page":"Responses Types","text":"ResponseSuccess <: LLMRequestResponse\n\nSuccessful response from the Responses API. Access the parsed response via .response.\n\n\n\n\n\n","title":"UniLM.ResponseSuccess"},{"category":"type","location":"api/responses/#UniLM.ResponseFailure","page":"Responses Types","text":"ResponseFailure <: LLMRequestResponse\n\nHTTP-level failure from the Responses API. Contains the response body, status code, and optional request ID.\n\n\n\n\n\n","title":"UniLM.ResponseFailure"},{"category":"type","location":"api/responses/#UniLM.ResponseCallError","page":"Responses Types","text":"ResponseCallError <: LLMRequestResponse\n\nException-level error during a Responses API call (network, parsing, etc.).\n\n\n\n\n\n","title":"UniLM.ResponseCallError"},{"category":"function","location":"api/responses/#UniLM.output_text","page":"Responses Types","text":"output_text(r::ResponseObject)::String\noutput_text(r::ResponseSuccess)::String\n\nExtract the concatenated text output from a response. On a ResponseFailure or ResponseCallError it throws an LLMResultError — there is no model output to return; guard with issuccess.\n\nExamples\n\nresult = respond(\"Hello!\")\noutput_text(result)  # => \"Hi there! How can I help?\"\n\n\n\n\n\n","title":"UniLM.output_text"},{"category":"function","location":"api/responses/#UniLM.function_calls","page":"Responses Types","text":"function_calls(r::ResponseObject)::Vector{Dict{String,Any}}\nfunction_calls(r::ResponseSuccess)::Vector{Dict{String,Any}}\n\nExtract function call outputs from a response.\n\nEach dict contains: \"id\", \"call_id\", \"name\", \"arguments\" (JSON string), \"status\".\n\nExamples\n\nresult = respond(\"What's the weather?\", tools=[function_tool(\"get_weather\", ...)])\nfor call in function_calls(result)\n    name = call[\"name\"]\n    args = JSON.parse(call[\"arguments\"])\n    # dispatch to your function...\nend\n\n\n\n\n\n","title":"UniLM.function_calls"},{"category":"function","location":"api/responses/#UniLM.url_citations","page":"Responses Types","text":"url_citations(r) -> Vector{Dict{String,Any}}\n\nURL-citation annotations on outputtext parts (from websearch).\n\n\n\n\n\n","title":"UniLM.url_citations"},{"category":"function","location":"api/responses/#UniLM.web_search_results","page":"Responses Types","text":"Raw web_search_call output items (request results via include).\n\n\n\n\n\n","title":"UniLM.web_search_results"},{"category":"function","location":"api/responses/#UniLM.file_search_results","page":"Responses Types","text":"Raw file_search_call output items.\n\n\n\n\n\n","title":"UniLM.file_search_results"},{"category":"function","location":"api/responses/#UniLM.image_generation_results","page":"Responses Types","text":"image_generation_results(r) -> Vector{String}\n\nBase64 image results from image_generation_call output items.\n\n\n\n\n\n","title":"UniLM.image_generation_results"},{"category":"function","location":"api/responses/#UniLM.code_interpreter_outputs","page":"Responses Types","text":"Raw code_interpreter_call output items.\n\n\n\n\n\n","title":"UniLM.code_interpreter_outputs"},{"category":"function","location":"api/responses/#UniLM.mcp_call_outputs","page":"Responses Types","text":"Raw mcp_call output items.\n\n\n\n\n\n","title":"UniLM.mcp_call_outputs"},{"category":"function","location":"api/responses/#UniLM.mcp_approval_requests","page":"Responses Types","text":"Raw mcp_approval_request output items (feed back via mcp_approval_response).\n\n\n\n\n\n","title":"UniLM.mcp_approval_requests"},{"category":"function","location":"api/responses/#UniLM.reasoning_summaries","page":"Responses Types","text":"reasoning_summaries(r) -> Vector{String}\n\nReasoning-summary text from each reasoning output item (OpenAI Responses) and each thought step (Gemini Interactions).\n\n\n\n\n\n","title":"UniLM.reasoning_summaries"},{"category":"function","location":"api/responses/#UniLM.reasoning_items","page":"Responses Types","text":"Raw reasoning output items.\n\n\n\n\n\n","title":"UniLM.reasoning_items"},{"category":"function","location":"api/responses/#UniLM.refusals","page":"Responses Types","text":"refusals(r) -> Vector{String}\n\nRefusal messages from any refusal content part of the output messages.\n\n\n\n\n\n","title":"UniLM.refusals"},{"category":"function","location":"api/responses/#UniLM.response_status","page":"Responses Types","text":"The response's lifecycle status string (e.g. completed, requires_action); failed/error for non-success results.\n\n\n\n\n\n","title":"UniLM.response_status"},{"category":"function","location":"api/responses/#UniLM.incomplete_details","page":"Responses Types","text":"Why a response is incomplete (the API's incomplete_details object), or nothing.\n\n\n\n\n\n","title":"UniLM.incomplete_details"},{"category":"function","location":"api/responses/#UniLM.usage_details","page":"Responses Types","text":"The response's raw token-usage dict; see token_usage for the typed TokenUsage.\n\n\n\n\n\n","title":"UniLM.usage_details"},{"category":"function","location":"api/responses/#UniLM.respond","page":"Responses Types","text":"respond(r::Respond; config=nothing, callback=nothing, cancel=nothing)\n\nSend a request to the OpenAI Responses API.\n\nReturns ResponseSuccess, ResponseFailure, or ResponseCallError.\n\nPer-call config::RequestConfig overrides timeouts and the retry budget (max_attempts); the process/scoped defaults apply otherwise.\n\nFor streaming, set stream=true and pass a callback:\n\ncallback(chunk::Union{String, ResponseObject}, close::Ref{Bool})\n\nSetting close[] = true — in the callback or from any other task — stops the stream at once with ResponseCallError(status=nothing, cause=UniLMCancelled(:callback, …)), unless the provider's completion marker — the terminal event — already arrived: then the outcome it recorded stands (a response with its usage, whose final callback has already run; or the failure the event reported). An exception thrown by the callback ends the call with that exception in cause, never retried. Time spent in the callback does not count toward stream_idle_timeout. A user InterruptException during a stream is not swallowed — it rethrows inside the task and surfaces as a TaskFailedException at fetch.\n\ncancel::Union{Nothing,CancelToken}: a CancelToken; nothing resolves the ambient token of with_cancel, at call entry. A cancel at any point — before connecting, during the response-header wait, mid-stream, or during a retry backoff — ends the call with ResponseCallError(status=nothing, cause=UniLMCancelled(:token, …)), never retried — unless the terminal event already arrived, as for close[] = true above. A pre-cancelled token sends nothing. A TCP connect or TLS handshake already in progress cannot be interrupted (HTTP.jl 2.7.1), so a cancel during one takes effect when it completes or reaches connect_timeout.\n\nLocal validation throws ArgumentError before any network I/O, streaming or not: when callback is passed without r.stream === true (it runs only on a stream, so it would be ignored), when r.service is an endpoint type that declares its capabilities and lists neither :responses (OpenAI wire) nor :agentic (Gemini Interactions), or when the provider's encoder rejects the request (e.g. an option the provider or model does not support).\n\nExamples\n\nr = Respond(input=\"Tell me a joke\")\nresult = respond(r)\nif result isa ResponseSuccess\n    println(output_text(result))\nend\n\n\n\n\n\nrespond(input; kwargs...)\n\nConvenience method: create a Respond from input + keyword arguments and send it.\n\nExamples\n\n# Simple text\nresult = respond(\"Tell me a joke\")\n\n# With instructions and model\nresult = respond(\"Translate: Hello\", instructions=\"You are a translator\", model=\"gpt-5.6-sol\")\n\n# With tools\nresult = respond(\"Search for Julia news\", tools=[web_search()])\n\n# Multi-turn\nr1 = respond(\"Tell me a joke\")\nr2 = respond(\"Tell me another\", previous_response_id=r1.response.id)\n\n# Streaming\nrespond(\"Tell me a story\", stream=true) do chunk, close\n    if chunk isa String\n        print(chunk)  # partial text delta\n    end\nend\n\n\n\n\n\nrespond(callback::Function, input; kwargs...)\n\ndo-block form for streaming. Automatically sets stream=true.\n\nExamples\n\nrespond(\"Tell me a story\") do chunk, close\n    if chunk isa String\n        print(chunk)\n    elseif chunk isa ResponseObject\n        println(\"\\nDone! Status: \", chunk.status)\n    end\nend\n\n\n\n\n\n","title":"UniLM.respond"},{"category":"function","location":"api/responses/#UniLM.get_response","page":"Responses Types","text":"get_response(response_id::String; service=OPENAIServiceEndpoint)\n\nRetrieve an existing response by its ID.\n\nExamples\n\nresult = get_response(\"resp_abc123\")\nif result isa ResponseSuccess\n    println(output_text(result))\nend\n\n\n\n\n\n","title":"UniLM.get_response"},{"category":"function","location":"api/responses/#UniLM.delete_response","page":"Responses Types","text":"delete_response(response_id::String; service=OPENAIServiceEndpoint)\n\nDelete a stored response by its ID. Returns a Dict with \"id\", \"object\", \"deleted\" keys.\n\nExamples\n\nresult = delete_response(\"resp_abc123\")\nresult[\"deleted\"]  # => true\n\n\n\n\n\n","title":"UniLM.delete_response"},{"category":"function","location":"api/responses/#UniLM.list_input_items","page":"Responses Types","text":"list_input_items(response_id::String; limit=20, order=\"desc\", after=nothing, service=OPENAIServiceEndpoint)\n\nList input items for a stored response. Returns a Dict with \"data\", \"first_id\", \"last_id\", \"has_more\".\n\nExamples\n\nitems = list_input_items(\"resp_abc123\")\nfor item in items[\"data\"]\n    println(item[\"type\"], \": \", item)\nend\n\n\n\n\n\n","title":"UniLM.list_input_items"},{"category":"function","location":"api/responses/#UniLM.cancel_response","page":"Responses Types","text":"cancel_response(response_id::String; service=OPENAIServiceEndpoint)\n\nCancel an in-progress response by its ID. Returns ResponseSuccess on success.\n\nExamples\n\n# Start a background response, then cancel it\nresult = respond(\"Write a very long essay\", background=true)\ncancel_result = cancel_response(result.response.id)\nif cancel_result isa ResponseSuccess\n    println(\"Cancelled: \", cancel_result.response.status)\nend\n\n\n\n\n\n","title":"UniLM.cancel_response"},{"category":"function","location":"api/responses/#UniLM.compact_response","page":"Responses Types","text":"compact_response(; model, input, kwargs...)\n\nCompact a conversation by running a compaction pass. Returns opaque, encrypted items that can be passed as input to subsequent requests, reducing token usage in long conversations.\n\nFields\n\nmodel::String: Model to use for compaction\ninput::Any: The conversation items to compact (typically the full conversation history)\n\nReturns a Dict with \"id\", \"object\", \"output\", and \"usage\" keys.\n\nExamples\n\ncompacted = compact_response(model=\"gpt-5.6-sol\", input=[\n    InputMessage(role=\"user\", content=\"Hello\"),\n    Dict(\"type\" => \"message\", \"role\" => \"assistant\", \"status\" => \"completed\",\n         \"content\" => [Dict(\"type\" => \"output_text\", \"text\" => \"Hi there!\")])\n])\n# Use compacted[\"output\"] as input to the next request\n\n\n\n\n\n","title":"UniLM.compact_response"},{"category":"function","location":"api/responses/#UniLM.count_input_tokens","page":"Responses Types","text":"count_input_tokens(; model, input, kwargs...)\n\nCount the number of input tokens a request would use without actually generating a response. Useful for estimating costs or checking whether input fits within the context window.\n\nReturns a Dict with \"object\" (\"response.input_tokens\") and \"input_tokens\" keys.\n\nExamples\n\nresult = count_input_tokens(model=\"gpt-5.6-sol\", input=\"Tell me a joke\")\nprintln(\"Input tokens: \", result[\"input_tokens\"])\n\n\n\n\n\n","title":"UniLM.count_input_tokens"},{"category":"type","location":"api/responses/#UniLM.InputMessage","page":"Responses Types","text":"InputMessage(; role, content)\n\nA structured input message for the Responses API.\n\nFields\n\nrole::String: \"user\", \"assistant\", \"system\", or \"developer\"\ncontent::Any: String or a vector of content parts (see input_text, input_image, input_file)\n\nExamples\n\nInputMessage(role=\"user\", content=\"What is 2+2?\")\nInputMessage(role=\"user\", content=[input_text(\"Describe this:\"), input_image(\"https://example.com/img.png\")])\n\n\n\n\n\n","title":"UniLM.InputMessage"},{"category":"function","location":"api/responses/#UniLM.input_text","page":"Responses Types","text":"input_text(text::String; cache_breakpoint=false)\n\nCreate an input_text content part for multimodal input messages.\n\ncache_breakpoint=true marks the end of this block as an explicit prompt-cache breakpoint (GPT-5.6 and later) by adding prompt_cache_breakpoint: {\"mode\": \"explicit\"}. OpenAI's prompt-caching guide: \"mark each desired breakpoint by adding prompt_cache_breakpoint: { \"mode\": \"explicit\" } to a supported content block inside an input message.\" Pair it with PromptCacheOptions(mode=\"explicit\") to cache only the prefixes you mark.\n\n\n\n\n\n","title":"UniLM.input_text"},{"category":"function","location":"api/responses/#UniLM.input_image","page":"Responses Types","text":"input_image(url=nothing; detail=nothing, file_id=nothing)\n\nCreate an input_image content part. Provide either an image url or a file_id. detail can be \"auto\", \"low\", or \"high\".\n\n\n\n\n\n","title":"UniLM.input_image"},{"category":"function","location":"api/responses/#UniLM.input_file","page":"Responses Types","text":"input_file(; url=nothing, id=nothing, file_data=nothing, filename=nothing)\n\nCreate an input_file content part. Provide a url, a file id, or inline file_data (base64). filename is recommended when passing file_data.\n\n\n\n\n\n","title":"UniLM.input_file"},{"category":"function","location":"api/responses/#UniLM.configuration_update","page":"Responses Types","text":"configuration_update(; effort::String)\n\nBuild a configuration_update input item, {\"type\": \"configuration_update\", \"reasoning\": {\"effort\": effort}}, that changes the reasoning effort of this and later responses without rewriting the request-level reasoning.effort, so the cached prompt prefix stays reusable. Place it in input before the next user message. Supported by the GPT-6 model family in standard, single-agent requests; the API rejects two adjacent updates. effort must be one of \"none\", \"minimal\", \"low\", \"medium\", \"high\", \"xhigh\", \"max\". See Change reasoning mid-conversation.\n\n\n\n\n\n","title":"UniLM.configuration_update"},{"category":"type","location":"api/responses/#UniLM.ResponseTool","page":"Responses Types","text":"ResponseTool\n\nAbstract supertype for Responses API tools. Subtypes: FunctionTool, WebSearchTool, FileSearchTool, MCPTool, ComputerUseTool, ComputerTool, ImageGenerationTool, CodeInterpreterTool, LocalShellTool, ShellTool, ApplyPatchTool and CustomTool.\n\n\n\n\n\n","title":"UniLM.ResponseTool"},{"category":"type","location":"api/responses/#UniLM.FunctionTool","page":"Responses Types","text":"FunctionTool(; name, description=nothing, parameters=nothing, strict=nothing,\n             async=nothing, allowed_callers=nothing, defer_loading=nothing, output_schema=nothing)\n\nA function tool for the Responses API.\n\nasync: true lets the model keep working while your application runs the tool; return its result later with the original call_id (GPT-6 models).\nallowed_callers: invocation contexts, \"direct\" and/or \"programmatic\"; any other value throws ArgumentError.\ndefer_loading: true defers the definition until the model loads it via tool search.\noutput_schema: JSON Schema of the JSON value encoded in the tool's string output.\n\nUnset fields are omitted from the request.\n\nExamples\n\nFunctionTool(\n    name=\"get_weather\",\n    description=\"Get current weather for a location\",\n    parameters=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"location\" => Dict(\"type\" => \"string\", \"description\" => \"City name\")\n        ),\n        \"required\" => [\"location\"]\n    )\n)\n\n\n\n\n\n","title":"UniLM.FunctionTool"},{"category":"type","location":"api/responses/#UniLM.WebSearchTool","page":"Responses Types","text":"WebSearchTool(; type=\"web_search\", search_context_size=\"medium\", user_location=nothing, filters=nothing)\n\nA web search tool for the Responses API. Allows the model to search the web.\n\ntype: \"web_search\" (GA, default) or the legacy \"web_search_preview\"\nsearch_context_size: \"low\", \"medium\", or \"high\"\nuser_location: Dict with keys like \"country\", \"city\", \"region\", \"timezone\"\nfilters: Dict with \"allowed_domains\" / \"blocked_domains\" (GA only)\n\nFetch sources/results back via include=[\"web_search_call.results\", \"web_search_call.action.sources\"].\n\n\n\n\n\n","title":"UniLM.WebSearchTool"},{"category":"type","location":"api/responses/#UniLM.FileSearchTool","page":"Responses Types","text":"FileSearchTool(; vector_store_ids, max_num_results=nothing, ranking_options=nothing, filters=nothing)\n\nA file search tool for the Responses API. Searches over uploaded vector stores.\n\n\n\n\n\n","title":"UniLM.FileSearchTool"},{"category":"type","location":"api/responses/#UniLM.MCPTool","page":"Responses Types","text":"MCPTool(; server_label, server_url=nothing, connector_id=nothing, authorization=nothing,\n        server_description=nothing, require_approval=\"never\", allowed_tools=nothing,\n        headers=nothing, tunnel_id=nothing)\n\nA Model Context Protocol (MCP) tool for the Responses API. Connect the model to a remote MCP server (server_url), an OpenAI connector (connector_id, e.g. \"connector_googledrive\", \"connector_gmail\", \"connector_dropbox\"), or a Secure MCP Tunnel (tunnel_id). Use authorization for an OAuth access token. require_approval/allowed_tools accept the string or object forms.\n\n\n\n\n\n","title":"UniLM.MCPTool"},{"category":"type","location":"api/responses/#UniLM.ComputerUseTool","page":"Responses Types","text":"ComputerUseTool(; display_width=1024, display_height=768, environment=nothing)\n\nA computer use tool for the Responses API. Allows the model to interact with a virtual display via screenshots, mouse, and keyboard.\n\n\n\n\n\n","title":"UniLM.ComputerUseTool"},{"category":"type","location":"api/responses/#UniLM.ImageGenerationTool","page":"Responses Types","text":"ImageGenerationTool(; background=nothing, output_format=nothing, output_compression=nothing,\n                    quality=nothing, size=nothing, model=nothing, action=nothing,\n                    moderation=nothing, partial_images=nothing, input_fidelity=nothing,\n                    input_image_mask=nothing)\n\nAn image generation tool for the Responses API. Allows the model to generate images inline during a response.\n\nmodel: image model, e.g. \"gpt-image-2\", \"gpt-image-2.5-flare\", \"gpt-image-2.5-sunburst\"\naction: \"generate\", \"edit\", or \"auto\"\nmoderation: \"auto\" or \"low\"\npartial_images: partial images to stream before the final image, 0–3\ninput_fidelity: \"high\" or \"low\" — how closely edits preserve input images (omit it for gpt-image-2, which always uses high fidelity)\ninput_image_mask: inpainting mask, Dict(\"file_id\" => …) or Dict(\"image_url\" => …)\nquality: \"low\", \"medium\", \"high\", \"auto\"; the 2.5 models add \"xhigh\" and \"max\"\n\naction, moderation, partial_images, and input_fidelity are validated at construction (ArgumentError). quality, background, and output_format are passed through unchecked (the accepted quality values depend on the model). Unset fields are omitted from the request.\n\n\n\n\n\n","title":"UniLM.ImageGenerationTool"},{"category":"type","location":"api/responses/#UniLM.CodeInterpreterTool","page":"Responses Types","text":"CodeInterpreterTool(; container=nothing, file_ids=nothing)\n\nA code interpreter tool for the Responses API. Allows the model to execute code in a sandboxed environment.\n\n\n\n\n\n","title":"UniLM.CodeInterpreterTool"},{"category":"type","location":"api/responses/#UniLM.ComputerTool","page":"Responses Types","text":"ComputerTool(; environment=nothing)\n\nGA computer-use tool (type:\"computer\") for newer models. Unlike ComputerUseTool (computer_use_preview) it carries no display_width/display_height.\n\n\n\n\n\n","title":"UniLM.ComputerTool"},{"category":"type","location":"api/responses/#UniLM.CustomTool","page":"Responses Types","text":"CustomTool(; name, description=nothing, format=nothing)\n\nCustom tool (type:\"custom\") with free-form text input, or a grammar-constrained input via format = Dict(\"type\"=>\"grammar\", \"syntax\"=>\"lark\"|\"regex\", \"definition\"=>...).\n\n\n\n\n\n","title":"UniLM.CustomTool"},{"category":"type","location":"api/responses/#UniLM.LocalShellTool","page":"Responses Types","text":"LocalShellTool()\n\nLocal shell tool (type:\"local_shell\"): the model emits shell commands you run on your own runtime (codex-style models).\n\n\n\n\n\n","title":"UniLM.LocalShellTool"},{"category":"type","location":"api/responses/#UniLM.ShellTool","page":"Responses Types","text":"ShellTool(; environment=nothing)\n\nHosted shell tool (type:\"shell\").\n\n\n\n\n\n","title":"UniLM.ShellTool"},{"category":"type","location":"api/responses/#UniLM.ApplyPatchTool","page":"Responses Types","text":"ApplyPatchTool()\n\nStructured file-edit tool (type:\"apply_patch\").\n\n\n\n\n\n","title":"UniLM.ApplyPatchTool"},{"category":"function","location":"api/responses/#UniLM.function_tool","page":"Responses Types","text":"function_tool(name, description=nothing; parameters=nothing, strict=nothing,\n              async=nothing, allowed_callers=nothing, defer_loading=nothing,\n              output_schema=nothing)\n\nShorthand constructor for FunctionTool.\n\n\n\n\n\nfunction_tool(d::AbstractDict)\n\nConstruct a FunctionTool from a dict. Accepts both the bare format {\"name\": ...} and the wrapped format {\"type\": \"function\", \"function\": {\"name\": ...}}. The keys \"description\", \"parameters\", \"strict\", \"async\", \"allowed_callers\", \"defer_loading\", and \"output_schema\" are copied when present.\n\n\n\n\n\n","title":"UniLM.function_tool"},{"category":"function","location":"api/responses/#UniLM.web_search","page":"Responses Types","text":"web_search(; context_size=\"medium\", location=nothing, type=\"web_search\", filters=nothing)\n\nShorthand constructor for WebSearchTool.\n\n\n\n\n\n","title":"UniLM.web_search"},{"category":"function","location":"api/responses/#UniLM.file_search","page":"Responses Types","text":"file_search(store_ids::Vector{String}; max_results=nothing, ranking=nothing, filters=nothing)\n\nShorthand constructor for FileSearchTool.\n\n\n\n\n\n","title":"UniLM.file_search"},{"category":"function","location":"api/responses/#UniLM.mcp_tool","page":"Responses Types","text":"mcp_tool(label, url=nothing; require_approval=\"never\", allowed_tools=nothing, headers=nothing,\n         connector_id=nothing, authorization=nothing, server_description=nothing, tunnel_id=nothing)\n\nShorthand constructor for MCPTool.\n\n\n\n\n\n","title":"UniLM.mcp_tool"},{"category":"function","location":"api/responses/#UniLM.computer_use","page":"Responses Types","text":"computer_use(; display_width=1024, display_height=768, environment=nothing)\n\nShorthand constructor for ComputerUseTool.\n\n\n\n\n\n","title":"UniLM.computer_use"},{"category":"function","location":"api/responses/#UniLM.image_generation_tool","page":"Responses Types","text":"image_generation_tool(; kwargs...)\n\nShorthand constructor for ImageGenerationTool.\n\n\n\n\n\n","title":"UniLM.image_generation_tool"},{"category":"function","location":"api/responses/#UniLM.code_interpreter","page":"Responses Types","text":"code_interpreter(; container=nothing, file_ids=nothing)\n\nShorthand constructor for CodeInterpreterTool.\n\n\n\n\n\n","title":"UniLM.code_interpreter"},{"category":"function","location":"api/responses/#UniLM.computer_tool","page":"Responses Types","text":"Construct a ComputerTool (GA computer tool); for the preview variant with display dimensions use computer_use.\n\n\n\n\n\n","title":"UniLM.computer_tool"},{"category":"function","location":"api/responses/#UniLM.custom_tool","page":"Responses Types","text":"Construct a CustomTool (custom tool); format optionally constrains input to a grammar.\n\n\n\n\n\n","title":"UniLM.custom_tool"},{"category":"function","location":"api/responses/#UniLM.local_shell","page":"Responses Types","text":"Construct a LocalShellTool (local_shell): the model emits commands you run on your own runtime.\n\n\n\n\n\n","title":"UniLM.local_shell"},{"category":"function","location":"api/responses/#UniLM.shell","page":"Responses Types","text":"Construct a ShellTool (hosted shell tool).\n\n\n\n\n\n","title":"UniLM.shell"},{"category":"function","location":"api/responses/#UniLM.apply_patch_tool","page":"Responses Types","text":"Construct an ApplyPatchTool (structured apply_patch file-edit tool).\n\n\n\n\n\n","title":"UniLM.apply_patch_tool"},{"category":"function","location":"api/responses/#UniLM.mcp_approval_response","page":"Responses Types","text":"mcp_approval_response(approval_request_id, approve; reason=nothing)\n\nBuild an mcp_approval_response input item to approve/deny a pending MCP tool call. Pass it back as an element of the next request's input.\n\n\n\n\n\n","title":"UniLM.mcp_approval_response"},{"category":"function","location":"api/responses/#UniLM.tool_choice_function","page":"Responses Types","text":"tool_choice_function(name)\n\nForce the model to call a specific function tool: {type:\"function\", name}.\n\n\n\n\n\n","title":"UniLM.tool_choice_function"},{"category":"function","location":"api/responses/#UniLM.tool_choice_hosted","page":"Responses Types","text":"tool_choice_hosted(type)\n\nForce a specific hosted tool, e.g. tool_choice_hosted(\"file_search\"), \"image_generation\", \"code_interpreter\". Note: the hosted web-search selector is \"web_search_preview\" (not \"web_search\").\n\n\n\n\n\n","title":"UniLM.tool_choice_hosted"},{"category":"function","location":"api/responses/#UniLM.tool_choice_allowed","page":"Responses Types","text":"tool_choice_allowed(mode, tools)\n\nConstrain the model to a subset of tools: {type:\"allowed_tools\", mode, tools}. mode is \"auto\" or \"required\"; tools is a vector of tool-reference dicts.\n\n\n\n\n\n","title":"UniLM.tool_choice_allowed"},{"category":"function","location":"api/responses/#UniLM.tool_choice_mcp","page":"Responses Types","text":"tool_choice_mcp(server_label; name=nothing)\n\nForce a specific MCP server (optionally a specific tool): {type:\"mcp\", server_label, name?}.\n\n\n\n\n\n","title":"UniLM.tool_choice_mcp"},{"category":"function","location":"api/responses/#UniLM.tool_choice_custom","page":"Responses Types","text":"tool_choice_custom(name)\n\nForce a specific custom tool: {type:\"custom\", name}.\n\n\n\n\n\n","title":"UniLM.tool_choice_custom"},{"category":"function","location":"api/responses/#UniLM.tool_result","page":"Responses Types","text":"tool_result(call_id, name, output) -> Dict\n\nNeutral multi-turn tool-result input item for the agentic verb. Feed a function's output back via respond(Respond(; previous_response_id=id, input=[tool_result(...)])) — respond takes the input positionally or as a Respond, so a keyword-only call is a MethodError — or through tool_loop. Wire-neutral: OpenAI serializes it as function_call_output (ignoring name); the Gemini encoder translates it to function_result (which requires name). output is the function's return value as a string.\n\n\n\n\n\n","title":"UniLM.tool_result"},{"category":"function","location":"api/responses/#UniLM.gemini_google_search","page":"Responses Types","text":"gemini_google_search() -> Dict\n\nHosted Google Search tool for the Gemini Interactions API. Pass in respond(...; tools=[gemini_google_search()], service=GEMINIServiceEndpoint).\n\n\n\n\n\n","title":"UniLM.gemini_google_search"},{"category":"function","location":"api/responses/#UniLM.gemini_code_execution","page":"Responses Types","text":"gemini_code_execution() -> Dict\n\nHosted code-execution tool for the Gemini Interactions API. Pass in respond(...; tools=[gemini_code_execution()], service=GEMINIServiceEndpoint).\n\n\n\n\n\n","title":"UniLM.gemini_code_execution"},{"category":"function","location":"api/responses/#UniLM.gemini_url_context","page":"Responses Types","text":"gemini_url_context() -> Dict\n\nHosted URL-context tool for the Gemini Interactions API. Pass in respond(...; tools=[gemini_url_context()], service=GEMINIServiceEndpoint).\n\n\n\n\n\n","title":"UniLM.gemini_url_context"},{"category":"type","location":"api/responses/#UniLM.TextConfig","page":"Responses Types","text":"TextConfig(; format=TextFormatSpec(), verbosity=nothing)\n\nWrapper for the text field in the Responses API request body.\n\n\n\n\n\n","title":"UniLM.TextConfig"},{"category":"type","location":"api/responses/#UniLM.TextFormatSpec","page":"Responses Types","text":"TextFormatSpec(; type=\"text\", name=nothing, description=nothing, schema=nothing, strict=nothing)\n\nOutput text format specification.\n\ntype: \"text\" (default), \"json_object\", or \"json_schema\"\nFor \"json_schema\": provide name, description, schema, and optionally strict\n\n\n\n\n\n","title":"UniLM.TextFormatSpec"},{"category":"function","location":"api/responses/#UniLM.text_format","page":"Responses Types","text":"text_format(; kwargs...)\n\nCreate a TextConfig with the given format options.\n\n\n\n\n\n","title":"UniLM.text_format"},{"category":"function","location":"api/responses/#UniLM.json_schema_format","page":"Responses Types","text":"json_schema_format(name, description, schema; strict=nothing)\n\nCreate a JSON Schema output format for structured output.\n\n\n\n\n\njson_schema_format(d::AbstractDict)\n\nConstruct a JSON Schema TextConfig from a dict with keys \"name\", \"description\", and \"schema\".\n\n\n\n\n\n","title":"UniLM.json_schema_format"},{"category":"function","location":"api/responses/#UniLM.json_object_format","page":"Responses Types","text":"json_object_format()\n\nCreate a JSON object output format (unstructured).\n\n\n\n\n\n","title":"UniLM.json_object_format"},{"category":"type","location":"api/responses/#UniLM.Reasoning","page":"Responses Types","text":"Reasoning(; effort=nothing, summary=nothing, generate_summary=nothing, context=nothing, mode=nothing)\n\nReasoning configuration for OpenAI and Gemini Interactions models.\n\neffort: OpenAI accepts \"none\", \"minimal\", \"low\", \"medium\", \"high\", \"xhigh\", and \"max\" — any other value throws ArgumentError at construction; each model supports a subset (GPT-6 Astra has no \"none\" or \"minimal\"; gpt-5.4-mini and gpt-5.4-nano stop at \"xhigh\"). GPT-6 Sol and Luna refuse \"minimal\" on the native OpenAI endpoint; use \"low\". Gemini 3.8 supports \"low\", \"medium\", and \"high\".\nsummary: \"auto\", \"concise\", or \"detailed\" — request a reasoning summary in the output.\ngenerate_summary: deprecated alias serialized as summary; prefer summary. Gemini Interactions accepts summary=\"auto\" for thought summaries.\ncontext: OpenAI persisted reasoning: \"auto\", \"current_turn\", or \"all_turns\".\nmode: OpenAI \"standard\" or \"pro\" execution; pro can consume more tokens.\n\n\n\n\n\n","title":"UniLM.Reasoning"},{"category":"type","location":"api/responses/#UniLM.PromptCacheOptions","page":"Responses Types","text":"PromptCacheOptions(; mode=nothing, ttl=nothing, prewarm=nothing, comparison_response_id=nothing)\n\nOpenAI prompt-cache controls for GPT-5.6 and later. mode is \"implicit\" or \"explicit\"; the supported ttl is \"30m\". Explicit mode caches only prefixes marked with prompt_cache_breakpoint in input content blocks (see input_text with cache_breakpoint=true).\n\nResponses API only:\n\nprewarm=true prepares the prompt cache without generating output; send the real request afterwards with the same prefix and prewarm unset.\ncomparison_response_id requests prompt-cache diagnostics against an earlier response; they come back in r.response.raw[\"prompt_cache_diagnostics\"].\n\nChat Completions accepts only mode and ttl: a Chat for OPENAIServiceEndpoint with prewarm or comparison_response_id set throws ArgumentError when encoded. Message content is a plain string, so a Chat cannot mark cache breakpoints: with mode=\"explicit\" its request has none, and per the Chat Completions reference such a request does not use prompt caching. Unset fields are omitted from the request. See OpenAI prompt caching.\n\n\n\n\n\n","title":"UniLM.PromptCacheOptions"},{"category":"type","location":"api/responses/#UniLM.ModerationConfig","page":"Responses Types","text":"ModerationConfig(; model, input_mode=nothing, output_mode=nothing)\n\nOpenAI moderated completions for Chat and Respond: the required moderation model (e.g. \"omni-moderation-latest\") checks the request input and the generated output. The optional input_mode / output_mode set the policy per side: \"score\" or \"block\". Serialized as {\"model\": …, \"policy\": {\"input\": {\"mode\": …}, \"output\": {\"mode\": …}}} with unset policy parts omitted. A respond result carries the outcome in r.response.raw[\"moderation\"] (\"input\" and \"output\" moderation results); LLMSuccess keeps no raw body, so Chat results do not expose it. See the moderation parameter of Create a model response.\n\n\n\n\n\n","title":"UniLM.ModerationConfig"},{"category":"section","location":"guide/structured_output/#structured_guide","page":"Structured Output","text":"Force the model to produce valid JSON conforming to a schema. Both APIs support this.\n\nnote: Provider support varies\nSchema-constrained output is fully supported on OpenAI and OpenAI-compatible providers. Support differs on the native backends — see the provider notes in the Multi-Backend guide before relying on strict schemas elsewhere.","title":"Structured Output"},{"category":"section","location":"guide/structured_output/#Chat-Completions","page":"Structured Output","text":"Use ResponseFormat to control the output format:","title":"Chat Completions"},{"category":"section","location":"guide/structured_output/#Free-Form-JSON","page":"Structured Output","text":"using UniLM\nusing JSON\n\nchat = Chat(\n    model=\"gpt-5.4-mini\",\n    response_format=ResponseFormat()  # type=\"json_object\"\n)\npush!(chat, Message(Val(:system), \"You output JSON. Always respond with valid JSON.\"))\npush!(chat, Message(Val(:user), \"List 3 programming languages with their year of creation.\"))\nprintln(\"Response format type: \", chat.response_format.type)\nprintln(\"Request body:\")\nprintln(JSON.json(chat))\nResponse format type: json_object\nRequest body:\n{\"messages\":[{\"content\":\"You output JSON. Always respond with valid JSON.\",\"role\":\"system\"},{\"content\":\"List 3 programming languages with their year of creation.\",\"role\":\"user\"}],\"model\":\"gpt-5.4-mini\",\"response_format\":{\"type\":\"json_object\"}}\n\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(JSON.json(JSON.parse(result.message.content), 2))\nelse\n    println(\"Request failed — see result for details\")\nend\n{\n  \"languages\": [\n    {\n      \"name\": \"Python\",\n      \"year_of_creation\": 1991\n    },\n    {\n      \"name\": \"Java\",\n      \"year_of_creation\": 1995\n    },\n    {\n      \"name\": \"JavaScript\",\n      \"year_of_creation\": 1995\n    }\n  ]\n}","title":"Free-Form JSON"},{"category":"section","location":"guide/structured_output/#JSON-Schema-(Strict)","page":"Structured Output","text":"schema = ResponseFormat(UniLM.JsonSchemaAPI(\n    name=\"languages\",\n    description=\"A list of programming languages\",\n    schema=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"languages\" => Dict(\n                \"type\" => \"array\",\n                \"items\" => Dict(\n                    \"type\" => \"object\",\n                    \"properties\" => Dict(\n                        \"name\" => Dict(\"type\" => \"string\"),\n                        \"year\" => Dict(\"type\" => \"integer\")\n                    ),\n                    \"required\" => [\"name\", \"year\"],\n                    \"additionalProperties\" => false\n                )\n            )\n        ),\n        \"required\" => [\"languages\"],\n        \"additionalProperties\" => false\n    ),\n    strict=true\n))\n\nchat = Chat(model=\"gpt-5.4-mini\", response_format=schema)\npush!(chat, Message(Val(:system), \"Return structured data about programming languages.\"))\npush!(chat, Message(Val(:user), \"List Julia, Python, and Rust\"))\nprintln(\"Schema name: \", schema.json_schema.name)\nprintln(\"Response format type: \", schema.type)\nprintln(\"Strict: \", schema.json_schema.strict)\nSchema name: languages\nResponse format type: json_schema\nStrict: true\n\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(JSON.json(JSON.parse(result.message.content), 2))\nelse\n    println(\"Request failed — see result for details\")\nend\n{\n  \"languages\": [\n    {\n      \"name\": \"Julia\",\n      \"year\": 2012\n    },\n    {\n      \"name\": \"Python\",\n      \"year\": 1991\n    },\n    {\n      \"name\": \"Rust\",\n      \"year\": 2010\n    }\n  ]\n}","title":"JSON Schema (Strict)"},{"category":"section","location":"guide/structured_output/#Responses-API","page":"Structured Output","text":"The Responses API uses TextConfig with convenience constructors:","title":"Responses API"},{"category":"section","location":"guide/structured_output/#JSON-Object","page":"Structured Output","text":"result = respond(\"List 3 colors as a JSON object\", text=json_object_format(), model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\n{\"color1\":\"red\",\"color2\":\"blue\",\"color3\":\"green\"}","title":"JSON Object"},{"category":"section","location":"guide/structured_output/#JSON-Schema","page":"Structured Output","text":"fmt = json_schema_format(\n    \"colors\",\n    \"A structured list of colors\",\n    Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"colors\" => Dict(\n                \"type\" => \"array\",\n                \"items\" => Dict(\n                    \"type\" => \"object\",\n                    \"properties\" => Dict(\n                        \"name\" => Dict(\"type\" => \"string\"),\n                        \"hex\" => Dict(\"type\" => \"string\")\n                    ),\n                    \"required\" => [\"name\", \"hex\"],\n                    \"additionalProperties\" => false\n                )\n            )\n        ),\n        \"required\" => [\"colors\"],\n        \"additionalProperties\" => false\n    ),\n    strict=true\n)\nprintln(\"Format type: \", fmt.format.type)\nprintln(\"Schema strict: \", fmt.format.strict)\nFormat type: json_schema\nSchema strict: true\n\nresult = respond(\"List red, green, and blue with their hex codes\", text=fmt, model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(JSON.json(JSON.parse(output_text(result)), 2))\nelse\n    println(\"Request failed — \", result)\nend\n{\n  \"colors\": [\n    {\n      \"hex\": \"#FF0000\",\n      \"name\": \"red\"\n    },\n    {\n      \"hex\": \"#00FF00\",\n      \"name\": \"green\"\n    },\n    {\n      \"hex\": \"#0000FF\",\n      \"name\": \"blue\"\n    }\n  ]\n}","title":"JSON Schema"},{"category":"section","location":"guide/structured_output/#Plain-Text-Format","page":"Structured Output","text":"tc = text_format()\nprintln(\"Default format type: \", tc.format.type)\nDefault format type: text","title":"Plain Text Format"},{"category":"section","location":"guide/structured_output/#Gemini","page":"Structured Output","text":"Native Gemini takes the same options: a Chat's response_format becomes generationConfig.responseFormat on generateContent, and the text format of a respond call becomes the Interactions response_format. Only the schema is sent — Gemini has no counterpart for the schema name, description, or strict.\n\ncapital = ResponseFormat(UniLM.JsonSchemaAPI(\n    name=\"capital\",\n    description=\"A capital city and its country\",\n    schema=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"city\" => Dict(\"type\" => \"string\"),\n            \"country\" => Dict(\"type\" => \"string\")\n        ),\n        \"required\" => [\"city\", \"country\"],\n        \"additionalProperties\" => false\n    )\n))\n\nchat = Chat(service=GEMINIServiceEndpoint, model=\"gemini-3.8-flash\",\n            reasoning_effort=\"low\", max_tokens=1024, response_format=capital)\npush!(chat, Message(Val(:system), \"You output JSON.\"))\npush!(chat, Message(Val(:user), \"Give the capital of Norway as JSON.\"))\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(sort(collect(keys(JSON.parse(result.message.content)))))\nelse\n    println(\"Request failed — \", result)\nend\n[\"city\", \"country\"]\n\nThe Interactions form passes the same schema through text:\n\nfmt = json_schema_format(\"capital\", \"A capital city and its country\", capital.json_schema.schema)\nresult = respond(\"Give the capital of Norway as JSON.\"; service=GEMINIServiceEndpoint,\n                 model=\"gemini-3.8-flash\", text=fmt, max_output_tokens=256)","title":"Gemini"},{"category":"section","location":"guide/structured_output/#Anthropic","page":"Structured Output","text":"Native Anthropic maps a JSON Schema response_format onto Claude's structured outputs: it becomes output_config.format = {type: \"json_schema\", schema}, and only the schema is sent (the name, description and strict have no counterpart — the output is always constrained to the schema). Claude has no schema-less JSON mode, so ResponseFormat() (json_object) throws ArgumentError before any request. FunctionSignature(strict=true) becomes the tool's own strict flag.\n\nchat = Chat(service=ANTHROPICServiceEndpoint, response_format=capital)   # default: claude-opus-5-5\npush!(chat, Message(Val(:system), \"You output JSON.\"))\npush!(chat, Message(Val(:user), \"Give the capital of Norway as JSON.\"))\nresult = chatrequest!(chat)","title":"Anthropic"},{"category":"section","location":"guide/structured_output/#Convenience-Constructors","page":"Structured Output","text":"Constructor Format\njson_object_format() Unstructured JSON\njson_schema_format(name, desc, schema) Schema-constrained JSON\ntext_format() Plain text (default)","title":"Convenience Constructors"},{"category":"section","location":"guide/structured_output/#See-Also","page":"Structured Output","text":"ResponseFormat — Chat Completions format type\nTextConfig, TextFormatSpec — Responses API format types","title":"See Also"},{"category":"section","location":"guide/retrieval/#retrieval_guide","page":"Retrieval & File Search","text":"Ground a model's answer in your own documents: upload a local file, index it into a vector store, then let the Responses file_search tool retrieve the relevant chunks at answer time. This is the end-to-end Retrieval-Augmented Generation (RAG) pipeline in UniLM.\n\nnote: Note\nFiles, vector stores, and file_search are OpenAI-only (see the Files API and Vector Stores API). Calling them on another endpoint fails capability validation at request time.\n\nwarning: Warning\nA file_search tool needs a real vector-store id. Do not hard-code a placeholder such as \"vs_abc123\" — obtain the id from create_vector_store and thread it through every later step, exactly as shown below.","title":"Retrieval & File Search"},{"category":"section","location":"guide/retrieval/#Pipeline-at-a-glance","page":"Retrieval & File Search","text":"upload_file — send the local file to the Files API (purpose=\"user_data\").\ncreate_vector_store — create a store and capture its real id.\ncreate_file_batch + poll_file_batch — attach the file(s) and block until indexing completes.\nrespond with tools=[file_search([vs_id])] — ask a question against that store.\noutput_text + file_search_results — read the grounded answer and the retrieved chunks.\n\nThe julia blocks below form one continuous session — each step reuses ids produced by the previous one. They hit the network (and need real ids), so they are shown as plain julia with expected output in comments rather than executed.","title":"Pipeline at a glance"},{"category":"section","location":"guide/retrieval/#1.-Upload-the-file","page":"Retrieval & File Search","text":"upload_file posts a file as multipart/form-data. For retrieval, use purpose=\"user_data\". On success it returns a FileSuccess wrapping a FileObject; the file id you need next is .response.id.\n\nusing UniLM\n\nup = upload_file(\"handbook.md\", \"user_data\")\n\nup isa FileSuccess || error(\"upload failed: \", up)\nfile_id = up.response.id\n@show file_id               # file_id = \"file-AbC123...\"\n@show up.response.filename  # \"handbook.md\"\n@show up.response.purpose   # \"user_data\"\n\nupload_file returns FileFailure (HTTP error) or FileCallError (transport error) instead of throwing on a failed request — always check the result type before reading .response.","title":"1. Upload the file"},{"category":"section","location":"guide/retrieval/#2.-Create-a-vector-store","page":"Retrieval & File Search","text":"create_vector_store returns a VectorStoreSuccess wrapping a VectorStoreObject. Capture its real id via .response.id (or the vector_store_id accessor) — this is the id every later step depends on.\n\nvs = create_vector_store(name=\"handbook\")\n\nvs isa VectorStoreSuccess || error(\"store creation failed: \", vs)\nvs_id = vector_store_id(vs.response)   # identical to vs.response.id\n@show vs_id                            # vs_id = \"vs_68f0...\"   ← the REAL id, not a placeholder\n\ntip: Tip\nYou can attach already-uploaded files at creation time with create_vector_store(file_ids=[file_id]), folding step 3's attach into this call (indexing still runs asynchronously, so you'd still poll — see below).","title":"2. Create a vector store"},{"category":"section","location":"guide/retrieval/#3.-Attach-the-file-and-wait-for-indexing","page":"Retrieval & File Search","text":"Files must be chunked and embedded before they are searchable. Attach one or more files as a batch with create_file_batch (it takes a Vector{String} of file ids), then block until the batch reaches a terminal status with poll_file_batch.\n\nbatch = create_file_batch(vs_id, [file_id])\nbatch isa VectorStoreBatchSuccess || error(\"batch failed: \", batch)\n\ndone = poll_file_batch(vs_id, batch.response.id; interval=2.0, timeout=300.0)\ndone isa VectorStoreBatchSuccess || error(\"indexing did not complete: \", done)\n\n@show done.response.status                            # \"completed\"\n@show get(done.response.file_counts, \"completed\", 0)  # 1\n\npoll_file_batch re-fetches the batch every interval seconds until its status is \"completed\", \"failed\", or \"cancelled\", returning that terminal VectorStoreBatchSuccess; it returns a VectorStoreCallError if timeout elapses first. Only proceed once the status is \"completed\" — searching a still-indexing store returns no chunks.\n\nTo attach a single file without the batch wrapper, use add_vector_store_file(vs_id, file_id); it returns a VectorStoreFileSuccess immediately with the file's initial status, so prefer the batch + poll route whenever you need to wait for indexing to finish.","title":"3. Attach the file and wait for indexing"},{"category":"section","location":"guide/retrieval/#4.-Ask-a-grounded-question","page":"Retrieval & File Search","text":"file_search builds the hosted tool from a vector of store ids. Pass the real vs_id from step 2, and add include=[\"file_search_call.results\"] so the API returns the retrieved chunks alongside the answer.\n\nresult = respond(\n    \"What is our policy on error handling?\";\n    tools=[file_search([vs_id]; max_results=5)],\n    include=[\"file_search_call.results\"],\n)\nresult isa ResponseSuccess || error(\"query failed: \", result)\n\nThe tool serialises to a small JSON object. Its wire shape is self-contained (no network), so it can be shown live — the id here is a placeholder for illustration only; the pipeline above uses the real vs_id:\n\ntool = file_search([\"vs_your_store_id\"]; max_results=5)  # placeholder id — wire shape only\nprintln(\"Store ids:   \", tool.vector_store_ids)\nprintln(\"Max results: \", tool.max_num_results)\nprintln(\"Wire JSON:   \", JSON.json(JSON.lower(tool)))\nStore ids:   [\"vs_your_store_id\"]\nMax results: 5\nWire JSON:   {\"max_num_results\":5,\"type\":\"file_search\",\"vector_store_ids\":[\"vs_your_store_id\"]}","title":"4. Ask a grounded question"},{"category":"section","location":"guide/retrieval/#5.-Read-the-answer-and-the-cited-chunks","page":"Retrieval & File Search","text":"The grounded answer is plain text via output_text. The retrieved chunks come from file_search_results, which returns the raw file_search_call output items; each item carries a \"results\" array (present because of the include= above). Those result entries are unparsed API dicts, so read their fields with get:\n\nprintln(output_text(result))    # the grounded, citation-backed answer\n\nfor call in file_search_results(result)\n    for chunk in get(call, \"results\", [])\n        fname = get(chunk, \"filename\", \"\")\n        score = get(chunk, \"score\", nothing)\n        text  = get(chunk, \"text\", \"\")\n        println(\"• \", fname, \"  (score=\", score, \")\")\n        println(\"  \", first(text, 160))\n    end\nend\n# • handbook.md  (score=0.83)\n#   Errors are surfaced as typed result values rather than thrown exceptions ...\n\nThe exact keys inside each chunk (file_id, filename, score, text, attributes) are defined by the OpenAI API — inspect a call dict to see everything returned. Without include=[\"file_search_call.results\"], file_search_results still returns the file_search_call items, but their \"results\" array is omitted: you get the answer, not the chunk text.","title":"5. Read the answer and the cited chunks"},{"category":"section","location":"guide/retrieval/#Cleanup","page":"Retrieval & File Search","text":"Vector stores and files persist on OpenAI's servers until you remove them:\n\ndelete_vector_store(vs_id)\ndelete_file(file_id)","title":"Cleanup"},{"category":"section","location":"guide/retrieval/#See-Also","page":"Retrieval & File Search","text":"upload_file, FileObject — Files API upload and parsed object\ncreate_vector_store, vector_store_id — store creation and id accessor\ncreate_file_batch, poll_file_batch, add_vector_store_file — attach files and wait for indexing\nfile_search, FileSearchTool — the Responses hosted retrieval tool\nrespond, output_text, file_search_results — run the query and read results\nFiles API, Vector Stores API — full API reference\nResponses API, Tool Calling — related guides","title":"See Also"},{"category":"section","location":"api/endpoints/#endpoints_api","page":"Service Endpoints","text":"Types for configuring multi-backend service endpoints, and the extension API a new backend implements.","title":"Service Endpoints"},{"category":"section","location":"api/endpoints/#Abstract-Types","page":"Service Endpoints","text":"","title":"Abstract Types"},{"category":"section","location":"api/endpoints/#Built-in-Endpoints","page":"Service Endpoints","text":"TYPESAFEServiceEndpoint is not a chat backend: it serves the TypeSafe System One API (Jev) and declares only :system_one and :models, so the chat, Responses and embedding verbs reject it up front.","title":"Built-in Endpoints"},{"category":"section","location":"api/endpoints/#Generic-Endpoint","page":"Service Endpoints","text":"","title":"Generic Endpoint"},{"category":"section","location":"api/endpoints/#Configuration","page":"Service Endpoints","text":"Each endpoint reads its configuration from environment variables:","title":"Configuration"},{"category":"section","location":"api/endpoints/#OpenAI-(default)","page":"Service Endpoints","text":"Variable Description\nOPENAI_API_KEY Your OpenAI API key","title":"OpenAI (default)"},{"category":"section","location":"api/endpoints/#Azure-OpenAI","page":"Service Endpoints","text":"Variable Description\nAZURE_OPENAI_BASE_URL Azure endpoint base URL\nAZURE_OPENAI_API_KEY Azure API key\nAZURE_OPENAI_API_VERSION API version (e.g. 2024-10-21, the latest dated GA version)\nAZURE_OPENAI_DEPLOY_NAME_<MODEL> Deployment for a model, read at call time: the model id upper-cased, every character other than A-Z/0-9 mapped to _ (AZURE_OPENAI_DEPLOY_NAME_GPT_5_2 for gpt-5.2)\n\nA registration made with add_azure_deploy_name! wins over the variable; a model with neither fails the call with an ArgumentError (in the result's cause) naming the variable to set. Azure's newer v1 API (no api-version) is not wrapped by AZUREServiceEndpoint; see Multi-Backend Support for reaching it through GenericOpenAIEndpoint (untested).","title":"Azure OpenAI"},{"category":"section","location":"api/endpoints/#Google-Gemini","page":"Service Endpoints","text":"Variable Description\nGEMINI_API_KEY Your Gemini API key\n\nBoth GEMINIServiceEndpoint (native generateContent) and GEMINIOpenAIServiceEndpoint (the OpenAI-compat shim) read GEMINI_API_KEY.","title":"Google Gemini"},{"category":"section","location":"api/endpoints/#Anthropic-(Claude)","page":"Service Endpoints","text":"Variable Description\nANTHROPIC_API_KEY Your Anthropic API key","title":"Anthropic (Claude)"},{"category":"section","location":"api/endpoints/#TypeSafe-(System-One-/-Jev)","page":"Service Endpoints","text":"Variable Description\nTYPESAFE_API_KEY Your TypeSafe API key\nTYPESAFE_BASE_URL API root override (default https://api.typesafe.ai)\nTYPESAFE_DEFAULT_MODEL Model used when a call names none (default jev-latest)","title":"TypeSafe (System One / Jev)"},{"category":"section","location":"api/endpoints/#Azure-Deployment-Mapping","page":"Service Endpoints","text":"Azure requires model-to-deployment name mappings. Use add_azure_deploy_name! to register custom mappings:\n\nusing UniLM\n\n# Register a custom deployment for a specific model\nUniLM.add_azure_deploy_name!(\"gpt-5.2\", \"my-gpt52-deploy\")\nprintln(\"Registered deployment: \", UniLM._MODEL_ENDPOINTS_AZURE_OPENAI[\"gpt-5.2\"])\ndelete!(UniLM._MODEL_ENDPOINTS_AZURE_OPENAI, \"gpt-5.2\")  # cleanup\nRegistered deployment: /openai/deployments/my-gpt52-deploy","title":"Azure Deployment Mapping"},{"category":"section","location":"api/endpoints/#Selecting-a-Backend","page":"Service Endpoints","text":"Pass the service keyword to any request constructor:\n\nchat = Chat(service=UniLM.AZUREServiceEndpoint, model=\"gpt-5.2\")\nprintln(\"Service: \", chat.service)\nprintln(\"Model: \", chat.model)\nService: AZUREServiceEndpoint\nModel: gpt-5.2","title":"Selecting a Backend"},{"category":"section","location":"api/endpoints/#extension_api","page":"Service Endpoints","text":"The functions a new backend adds methods to, and the stream-state types its handlers mutate. They are declared public but not exported: qualify them (UniLM.get_url) when adding methods. Custom Backends walks through both cases — an OpenAI-compatible provider (routing and authentication only) and a provider with its own wire (the chat and agentic seams). Pre-1.0, this contract can still change in a minor release, under the CHANGELOG's Breaking heading.","title":"Extension API"},{"category":"section","location":"api/endpoints/#Routing,-authentication-and-defaults","page":"Service Endpoints","text":"provider_capabilities (exported) is the capability hook: an endpoint that defines it opts into validation against that set.","title":"Routing, authentication and defaults"},{"category":"section","location":"api/endpoints/#Chat-wire-seam","page":"Service Endpoints","text":"","title":"Chat wire seam"},{"category":"section","location":"api/endpoints/#Agentic-wire-seam","page":"Service Endpoints","text":"","title":"Agentic wire seam"},{"category":"type","location":"api/endpoints/#UniLM.ServiceEndpoint","page":"Service Endpoints","text":"ServiceEndpoint\n\nAbstract supertype for LLM service backends. Subtypes control URL routing and authentication.\n\nOpenAI-compatible backends subtype OpenAIWireEndpoint (itself a subtype of ServiceEndpoint) to inherit the chat request/response encoding and SSE handling; backends with a native wire (Anthropic, Gemini) subtype ServiceEndpoint directly and implement the wire seam (encode_request/decode_response/handle_sse_event!) themselves.\n\nBuilt-in subtypes:\n\nOPENAIServiceEndpoint — OpenAI API (default)\nAZUREServiceEndpoint — Azure OpenAI Service\nGEMINIOpenAIServiceEndpoint — Google Gemini via OpenAI-compatible endpoint\nGEMINIServiceEndpoint — Google Gemini native generateContent API\nANTHROPICServiceEndpoint — Anthropic (Claude) native Messages API\nGenericOpenAIEndpoint — any OpenAI-compatible provider (Ollama, Mistral, vLLM, etc.)\n\n\n\n\n\n","title":"UniLM.ServiceEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.OpenAIWireEndpoint","page":"Service Endpoints","text":"OpenAIWireEndpoint <: ServiceEndpoint\n\nAbstract supertype for backends that speak the OpenAI-compatible chat wire. Subtypes inherit the OpenAI Chat Completions request/response encoding (encode_request/decode_response) and the default SSE stream handling (handle_sse_event!) for free, so a new OpenAI-compatible provider defines only get_url and auth_header (plus, optionally, the _api_base_url pattern that routes the Responses/agentic surface).\n\nBackends that speak a genuinely different wire (Anthropic's Messages API, Gemini's native generateContent) subtype ServiceEndpoint directly and additionally implement encode_request, decode_response, and handle_sse_event!. A bare ServiceEndpoint subtype that omits those methods fails with a MethodError at call time rather than silently emitting OpenAI-shaped requests to a foreign API.\n\nBuilt-in OpenAI-wire subtypes: OPENAIServiceEndpoint, AZUREServiceEndpoint, GEMINIOpenAIServiceEndpoint, GenericOpenAIEndpoint, DeepSeekEndpoint.\n\n\n\n\n\n","title":"UniLM.OpenAIWireEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.OPENAIServiceEndpoint","page":"Service Endpoints","text":"OpenAI API service endpoint (default). Requires OPENAI_API_KEY env variable.\n\n\n\n\n\n","title":"UniLM.OPENAIServiceEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.AZUREServiceEndpoint","page":"Service Endpoints","text":"Azure OpenAI Service endpoint. Requires AZURE_OPENAI_BASE_URL, AZURE_OPENAI_API_KEY, and AZURE_OPENAI_API_VERSION env variables.\n\n\n\n\n\n","title":"UniLM.AZUREServiceEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.GEMINIServiceEndpoint","page":"Service Endpoints","text":"Native Google Gemini generateContent API (x-goog-api-key; model in URL). Requires GEMINI_API_KEY.\n\n\n\n\n\n","title":"UniLM.GEMINIServiceEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.GEMINIOpenAIServiceEndpoint","page":"Service Endpoints","text":"Google Gemini endpoint (OpenAI-compatible). Requires GEMINI_API_KEY env variable.\n\n\n\n\n\n","title":"UniLM.GEMINIOpenAIServiceEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.ANTHROPICServiceEndpoint","page":"Service Endpoints","text":"Anthropic (Claude) native Messages API endpoint. Requires the ANTHROPIC_API_KEY env variable. Native wire format (content blocks, top-level system, user/assistant roles) — NOT OpenAI-compatible.\n\n\n\n\n\n","title":"UniLM.ANTHROPICServiceEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.TYPESAFEServiceEndpoint","page":"Service Endpoints","text":"TYPESAFEServiceEndpoint <: ServiceEndpoint\n\nTypeSafe System One API endpoint (model family Jev). Requires the TYPESAFE_API_KEY environment variable; TYPESAFE_BASE_URL overrides the API root and TYPESAFE_DEFAULT_MODEL the default model name.\n\nThis endpoint speaks neither the OpenAI chat wire nor any other chat wire: it declares only :system_one and :models, so ask and list_models accept it while the verbs — chatrequest!, respond, embeddingrequest!, moderate and the other OpenAI platform verbs — reject it up front with an ArgumentError rather than posting a request no server here would answer. A Chat or an Embeddings that names this endpoint can be built only with an explicit model= (without one the constructor throws ArgumentError, since there is no default model to resolve); sending it is refused.\n\n\n\n\n\n","title":"UniLM.TYPESAFEServiceEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.GenericOpenAIEndpoint","page":"Service Endpoints","text":"GenericOpenAIEndpoint <: OpenAIWireEndpoint\n\nConfigurable endpoint for any OpenAI-compatible API provider. Supports Chat Completions, Embeddings, and (where the provider implements it) the Responses API.\n\nFields\n\nbase_url::String: Base URL without trailing slash (e.g., \"http://localhost:11434\")\napi_key::String: API key for Bearer auth. Use \"\" for local servers with no auth.\n\nExample\n\n# Ollama (local)\nchat = Chat(service=GenericOpenAIEndpoint(\"http://localhost:11434\", \"\"), model=\"llama3.1\")\n\n# Mistral\nchat = Chat(service=GenericOpenAIEndpoint(\"https://api.mistral.ai\", ENV[\"MISTRAL_API_KEY\"]),\n            model=\"mistral-large-latest\")\n\n\n\n\n\n","title":"UniLM.GenericOpenAIEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.ServiceEndpointSpec","page":"Service Endpoints","text":"ServiceEndpointSpec\n\nType alias accepting both marker types (OPENAIServiceEndpoint) and instances (GenericOpenAIEndpoint(...)). Used as the type of service fields.\n\n\n\n\n\n","title":"UniLM.ServiceEndpointSpec"},{"category":"function","location":"api/endpoints/#UniLM.OllamaEndpoint","page":"Service Endpoints","text":"OllamaEndpoint(; base_url=\"http://localhost:11434\") -> GenericOpenAIEndpoint\n\nPre-configured endpoint for Ollama local server.\n\n\n\n\n\n","title":"UniLM.OllamaEndpoint"},{"category":"function","location":"api/endpoints/#UniLM.MistralEndpoint","page":"Service Endpoints","text":"MistralEndpoint(; api_key=ENV[\"MISTRAL_API_KEY\"]) -> GenericOpenAIEndpoint\n\nPre-configured endpoint for Mistral AI API.\n\n\n\n\n\n","title":"UniLM.MistralEndpoint"},{"category":"type","location":"api/endpoints/#UniLM.DeepSeekEndpoint","page":"Service Endpoints","text":"DeepSeekEndpoint <: OpenAIWireEndpoint\n\nPre-configured endpoint for DeepSeek API. Supports chat completions, tool calling, FIM completion, and prefix completion.\n\nFIM and prefix completion use the beta base URL (https://api.deepseek.com/beta).\n\n\n\n\n\n","title":"UniLM.DeepSeekEndpoint"},{"category":"function","location":"api/endpoints/#UniLM.add_azure_deploy_name!","page":"Service Endpoints","text":"add_azure_deploy_name!(model::String, deploy_name::String)\n\nRegister an Azure OpenAI deployment for a given model name. Without a registration the deployment is read from AZURE_OPENAI_DEPLOY_NAME_<MODEL> (model id upper-cased, other characters mapped to _) when a request is built.\n\nExample\n\nadd_azure_deploy_name!(\"gpt-5.2\", \"my-gpt52-deployment\")\n\n\n\n\n\n","title":"UniLM.add_azure_deploy_name!"},{"category":"function","location":"api/endpoints/#UniLM.get_url","page":"Service Endpoints","text":"get_url(request) -> String\nget_url(service, request) -> String\n\nThe URL a request is sent to. get_url(chat), get_url(emb) and get_url(r::Respond) dispatch on the request's service, so a backend adds get_url(::MyEndpoint, ::Chat) (and, for the surfaces it serves, ::Embeddings or ::Respond): dispatch on the instance for an endpoint with fields, on ::Type{MyEndpoint} for a singleton. The built-in methods return the provider's documented URLs.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.get_url"},{"category":"function","location":"api/endpoints/#UniLM.auth_header","page":"Service Endpoints","text":"auth_header(service) -> Vector{Pair{String,String}}\n\nThe headers sent with every request to service: its credentials and the Content-Type. A backend adds auth_header(::MyEndpoint) (or auth_header(::Type{MyEndpoint}) for a singleton endpoint). The built-in endpoints read their key from the environment when a request is built, so a missing key surfaces in the verb's call-error result; a GenericOpenAIEndpoint with an empty key sends no Authorization header.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.auth_header"},{"category":"function","location":"api/endpoints/#UniLM.default_model","page":"Service Endpoints","text":"default_model(service) -> Union{String, Nothing}\n\nReturn the default chat/completions model for the given service endpoint. Returns nothing for generic and user-defined endpoints (model must be specified explicitly; Chat then throws an ArgumentError when it is not). A backend adds default_model(::MyEndpoint) to make model= optional for it.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.default_model"},{"category":"function","location":"api/endpoints/#UniLM.encode_request","page":"Service Endpoints","text":"encode_request(service, chat::Chat) -> String\n\nSerialize chat into the provider's request body. The OpenAIWireEndpoint default emits OpenAI Chat Completions JSON. An ArgumentError thrown here — an option the provider or model cannot express — is local validation: chatrequest! calls the encoder before any network I/O and lets it propagate.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.encode_request"},{"category":"function","location":"api/endpoints/#UniLM.decode_response","page":"Service Endpoints","text":"decode_response(service, resp::HTTP.Response)\n\nParse a provider's 200 response into (; message::Message, usage::Union{TokenUsage,Nothing}). The OpenAIWireEndpoint default reads OpenAI Chat Completions (extract_message). A body that is not a valid reply should throw: chatrequest! reports the exception as an LLMCallError carrying it in cause, never as an empty success.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.decode_response"},{"category":"function","location":"api/endpoints/#UniLM.handle_sse_event!","page":"Service Endpoints","text":"handle_sse_event!(service, event::AbstractString, payload::AbstractString,\n                  state::StreamState) -> Symbol\n\nPer-provider chat-streaming event handler — the seam that replaces decode_stream_chunk (removed in 0.11.3). Called by the stream driver once per complete data: payload, with the current SSE event name. Mutates state and returns\n\n:continue — keep reading;\n:done     — sentinel end-of-stream ([DONE], Anthropic message_stop);\n:error    — terminal in-band error; the handler MUST store the decoded payload in state.error (the driver then returns a non-success result).\n\nText deltas are appended to BOTH state.content and state.pending_delta (the driver forwards the latter to the streaming callback). Throwing on an undecodable payload is safe: the dispatcher logs, counts, and drops the line. The OpenAIWireEndpoint default reads OpenAI Chat Completions chunks and ends at [DONE]; a provider without a sentinel (native Gemini) never returns :done, and its stream ends at EOF with state.finish_reason recorded.\n\nPublic extension API (not exported): a backend adds a method for its own service type (dispatch is on the first argument); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.handle_sse_event!"},{"category":"type","location":"api/endpoints/#UniLM.StreamState","page":"Service Endpoints","text":"StreamState()\n\nMutable accumulator for one streamed chat attempt, filled by handle_sse_event! and read by the stream driver. A handler appends text to both content and pending_delta (the driver forwards the latter to the streaming callback), refusal text to refusal, and records finish_reason, usage (a TokenUsage) and, for an in-band error, the decoded payload in error. Tool calls live in tool_calls by index as Dict(\"id\" => …, \"type\" => \"function\", \"function\" => Dict(\"name\" => …, \"arguments\" => …)); \"complete\" => true on an entry tells the driver the call has finished streaming. The raw_* fields capture provider-native content blocks for verbatim round-trip, and sse_dropped counts undecodable payloads. A retried attempt starts from a fresh state.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.StreamState"},{"category":"function","location":"api/endpoints/#UniLM.encode_agentic","page":"Service Endpoints","text":"encode_agentic(service, r::Respond) -> String\n\nSerialize r into the request body of service's agentic surface. The OpenAIWireEndpoint default emits OpenAI Responses JSON; a backend with its own agentic wire overrides it (the Gemini Interactions backend is the in-repo reference). An ArgumentError thrown here — a field the wire cannot express — is local validation: respond calls the encoder before any network I/O and lets it propagate.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.encode_agentic"},{"category":"function","location":"api/endpoints/#UniLM.decode_agentic","page":"Service Endpoints","text":"decode_agentic(service, resp::HTTP.Response) -> ResponseObject\n\nParse service's 200 response into the neutral ResponseObject, whose output uses the OpenAI Responses item shapes the accessors read. A body that is not a valid response should throw: respond and the lifecycle operations report the exception as a ResponseCallError carrying it in cause.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.decode_agentic"},{"category":"function","location":"api/endpoints/#UniLM.decode_agentic_stream","page":"Service Endpoints","text":"decode_agentic_stream(service, chunk::String, state::AgenticStreamState)\n\nStreaming half of the agentic wire seam: consume one raw read's bytes, mutate state (an AgenticStreamState; text deltas go to both textbuff and pending_delta), and return (; done, event, data, terminal): done is true at a terminal event, terminal is :completed, :failed, :incomplete or :error, and data holds that event's decoded payload. Default: OpenAI Responses SSE via _parse_response_stream_chunk.\n\nPublic extension API (not exported); see the Custom Backends guide.\n\n\n\n\n\n","title":"UniLM.decode_agentic_stream"},{"category":"type","location":"api/endpoints/#UniLM.AgenticStreamState","page":"Service Endpoints","text":"AgenticStreamState()\n\nPublic extension API (not exported); see the Custom Backends guide.\n\nMutable per-stream assembly state for the agentic streaming seam (decode_agentic_stream). Carries the layer-1/2 SSE machinery state (carry partial-line buffer, last_event sticky event name), the accumulated output text (textbuff), the not-yet-forwarded text deltas (pending_delta), the per-stream drop count (sse_dropped), and — for providers whose terminal event omits the step list (Gemini Interactions) — a per-index registry of assembled steps: steps maps a step index to its (mutable) step dict, and order records first-seen index order for deterministic output rebuilding. text_by_step holds each step's streamed string (answer text, function-call argument JSON, or thought signature, depending on the step type), read once when the interaction completes.\n\n\n\n\n\n","title":"UniLM.AgenticStreamState"},{"category":"section","location":"api/uploads/#uploads_api","page":"Uploads","text":"Resumable, multi-part uploads for large files — beyond the single-request limit of the Files API. Create an upload, add its parts, then complete it to receive a FileObject. OpenAI only.","title":"Uploads API"},{"category":"section","location":"api/uploads/#Objects","page":"Uploads","text":"","title":"Objects"},{"category":"section","location":"api/uploads/#Result-Types","page":"Uploads","text":"","title":"Result Types"},{"category":"section","location":"api/uploads/#Request-Functions","page":"Uploads","text":"","title":"Request Functions"},{"category":"section","location":"api/uploads/#Usage","page":"Uploads","text":"# Start a resumable upload for a large file\nup = create_upload(filename=\"big.jsonl\", purpose=\"batch\",\n                   bytes=filesize(\"big.jsonl\"), mime_type=\"application/jsonl\")\n\n# Add one or more parts, collecting their ids\npart = add_upload_part(up.response.id, read(\"big.jsonl\"))\npart_ids = [part.response.id]\n\n# Complete to finalize (yields a FileObject), or cancel to abort\ndone = complete_upload(up.response.id, part_ids)\ndone isa UploadSuccess && println(\"File: \", done.response.file.id)\n# cancel_upload(up.response.id)","title":"Usage"},{"category":"type","location":"api/uploads/#UniLM.UploadObject","page":"Uploads","text":"UploadObject\n\nA resumable upload session: id, status, filename, and bytes; once the upload is completed, file holds the resulting FileObject. raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.UploadObject"},{"category":"type","location":"api/uploads/#UniLM.UploadPartObject","page":"Uploads","text":"UploadPartObject\n\nA single uploaded part; its id is passed to complete_upload to assemble the final file. raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.UploadPartObject"},{"category":"type","location":"api/uploads/#UniLM.UploadSuccess","page":"Uploads","text":"Successful create/complete/cancel result wrapping an UploadObject.\n\n\n\n\n\n","title":"UniLM.UploadSuccess"},{"category":"type","location":"api/uploads/#UniLM.UploadPartSuccess","page":"Uploads","text":"Successful add_upload_part result wrapping an UploadPartObject.\n\n\n\n\n\n","title":"UniLM.UploadPartSuccess"},{"category":"type","location":"api/uploads/#UniLM.UploadFailure","page":"Uploads","text":"Uploads API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.UploadFailure"},{"category":"type","location":"api/uploads/#UniLM.UploadCallError","page":"Uploads","text":"Uploads API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout.\n\n\n\n\n\n","title":"UniLM.UploadCallError"},{"category":"function","location":"api/uploads/#UniLM.create_upload","page":"Uploads","text":"create_upload(; filename, purpose, bytes, mime_type, service=OPENAIServiceEndpoint)\n\npurpose takes the Files API purposes (\"assistants\", \"batch\", \"fine-tune\", \"vision\", \"user_data\", \"evals\"); any other value throws ArgumentError before any request.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.create_upload"},{"category":"function","location":"api/uploads/#UniLM.add_upload_part","page":"Uploads","text":"add_upload_part(upload_id, data::Vector{UInt8}; service=OPENAIServiceEndpoint)  (multipart)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.add_upload_part"},{"category":"function","location":"api/uploads/#UniLM.complete_upload","page":"Uploads","text":"complete_upload(upload_id, part_ids; md5=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.complete_upload"},{"category":"function","location":"api/uploads/#UniLM.cancel_upload","page":"Uploads","text":"cancel_upload(upload_id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.cancel_upload"},{"category":"section","location":"api/webhooks/#webhooks_api","page":"Webhooks","text":"Verify inbound webhook signatures (Standard Webhooks / HMAC-SHA256) and parse events into typed values. This is an inbound utility with no provider endpoint — HMAC is built on the SHA standard library. OpenAI webhooks.","title":"Webhooks API"},{"category":"section","location":"api/webhooks/#Types-and-Constants","page":"Webhooks","text":"","title":"Types and Constants"},{"category":"section","location":"api/webhooks/#Functions","page":"Webhooks","text":"","title":"Functions"},{"category":"section","location":"api/webhooks/#Usage","page":"Webhooks","text":"# Verify an inbound webhook, then parse it into a typed event\nif verify_webhook(payload, headers, ENV[\"OPENAI_WEBHOOK_SECRET\"])\n    event = parse_webhook(payload)\n    event.type in WEBHOOK_EVENTS && println(\"Event: \", event.type, \" (\", event.id, \")\")\nend","title":"Usage"},{"category":"type","location":"api/webhooks/#UniLM.WebhookEvent","page":"Webhooks","text":"WebhookEvent\n\nA parsed webhook event from parse_webhook: id, type, and created_at; data holds the event payload and raw the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.WebhookEvent"},{"category":"constant","location":"api/webhooks/#UniLM.WEBHOOK_EVENTS","page":"Webhooks","text":"Known OpenAI webhook event types.\n\n\n\n\n\n","title":"UniLM.WEBHOOK_EVENTS"},{"category":"function","location":"api/webhooks/#UniLM.verify_webhook","page":"Webhooks","text":"verify_webhook(payload::AbstractString, headers, secret::AbstractString; tolerance_seconds=300) -> Bool\n\nVerify an OpenAI webhook signature (Standard Webhooks). payload is the raw request body; headers is a Dict or iterable of pairs containing webhook-id, webhook-timestamp, and webhook-signature; secret is the endpoint signing secret (with or without the whsec_ prefix). Returns true iff a fresh, validly-signed v1 signature is present.\n\nReplay protection: timestamps outside ±tolerance_seconds of now are rejected, as are timestamps that are not finite numbers — pass tolerance_seconds=Inf to skip the time check (e.g. when replaying a stored fixture). Uses a constant-time digest compare. Negative or NaN tolerances throw ArgumentError.\n\nThrows ArgumentError when secret is not valid base64: that is a misconfiguration of this endpoint, and reporting it as an unverified signature would silently drop every webhook. A false return therefore always means the message failed verification.\n\n\n\n\n\n","title":"UniLM.verify_webhook"},{"category":"function","location":"api/webhooks/#UniLM.parse_webhook","page":"Webhooks","text":"parse_webhook(payload::AbstractString) -> WebhookEvent\n\nParse a webhook JSON payload into a typed WebhookEvent. Verify the signature with verify_webhook first.\n\n\n\n\n\n","title":"UniLM.parse_webhook"},{"category":"section","location":"guide/streaming/#streaming_guide","page":"Streaming","text":"Both APIs support real-time streaming of generated tokens, so you can display partial results as they arrive.","title":"Streaming"},{"category":"section","location":"guide/streaming/#Chat-Completions-Streaming","page":"Streaming","text":"Set stream=true and provide a callback:\n\nchat = Chat(model=\"gpt-5.4-mini\", stream=true)\npush!(chat, Message(Val(:system), \"You are a poet.\"))\npush!(chat, Message(Val(:user), \"Write a very short 2-line poem about coding.\"))\ntask = chatrequest!(chat, callback=function(chunk, close)\n    if chunk isa String\n        print(chunk)\n    elseif chunk isa Message\n        println(\"\\n--- done ---\")\n    end\nend)\nresult = fetch(task)\nif result isa LLMSuccess\n    println(result.message.content)\nelse\n    println(\"Request failed — see result for details\")\nend\nCode hums softly in the dark,\nTurning thoughts to sparks.\n--- done ---\nCode hums softly in the dark,\nTurning thoughts to sparks.\n\nThe Chat Completions callback fires in a fixed sequence: with a String argument as text arrives — each argument is newly-generated text forwarded verbatim (several wire deltas may be coalesced into one callback), so multibyte characters are never split across chunk boundaries — and then exactly once at end-of-stream with the fully assembled Message, whose content equals the concatenation of every forwarded String.","title":"Chat Completions Streaming"},{"category":"section","location":"guide/streaming/#streaming_stop","page":"Streaming","text":"The callback receives a Ref{Bool} that you can set to true to stop streaming:\n\ntask = chatrequest!(chat, callback=function(chunk, close)\n    if chunk isa String\n        print(chunk)\n        if contains(chunk, \"bad word\")\n            close[] = true  # stop the stream\n        end\n    end\nend)\nresult = fetch(task)   # LLMCallError whose cause is UniLMCancelled(:callback, …)\n\nA stop takes effect at once: the connection is aborted, and the call ends with LLMCallError(status = nothing, cause = UniLMCancelled(:callback, elapsed)) (a ResponseCallError on the Responses path). It is never retried, no further callback runs, and the partial reply is not appended to the chat — unless the provider's completion marker already arrived: then the turn is committed, the final-message callback runs, and usage may be missing. On a Chat stream the marker is the chunk that carries the finish reason, which on the OpenAI wire comes before the usage chunk and [DONE], so a stop in that window — or from the final-message callback itself — returns the completed turn as the success it is. On the Responses path the marker is the terminal event: the response it carries stands, and its callback has already run.\n\nThe flag can also be set from another task that holds it. To stop a stream from outside the callback, cancel a CancelToken instead: pass cancel=tok (or run the call inside with_cancel(tok)) and call cancel!(tok) from any task. The result has the same shape, with cause.source == :token:\n\ntok = CancelToken()\ntask = chatrequest!(chat; cancel=tok, callback=(chunk, close) -> chunk isa String && print(chunk))\n# … later, from any task:\ncancel!(tok)\nresult = fetch(task)   # LLMCallError whose cause is UniLMCancelled(:token, …)\n\nA stop requested while the stream task is inside your callback takes effect when the callback returns. See Cancellation for the full contract.","title":"Stopping a Stream Early"},{"category":"section","location":"guide/streaming/#Exceptions-from-callbacks","page":"Streaming","text":"An exception thrown by callback or on_tool_call ends the call: fetch returns an LLMCallError (or ResponseCallError) whose cause is that exception. The call is never retried, nothing is appended to the chat, and no callback runs after it. A user InterruptException is the exception to that rule: it propagates, so fetch throws a TaskFailedException wrapping it.","title":"Exceptions from callbacks"},{"category":"section","location":"guide/streaming/#Streamed-Tool-Calls","page":"Streaming","text":"When the model streams tool calls, pass on_tool_call to be notified as each call completes. It fires at most once per tool call, in call order, receiving a fully assembled ToolCall whose arguments are already parsed (a zero-argument call arrives as an empty Dict); a call whose arguments do not parse as JSON is skipped with a warning rather than delivered. The text callback and on_tool_call are independent, so a single request can stream assistant text and surface tool calls as they finish:\n\n# weather_tool defined as in the Tool Calling guide\nchat = Chat(model=\"gpt-5.2\", tools=[weather_tool], stream=true)\npush!(chat, Message(Val(:system), \"Use the tools you are given.\"))\npush!(chat, Message(Val(:user), \"What's the weather in Paris and Tokyo?\"))\n\ntask = chatrequest!(chat;\n    callback = (chunk, close) -> chunk isa String && print(chunk),\n    on_tool_call = tc -> println(\"\\ntool call: \", tc.func.name, \" \", tc.func.arguments),\n)\nresult = fetch(task)\n\non_tool_call is supported on the chatrequest! streaming path for every chat provider (OpenAI-wire, native Anthropic, native Gemini); the Responses-API respond path does not surface it. It is a notification hook — the final assembled Message still carries every tool call (alongside any assistant text), so code that does not set on_tool_call loses nothing and can read result.message.tool_calls after fetch. The one call it omits is a partial one: on a turn that did not finish with \"tool_calls\" (cut at \"length\", say), a call whose arguments were cut off before they formed a JSON object is dropped, while the turn keeps its text, finish reason and usage.\n\ncallback and on_tool_call require stream=true: passed to a non-streaming call, they raise ArgumentError before any request is sent, rather than being ignored.","title":"Streamed Tool Calls"},{"category":"section","location":"guide/streaming/#Responses-API-Streaming","page":"Streaming","text":"The Responses API provides an even cleaner streaming interface using Julia's do-block syntax:\n\ntask = respond(\"Write a haiku about Julia programming.\", model=\"gpt-5.4-mini\") do chunk, close\n    if chunk isa String\n        print(chunk)\n    elseif chunk isa UniLM.ResponseObject\n        println(\"\\nDone! Status: \", chunk.status)\n    end\nend\nresult = fetch(task)\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nMethods bloom at dawn\nType-stable rivers flow fast\nJulia sings in code\nDone! Status: completed\nMethods bloom at dawn\nType-stable rivers flow fast\nJulia sings in code\n\nThe do-block form automatically sets stream=true.","title":"Responses API Streaming"},{"category":"section","location":"guide/streaming/#With-Explicit-Configuration","page":"Streaming","text":"r = Respond(\n    input=\"Explain quantum computing step by step\",\n    model=\"gpt-5.2\",\n    stream=true,\n    max_output_tokens=2000,\n)\nprintln(\"Stream enabled: \", r.stream)\nprintln(\"Request preview:\")\nprintln(JSON.json(r))\nStream enabled: true\nRequest preview:\n{\"input\":\"Explain quantum computing step by step\",\"max_output_tokens\":2000,\"model\":\"gpt-5.2\",\"stream\":true}","title":"With Explicit Configuration"},{"category":"section","location":"guide/streaming/#Streaming-Across-Providers","page":"Streaming","text":"Streaming is not OpenAI-only. The native Anthropic (ANTHROPICServiceEndpoint) and native Gemini (GEMINIServiceEndpoint) backends stream with the same callback / do-block API shown above — only the service (and model) change:\n\n# Native Anthropic streaming (Chat Completions)\nchat = Chat(service=ANTHROPICServiceEndpoint, stream=true)\npush!(chat, Message(Val(:system), \"You are a poet.\"))\npush!(chat, Message(Val(:user), \"Two lines about the sea.\"))\ntask = chatrequest!(chat, callback=(chunk, close) -> chunk isa String && print(chunk))\nfetch(task)\n\n# Native Gemini streaming (Chat Completions)\nchat = Chat(service=GEMINIServiceEndpoint, stream=true)\npush!(chat, Message(Val(:system), \"You are a poet.\"))\npush!(chat, Message(Val(:user), \"Two lines about the mountains.\"))\ntask = chatrequest!(chat, callback=(chunk, close) -> chunk isa String && print(chunk))\nfetch(task)\n\nStreaming preserves provider-native fidelity too: for Anthropic streams, the content blocks (including thinking blocks and their signatures) are re-assembled verbatim from the SSE deltas and attached to the final Message.provider_content, so a streamed tool-calling turn round-trips exactly like a non-streamed one.\n\nProviders on the OpenAI-compatible Chat Completions standard (DeepSeek, Ollama, vLLM, LM Studio, …) stream through the same stream=true + callback path.","title":"Streaming Across Providers"},{"category":"section","location":"guide/streaming/#Dropped-SSE-Payloads","page":"Streaming","text":"An SSE data: payload the parser cannot read is dropped rather than allowed to abort the turn, and each drop is counted for that stream. The count rides the result as sse_dropped — on LLMSuccess, LLMFailure, ResponseSuccess and ResponseFailure alike — and a single warning naming the count, model and surface fires once the stream finalizes, not per line. sse_dropped == 0 means a clean stream, and is what a non-streamed call always reports. A non-zero count means the turn was assembled from an incomplete wire: a provider-side or transport anomaly, never routine operation. On a truncated stream it is often the reason no message could be built at all, so it is worth reading on a failure result as well as on a success.","title":"Dropped SSE Payloads"},{"category":"section","location":"guide/streaming/#Notes","page":"Streaming","text":"A streaming call returns a Task (spawned with Threads.@spawn, on the default thread pool); fetch it for the final typed result. It works on any thread layout, a single thread included — but your callbacks run on that task, so keep them short and hand heavy work to another task (see Concurrency, Tasks and Cancellation).\nThe returned Task throws only for a user InterruptException; every failure — a timeout, a stop, a callback exception — is a typed result from fetch.\nThe close Ref{Bool} can be set to true from the callback (or from any task holding it) to terminate the stream early; see Stopping a Stream Early.\nOn completion, the Chat Completions callback receives a Message; the Responses API callback receives a ResponseObject.\nA Chat stream is final at its end-of-stream sentinel ([DONE], Anthropic's message_stop): the final Message callback runs at once, and the result follows when the HTTP body ends — at most 0.5 s later. A server that keeps the body open longer has that connection closed instead of reused.\nEach stream uses its own HTTP/1.1 connection, so a consumer that applies backpressure to one stream cannot stall its siblings; non-streaming requests may share an HTTP/2 connection.\nStreamed usage: OPENAIServiceEndpoint and DeepSeekEndpoint streams request stream_options = {\"include_usage\": true} automatically when stream_options is unset, so a streamed turn carries token usage (on .usage) and accrues cost like a non-streamed one. Native Anthropic and Gemini streams report usage on their own. Other OpenAI-compatible servers (Azure, the Gemini compat shim, generic endpoints) are not sent the field, since some reject it: set stream_options=Dict(\"include_usage\" => true) where the server supports it, or the streamed turn reports no usage and accrues $0. Empty-choices chunks, : keep-alive comment lines, and provider preambles (e.g. Azure content-filter results) are all tolerated without affecting the stream.\nA provider error mid-stream on an otherwise-200 response (e.g. an Anthropic overloaded_error) surfaces as an LLMFailure/LLMCallError, never a truncated LLMSuccess — the else branch in the examples above catches it.\nOne Chat per in-flight call. A Chat is unsynchronized mutable state, so do not share one across concurrent streams, and do not push! to it while its stream task is still running. Use fork to fan out — see Concurrency, Tasks and Cancellation.","title":"Notes"},{"category":"section","location":"guide/streaming/#See-Also","page":"Streaming","text":"Timeouts & Retries — the stream idle bound (wire idleness only: time in your callbacks is not counted) and what a completed-then-torn-down turn resolves to.\nConcurrency, Tasks and Cancellation — streaming into a Channel, backpressure, and cancelling a stream from another task.","title":"See Also"},{"category":"section","location":"api/moderations/#moderations_api","page":"Moderations","text":"Classify text and images for policy violations, returning per-category flags and confidence scores. Moderation is free. OpenAI only.","title":"Moderations API"},{"category":"section","location":"api/moderations/#Parsed-Objects","page":"Moderations","text":"","title":"Parsed Objects"},{"category":"section","location":"api/moderations/#Result-Types","page":"Moderations","text":"","title":"Result Types"},{"category":"section","location":"api/moderations/#Functions","page":"Moderations","text":"","title":"Functions"},{"category":"section","location":"api/moderations/#Usage","page":"Moderations","text":"# Classify text for policy violations (free)\nresult = moderate(\"...text to check...\")\nresult isa ModerationSuccess && println(\"Flagged? \", is_flagged(result))\n\n# Inspect per-category flags and scores\nif result isa ModerationSuccess\n    for m in result.response.results\n        m.flagged && println(m.categories)\n    end\nend","title":"Usage"},{"category":"type","location":"api/moderations/#UniLM.ModerationResult","page":"Moderations","text":"ModerationResult\n\nOne classification result: flagged (any policy violation), categories (per-category booleans), and category_scores (per-category confidence); raw holds the unparsed JSON result.\n\n\n\n\n\n","title":"UniLM.ModerationResult"},{"category":"type","location":"api/moderations/#UniLM.ModerationResponse","page":"Moderations","text":"ModerationResponse\n\nA moderation response: results, one ModerationResult per input, and the model used; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.ModerationResponse"},{"category":"type","location":"api/moderations/#UniLM.ModerationSuccess","page":"Moderations","text":"Successful moderate result wrapping a ModerationResponse.\n\n\n\n\n\n","title":"UniLM.ModerationSuccess"},{"category":"type","location":"api/moderations/#UniLM.ModerationFailure","page":"Moderations","text":"Moderations API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.ModerationFailure"},{"category":"type","location":"api/moderations/#UniLM.ModerationCallError","page":"Moderations","text":"Moderations API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout.\n\n\n\n\n\n","title":"UniLM.ModerationCallError"},{"category":"function","location":"api/moderations/#UniLM.moderate","page":"Moderations","text":"moderate(input; model=\"omni-moderation-latest\", service=OPENAIServiceEndpoint)\n\nClassify input (a String, a vector of strings, or a vector of multi-modal content parts) for policy violations (free). Returns ModerationSuccess, ModerationFailure, or ModerationCallError. A 200 whose results array does not hold exactly one row per input (one row for a string or a parts vector, one per string of a string vector) is a ModerationCallError: a verdict that cannot be matched to its input is no verdict.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.moderate"},{"category":"function","location":"api/moderations/#UniLM.is_flagged","page":"Moderations","text":"is_flagged(r) -> Bool\n\nTrue if any moderation result is flagged. Works on ModerationResult/ModerationResponse/ModerationSuccess.\n\nA call that did not succeed has no verdict, so ModerationFailure and ModerationCallError throw an ArgumentError rather than answering false: in the usual is_flagged(moderate(text)) && reject() shape, a false from a failed call would wave unmoderated content straight through. Check issuccess first, or handle the throw, and decide explicitly what an unavailable verdict should mean.\n\n\n\n\n\n","title":"UniLM.is_flagged"},{"category":"section","location":"stability/#Versioning-and-Stability","page":"Versioning & Stability","text":"UniLM follows Semantic Versioning, with the pre-1.0 conventions below. The aim is that the version number alone tells you whether an upgrade can break your code.","title":"Versioning & Stability"},{"category":"section","location":"stability/#What-0.x-means-here","page":"Versioning & Stability","text":"UniLM is pre-1.0 and under active development. While on 0.x:\n\nMINOR releases (e.g. 0.13.0 → 0.14.0) may include breaking changes. Every breaking change is listed under a Breaking heading in the CHANGELOG, with migration notes.\nPATCH releases (e.g. 0.14.0 → 0.14.1) never contain breaking changes — only fixes, additions, and documentation.\n\nPin accordingly: allowing patch upgrades is always safe; allowing minor upgrades means reading the CHANGELOG's Breaking section first.","title":"What 0.x means here"},{"category":"section","location":"stability/#Breaking-changes-are-batched","page":"Versioning & Stability","text":"Breaking changes are grouped into infrequent minor releases rather than dribbled across many small ones. When one lands, related changes tend to land with it, so you migrate once per minor instead of repeatedly.","title":"Breaking changes are batched"},{"category":"section","location":"stability/#Renames-keep-their-old-names","page":"Versioning & Stability","text":"When an exported name changes, the old name keeps working as an alias for at least until 1.0. A rename is a deprecation path, not a same-day hard break.","title":"Renames keep their old names"},{"category":"section","location":"stability/#Toward-1.0","page":"Versioning & Stability","text":"There is no date. 1.0 is defined by contract, not calendar — it ships when these hold:\n\na stable provider-extension contract, so adding or maintaining a backend does not depend on internals that shift underneath it;\na unified tool API across the chat and agentic surfaces, so a tool is defined once and used the same way everywhere;\nsemver-strict guarantees from then on: after 1.0, a breaking change requires a major version bump.","title":"Toward 1.0"},{"category":"section","location":"stability/#The-CHANGELOG-is-authoritative","page":"Versioning & Stability","text":"The CHANGELOG is the authoritative migration record. If a release breaks something, the exact change and how to migrate are documented there under that version's Breaking heading.","title":"The CHANGELOG is authoritative"},{"category":"section","location":"guide/tool_calling/#tools_guide","page":"Tool Calling","text":"Both the Chat Completions and Responses APIs support function/tool calling — the model can decide to invoke functions you define, and you return the results.","title":"Tool Calling"},{"category":"section","location":"guide/tool_calling/#Chat-Completions-Tool-Calling","page":"Tool Calling","text":"GPT-5.6, GPT-6 Sol, and GPT-6 Luna models require reasoning_effort=\"none\" when using Chat tools. Use Respond to combine reasoning with tool calls; GPT-6 Astra tools also require the Responses API.","title":"Chat Completions Tool Calling"},{"category":"section","location":"guide/tool_calling/#Defining-Tools","page":"Tool Calling","text":"Wrap your function schema in a Tool:\n\nusing UniLM\nusing JSON\n\nweather_tool = Tool(\n    func=FunctionSignature(\n        name=\"get_weather\",\n        description=\"Get current weather for a location\",\n        parameters=Dict(\n            \"type\" => \"object\",\n            \"properties\" => Dict(\n                \"location\" => Dict(\"type\" => \"string\", \"description\" => \"City name\"),\n                \"unit\" => Dict(\"type\" => \"string\", \"enum\" => [\"celsius\", \"fahrenheit\"])\n            ),\n            \"required\" => [\"location\"]\n        )\n    )\n)\nprintln(\"Tool type: \", weather_tool.type)\nprintln(\"Function name: \", weather_tool.func.name)\nprintln(\"Tool JSON:\")\nprintln(JSON.json(JSON.lower(weather_tool)))\nTool type: function\nFunction name: get_weather\nTool JSON:\n{\"function\":{\"name\":\"get_weather\",\"description\":\"Get current weather for a location\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"City name\",\"type\":\"string\"},\"unit\":{\"enum\":[\"celsius\",\"fahrenheit\"],\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"}},\"type\":\"function\"}","title":"Defining Tools"},{"category":"section","location":"guide/tool_calling/#Strict-Function-Calling","page":"Tool Calling","text":"Pass strict=true to make the API guarantee that tool-call arguments conform to your schema (no extra keys, all required fields present). A strict schema must set additionalProperties => false on every object and mark every property as required — the API rejects strict-invalid schemas with a 400. Omitting strict (the default) sends no flag at all: the request body is identical to previous UniLM versions.\n\nstrict_tool = Tool(\n    func=FunctionSignature(\n        name=\"get_weather\",\n        description=\"Get current weather for a location\",\n        parameters=Dict(\n            \"type\" => \"object\",\n            \"properties\" => Dict(\n                \"location\" => Dict(\"type\" => \"string\", \"description\" => \"City name\")\n            ),\n            \"required\" => [\"location\"],\n            \"additionalProperties\" => false\n        ),\n        strict=true\n    )\n)\nprintln(JSON.json(strict_tool))\n{\"function\":{\"name\":\"get_weather\",\"description\":\"Get current weather for a location\",\"parameters\":{\"additionalProperties\":false,\"properties\":{\"location\":{\"description\":\"City name\",\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"},\"strict\":true},\"type\":\"function\"}","title":"Strict Function Calling"},{"category":"section","location":"guide/tool_calling/#Making-Tool-Enabled-Requests","page":"Tool Calling","text":"chat = Chat(model=\"gpt-5.2\", tools=[weather_tool])\npush!(chat, Message(Val(:system), \"You are a helpful assistant with access to weather data.\"))\npush!(chat, Message(Val(:user), \"What's the weather in Paris?\"))\nprintln(\"Chat has \", length(chat.tools), \" tool(s) registered\")\nprintln(\"Request body:\")\nprintln(JSON.json(chat))\nChat has 1 tool(s) registered\nRequest body:\n{\"messages\":[{\"content\":\"You are a helpful assistant with access to weather data.\",\"role\":\"system\"},{\"content\":\"What's the weather in Paris?\",\"role\":\"user\"}],\"model\":\"gpt-5.2\",\"parallel_tool_calls\":false,\"tools\":[{\"function\":{\"name\":\"get_weather\",\"description\":\"Get current weather for a location\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"City name\",\"type\":\"string\"},\"unit\":{\"enum\":[\"celsius\",\"fahrenheit\"],\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"}},\"type\":\"function\"}]}","title":"Making Tool-Enabled Requests"},{"category":"section","location":"guide/tool_calling/#Handling-Tool-Calls","page":"Tool Calling","text":"When the model wants to call a function, the result message will have finish_reason == \"tool_calls\":\n\nchat = Chat(\n    model=\"gpt-5.4-mini\",\n    tools=[weather_tool],\n    tool_choice=UniLM.GPTToolChoice(func=:get_weather)\n)\npush!(chat, Message(Val(:system), \"Use the provided tools to answer.\"))\npush!(chat, Message(Val(:user), \"What's the weather in Paris?\"))\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(\"Finish reason: \", result.message.finish_reason)\n    tc = result.message.tool_calls[1]\n    println(\"Function: \", tc.func.name)\n    println(\"Arguments: \", JSON.json(tc.func.arguments, 2))\nelse\n    println(\"Request failed — see result for details\")\nend\nFinish reason: tool_calls\nFunction: get_weather\nArguments: {\n  \"location\": \"Paris\",\n  \"unit\": \"celsius\"\n}\n\nThe finish reason is the provider's own: a turn that carries tool calls reads \"tool_calls\" when the provider finished it with \"stop\" or reported none, and keeps any other reason (\"length\", \"content_filter\", a provider-specific value) — such a turn may hold partial calls, so do not run them. Partial calls are dropped: a call cut off before its arguments formed a JSON object is removed from such a turn, which keeps its text, finish reason and usage. On a \"tool_calls\" turn, whose calls would run, arguments that do not parse are an LLMCallError instead.\n\ntip: Streaming tool calls\nWhen streaming (stream=true), pass on_tool_call to chatrequest! to be notified as each tool call completes — at most once per call — instead of waiting for the final message. See the Streaming guide.\n\nnote: Gemini calls without wire ids\nGemini's chat path (generateContent) may omit FunctionCall.id. UniLM assigns such calls a synthetic positional id (unilm_call_1, unilm_call_2, …) so parallel tool results correlate correctly; synthetic ids never appear on the Gemini wire (the re-encoded request omits the id and correlates positionally, per the API contract). The unilm_call_ prefix is reserved. This applies to the chat surface only — the Interactions API always returns server-generated call ids.","title":"Handling Tool Calls"},{"category":"section","location":"guide/tool_calling/#Controlling-Tool-Choice","page":"Tool Calling","text":"# Let the model decide\nchat = Chat(tools=[weather_tool], tool_choice=\"auto\", reasoning_effort=\"none\")\n\n# Force the model to use a tool\nchat = Chat(tools=[weather_tool], tool_choice=\"required\", reasoning_effort=\"none\")\n\n# Prevent tool use\nchat = Chat(tools=[weather_tool], tool_choice=\"none\", reasoning_effort=\"none\")","title":"Controlling Tool Choice"},{"category":"section","location":"guide/tool_calling/#Responses-API-Tool-Calling","page":"Tool Calling","text":"The Responses API makes tool calling more ergonomic with dedicated types.","title":"Responses API Tool Calling"},{"category":"section","location":"guide/tool_calling/#Function-Tools","page":"Tool Calling","text":"tool = function_tool(\n    \"calculate\",\n    \"Evaluate a math expression\",\n    parameters=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"expression\" => Dict(\"type\" => \"string\")\n        ),\n        \"required\" => [\"expression\"]\n    ),\n    strict=true\n)\nprintln(\"Tool: \", tool.name, \" (strict=\", tool.strict, \")\")\nprintln(\"JSON: \", JSON.json(JSON.lower(tool)))\nTool: calculate (strict=true)\nJSON: {\"description\":\"Evaluate a math expression\",\"name\":\"calculate\",\"parameters\":{\"properties\":{\"expression\":{\"type\":\"string\"}},\"required\":[\"expression\"],\"type\":\"object\"},\"strict\":true,\"type\":\"function\"}\n\nweather_fn = function_tool(\n    \"get_weather\",\n    \"Get current weather for a location\",\n    parameters=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"location\" => Dict(\"type\" => \"string\", \"description\" => \"City name\"),\n            \"unit\" => Dict(\"type\" => \"string\", \"enum\" => [\"celsius\", \"fahrenheit\"])\n        ),\n        \"required\" => [\"location\"]\n    )\n)\nresult = respond(\"What's the weather in Tokyo? Use celsius.\", tools=[weather_fn], model=\"gpt-5.4-mini\")\ncalls = function_calls(result)   # empty for a failed call\nif !isempty(calls)\n    println(\"Function: \", calls[1][\"name\"])\n    println(\"Arguments: \", JSON.json(JSON.parse(calls[1][\"arguments\"]), 2))\nelse\n    println(\"No function calls — \", result isa ResponseSuccess ? output_text(result) : result)\nend\nFunction: get_weather\nArguments: {\n  \"location\": \"Tokyo\",\n  \"unit\": \"celsius\"\n}","title":"Function Tools"},{"category":"section","location":"guide/tool_calling/#Web-Search","page":"Tool Calling","text":"The model can search the web — no function implementation needed:\n\nws = web_search(context_size=\"high\")\nprintln(\"Web search tool type: \", typeof(ws))\nprintln(\"Context size: \", ws.search_context_size)\nWeb search tool type: WebSearchTool\nContext size: high\n\nresult = respond(\n    \"What is the latest stable release of the Julia programming language?\",\n    tools=[web_search()],\n    model=\"gpt-5.4-mini\"\n)\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nThe latest **stable** release of Julia is **v1.13.0**, released on **September 10, 2026**. The Julia downloads page lists **v1.13.0 (September 9, 2026)** as the current stable release, and the Julia blog/announcement confirms that Julia 1.13 has been released. ([julialang.org](https://www.julialang.org/downloads/manual-downloads/?utm_source=openai))\n\nIf you want, I can also tell you the current **LTS** version or the best way to install it.","title":"Web Search"},{"category":"section","location":"guide/tool_calling/#File-Search","page":"Tool Calling","text":"Search over your uploaded vector stores:\n\nresult = respond(\n    \"Find the error handling policy\",\n    tools=[file_search([\"vs_store_id_123\"], max_results=5)]\n)","title":"File Search"},{"category":"section","location":"guide/tool_calling/#Combining-Tools","page":"Tool Calling","text":"Mix different tool types freely:\n\ntools = [\n    web_search(),\n    function_tool(\"save_summary\", \"Save a summary to the database\",\n        parameters=Dict(\n            \"type\" => \"object\",\n            \"properties\" => Dict(\n                \"title\" => Dict(\"type\" => \"string\"),\n                \"content\" => Dict(\"type\" => \"string\")\n            )\n        )\n    )\n]\nprintln(\"Number of tools: \", length(tools))\nfor t in tools\n    println(\"  - \", typeof(t))\nend\nNumber of tools: 2\n  - WebSearchTool\n  - FunctionTool","title":"Combining Tools"},{"category":"section","location":"guide/tool_calling/#Tool-Choice,-Tool-Results-and-Hosted-Tools","page":"Tool Calling","text":"Constrain which tool the model may call with the tool_choice= builders (tool_choice_function, tool_choice_hosted, tool_choice_allowed, tool_choice_mcp, tool_choice_custom):\n\nr = Respond(input=\"What's the weather?\",\n            tools=[function_tool(\"get_weather\", \"Get weather\",\n                       parameters=Dict(\"type\" => \"object\",\n                                       \"properties\" => Dict(\"location\" => Dict(\"type\" => \"string\"))))],\n            tool_choice=tool_choice_function(\"get_weather\"))\nprintln(r.tool_choice)\nDict{Symbol, Any}(:type => \"function\", :name => \"get_weather\")\n\nReturn a tool's output on the next turn with tool_result:\n\nrespond(Respond(; previous_response_id=r1.response.id,\n                input=[tool_result(\"call_abc\", \"get_weather\", \"72F and sunny\")]))\n\nGemini Interactions adds server-side hosted tools — see the Agentic Workflows guide for gemini_google_search and friends. When Gemini returns tool calls, the provider's opaque reasoning token is preserved on ToolCall.thought_signature and echoed automatically on the next turn.","title":"Tool Choice, Tool Results & Hosted Tools"},{"category":"section","location":"guide/tool_calling/#Automated-Tool-Loop","page":"Tool Calling","text":"Instead of manually handling tool calls, use tool_loop! (Chat Completions) or tool_loop (Responses API) for automatic dispatch. Both send a request, run the calls the model asked for through your dispatcher (or the CallableTool callables), send the results back, and repeat until the model answers in text, a request fails, or max_turns (default 10) round-trips are used.\n\nWhat reaches the model. A dispatcher's String result is sent as is; any other value is sent JSON-encoded (a Dict becomes a JSON object, nothing becomes null). A dispatcher that throws sends \"Error: <message>\" as that call's output and records a failed ToolCallOutcome, so the model can react; an InterruptException propagates instead, after tool_loop! removes the interrupted turn from the chat.\nConcurrency and cancellation. tool_concurrency = n runs up to n of a turn's calls at once on spawned tasks (the dispatcher must then be thread-safe; results go back in call order). cancel = tok — or an ambient with_cancel scope — makes the loop cancellable: every turn, its request and its dispatches observe the token, and a cancelled loop returns completed=false with a call error whose cause is UniLMCancelled. See Concurrency, Tasks and Cancellation.\nmax_turns below 1 throws ArgumentError; when the turns run out, response is the last response the model sent (a tool-call turn whose calls ran), with completed=false and llm_error = \"max turns (N) exhausted\".","title":"Automated Tool Loop"},{"category":"section","location":"guide/tool_calling/#Chat-Completions","page":"Tool Calling","text":"ct = CallableTool(weather_tool, (name, args) -> \"22C, sunny in $(args[\"location\"])\")\nprintln(\"Callable tool wrapping: \", ct.tool.func.name)\nCallable tool wrapping: get_weather\n\nchat = Chat(model=\"gpt-5.2\", tools=[ct.tool])\npush!(chat, Message(Val(:system), \"You are a helpful assistant.\"))\npush!(chat, Message(Val(:user), \"What's the weather in Paris?\"))\nresult = tool_loop!(chat; tools=[ct])\n# result.completed == true when the model gives a text response\n\ntool_loop! needs chat.history == true — each follow-up request carries the assistant turn its tool results answer — and throws ArgumentError before any request otherwise. It runs a turn's calls only when the turn finished with \"tool_calls\". A turn that carries calls but finished for another reason (\"length\", \"content_filter\", …) may hold partial calls: none runs, the loop stops with completed=false and an llm_error naming the reason, and that unanswered assistant turn is removed from chat, so the conversation stays sendable. A turn cancelled between dispatches, or interrupted by an InterruptException from a dispatch, is removed the same way. A text turn completes the loop only when it finished with \"stop\" or reported no reason (a \"tool_calls\" finish with no calls counts too); \"length\" ends it with completed=false and llm_error = \"Model output was truncated by the token limit\", and any other reason (\"content_filter\", a provider-specific value) with an llm_error naming the reason. callback and on_tool_call are passed to chatrequest! unchanged, so they need a Chat with stream=true.","title":"Chat Completions"},{"category":"section","location":"guide/tool_calling/#Responses-API","page":"Tool Calling","text":"ct = CallableTool(\n    function_tool(\"get_weather\", \"Get weather\", parameters=Dict(...)),\n    (name, args) -> \"22C, sunny\")\nresult = tool_loop(\"What's the weather?\"; tools=[ct])\n\nThe Responses loop chains turns through previous_response_id — or, when the Respond sets conversation, through the conversation alone (the API rejects the two together). It runs function calls only on a completed or requires_action turn; any other status (for example incomplete, whose calls may be partial) stops it with completed=false and an llm_error naming the status and the incomplete_details reason. A call whose arguments are not a JSON object is answered with an \"Error: invalid arguments: …\" output, like a dispatcher error, and the loop goes on. A turn that requests a client-side action the loop cannot execute — custom_tool_call, apply_patch_call, local_shell_call, computer_call, a shell_call the platform did not run itself (a hosted shell answers its own call with a shell_call_output in the same output) or an mcp_approval_request — stops it with completed=false, naming the pending type, and none of that turn's calls run.","title":"Responses API"},{"category":"section","location":"guide/tool_calling/#MCP-Tool-Integration","page":"Tool Calling","text":"MCP servers expose tools that integrate directly with the tool loop via mcp_tools and mcp_tools_respond. See the MCP Guide for full details.\n\n# Chat Completions + MCP\nsession = mcp_connect(`npx server`)\ntools = mcp_tools(session)\nchat = Chat(model=\"gpt-5.2\", tools=map(t -> t.tool, tools))\npush!(chat, Message(Val(:system), \"You are a helpful assistant.\"))\npush!(chat, Message(Val(:user), \"Do something\"))\nresult = tool_loop!(chat; tools)\n\n# Responses API + MCP\ntools = mcp_tools_respond(session)\nresult = tool_loop(\"Do something\"; tools=tools)","title":"MCP Tool Integration"},{"category":"section","location":"guide/tool_calling/#Inspecting-the-Result","page":"Tool Calling","text":"tool_loop / tool_loop! return a ToolLoopResult: the last response the loop received, the list of tool_calls that ran (each a ToolCallOutcome), turns_used, whether it completed, and — when it did not — the llm_error saying why.\n\nresult = tool_loop(\"What's the weather in Paris and Tokyo?\"; tools=[ct])\n\nif result.completed\n    println(output_text(result.response))\nelse\n    # completed=false: a failed or cancelled request, max_turns, truncated or filtered\n    # output, or a pending action the loop cannot run — llm_error says which\n    println(\"Stopped after $(result.turns_used) turns: \", result.llm_error)\nend\n\nfor oc in result.tool_calls          # one ToolCallOutcome per executed tool call\n    status = oc.success ? \"ok\" : \"error: $(oc.error)\"\n    println(oc.tool_name, oc.arguments, \" -> \", status)\nend","title":"Inspecting the Result"},{"category":"section","location":"guide/tool_calling/#See-Also","page":"Tool Calling","text":"Tool, FunctionSignature — Chat Completions tool types\nFunctionTool, WebSearchTool, FileSearchTool — Responses API tool types\nfunction_tool, web_search, file_search — convenience constructors\nCallableTool, ToolCallOutcome, ToolLoopResult — tool loop types\ntool_loop!, tool_loop — automated tool dispatch\nMCP Guide — MCP server integration","title":"See Also"},{"category":"section","location":"guide/timeouts/#timeouts_guide","page":"Timeouts & Retries","text":"Every UniLM operation — a provider request, a stream, an MCP exchange, or a Realtime WebSocket — waits on a peer only under a bounded, configurable limit, and reports a breach as a typed error. This guide covers the bounds, how to change them, and the typed failures you get when one fires.\n\nOne deliberate exception: an open Realtime session's lifetime is the caller's to decide, so nothing bounds it (see Realtime sessions).","title":"Timeouts & Retries"},{"category":"section","location":"guide/timeouts/#The-one-config-struct","page":"Timeouts & Retries","text":"All bounds live on a single RequestConfig, resolved per call on every request verb (and captured at connect time by the MCP and Realtime sessions):\n\nField Default Bounds\nconnect_timeout 10.0 s Establishing the connection, per attempt.\nrequest_timeout 600.0 s The whole non-streaming exchange, per attempt; a stream's exchange up to its first byte.\nstream_idle_timeout 120.0 s Byte-gap between raw stream chunks (see Streaming).\ntotal_deadline 900.0 s Across all attempts including backoff; for streams, until the first byte.\nmax_attempts 3 Total attempts (1 = no retry).\nmcp_connect_timeout 120.0 s MCP spawn → initialize handshake complete.\nmcp_request_timeout 120.0 s One MCP request/response exchange.\n\nAll timeout fields are seconds (Float64). Set any of them to Inf to disable that bound. The constructor rejects NaN, non-positive values and finite values above 1e9 seconds (use Inf to disable a bound) with an ArgumentError, and requires max_attempts ≥ 1 — a silently-NaN bound would compare false against every check and reintroduce an unbounded wait, and a finite value too large for a timer would fail deep inside a call instead of at construction.\n\nusing UniLM\n\ncfg = RequestConfig(request_timeout = 120.0, max_attempts = 5)\n\n# Copy-with-overrides — change one field, keep the rest:\npatient = RequestConfig(cfg; total_deadline = 3600.0)\n\n# Disable the stream idle bound entirely (never idle-kill a stream):\nno_idle = RequestConfig(stream_idle_timeout = Inf)","title":"The one config struct"},{"category":"section","location":"guide/timeouts/#Why-these-defaults","page":"Timeouts & Retries","text":"The defaults mirror the official provider SDKs, with headroom for HTTP.jl's heavier connect path:\n\nrequest_timeout = 600s matches the OpenAI and Anthropic Python SDKs (Timeout(600.0, connect=5.0)) and the OpenAI Node SDK (10-minute default).\nmax_attempts = 3 matches those SDKs' two automatic retries (two retries after the first attempt = three attempts).\nconnect_timeout = 10s is 2× the SDKs' 5s connect, headroom for HTTP.jl's heavier connect; a false connect-timeout costs one retry and has no side effects.\nstream_idle_timeout = 120s is stricter than the reference SDK stack's 600s streaming read timeout. It is provisional, backed by the per-provider gap measurement under Measured stream gaps below, and the per-call override is the escape hatch for unusually silent reasoning streams.\ntotal_deadline = 900s and the 120s MCP bounds cap cold starts (a cold npx MCP server on a slow link can legitimately approach the connect bound — the timeout message names the override to raise).","title":"Why these defaults"},{"category":"section","location":"guide/timeouts/#Measured-stream-gaps","page":"Timeouts & Retries","text":"The 120s idle default is provisional and backed by a measurement (scripts/measure_stream_gaps.jl) of the maximum inter-byte gap per provider — the exact quantity the idle guard watches, which provider keep-alives (SSE comments, Anthropic pings) reset. The table was last measured on 2026-07-18, on models that are no longer the defaults:\n\nProvider Model Max inter-byte gap Headroom vs 120s\nOpenAI (reasoning) gpt-5.5 8.5s (504 chunks, 16.1s stream) 14×\nAnthropic (extended thinking) — not measured this release —\nGemini gemini-3.5-flash 15.9s (55 chunks, 19.8s stream) 7.5×\n\nThe Gemini stream held a healthy ~16-second silent gap mid-thinking — the class of pause the byte-gap default must tolerate; both measured maxima sit well under the 60s raise-the-default threshold. A healthy gap above 60s in a future measurement raises the default.","title":"Measured stream gaps"},{"category":"section","location":"guide/timeouts/#The-four-channels","page":"Timeouts & Retries","text":"A config is resolved, in precedence order:\n\nthe per-call config= keyword on any request verb,\na with_request_config dynamic scope,\nthe process default (set_default_config!),\nthe built-in defaults above.\n\ncurrent_config() returns whatever is in force right now.","title":"The four channels"},{"category":"section","location":"guide/timeouts/#1.-Per-call","page":"Timeouts & Retries","text":"chatrequest!(chat; config = RequestConfig(request_timeout = 60.0))\nrespond(\"summarize this\"; config = RequestConfig(max_attempts = 1))","title":"1. Per call"},{"category":"section","location":"guide/timeouts/#2.-A-dynamic-scope","page":"Timeouts & Retries","text":"with_request_config merges the given keywords over the current config for the duration of the block, and propagates into tasks spawned inside it (including the task a streaming call runs on):\n\nwith_request_config(request_timeout = 30.0, max_attempts = 1) do\n    chatrequest!(chat)          # both calls see the 30s / no-retry config\n    embeddingrequest!(emb)\nend","title":"2. A dynamic scope"},{"category":"section","location":"guide/timeouts/#3.-The-process-default-(notebooks)","page":"Timeouts & Retries","text":"A notebook cell cannot hold a dynamic scope open across cells, so set a process default once:\n\nset_default_config!(request_timeout = 45.0, stream_idle_timeout = 300.0)\n# ... every later call in the session inherits it, unless overridden ...\ncurrent_config().request_timeout    # 45.0\n\nset_default_config!(cfg) replaces the default with cfg; the keyword form merges over the current default.","title":"3. The process default (notebooks)"},{"category":"section","location":"guide/timeouts/#4.-Defaults","page":"Timeouts & Retries","text":"Do nothing and you get the table above.","title":"4. Defaults"},{"category":"section","location":"guide/timeouts/#timeout_streams","page":"Timeouts & Retries","text":"Streams are governed differently from single-shot requests:\n\nUntil the first byte, a stream attempt is bound by min(request_timeout, remaining total_deadline): a peer that has not sent the response headers by then — still reading the request, or never answering — ends the attempt with UniLMTimeout(:request, …), which is retried like any per-attempt timeout. stream_idle_timeout bounds that wait too, because HTTP.jl caps the response-header wait with its read-idle timer, so the effective pre-first-byte bound is min(request_timeout, remaining total_deadline, stream_idle_timeout); when the idle bound is the smallest, the breach reports phase :stream_idle even though no byte arrived. A TCP connect or TLS handshake already in progress is not cut short: it finishes, or reaches connect_timeout, first. A stream that never starts fails typed like any other request.\nAfter the first byte, only the idle bound runs: stream_idle_timeout is the maximum gap between raw byte chunks off the socket — NOT between parsed events. SSE comment lines and Anthropic ping events are real bytes, so they reset the idle clock. The bound measures wire idleness only: time the stream task spends inside your callback or on_tool_call is not counted, and the clock restarts when the callback returns, so a slow consumer never idle-kills a healthy stream. A stream that keeps delivering bytes within the bound runs as long as it needs; one whose provider goes silent for longer than the bound (a long silent reasoning phase, say) is ended — raise the bound for those.\nA 1-byte-per-interval trickle keeps a stream alive. The guard watches raw byte arrival, so a server dribbling a byte inside every idle window is, by construction, not idle. This is a deliberate, documented limit: distinguishing \"healthy but slow\" from \"hung but trickling\" is not possible from byte timing alone, and the safe choice is to not kill a stream that is still delivering bytes.\nA completed turn is never discarded or re-sent. Once a stream has recorded its terminal state — the assembled message, or the provider's own completion marker — anything that goes wrong while the connection is torn down is teardown noise: an idle breach, the native read-idle timer, a transport reset, a truncated read. The generation is already billed and its deltas already delivered, so the result is finalized as a success rather than failed or retried; re-POSTing it would bill a second generation for one caller request. The same holds for a stop or cancel that lands then: the turn is committed, the final-message callback runs, and usage may be missing. Trailing bytes past the breach (a late usage frame) may be lost — accepted. This holds on every provider and on both the Chat and agentic stream drivers. Gemini is the clearest case, since its stream has no sentinel at all and simply ends at EOF. Failures that are not teardown-shaped — a throwing callback, a decoding bug — still surface as failures.\n\n# Raise the idle bound for a reasoning-heavy stream expected to go quiet\n# (`chat` was built with stream=true):\ntask = chatrequest!(chat; config = RequestConfig(stream_idle_timeout = 300.0),\n                    callback = (chunk, close) -> chunk isa String && print(chunk))\nresult = fetch(task)","title":"Stream semantics"},{"category":"section","location":"guide/timeouts/#timeout_realtime","page":"Timeouts & Retries","text":"The Realtime WebSocket surface takes the same config= keyword and resolves it the same four ways. Two phases are bounded, and one deliberately is not:\n\nrealtime_connect(model = \"gpt-realtime-2\",\n                 config = RequestConfig(connect_timeout = 5.0,\n                                        stream_idle_timeout = 30.0)) do session\n    realtime_send(session, response_create())\n    event = realtime_receive(session)     # bounded by stream_idle_timeout\nend\n\nOpening is bounded by connect_timeout. A peer that accepts the TCP connection but never finishes the upgrade throws UniLMTimeout(:connect, …) instead of blocking; an open that fails for another reason (a 401 upgrade response, say) throws that error, even when it fails just before the bound. An upgrade that completes only after the caller received the timeout is closed without running the handler.\nrealtime_receive is bounded by stream_idle_timeout, taken from the config the session captured at connect time. A breach throws UniLMTimeout(:stream_idle, …). A parked socket read cannot be polled out of, so the guard closes the socket to unblock it — and HTTP.jl's WebSocket close gives the peer up to ~5 s to acknowledge, so the breach surfaces within [limit, limit + ~5 s] rather than exactly at the limit. Set the bound with that grace in mind; Inf disables it and parks indefinitely.\nThe session's lifetime is not bounded. Once your handler is running, how long it stays connected is your decision, not a timeout's — a Realtime session is meant to sit idle waiting for the user to speak. Bound the conversation yourself if you need to.\n\nThe unlike-HTTP shapes here are that Realtime throws rather than returning a typed result value, and that realtime_connect makes a single attempt — max_attempts does not apply to it. It also throws ArgumentError before any I/O for a service other than OPENAIServiceEndpoint: the Realtime WebSocket is served by api.openai.com, and another endpoint's credentials must not be sent there. Neither phase observes a CancelToken. mint_realtime_secret is an ordinary HTTP verb: a single bounded attempt returning RealtimeSecretSuccess, RealtimeFailure, or RealtimeCallError — the latter also for a 200 that carries no non-empty secret.","title":"Realtime sessions"},{"category":"section","location":"guide/timeouts/#Typed-failures","page":"Timeouts & Retries","text":"A timeout is never a silent stall and never a fabricated HTTP status.\n\nValue-returning surfaces (chatrequest!, embeddingrequest!, respond, the image, FIM, System One and platform verbs) return their usual call-error result with status = nothing and the UniLMTimeout on the cause field — every *CallError type carries cause:\n\nresult = chatrequest!(chat; config = RequestConfig(request_timeout = 5.0))\nif result isa LLMCallError && result.cause isa UniLMTimeout\n    t = result.cause\n    @warn \"timed out\" phase=t.phase elapsed=t.elapsed limit=t.limit\nend\n\nUniLMTimeout.phase is one of :connect, :request, :stream_idle, or :deadline.\n\nStreaming returns a Task that resolves to the same typed results: a timeout anywhere in the stream is an LLMCallError / ResponseCallError with cause::UniLMTimeout, never a TaskFailedException (the task throws only for a user InterruptException). Always fetch a streaming task and handle failure:\n\ntask = chatrequest!(chat; callback = (chunk, close) -> chunk isa String && print(chunk))\nresult = fetch(task)     # LLMSuccess, or a call-error carrying the UniLMTimeout\n\nLatency of a breach: a non-streaming attempt's bound fires within a few milliseconds of its limit, and a successful call pays no detection delay — its completion wakes the caller directly. The stream idle check runs every min(stream_idle_timeout / 4, 5) s, so an idle breach surfaces within [limit, limit + period].\nMCP surfaces are throw-based: a timeout throws MCPTimeoutError, whose message names the override to raise.","title":"Typed failures"},{"category":"section","location":"guide/timeouts/#Retries","page":"Timeouts & Retries","text":"Automatic retries apply to the inference verbs — chatrequest!, embeddingrequest!, respond, fim_complete, prefix_complete, generate_image, edit_image, ask, list_models, and the tool_loop family through them (streams retry only before the first callback fires). Every other verb makes a single bounded attempt and max_attempts has no effect on it: the platform and lifecycle verbs (batch, container, conversation, file, fine-tuning, moderation, upload, vector-store, audio, realtime, and the Responses lifecycle operations), including upload_file — a create is never retried, because a POST that timed out or drew a gateway 5xx may already have stored the file. The polling helpers poll_batch and poll_file_batch instead poll through retryable statuses and per-attempt timeouts until their own wall-clock timeout.\n\nmax_attempts (default 3) caps the total attempts. All attempts share the single total_deadline:\n\nA retryable outcome (HTTP 408/429/500/502/503/504/529, a per-attempt connect/request timeout, or a transport-level IO error) is retried with full-jitter exponential backoff.\nRetry-After is a floor, not a replacement. The pause is the header's wait plus a random spread above it — at most the exponential backoff, and at most half the budget left after the floor — so clients that received the same header do not retry in lockstep, and the next attempt keeps at least half of what remains.\nA retry that could not finish inside the remaining deadline is not attempted. If the pause would exceed the remaining total_deadline — with a header, only when the header's own wait does — the call fails immediately and returns the last real response: a budget-exhausted 429 stays a 429, never a fabricated timeout.\nAn InterruptException and a cancellation are never retried.\n\n# Three attempts, but give up entirely after 30s of wall-clock:\nrespond(\"…\"; config = RequestConfig(max_attempts = 3, total_deadline = 30.0))","title":"Retries"},{"category":"section","location":"guide/timeouts/#MCP-timeouts","page":"Timeouts & Retries","text":"# Per-connection bounds captured at connect time:\nsession = mcp_connect(`npx -y @modelcontextprotocol/server-filesystem /tmp`;\n    config = RequestConfig(mcp_connect_timeout = 60.0, mcp_request_timeout = 30.0))\n\n# The request-phase timeout resolves at CALL time:\n#   explicit timeout  >  ambient with_request_config scope  >  captured config\ncall_tool(session, \"read_file\", Dict(\"path\" => \"/tmp/x\"); timeout = 10.0)\n\nConnect wraps spawn → initialize → notifications/initialized → tool/resource/prompt discovery under mcp_connect_timeout. A command-not-found fails immediately (it does not ride the timer). The timeout message names the per-connect override.\nThe per-call bound covers the wait for the session too. Calls on one session run one at a time, in arrival order. A call that cannot acquire the session within its bound throws MCPTimeoutError with phase :queue without touching it; once it holds the session, its exchange gets the full bound (phase :request), so a waiter's clock never cuts the exchange in progress short.\nStdio requests are session-fatal on timeout. A read blocked on an unresponsive server can be released only by killing the server, so on timeout the watchdog closes the pipe, MCPTimeoutError is thrown at about the bound, and the transport (including any wrapper's child process group) is torn down. A reply that races the teardown is discarded: the call is a timeout.\nAuto-respawn is opt-in and covers hangs and crashes. A timeout closes a stdio session with cause :timeout; a server that exits or whose pipe breaks throws MCPCrashError on the call in flight and closes it with cause :crash. Either way the next call raises MCPSessionClosedError — unless you opened the session with auto_respawn = true, which respawns the same command (fresh handshake, logged loudly) and runs the call. In-memory server state is lost on respawn, which is why it is off by default; silent respawn would fabricate session continuity.\nAmbient scope reaches bridged tools. Tools bridged into a tool loop pass no keyword arguments, so wrap the loop in with_request_config to bound their MCP calls:\n\nwith_request_config(mcp_request_timeout = 15.0) do\n    tool_loop!(chat; tools = mcp_tools(session))\nend\n\nMCP over HTTP is not session-fatal on a request timeout (request/response correlation is per-POST); the session survives, and the timed-out request is cancelled on the server with a best-effort notifications/cancelled.","title":"MCP timeouts"},{"category":"section","location":"guide/timeouts/#timeout_concurrency","page":"Timeouts & Retries","text":"Bounds are per operation. What may be shared between concurrent operations — one Chat or Embeddings per in-flight call, fork for fan-out, one MCPSession per parallel worker — and how cancellation, streaming backpressure and the thread layout interact with these bounds is covered in Concurrency, Tasks and Cancellation. Two facts that matter for the bounds themselves:\n\nA call queued behind another on an MCPSession waits within its own per-call bound (MCPTimeoutError(:queue) when it runs out), and mcp_disconnect! waits for the exchange in progress.\nEach stream holds its own HTTP/1.1 connection, and HTTP.jl sets no per-host connection cap by default, so a wide streaming fan-out does not queue for connections below the reach of RequestConfig.","title":"Concurrency"},{"category":"section","location":"guide/timeouts/#Migrating-from-retries","page":"Timeouts & Retries","text":"The retries keyword is removed (no compatibility alias). It was a recursion seed that counted toward a 30-attempt ceiling, so its direction is the inverse of an attempt count. Migrate by intent:\n\nOld intent Old code New code\nDisable retries chatrequest!(chat; retries = 30) (retries=N = \"N attempts already spent\"; 30 hit the ceiling) chatrequest!(chat; config = RequestConfig(max_attempts = 1))\nOld default (retries) chatrequest!(chat; retries = 0) or chatrequest!(chat) chatrequest!(chat) (now 3 attempts, was up to 30)\n\n# Before — disable retries (the old disable switch was retries=30, not retries=0)\nresult = respond(\"…\"; retries = 30)\n\n# After\nresult = respond(\"…\"; config = RequestConfig(max_attempts = 1))\n\nA removed keyword raises MethodError — there is no silent behavior change.","title":"Migrating from retries"},{"category":"section","location":"guide/timeouts/#See-also","page":"Timeouts & Retries","text":"Concurrency, Tasks and Cancellation — sharing, fan-out, cancellation and the thread layout.\nStreaming — the callback / do-block streaming API.\nMCP (Model Context Protocol) — the MCP client and server.\nRealtime API — the WebSocket type/function reference.\nTimeouts & Request Configuration — the type/function reference.","title":"See also"},{"category":"section","location":"api/conversations/#conversations_api","page":"Conversations","text":"Create, retrieve, update, and delete durable, server-side conversations, and manage their items. A conversation id feeds the Responses API for multi-turn exchanges without resending history. OpenAI only.","title":"Conversations API"},{"category":"section","location":"api/conversations/#Parsed-Objects","page":"Conversations","text":"","title":"Parsed Objects"},{"category":"section","location":"api/conversations/#Result-Types","page":"Conversations","text":"","title":"Result Types"},{"category":"section","location":"api/conversations/#Request-Functions","page":"Conversations","text":"","title":"Request Functions"},{"category":"section","location":"api/conversations/#Accessors","page":"Conversations","text":"","title":"Accessors"},{"category":"section","location":"api/conversations/#Usage","page":"Conversations","text":"# Create a durable, server-side conversation\nresult = create_conversation(metadata=Dict(\"topic\" => \"support\"))\nresult isa ConversationSuccess && println(\"Created: \", conversation_id(result.response))\n\n# Retrieve, then update its metadata\nretrieve_conversation(\"conv_abc123\")\nupdate_conversation(\"conv_abc123\", Dict(\"status\" => \"closed\"))\n\n# Add items, list them, then clean up\nadd_conversation_items(\"conv_abc123\", [InputMessage(role=\"user\", content=\"Hello\")])\nitems = list_conversation_items(\"conv_abc123\", limit=20)\ndelete_conversation_item(\"conv_abc123\", \"msg_xyz\")\ndelete_conversation(\"conv_abc123\")","title":"Usage"},{"category":"type","location":"api/conversations/#UniLM.ConversationObject","page":"Conversations","text":"ConversationObject\n\nA durable, server-side conversation: id, optional created_at and metadata; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.ConversationObject"},{"category":"type","location":"api/conversations/#UniLM.ConversationItem","page":"Conversations","text":"ConversationItem\n\nA single item in a conversation: id and optional type; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.ConversationItem"},{"category":"type","location":"api/conversations/#UniLM.ConversationItemList","page":"Conversations","text":"ConversationItemList\n\nA page of ConversationItems from list_conversation_items; has_more signals further pages, and first_id/last_id bound the page.\n\n\n\n\n\n","title":"UniLM.ConversationItemList"},{"category":"type","location":"api/conversations/#UniLM.ConversationSuccess","page":"Conversations","text":"Successful create/retrieve/update result wrapping a ConversationObject; also the result of delete_conversation_item, which the service answers with the updated conversation.\n\n\n\n\n\n","title":"UniLM.ConversationSuccess"},{"category":"type","location":"api/conversations/#UniLM.ConversationItemListSuccess","page":"Conversations","text":"Successful list_conversation_items / add_conversation_items result wrapping a ConversationItemList.\n\n\n\n\n\n","title":"UniLM.ConversationItemListSuccess"},{"category":"type","location":"api/conversations/#UniLM.ConversationItemSuccess","page":"Conversations","text":"Successful result wrapping a single ConversationItem.\n\n\n\n\n\n","title":"UniLM.ConversationItemSuccess"},{"category":"type","location":"api/conversations/#UniLM.ConversationDeleteSuccess","page":"Conversations","text":"Successful delete_conversation result: the service confirmed (deleted is always true) the removal of id.\n\n\n\n\n\n","title":"UniLM.ConversationDeleteSuccess"},{"category":"type","location":"api/conversations/#UniLM.ConversationFailure","page":"Conversations","text":"Conversations API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.ConversationFailure"},{"category":"type","location":"api/conversations/#UniLM.ConversationCallError","page":"Conversations","text":"Conversations API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout.\n\n\n\n\n\n","title":"UniLM.ConversationCallError"},{"category":"function","location":"api/conversations/#UniLM.create_conversation","page":"Conversations","text":"create_conversation(; items=nothing, metadata=nothing, service=OPENAIServiceEndpoint)\n\nCreate a conversation. items is an optional vector of input items (e.g. InputMessage). Returns ConversationSuccess, ConversationFailure, or ConversationCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.create_conversation"},{"category":"function","location":"api/conversations/#UniLM.retrieve_conversation","page":"Conversations","text":"retrieve_conversation(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.retrieve_conversation"},{"category":"function","location":"api/conversations/#UniLM.update_conversation","page":"Conversations","text":"update_conversation(id, metadata; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.update_conversation"},{"category":"function","location":"api/conversations/#UniLM.delete_conversation","page":"Conversations","text":"delete_conversation(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.delete_conversation"},{"category":"function","location":"api/conversations/#UniLM.add_conversation_items","page":"Conversations","text":"add_conversation_items(conversation_id, items; service=OPENAIServiceEndpoint)\n\nAppend input items to a conversation. Returns ConversationItemListSuccess.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.add_conversation_items"},{"category":"function","location":"api/conversations/#UniLM.list_conversation_items","page":"Conversations","text":"list_conversation_items(conversation_id; limit=nothing, order=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_conversation_items"},{"category":"function","location":"api/conversations/#UniLM.delete_conversation_item","page":"Conversations","text":"delete_conversation_item(conversation_id, item_id; service=OPENAIServiceEndpoint)\n\nDelete one item. The service answers with the updated conversation, so a success is a ConversationSuccess wrapping that ConversationObject.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.delete_conversation_item"},{"category":"function","location":"api/conversations/#UniLM.conversation_id","page":"Conversations","text":"conversation_id(c::ConversationObject) -> String\n\nReturn the id of a ConversationObject.\n\n\n\n\n\n","title":"UniLM.conversation_id"},{"category":"section","location":"llm/#UniLM.jl-—-LLM-Reference","page":"LLM Reference","text":"Single-file reference for LLM code-generation systems. This is a Julia package. Julia ≥ 1.13 · Deps: HTTP.jl (≥ 2.7.1), JSON.jl, Base64, SHA Repo: https://github.com/algunion/UniLM.jl","title":"UniLM.jl — LLM Reference"},{"category":"section","location":"llm/#Installation","page":"LLM Reference","text":"using Pkg\nPkg.add(\"UniLM\")\nusing UniLM","title":"Installation"},{"category":"section","location":"llm/#Environment-Variables","page":"LLM Reference","text":"Variable Required by Description\nOPENAI_API_KEY OPENAIServiceEndpoint (default) OpenAI API key\nAZURE_OPENAI_BASE_URL AZUREServiceEndpoint Azure deployment base URL\nAZURE_OPENAI_API_KEY AZUREServiceEndpoint Azure API key\nAZURE_OPENAI_API_VERSION AZUREServiceEndpoint Azure API version string\nAZURE_OPENAI_DEPLOY_NAME_<MODEL> AZUREServiceEndpoint Deployment for a model, read at call time: model id upper-cased, other characters → _ (AZURE_OPENAI_DEPLOY_NAME_GPT_5_2)\nGEMINI_API_KEY GEMINIServiceEndpoint Google Gemini API key\nANTHROPIC_API_KEY ANTHROPICServiceEndpoint Anthropic (Claude) API key\nDEEPSEEK_API_KEY DeepSeekEndpoint DeepSeek API key\nMISTRAL_API_KEY MistralEndpoint Mistral AI API key\nTYPESAFE_API_KEY TYPESAFEServiceEndpoint TypeSafe System One (Jev) API key\nTYPESAFE_BASE_URL TYPESAFEServiceEndpoint Optional API root override (default https://api.typesafe.ai)\nTYPESAFE_DEFAULT_MODEL TYPESAFEServiceEndpoint Model used when a call names none (default jev-latest)","title":"Environment Variables"},{"category":"section","location":"llm/#The-APIs","page":"LLM Reference","text":"UniLM.jl wraps these surfaces:\n\nChat Completions (Chat + chatrequest!) — stateful, message-based conversations with tool calling, streaming, structured output. Supports OpenAI, Azure, Gemini (native), Anthropic (native), DeepSeek, Ollama, Mistral, and any OpenAI-compatible provider.\nResponses API (Respond + respond) — newer, more flexible API with built-in tools (web search, file search), multi-turn chaining via previous_response_id, reasoning support for O-series models, structured output. OpenAI Responses by default; the unified respond verb also targets Google's Gemini Interactions via service=GEMINIServiceEndpoint (see the Agentic Workflows guide).\nImage Generation (ImageGeneration + generate_image) — text-to-image with gpt-image-2. OpenAI only.\nEmbeddings (Embeddings + embeddingrequest!) — vector embeddings. Multi-provider via service parameter.\nFIM Completion (FIMCompletion + fim_complete) — code infilling. DeepSeek (beta), Mistral, Ollama.\nSystem One (ask, plus nl_dispatch and @branch) — TypeSafe's Jev: enumerated questions about a piece of state, answered with a typed value and a calibrated probability distribution instead of generated text. service=TYPESAFEServiceEndpoint only; not a chat backend.\n\nWhich API to use:\n\nChat Completions — best for multi-turn conversations; broadest provider support. Use for chat, tool calling, or streaming across any supported backend.\nResponses API — simpler for single-shot or chained requests; built-in web search, file search, MCP, computer use tools. OpenAI Responses plus Google's Gemini Interactions via the unified respond verb (see the Agentic Workflows guide).\nFIM Completion — code infilling between prefix and suffix. DeepSeek (beta), Mistral and Ollama (vLLM's completions endpoint does not accept suffix).\nSystem One — classify, rank, screen or route unstructured input when the outcomes are a fixed set you can enumerate and you want a probability, not prose. ask returns typed results; nl_dispatch turns the answer into ordinary Julia multiple dispatch and @branch into an inline switch, both throwing rather than guessing a branch.\n\n","title":"The APIs"},{"category":"section","location":"llm/#Service-Endpoints","page":"LLM Reference","text":"abstract type ServiceEndpoint end\nabstract type OpenAIWireEndpoint <: ServiceEndpoint end   # OpenAI-wire backends inherit encode/decode/SSE\nstruct OPENAIServiceEndpoint <: OpenAIWireEndpoint end   # default — uses OPENAI_API_KEY\nstruct AZUREServiceEndpoint  <: OpenAIWireEndpoint end   # uses AZURE_OPENAI_* env vars\nstruct GEMINIServiceEndpoint <: ServiceEndpoint end       # native generateContent — GEMINI_API_KEY\nstruct GEMINIOpenAIServiceEndpoint <: OpenAIWireEndpoint end # Gemini via OpenAI-compat shim — GEMINI_API_KEY\nstruct ANTHROPICServiceEndpoint <: ServiceEndpoint end    # native Messages API — ANTHROPIC_API_KEY\nstruct TYPESAFEServiceEndpoint <: ServiceEndpoint end     # TypeSafe System One (Jev) — TYPESAFE_API_KEY\nstruct GenericOpenAIEndpoint <: OpenAIWireEndpoint    # any OpenAI-compatible provider\n    base_url::String\n    api_key::String\nend\n# DeepSeekEndpoint <: OpenAIWireEndpoint  (constructor below)\n\n# Convenience constructors\nOllamaEndpoint(; base_url=\"http://localhost:11434\")   # Ollama local\nMistralEndpoint(; api_key=ENV[\"MISTRAL_API_KEY\"])     # Mistral AI\nDeepSeekEndpoint(; api_key=ENV[\"DEEPSEEK_API_KEY\"])   # DeepSeek\n\n# Type alias for service fields — accepts both marker types and instances:\nconst ServiceEndpointSpec = Union{Type{<:ServiceEndpoint}, ServiceEndpoint}\n# Built-in types: Chat(service=OPENAIServiceEndpoint)      — passed as the type\n# Instance types: Chat(service=DeepSeekEndpoint())          — passed as a constructed value","title":"Service Endpoints"},{"category":"section","location":"llm/#Provider-Compatibility","page":"LLM Reference","text":"UniLM talks to native provider APIs where it implements them, and rides the OpenAI-compatible standard elsewhere:\n\nAccess path Providers\nNative backends (own wire format) OpenAI (Chat + Responses), Anthropic (Messages), Gemini (generateContent + agentic Interactions)\nChat Completions (OpenAI-compat) OpenAI, Azure, DeepSeek, Mistral, Ollama, vLLM, LM Studio, Gemini (compat shim)\nEmbeddings (OpenAI-compat) OpenAI, Gemini (compat shim), Mistral, Ollama, vLLM\nResponses API OpenAI, Ollama, vLLM, Amazon Bedrock (emerging Open Responses)\nImage Generation OpenAI\n\nAnthropic and native Gemini use their own APIs here (ANTHROPICServiceEndpoint / GEMINIServiceEndpoint) — not an OpenAI-compat shim — and that is the recommended path. The OpenAI-compat Gemini shim (GEMINIOpenAIServiceEndpoint) exists for embeddings and drop-in compatibility.\n\nRegister additional Azure deployments at runtime:\n\nadd_azure_deploy_name!(model::String, deploy_name::String)\n# e.g. add_azure_deploy_name!(\"gpt-5.2\", \"my-deployment\")\n\nPass the backend via the service keyword on Chat, Respond, or ImageGeneration:\n\nChat(service=AZUREServiceEndpoint, model=\"gpt-5.2\")\nRespond(service=OPENAIServiceEndpoint, input=\"Hello\")\n\n","title":"Provider Compatibility"},{"category":"section","location":"llm/#Chat-Completions-API","page":"LLM Reference","text":"","title":"Chat Completions API"},{"category":"section","location":"llm/#Chat","page":"LLM Reference","text":"@kwdef struct Chat\n    service::ServiceEndpointSpec = OPENAIServiceEndpoint\n    model::String = \"\"\n    messages::Vector{Message} = Message[]\n    history::Bool = true\n    tools::Union{Vector{Tool},Nothing} = nothing\n    tool_choice::Union{String,GPTToolChoice,Nothing} = nothing\n    parallel_tool_calls::Union{Bool,Nothing} = false\n    temperature::Union{Float64,Nothing} = nothing       # 0.0–2.0, mutually exclusive with top_p\n    top_p::Union{Float64,Nothing} = nothing              # 0.0–1.0, mutually exclusive with temperature\n    n::Union{Int64,Nothing} = nothing                    # must be 1: a result carries one choice\n    stream::Union{Bool,Nothing} = nothing\n    stop::Union{Vector{String},String,Nothing} = nothing # max 4 sequences\n    max_tokens::Union{Int64,Nothing} = nothing\n    max_completion_tokens::Union{Int64,Nothing} = nothing # preferred over max_tokens; reasoning models reject max_tokens\n    presence_penalty::Union{Float64,Nothing} = nothing   # -2.0 to 2.0\n    response_format::Union{ResponseFormat,Nothing} = nothing\n    frequency_penalty::Union{Float64,Nothing} = nothing  # -2.0 to 2.0\n    logit_bias::Union{AbstractDict{String,<:Real},Nothing} = nothing  # values in [-100, 100]\n    user::Union{String,Nothing} = nothing\n    seed::Union{Int64,Nothing} = nothing\n    reasoning_effort::Union{String,Nothing} = nothing    # none|minimal|low|medium|high|xhigh|max (per-model subset)\n    stream_options::Union{AbstractDict,Nothing} = nothing # unset: OpenAI/DeepSeek streams send {\"include_usage\": true}\n    verbosity::Union{String,Nothing} = nothing           # low|medium|high\n    store::Union{Bool,Nothing} = nothing\n    metadata::Union{AbstractDict,Nothing} = nothing\n    service_tier::Union{String,Nothing} = nothing        # auto|default|flex|scale|priority|fast\n    logprobs::Union{Bool,Nothing} = nothing\n    top_logprobs::Union{Int64,Nothing} = nothing\n    prediction::Union{AbstractDict,Nothing} = nothing\n    modalities::Union{Vector{String},Nothing} = nothing\n    audio::Union{AbstractDict,Nothing} = nothing\n    web_search_options::Union{AbstractDict,Nothing} = nothing\n    prompt_cache_key::Union{String,Nothing} = nothing\n    safety_identifier::Union{String,Nothing} = nothing   # replaces deprecated `user`\n    _cumulative_cost::Ref{Float64} = Ref(0.0)            # internal\n    prompt_cache_options::Union{PromptCacheOptions,Nothing} = nothing  # gpt-5.6 and later\n    moderation::Union{ModerationConfig,Nothing} = nothing\nend\n\n_cumulative_cost is internal bookkeeping for cumulative_cost — read it through that accessor, never directly.\n\nModel defaults: the declared default is the sentinel \"\"; the constructor resolves it, so Chat().model reads back \"gpt-5.6-sol\". Per provider: \"gpt-5.6-sol\" for OpenAI, \"gpt-5.2\" for Azure, \"gemini-3.8-flash\" for Gemini (native and OpenAI-compat), \"claude-opus-5-5\" for native Anthropic (whose max_tokens defaults to 16000), \"deepseek-flash\" for DeepSeek. For GenericOpenAIEndpoint / OllamaEndpoint / MistralEndpoint and user-defined endpoints without a UniLM.default_model method there is no default — an unset model throws ArgumentError at construction.\nhistory=true: responses are automatically appended to messages.\ntemperature and top_p are mutually exclusive (constructor throws ArgumentError).\nparallel_tool_calls is auto-set to nothing when tools is nothing.\nParameter validation: the constructor validates at construction time — temperature ∈ [0.0, 2.0], top_p ∈ [0.0, 1.0], n must be 1 (a result carries a single choice), max_tokens / max_completion_tokens ≥ 1, presence_penalty ∈ [-2.0, 2.0], frequency_penalty ∈ [-2.0, 2.0], top_logprobs ∈ [0, 20], every logit_bias value ∈ [-100, 100], reasoning_effort ∈ {\"none\", \"minimal\", \"low\", \"medium\", \"high\", \"xhigh\", \"max\"}. Violations throw ArgumentError. An empty tools vector is stored as nothing; a Vector{<:CallableTool} is accepted and stored as its Tools.\nProvider/model limits (GPT-5.6/GPT-6 tool and sampling rules, the Anthropic and native-Gemini field mappings) are checked by the provider's encoder: chatrequest! throws their ArgumentError before any network I/O.\nprompt_cache_options is supported for gpt-5.6 and later (gpt-5.4-mini answered HTTP 400 \"promptcacheoptions is not supported on this model\" on 2026-09-22); Chat Completions takes only its mode and ttl — chatrequest! on a native OpenAI Chat with prewarm or comparison_response_id set throws ArgumentError before any request. Message content is a plain string, so Chat messages cannot carry cache breakpoints, and the Chat Completions reference says an explicit-mode request without breakpoints \"does not use prompt caching\": mode=\"explicit\" on a Chat turns prompt caching off. moderation takes a ModerationConfig.","title":"Chat"},{"category":"section","location":"llm/#Message","page":"LLM Reference","text":"@kwdef struct Message\n    role::String                                          # \"system\", \"user\", \"assistant\" or \"tool\" (else ArgumentError)\n    content::Union{String,Nothing} = nothing\n    name::Union{String,Nothing} = nothing\n    finish_reason::Union{String,Nothing} = nothing        # response-only: \"stop\", \"tool_calls\", \"length\", \"content_filter\", …; nothing if none reported\n    refusal_message::Union{String,Nothing} = nothing\n    tool_calls::Union{Nothing,Vector{ToolCall}} = nothing\n    tool_call_id::Union{String,Nothing} = nothing         # required when role == \"tool\"\n    provider_content::Union{Nothing,ProviderContent} = nothing\nend\n\nValidation: role must be one of the four roles; at least one of content, tool_calls, or refusal_message must be non-nothing; tool_call_id is required when role == \"tool\".\n\nOn the wire a message never sends finish_reason (response-only) or provider_content, and a refusal travels as refusal (with content: null). A tool-call turn reads finish_reason == \"tool_calls\" when the provider finished it with \"stop\" or reported none, and keeps any other reason (\"length\", \"content_filter\", …). Partial calls are dropped: on a turn that kept another reason, a call whose arguments are not a JSON object (cut off by the token limit) is removed and the turn — text, reason, usage — kept; under \"tool_calls\" such arguments are an LLMCallError.\n\nprovider_content carries provider-native blocks captured for verbatim round-trip — Anthropic thinking blocks (:anthropic), Gemini parts with thought signatures (:gemini), DeepSeek reasoning_content (:deepseek, echoed on requests that carry tools); it never serializes on the OpenAI wire.\n\nConvenience constructors:\n\nMessage(Val(:system), \"You are a helpful assistant\")\nMessage(Val(:user), \"Hello!\")\n\nRole constants: RoleSystem = \"system\", RoleUser = \"user\", RoleAssistant = \"assistant\".","title":"Message"},{"category":"section","location":"llm/#chatrequest!","page":"LLM Reference","text":"# Mutating form — sends chat.messages, appends response when history=true\nchatrequest!(chat::Chat; config=nothing, callback=nothing, on_tool_call=nothing, cancel=nothing)\n    -> LLMSuccess | LLMFailure | LLMCallError | Task\n\n# Keyword-argument convenience form — builds a Chat internally\nchatrequest!(; messages, config=nothing, cancel=nothing, chat_kwargs...) -> same\nchatrequest!(; systemprompt, userprompt, config=nothing, cancel=nothing, chat_kwargs...) -> same\n\nNon-streaming: returns LLMSuccess, LLMFailure, or LLMCallError.\nStreaming (stream=true): returns a Task whose fetch yields the same typed results. Pass a callback(chunk::Union{String,Message}, close::Ref{Bool}) — text deltas arrive as Strings (verbatim, in order), then the assembled Message at end-of-stream.\nStreaming tool calls: pass on_tool_call(tc::ToolCall) to be notified at most once per completed streamed tool call, as calls finish (a call whose arguments do not parse is skipped with a warning; see the Streaming guide).\nLocal validation throws before any network I/O, streaming or not: ArgumentError when callback or on_tool_call is passed without stream=true (\"callback and ontoolcall require stream=true\"), the service declares capabilities without :chat, or the provider's encoder rejects the request; InvalidConversationError when history=true and the conversation ends with an assistant message (the reply could not be appended).\nThe keyword form takes EITHER messages (copied, never mutated) OR both systemprompt and userprompt (a String or a Message each); anything else throws ArgumentError. Every other keyword is a Chat field; history controls whether the reply is appended, not what is sent.\nRetries transient statuses (408/429/500/502/503/504/529) with exponential backoff and jitter under the resolved RequestConfig — max_attempts (default 3) and total_deadline bound the attempts. Retry-After is a floor under the jitter (at most half the budget left after the floor is spread above it), so a rate-limited fan-out does not retry in lockstep; a retry whose wait would exceed the remaining total_deadline is not attempted — the call returns the last real response rather than sleeping past the deadline. Timeouts surface as LLMCallError with status=nothing and the UniLMTimeout in .cause.\ncancel::Union{Nothing,CancelToken} (default: the ambient with_cancel token): a cancel before connecting, during the header wait, mid-stream or during a backoff ends the call with LLMCallError(status=nothing, cause=UniLMCancelled(:token, …)) — never retried, nothing appended, no terminal callback — unless the provider's completion marker already arrived: then the turn is committed, the final-message callback runs, and usage may be missing (on the OpenAI wire the marker is the chunk carrying finish_reason, which precedes the usage chunk and [DONE]). A pre-cancelled token sends nothing. A TCP connect / TLS handshake in progress finishes (or reaches connect_timeout) first.\nStreaming stop: close[] = true (in the callback or from any task) ends the stream at once with LLMCallError(status=nothing, cause=UniLMCancelled(:callback, …)), unless the provider's completion marker already arrived — then the turn is committed, the final-message callback runs, and usage may be missing. An exception thrown by callback/on_tool_call ends the call with that exception in .cause (never retried, nothing appended). Time inside callbacks is not counted by stream_idle_timeout.\nA stream is final at its sentinel ([DONE], Anthropic message_stop): the final Message callback runs at once, and the result follows when the HTTP body ends, at most 0.5 s later (a body held open longer has its connection closed, not reused).\nStreaming retry boundary: transient failures (including the in-band overloaded_error, the documented 529 equivalent) are retried inside the task only until the first callback/on_tool_call invocation; afterwards failures surface typed. A user InterruptException propagates — fetch throws a TaskFailedException instead of returning a result value.\nStreams use HTTP/1.1 (one connection per stream); OpenAI and DeepSeek streams request include_usage when stream_options is unset, so streamed turns are costed.","title":"chatrequest!"},{"category":"section","location":"llm/#Conversation-Management","page":"LLM Reference","text":"push!(chat, message)       # append a Message (the FIRST message must be a system Message)\npop!(chat)                 # remove last message\nupdate!(chat, message)     # append if history=true\nissendvalid(chat) -> Bool  # check conversation rules (see below)\nlength(chat)               # number of messages\nisempty(chat)              # true if no messages\nchat[i]                    # index into messages\nchat[i] = msg              # replace a message — NOT validated (nor are direct edits to chat.messages)\n\nissendvalid is true only when ALL of these hold — note it is stricter than push!, with no tool exemption:\n\nat least 2 messages;\nthe first message is a system message;\nthe last message is a user message;\nno two adjacent messages share a role — including tool, which push! does permit consecutively.\n\nImportant: A Chat must begin with a system message. push! throws InvalidConversationError on a non-system message pushed onto an empty Chat, on a system message pushed once the conversation has started, and on consecutive same-role messages (except tool), so chat = Chat(); push!(chat, Message(Val(:user), \"…\")) raises rather than silently leaving the chat empty. pop! on an empty Chat likewise throws InvalidConversationError rather than returning nothing. Use respond(\"…\") for a single turn without a system prompt.","title":"Conversation Management"},{"category":"section","location":"llm/#Tool-Calling-Types","page":"LLM Reference","text":"# Define a function the model can call\n@kwdef mutable struct FunctionSignature\n    name::String\n    description::Union{String,Nothing} = nothing\n    parameters::Union{AbstractDict,Nothing} = nothing   # JSON Schema dict\n    strict::Union{Bool,Nothing} = nothing               # strict function calling; nothing = omit (API default)\nend\n\n# Wrap it for the tools parameter\n@kwdef struct Tool\n    type::String = \"function\"\n    func::FunctionSignature\nend\nTool(d::AbstractDict)   # construct from dict with keys \"name\", \"description\", \"parameters\", \"strict\"\n\n# Returned by model when it wants to call a function\n@kwdef struct ToolCall\n    id::String\n    type::String = \"function\"\n    func::GPTFunction       # has .name::String and .arguments::AbstractDict\n    # Gemini-3's opaque tool-call signature. Set only by the Gemini decoder\n    # (nothing on every other provider) and echoed verbatim on the next turn;\n    # deliberately excluded from JSON.lower, so OpenAI-wire bytes are unchanged.\n    thought_signature::Union{Nothing,String} = nothing\nend\n\n# Your result after executing the function\nstruct FunctionCallResult{T}\n    name::Union{String,Symbol}\n    origincall::GPTFunction\n    result::T\nend\n\nThe pre-rename names GPTTool, GPTToolCall, GPTFunctionSignature, and GPTFunctionCallResult remain exported as aliases of these types, so existing code keeps working unchanged.","title":"Tool Calling Types"},{"category":"section","location":"llm/#ResponseFormat-(Structured-Output)","page":"LLM Reference","text":"@kwdef struct ResponseFormat\n    type::String = \"json_object\"                               # \"json_object\" or \"json_schema\"\n    json_schema::Union{JsonSchemaAPI,AbstractDict,Nothing} = nothing\nend\nResponseFormat(json_schema)  # shorthand, sets type=\"json_schema\"\n\n@kwdef struct JsonSchemaAPI   # not exported — construct as UniLM.JsonSchemaAPI(...)\n    name::String\n    description::String\n    schema::AbstractDict\n    strict::Union{Bool,Nothing} = nothing   # strict schema adherence; nothing = omit (API default)\nend\nUniLM.json_schema(name, description, schema; strict=nothing)  # → ResponseFormat(JsonSchemaAPI(...)); not exported","title":"ResponseFormat (Structured Output)"},{"category":"section","location":"llm/#Chat-Completions-Example","page":"LLM Reference","text":"using UniLM\n\n# Build conversation\nchat = Chat(model=\"gpt-5.2\")\npush!(chat, Message(Val(:system), \"You are a helpful assistant.\"))\npush!(chat, Message(Val(:user), \"What is the capital of France?\"))\n\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(result.message.content)     # \"Paris...\"\n    # chat.messages already has the response appended (history=true)\nend\n\n# One-shot via keywords\nresult = chatrequest!(\n    systemprompt=\"You are a translator.\",\n    userprompt=\"Translate 'hello' to French.\",\n    model=\"gpt-5.2\"\n)","title":"Chat Completions Example"},{"category":"section","location":"llm/#Tool-Calling-Example-(Chat)","page":"LLM Reference","text":"weather_tool = Tool(func=FunctionSignature(\n    name=\"get_weather\",\n    description=\"Get current weather\",\n    parameters=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\"location\" => Dict(\"type\" => \"string\")),\n        \"required\" => [\"location\"]\n    )\n))\n\nchat = Chat(model=\"gpt-5.2\", tools=[weather_tool])\npush!(chat, Message(Val(:system), \"You help with weather.\"))\npush!(chat, Message(Val(:user), \"Weather in Paris?\"))\n\nresult = chatrequest!(chat)\nif result isa LLMSuccess && result.message.finish_reason == \"tool_calls\"\n    for tc in result.message.tool_calls\n        # tc.func.name == \"get_weather\", tc.func.arguments == Dict(\"location\" => \"Paris\")\n        answer = \"22°C, sunny\"  # your function result\n        push!(chat, Message(role=\"tool\", content=answer, tool_call_id=tc.id))\n    end\n    result2 = chatrequest!(chat)\n    println(result2.message.content)\nend","title":"Tool Calling Example (Chat)"},{"category":"section","location":"llm/#Streaming-Example-(Chat)","page":"LLM Reference","text":"chat = Chat(model=\"gpt-5.2\", stream=true)\npush!(chat, Message(Val(:system), \"You are helpful.\"))\npush!(chat, Message(Val(:user), \"Tell me a story.\"))\n\ntask = chatrequest!(chat; callback = function (chunk, close_ref)\n    if chunk isa String\n        print(chunk)            # partial text delta\n    elseif chunk isa Message\n        println(\"\\n[Done]\")     # final assembled message\n        # close_ref[] = true    # to stop early → LLMCallError, cause = UniLMCancelled(:callback, …)\n    end\nend)\n\nresult = fetch(task)  # LLMSuccess when complete, else a typed failure\n\n","title":"Streaming Example (Chat)"},{"category":"section","location":"llm/#Responses-API","page":"LLM Reference","text":"","title":"Responses API"},{"category":"section","location":"llm/#Respond","page":"LLM Reference","text":"@kwdef struct Respond\n    service::ServiceEndpointSpec = OPENAIServiceEndpoint\n    model::String = \"\"                                # declared as \"\" and resolved by the constructor\n    input::Union{String, Vector}                             # String, Vector{InputMessage}, or Vector{Dict}\n    instructions::Union{String,Nothing} = nothing\n    tools::Union{Vector,Nothing} = nothing                  # untyped: ResponseTool, CallableTool, Dict; a Chat Tool becomes a FunctionTool\n    tool_choice::Union{String,AbstractDict,Nothing} = nothing  # \"auto\"/\"none\"/\"required\", or a tool_choice_* Dict (see below)\n    parallel_tool_calls::Union{Bool,Nothing} = nothing\n    temperature::Union{Float64,Nothing} = nothing           # 0.0–2.0, mutually exclusive with top_p\n    top_p::Union{Float64,Nothing} = nothing                 # 0.0–1.0\n    max_output_tokens::Union{Int64,Nothing} = nothing\n    stream::Union{Bool,Nothing} = nothing\n    text::Union{TextConfig,Nothing} = nothing               # output format\n    reasoning::Union{Reasoning,Nothing} = nothing           # O-series models\n    truncation::Union{String,Nothing} = nothing             # \"auto\" or \"disabled\"\n    store::Union{Bool,Nothing} = nothing                    # store for later retrieval\n    metadata::Union{AbstractDict,Nothing} = nothing\n    previous_response_id::Union{String,Nothing} = nothing   # multi-turn chaining\n    user::Union{String,Nothing} = nothing\n    background::Union{Bool,Nothing} = nothing\n    include::Union{Vector{String},Nothing} = nothing\n    max_tool_calls::Union{Int64,Nothing} = nothing\n    service_tier::Union{String,Nothing} = nothing           # \"auto\",\"default\",\"flex\",\"scale\",\"priority\",\"fast\",\"ultrafast\"\n    top_logprobs::Union{Int64,Nothing} = nothing            # 0–20\n    prompt::Union{AbstractDict,Nothing} = nothing           # v1/prompts shuts down on November 30, 2026\n    prompt_cache_key::Union{String,Nothing} = nothing\n    prompt_cache_retention::Union{String,Nothing} = nothing  # \"in_memory\",\"24h\" (older models)\n    safety_identifier::Union{String,Nothing} = nothing\n    conversation::Union{Any,Nothing} = nothing              # String or Dict (not with previous_response_id); the declared type collapses to Any\n    context_management::Union{Vector,Nothing} = nothing\n    stream_options::Union{AbstractDict,Nothing} = nothing\n    prompt_cache_options::Union{PromptCacheOptions,Nothing} = nothing\n    moderation::Union{ModerationConfig,Nothing} = nothing\nend\n\nwarning: Gemini Interactions rejects unmappable fields\nWith service=GEMINIServiceEndpoint, a set Respond field the Interactions wire has no equivalent for makes respond throw ArgumentError before any request rather than being silently dropped, so a request never goes out quietly ignoring what you asked for. That wire maps model, input, instructions, tools, tool_choice, temperature, top_p, max_output_tokens, stream, text (its format becomes the top-level response_format; verbosity is rejected), store, previous_response_id, background, and reasoning (effort and automatic summaries only). Every other field — moderation, prompt_cache_options, and the rest of the OpenAI-only options — is rejected; leave it unset, or send the request to an OpenAI Responses service. A FunctionTool with async, allowed_callers, defer_loading, or output_schema set is rejected the same way.","title":"Respond"},{"category":"section","location":"llm/#Input-Helpers","page":"LLM Reference","text":"# Structured input messages\nInputMessage(role=\"user\", content=\"Hello\")\nInputMessage(role=\"user\", content=[input_text(\"Describe:\"), input_image(\"https://...\")])\n\n# Content part constructors\ninput_text(text::String; cache_breakpoint=false)                      # → Dict(:type=>\"input_text\", :text=>...); cache_breakpoint=true adds prompt_cache_breakpoint={\"mode\":\"explicit\"}\ninput_image(url=nothing; detail=nothing, file_id=nothing)             # → Dict(:type=>\"input_image\", ...); pass url OR file_id, detail: \"auto\",\"low\",\"high\"\ninput_file(; url=nothing, id=nothing, file_data=nothing, filename=nothing)  # → Dict(:type=>\"input_file\", ...); pass one of url / id / file_data (base64)\n\n# Input item: change reasoning effort mid-conversation (GPT-6) without rewriting the cached prefix\nconfiguration_update(; effort)   # → Dict(:type=>\"configuration_update\", :reasoning=>Dict(:effort=>...)); effort: \"none\",\"minimal\",\"low\",\"medium\",\"high\",\"xhigh\",\"max\"\n\ninput_image and input_file throw ArgumentError when none of their source arguments is given.","title":"Input Helpers"},{"category":"section","location":"llm/#Tool-Types","page":"LLM Reference","text":"abstract type ResponseTool end\n\n@kwdef struct FunctionTool <: ResponseTool\n    name::String\n    description::Union{String,Nothing} = nothing\n    parameters::Union{AbstractDict,Nothing} = nothing\n    strict::Union{Bool,Nothing} = nothing\n    async::Union{Bool,Nothing} = nothing                    # GPT-6 async tool calling\n    allowed_callers::Union{Vector{String},Nothing} = nothing  # \"direct\" and/or \"programmatic\"; anything else throws\n    defer_loading::Union{Bool,Nothing} = nothing            # loaded via tool search\n    output_schema::Union{AbstractDict,Nothing} = nothing    # JSON Schema of the string output\nend\n\n@kwdef struct WebSearchTool <: ResponseTool\n    type::String = \"web_search\"                             # GA; legacy \"web_search_preview\"\n    search_context_size::String = \"medium\"                  # \"low\",\"medium\",\"high\"\n    user_location::Union{AbstractDict,Nothing} = nothing\n    filters::Union{AbstractDict,Nothing} = nothing          # \"allowed_domains\"/\"blocked_domains\" (GA only)\nend\n\n@kwdef struct FileSearchTool <: ResponseTool\n    vector_store_ids::Vector{String}\n    max_num_results::Union{Int,Nothing} = nothing\n    ranking_options::Union{AbstractDict,Nothing} = nothing\n    filters::Union{AbstractDict,Nothing} = nothing\nend\n\n# Reach a remote MCP server (server_url), an OpenAI connector (connector_id, e.g.\n# \"connector_googledrive\"), or a Secure MCP Tunnel (tunnel_id) — hence no single\n# required target field beyond server_label.\n@kwdef struct MCPTool <: ResponseTool\n    server_label::String\n    server_url::Union{String, Nothing} = nothing\n    connector_id::Union{String, Nothing} = nothing\n    authorization::Union{String, Nothing} = nothing         # OAuth access token\n    server_description::Union{String, Nothing} = nothing\n    require_approval::Union{String, AbstractDict, Nothing} = \"never\"\n    allowed_tools::Union{Vector{String}, AbstractDict, Nothing} = nothing\n    headers::Union{AbstractDict, Nothing} = nothing\n    tunnel_id::Union{String, Nothing} = nothing\nend\n\n@kwdef struct ComputerUseTool <: ResponseTool\n    display_width::Int = 1024\n    display_height::Int = 768\n    environment::Union{String, Nothing} = nothing\nend\n\n# action, moderation, partial_images, and input_fidelity are validated at construction\n# (ArgumentError); quality, background, and output_format pass through unchecked\n# (the accepted quality values depend on the model).\n@kwdef struct ImageGenerationTool <: ResponseTool\n    background::Union{String, Nothing} = nothing\n    output_format::Union{String, Nothing} = nothing\n    output_compression::Union{Int, Nothing} = nothing\n    quality::Union{String, Nothing} = nothing               # \"low\",\"medium\",\"high\",\"auto\"; 2.5 models add \"xhigh\",\"max\"\n    size::Union{String, Nothing} = nothing\n    model::Union{String, Nothing} = nothing                 # e.g. \"gpt-image-2.5-flare\"\n    action::Union{String, Nothing} = nothing                # \"generate\",\"edit\",\"auto\"\n    moderation::Union{String, Nothing} = nothing            # \"auto\",\"low\"\n    partial_images::Union{Int, Nothing} = nothing           # 0–3\n    input_fidelity::Union{String, Nothing} = nothing        # \"high\",\"low\"\n    input_image_mask::Union{AbstractDict, Nothing} = nothing  # \"file_id\" or \"image_url\"\nend\n\n@kwdef struct CodeInterpreterTool <: ResponseTool\n    container::Union{AbstractDict, Nothing} = nothing\n    file_ids::Union{Vector{String}, Nothing} = nothing\nend\n\nConvenience constructors:\n\nfunction_tool(name, description=nothing; parameters=nothing, strict=nothing, async=nothing,\n              allowed_callers=nothing, defer_loading=nothing, output_schema=nothing)\nfunction_tool(d::AbstractDict)           # from a dict with \"name\" and optionally \"description\", \"parameters\", \"strict\",\n                                         # \"async\", \"allowed_callers\", \"defer_loading\", \"output_schema\"\nweb_search(; context_size=\"medium\", location=nothing, type=\"web_search\", filters=nothing)\nfile_search(store_ids; max_results=nothing, ranking=nothing, filters=nothing)\nmcp_tool(label, url=nothing; require_approval=\"never\", allowed_tools=nothing, headers=nothing,\n         connector_id=nothing, authorization=nothing, server_description=nothing, tunnel_id=nothing)\ncomputer_use(; display_width=1024, display_height=768, environment=nothing)\nimage_generation_tool(; kwargs...)\ncode_interpreter(; container=nothing, file_ids=nothing)","title":"Tool Types"},{"category":"section","location":"llm/#Text-Format-/-Structured-Output","page":"LLM Reference","text":"@kwdef struct TextFormatSpec\n    type::String = \"text\"                                   # \"text\",\"json_object\",\"json_schema\"\n    name::Union{String,Nothing} = nothing\n    description::Union{String,Nothing} = nothing\n    schema::Union{AbstractDict,Nothing} = nothing\n    strict::Union{Bool,Nothing} = nothing\nend\n\n@kwdef struct TextConfig\n    format::TextFormatSpec = TextFormatSpec()\n    verbosity::Union{String,Nothing} = nothing              # \"low\",\"medium\",\"high\" (gpt-5.x)\nend\n\nConvenience constructors:\n\ntext_format(; verbosity=nothing, kwargs...)                  # kwargs go to TextFormatSpec; verbosity to TextConfig\njson_schema_format(name, description, schema; strict=nothing) # JSON Schema output\njson_schema_format(d::AbstractDict)                          # from dict with keys \"name\", \"description\", \"schema\"\njson_object_format()                                         # unstructured JSON","title":"Text Format / Structured Output"},{"category":"section","location":"llm/#Reasoning-controls","page":"LLM Reference","text":"@kwdef struct Reasoning\n    effort::Union{String,Nothing} = nothing                 # \"none\",\"minimal\",\"low\",\"medium\",\"high\",\"xhigh\",\"max\" (per-model subset)\n    generate_summary::Union{String,Nothing} = nothing       # deprecated alias, serialized as summary\n    summary::Union{String,Nothing} = nothing                # \"auto\",\"concise\",\"detailed\"\n    context::Union{String,Nothing} = nothing                # \"auto\",\"current_turn\",\"all_turns\"\n    mode::Union{String,Nothing} = nothing                   # \"standard\",\"pro\"\nend\n\nRespond(input=\"Hard math problem\", model=\"gpt-5.4-mini\", reasoning=Reasoning(effort=\"high\"))\n\nGPT-5.6 and later use typed prompt-cache options on Respond and Chat:\n\n@kwdef struct PromptCacheOptions\n    mode::Union{String,Nothing} = nothing                    # \"implicit\" or \"explicit\"\n    ttl::Union{String,Nothing} = nothing                     # \"30m\"\n    prewarm::Union{Bool,Nothing} = nothing                   # Responses only: prepare the cache, no output\n    comparison_response_id::Union{String,Nothing} = nothing  # Responses only: request cache diagnostics\nend\nRespond(input=\"Hello\", model=\"gpt-5.6-luna\",\n        reasoning=Reasoning(effort=\"low\", context=\"current_turn\", mode=\"standard\"),\n        prompt_cache_options=PromptCacheOptions(mode=\"explicit\", ttl=\"30m\"))\n\nUse prompt_cache_options in place of legacy prompt_cache_retention for these models. Explicit mode requires cache breakpoints in input content to create cache writes — mark them with input_text(text; cache_breakpoint=true), a Responses input part; Chat messages cannot carry one, so explicit mode on a Chat means no prompt caching. Chat Completions takes only mode and ttl (a native OpenAI Chat with prewarm or comparison_response_id set throws ArgumentError). Diagnostics requested with comparison_response_id come back in r.response.raw[\"prompt_cache_diagnostics\"]. Unset fields are omitted. OpenAI prompt caching.","title":"Reasoning controls"},{"category":"section","location":"llm/#Moderation","page":"LLM Reference","text":"@kwdef struct ModerationConfig\n    model::String                                 # required, e.g. \"omni-moderation-latest\"\n    input_mode::Union{String,Nothing} = nothing   # \"score\" or \"block\"\n    output_mode::Union{String,Nothing} = nothing  # \"score\" or \"block\"\nend\nRespond(input=\"Hello\", moderation=ModerationConfig(model=\"omni-moderation-latest\"))\nChat(moderation=ModerationConfig(model=\"omni-moderation-latest\", input_mode=\"block\"))\n\nThe moderation field on Respond and Chat runs a moderation model over the request input and the generated output. model is required, as in both the Responses and the Chat Completions API references: ModerationConfig() without it throws UndefKeywordError, and a mode other than \"score\"/\"block\" throws ArgumentError. It serializes as {\"model\": …, \"policy\": {\"input\": {\"mode\": …}, \"output\": {\"mode\": …}}} with unset policy parts omitted. A respond result carries the outcome in r.response.raw[\"moderation\"] (\"input\" and \"output\" moderation results); LLMSuccess keeps no raw body, so Chat results do not expose it.","title":"Moderation"},{"category":"section","location":"llm/#respond","page":"LLM Reference","text":"# Struct form\nrespond(r::Respond; config=nothing, callback=nothing, cancel=nothing)\n    -> ResponseSuccess | ResponseFailure | ResponseCallError | Task\n\n# Convenience — builds Respond internally\nrespond(input; kwargs...) -> same\n\n# do-block streaming — auto-sets stream=true\nrespond(callback::Function, input; kwargs...) -> Task\n\nStreaming callback signature: callback(chunk::Union{String, ResponseObject}, close::Ref{Bool}); close[] = true (or a cancel! on the call's token) stops the stream with ResponseCallError(status=nothing, cause=UniLMCancelled(...)), unless the provider's completion marker — the terminal event — already arrived: then the outcome it recorded stands (a response with its usage, whose final callback has run; or the reported failure). A callback exception ends the call with it in .cause.\nLocal validation throws ArgumentError before any network I/O: a callback passed without stream=true (\"callback requires stream=true\"), a service that declares capabilities with neither :responses nor :agentic, or an encoder rejection (a field the wire or model cannot express).\nRetries retryable statuses (408/429/500/502/503/504/529) up to config.max_attempts (default 3) with full-jitter backoff bounded by config.total_deadline; Retry-After is a floor under the jitter, and a retry whose wait would exceed the remaining deadline is not attempted — it returns the last real response instead of sleeping past it. Every attempt is time-bounded — a silent peer fails with a typed timeout inside ResponseCallError (status = nothing, cause::UniLMTimeout), never a hang. Every exception that ends a call (including a cancellation, cause::UniLMCancelled) is in .cause.\nParameter validation: temperature ∈ [0.0, 2.0], top_p ∈ [0.0, 1.0], max_output_tokens ≥ 1, top_logprobs ∈ [0, 20]; conversation cannot be combined with previous_response_id, background=true requires store not false, and prompt_cache_retention cannot be combined with prompt_cache_options; Reasoning(effort=…) must be one of none, minimal, low, medium, high, xhigh, max. Violations throw ArgumentError.","title":"respond"},{"category":"section","location":"llm/#Response-Accessors","page":"LLM Reference","text":"output_text(result::ResponseSuccess)::String                # concatenated text output (\"\\n\" between parts)\noutput_text(result::ResponseFailure)                        # throws LLMResultError\noutput_text(result::ResponseCallError)                      # throws LLMResultError\n\nfunction_calls(result::ResponseSuccess)::Vector{Dict{String,Any}}   # empty on a failure result\n# Each dict has: \"id\", \"call_id\", \"name\", \"arguments\" (JSON string), \"status\"","title":"Response Accessors"},{"category":"section","location":"llm/#Response-Management-Functions","page":"LLM Reference","text":"get_response(id::String; service=OPENAIServiceEndpoint, config=nothing)    -> ResponseSuccess | ResponseFailure | ResponseCallError\ndelete_response(id::String; service=OPENAIServiceEndpoint, config=nothing) -> Dict | ResponseFailure | ResponseCallError\nlist_input_items(id::String; limit=20, order=\"desc\", after=nothing, service=OPENAIServiceEndpoint, config=nothing) -> Dict | ...\ncancel_response(id::String; service=OPENAIServiceEndpoint, config=nothing) -> ResponseSuccess | ...\ncompact_response(; model=\"gpt-5.6-sol\", input, service=OPENAIServiceEndpoint, config=nothing) -> Dict | ...\ncount_input_tokens(; model=\"gpt-5.6-sol\", input, instructions=nothing, tools=nothing, service=OPENAIServiceEndpoint, config=nothing) -> Dict | ...\n\nEach makes a single bounded attempt (no retries) and observes the ambient with_cancel token; every call error carries its exception in .cause.","title":"Response Management Functions"},{"category":"section","location":"llm/#ResponseObject","page":"LLM Reference","text":"@kwdef struct ResponseObject\n    id::String\n    status::String\n    model::String\n    output::Vector{Any}\n    usage::Union{Dict{String,Any},Nothing} = nothing\n    error::Union{Dict{String,Any}, String, Nothing} = nothing   # provider error object, or a plain message\n    metadata::Union{Dict{String,Any},Nothing} = nothing\n    raw::Dict{String,Any}\nend\n\nA generation whose terminal status is \"failed\" is reported as a ResponseFailure, not a success — on both agentic wires, streamed and non-streamed alike. The failure carries the wire body verbatim, so the response's own error and metadata are preserved on .response. Only \"failed\" flips: incomplete, cancelled, expired, in_progress, queued and requires_action are legitimate terminals of the capped, background and tool-action flows and stay ResponseSuccess, inspectable via response_status and incomplete_details. An incomplete generation — one cut short by a token or output cap — therefore arrives as a ResponseSuccess carrying its partial output, .response.status == \"incomplete\", and the wire's incomplete_details verbatim. A terminal response.incomplete event decodes exactly as the non-streamed body does, so issuccess reflects what happened rather than how the call was made.","title":"ResponseObject"},{"category":"section","location":"llm/#Responses-API-Examples","page":"LLM Reference","text":"using UniLM, JSON\n\n# Basic\nresult = respond(\"Tell me a joke\")\nif result isa ResponseSuccess\n    println(output_text(result))\nend\n\n# With instructions\nresult = respond(\"Hello\", instructions=\"You are a pirate. Respond in pirate speak.\")\n\n# Multi-turn via chaining\nr1 = respond(\"Tell me a joke\")\nr2 = respond(\"Tell me another\", previous_response_id=r1.response.id)\n\n# Structured output\nschema = Dict(\n    \"type\" => \"object\",\n    \"properties\" => Dict(\n        \"name\" => Dict(\"type\" => \"string\"),\n        \"age\" => Dict(\"type\" => \"integer\")\n    ),\n    \"required\" => [\"name\", \"age\"],\n    \"additionalProperties\" => false\n)\nresult = respond(\"Extract: John is 30 years old\",\n    text=json_schema_format(\"person\", \"A person\", schema, strict=true))\nparsed = JSON.parse(output_text(result))\n\n# Web search\nresult = respond(\"Latest Julia language news\", tools=[web_search()])\n\n# Function calling\ntool = function_tool(\"get_weather\", \"Get weather\",\n    parameters=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\"location\" => Dict(\"type\" => \"string\")),\n        \"required\" => [\"location\"]\n    ))\nresult = respond(\"Weather in NYC?\", tools=ResponseTool[tool])\nfor call in function_calls(result)\n    println(call[\"name\"], \": \", call[\"arguments\"])\nend\n\n# Streaming (do-block)\nrespond(\"Tell me a story\") do chunk, close_ref\n    if chunk isa String\n        print(chunk)\n    elseif chunk isa ResponseObject\n        println(\"\\nDone: \", chunk.status)\n    end\nend\n\n# Reasoning\nresult = respond(\"Prove that √2 is irrational\", model=\"gpt-5.4-mini\",\n    reasoning=Reasoning(effort=\"high\", summary=\"concise\"))\n\n# Multimodal input\nresult = respond([\n    InputMessage(role=\"user\", content=[\n        input_text(\"What's in this image?\"),\n        input_image(\"https://example.com/photo.jpg\")\n    ])\n])\n\n# Count tokens without generating\ntokens = count_input_tokens(model=\"gpt-5.2\", input=\"Hello world\")\nprintln(tokens[\"input_tokens\"])\n\n","title":"Responses API Examples"},{"category":"section","location":"llm/#Image-Generation-API","page":"LLM Reference","text":"","title":"Image Generation API"},{"category":"section","location":"llm/#ImageGeneration","page":"LLM Reference","text":"@kwdef struct ImageGeneration\n    service::ServiceEndpointSpec = OPENAIServiceEndpoint\n    model::String = \"\"                                      # sentinel — see below\n    prompt::String\n    n::Union{Int,Nothing} = nothing                         # 1–10\n    size::Union{String,Nothing} = nothing                   # \"1024x1024\",\"1536x1024\",\"1024x1536\",\"auto\"; gpt-image-2+: any WIDTHxHEIGHT, multiples of 16, up to 3840x2160\n    quality::Union{String,Nothing} = nothing                # \"low\",\"medium\",\"high\",\"auto\"; gpt-image-2.5-flare/-sunburst add \"xhigh\",\"max\"\n    background::Union{String,Nothing} = nothing             # \"transparent\",\"opaque\",\"auto\"\n    output_format::Union{String,Nothing} = nothing          # \"png\",\"webp\",\"jpeg\"\n    output_compression::Union{Int,Nothing} = nothing        # 0–100 (webp/jpeg only)\n    user::Union{String,Nothing} = nothing\n    moderation::Union{String,Nothing} = nothing             # \"auto\",\"low\"\nend\n\ninput_fidelity is an edit-only parameter: it lives on ImageEdit, and ImageGeneration(...; input_fidelity=...) is a MethodError.\n\nUnlike Chat and Respond, ImageGeneration does not resolve its model at construction: the field stays \"\" and resolves at serialization time, to \"gpt-image-2\" for OpenAI. So ImageGeneration(prompt=\"…\").model reads back as the empty string — pass model= explicitly if you need to read it, or inspect JSON.json(ig) to see what will go on the wire. A service with no default image model throws ArgumentError at serialization.","title":"ImageGeneration"},{"category":"section","location":"llm/#generate_image","page":"LLM Reference","text":"generate_image(ig::ImageGeneration; config=nothing, cancel=nothing) -> ImageSuccess | ImageFailure | ImageCallError\ngenerate_image(prompt::String; config=nothing, cancel=nothing, kwargs...) -> same   # convenience\nedit_image(e::ImageEdit; config=nothing, cancel=nothing)                 -> same\nedit_image(image, prompt; mask=nothing, service=OPENAIServiceEndpoint, config=nothing, cancel=nothing, kwargs...)\n\nRetries transient statuses (408/429/500/502/503/504/529) inside the request budget (RequestConfig.max_attempts, default 3; Retry-After is a floor under the jitter) — a retry whose wait would exceed the remaining total_deadline is not attempted, so the call returns the last real response rather than sleeping past it. Bound per call with config=RequestConfig(...); a cancelled call returns ImageCallError with cause::UniLMCancelled.","title":"generate_image"},{"category":"section","location":"llm/#Response-Types","page":"LLM Reference","text":"@kwdef struct ImageObject\n    b64_json::Union{String,Nothing} = nothing\n    revised_prompt::Union{String,Nothing} = nothing\n    url::Union{String,Nothing} = nothing          # set when the image was delivered by URL\nend\n\n@kwdef struct ImageResponse\n    created::Int64\n    data::Vector{ImageObject}\n    usage::Union{Dict{String,Any},Nothing} = nothing\n    raw::Dict{String,Any}\nend","title":"Response Types"},{"category":"section","location":"llm/#Accessors","page":"LLM Reference","text":"image_data(result::ImageSuccess)::Vector{String}       # per image: its base64 data, else its URL\nimage_data(result::ImageFailure)                        # throws LLMResultError\nimage_data(result::ImageCallError)                      # throws LLMResultError\nsave_image(img_b64::String, filepath::String)           # decode + write to disk, returns filepath","title":"Accessors"},{"category":"section","location":"llm/#Image-Generation-Example","page":"LLM Reference","text":"using UniLM\n\nresult = generate_image(\"A watercolor painting of a Julia butterfly\",\n    size=\"1024x1024\", quality=\"high\")\n\nif result isa ImageSuccess\n    imgs = image_data(result)\n    save_image(imgs[1], \"butterfly.png\")\n    println(\"Saved! Revised prompt: \", result.response.data[1].revised_prompt)\nend\n\n# Multiple images with transparent background\nresult = generate_image(\"Minimalist logo\",\n    n=3, background=\"transparent\", output_format=\"png\")\n\n","title":"Image Generation Example"},{"category":"section","location":"llm/#Embeddings-API","page":"LLM Reference","text":"","title":"Embeddings API"},{"category":"section","location":"llm/#Embeddings","page":"LLM Reference","text":"struct Embeddings\n    service::ServiceEndpointSpec     # default: OPENAIServiceEndpoint\n    model::String                    # default resolved per provider at construction\n    input::Union{String,Vector{String}}\n    embeddings::Union{Vector{Float64},Vector{Vector{Float64}}}   # pre-allocated, filled in place\n    user::Union{String,Nothing}\n    dimensions::Union{Int,Nothing}          # requested output dimensionality\n    encoding_format::Union{String,Nothing}  # \"float\" or nothing; anything else throws ArgumentError\nend\n\nEmbeddings(input::String; service=OPENAIServiceEndpoint, model=\"\",\n           dimensions=nothing, encoding_format=nothing, user=nothing)\nEmbeddings(input::Vector{String}; service=OPENAIServiceEndpoint, model=\"\",\n           dimensions=nothing, encoding_format=nothing, user=nothing)\n\nModel defaults: \"text-embedding-3-small\" for OpenAI, \"gemini-embedding-001\" for Gemini (OpenAI-compat shim). For generic/DeepSeek endpoints, model must be specified explicitly or the constructor throws ArgumentError naming the service. An empty Vector{String} input also throws.\n\nThe embeddings buffer is pre-allocated to something(dimensions, 1536) zeros per input, filled in place, and resized to the length the model returns (a 3072-dimension model needs no adjustment). Because a pre-zeroed slot would be indistinguishable from a real vector, a response that does not cover every input exactly once — a missing or duplicated row index — is reported as an EmbeddingCallError rather than left as zeros. The result aliases the request (result.embeddings === emb), so use one Embeddings per concurrent call.","title":"Embeddings"},{"category":"section","location":"llm/#embeddingrequest!","page":"LLM Reference","text":"embeddingrequest!(emb::Embeddings; config=nothing, cancel=nothing) -> EmbeddingSuccess | EmbeddingFailure | EmbeddingCallError\n\nReturns an EmbeddingSuccess/EmbeddingFailure/EmbeddingCallError (a <: LLMRequestResponse). Fills emb.embeddings in-place; embedding_vectors(result) returns the vectors (and throws LLMResultError on a failure). Retries transient statuses (408/429/500/502/503/504/529) with backoff and jitter under the resolved RequestConfig (max_attempts, total_deadline; Retry-After is a floor under the jitter), but a retry whose wait would exceed the remaining deadline is not attempted — it returns the last real response rather than sleeping past it. Timeouts surface as EmbeddingCallError with status=nothing and the UniLMTimeout in .cause; a cancelled call carries UniLMCancelled there. A service that declares capabilities without :embeddings throws ArgumentError before any request.","title":"embeddingrequest!"},{"category":"section","location":"llm/#Embeddings-Example","page":"LLM Reference","text":"using UniLM, LinearAlgebra\n\nemb = Embeddings(\"What is Julia?\")\nembeddingrequest!(emb)\nprintln(emb.embeddings[1:5])  # first 5 dimensions\n\n# Batch + cosine similarity\nemb = Embeddings([\"cat\", \"dog\", \"airplane\"])\nembeddingrequest!(emb)\nsimilarity = dot(emb.embeddings[1], emb.embeddings[2]) /\n    (norm(emb.embeddings[1]) * norm(emb.embeddings[2]))\n\n","title":"Embeddings Example"},{"category":"section","location":"llm/#Cost-Tracking","page":"LLM Reference","text":"","title":"Cost Tracking"},{"category":"section","location":"llm/#TokenUsage","page":"LLM Reference","text":"@kwdef struct TokenUsage\n    prompt_tokens::Int = 0\n    completion_tokens::Int = 0\n    total_tokens::Int = 0\n    cached_tokens::Int = 0      # subset of prompt_tokens served from the prompt cache\n    reasoning_tokens::Int = 0   # subset of completion_tokens spent on hidden reasoning\nend\n\ncached_tokens and reasoning_tokens come from the providers' *_tokens_details objects and default to 0 when the provider omits them. cached_tokens is load-bearing for cost: it is billed at the discounted cached-input rate.","title":"TokenUsage"},{"category":"section","location":"llm/#Functions","page":"LLM Reference","text":"token_usage(result::LLMRequestResponse)::TokenUsage   # never Nothing — zero usage for a failure\nestimated_cost(result; model=nothing, pricing=DEFAULT_PRICING)   # per-call cost estimate (Float64)\ncumulative_cost(chat::Chat)::Float64                             # running total for a Chat instance\n\nDEFAULT_PRICING   # lock-guarded AbstractDict{String, PriceRow} (not a Dict)\n                  # where PriceRow = @NamedTuple{input::Float64, cached_input::Float64, output::Float64}\n\ntoken_usage returns a TokenUsage, never nothing, for every result of a token-billed API — chat, Responses, embeddings, image generation, FIM and System One. Failures of those APIs report all-zero usage.\nBoth functions throw ArgumentError for result types outside the token-billed APIs — audio, batch, container, conversation, file, fine-tuning, moderation, upload, vector-store, realtime. Those calls carry no token usage at all, and a 0.0 would be indistinguishable from a genuinely free call.\nThe model is inferred from the result (self.model for Chat, response.model for Responses, FIM and System One, embeddings.model for Embeddings). An image result reports its usage but estimated_cost returns 0.0 for it unless model= names a row in pricing (image models have no default rows).\nDEFAULT_PRICING is safe to share across tasks: add or replace a row with DEFAULT_PRICING[model] = row while requests run; get, haskey, delete!, pop!, empty!, keys and length work as on a Dict, iteration walks a snapshot, and copy / merge / filter return a plain Dict. pricing= accepts any AbstractDict{String, PriceRow}.\nDEFAULT_PRICING values are USD per token, not per 1M tokens — provider list prices divided by 1_000_000 (e.g. \"gpt-5.6-sol\" is (input = 4.0e-6, cached_input = 4.0e-7, output = 2.0e-5)). A pricing= dict you supply must use the same per-token convention, or your estimate is off by a factor of a million.\nLookup: the exact id, then the id without a dated snapshot suffix (OpenAI -YYYY-MM-DD, Anthropic -YYYYMMDD), and a versioned jev-X.Y.Z id without its own row is priced at the jev-latest row. Other unpriced models return 0.0 and log one warning per model id — pass pricing= (or add a row) to price custom models. The formula bills min(cached_tokens, prompt_tokens) at cached_input, the remaining prompt tokens at input, and completion_tokens at output (reasoning tokens are already counted within completion tokens).\n\nCurrent OpenAI, Gemini, and TypeSafe rows were verified September 22, 2026 (Anthropic and DeepSeek rows September 24, 2026; the DeepSeek rows are its peak rates, and off-peak hours bill half). These are estimates for standard short-context text requests, excluding cache-write premiums, long-context surcharges, nonstandard service tiers, multimodal rates, and hosted-tool fees. Anthropic prompt_tokens include cache reads and writes (cached_tokens is the read share), and DeepSeek cache hits (prompt_cache_hit_tokens) are billed at the cached rate. A streamed Chat turn accrues cost when its stream reports usage: OpenAI and DeepSeek streams request it automatically, native Anthropic and Gemini report it, and other OpenAI-compatible servers need stream_options=Dict(\"include_usage\" => true). Gemini 3.8/3.7/3.6 Flash introductory rates expire December 31, 2026; refresh pricing before estimating later calls.","title":"Functions"},{"category":"section","location":"llm/#Cost-Tracking-Example","page":"LLM Reference","text":"chat = Chat(model=\"gpt-5.2\")\npush!(chat, Message(Val(:system), \"You are helpful.\"))\npush!(chat, Message(Val(:user), \"Hello!\"))\n\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    usage = token_usage(result)\n    cost = estimated_cost(result)\n    println(\"Tokens: $(usage.total_tokens), Cost: \\$$(round(cost; digits=6))\")\n    println(\"Cumulative: \\$$(round(cumulative_cost(chat); digits=6))\")\nend\n\n","title":"Cost Tracking Example"},{"category":"section","location":"llm/#Conversation-Forking","page":"LLM Reference","text":"fork(chat::Chat)::Chat          # deep-copy every field but `service` (shared); cumulative cost copied by value (fresh Ref)\nfork(chat::Chat, n::Int)::Vector{Chat}  # create n independent forks","title":"Conversation Forking"},{"category":"section","location":"llm/#Fork-Example","page":"LLM Reference","text":"chat = Chat(model=\"gpt-5.2\")\npush!(chat, Message(Val(:system), \"You are a creative writer.\"))\npush!(chat, Message(Val(:user), \"Start a story about a robot.\"))\nchatrequest!(chat)\n\n# Fork into 3 independent continuations\nforks = fork(chat, 3)\nfor (i, f) in enumerate(forks)\n    push!(f, Message(Val(:user), \"Continue the story with ending $i.\"))\n    chatrequest!(f)\nend\n\n","title":"Fork Example"},{"category":"section","location":"llm/#Tool-Loop","page":"LLM Reference","text":"Automated tool dispatch for both APIs. Wraps a tool schema with a callable function.","title":"Tool Loop"},{"category":"section","location":"llm/#CallableTool","page":"LLM Reference","text":"struct CallableTool{T}\n    tool::T              # Tool or FunctionTool\n    callable::Function   # (name::String, args::Dict{String,Any}) -> String (any other value is sent JSON-encoded)\nend","title":"CallableTool"},{"category":"section","location":"llm/#to_tool","page":"LLM Reference","text":"to_tool(x)  # identity for Tool, FunctionTool, CallableTool; converts AbstractDict to Tool","title":"to_tool"},{"category":"section","location":"llm/#ToolCallOutcome-/-ToolLoopResult","page":"LLM Reference","text":"# Per-call record\nstruct ToolCallOutcome\n    tool_name::String\n    arguments::Dict{String,Any}\n    result::Union{FunctionCallResult,Nothing}\n    success::Bool\n    error::Union{String,Nothing}\nend\n\n# Loop result\nstruct ToolLoopResult\n    response::LLMRequestResponse        # the last response received (on max_turns: the last tool-call turn)\n    tool_calls::Vector{ToolCallOutcome}\n    turns_used::Int\n    completed::Bool                     # true only for a final text answer that finished normally\n    llm_error::Union{String,Nothing}    # why it stopped, when not completed\nend","title":"ToolCallOutcome / ToolLoopResult"},{"category":"section","location":"llm/#tool_loop!-(Chat-Completions)","page":"LLM Reference","text":"tool_loop!(chat, dispatcher; max_turns=10, config=nothing, callback=nothing, on_tool_call=nothing,\n           cancel=nothing, tool_concurrency=1) -> ToolLoopResult\ntool_loop!(chat; tools::Vector{<:CallableTool}, kwargs...) -> ToolLoopResult   # kwargs as above\n\ncallback and on_tool_call are the streaming hooks from chatrequest!, applied to every turn of the loop and passed through unchanged (so they need stream=true, else ArgumentError before any request). Rules:\n\nchat.history must be true (else ArgumentError before any request); max_turns < 1 and tool_concurrency < 1 throw ArgumentError.\nCalls run only on a turn whose finish_reason == \"tool_calls\". A turn with calls that finished otherwise (\"length\", \"content_filter\", …) runs none: completed=false, llm_error names the reason, and the unanswered assistant turn is removed from chat.\nA text turn completes the loop only when it finished with \"stop\" or no reason (or \"tool_calls\" with no calls); \"length\" gives completed=false with llm_error = \"Model output was truncated by the token limit\", any other reason completed=false with an llm_error naming it.\nA dispatcher's String result is sent as is, any other value JSON-encoded; a throwing dispatcher sends \"Error: <message>\" and records a failed ToolCallOutcome; an InterruptException propagates after the interrupted turn is removed from chat.\nOn max_turns exhaustion response is the last real response, completed=false, llm_error = \"max turns (N) exhausted\".\ncancel (default: the ambient token) scopes every turn and dispatch; a cancelled loop returns completed=false with an LLMCallError whose cause is UniLMCancelled (a turn cancelled between dispatches is removed from chat).\ntool_concurrency = n > 1 runs up to n of a turn's calls at once on spawned tasks (thread-safe dispatcher required); results are sent in call order.","title":"tool_loop! (Chat Completions)"},{"category":"section","location":"llm/#tool_loop-(Responses-API)","page":"LLM Reference","text":"tool_loop(r::Respond, dispatcher; max_turns=10, config=nothing, cancel=nothing, tool_concurrency=1) -> ToolLoopResult\ntool_loop(r::Respond; max_turns=10, config=nothing, cancel=nothing, tool_concurrency=1) -> ToolLoopResult   # callables from r.tools\ntool_loop(input, dispatcher; max_turns=10, config=nothing, cancel=nothing, tool_concurrency=1, respond_kwargs...)\ntool_loop(input::String; tools, max_turns=10, config=nothing, cancel=nothing, tool_concurrency=1, respond_kwargs...)\n\nTurns chain through previous_response_id, or through conversation alone when the Respond sets it. Function calls run only on a completed or requires_action turn; another status stops the loop (completed=false, llm_error naming the status and the incomplete_details reason). Call arguments that are not a JSON object are answered with \"Error: invalid arguments: …\" and the loop continues. A turn requesting a client-side action the loop cannot run (custom_tool_call, apply_patch_call, local_shell_call, computer_call, a shell_call without a shell_call_output in the same output, mcp_approval_request) stops it with completed=false, naming the type, and runs none of the turn's calls. Result encoding, max_turns, cancel and tool_concurrency behave as in tool_loop! (a cancelled loop's response is a ResponseCallError).\n\n","title":"tool_loop (Responses API)"},{"category":"section","location":"llm/#MCP-Client","page":"LLM Reference","text":"Native MCP client (JSON-RPC 2.0 over stdio or Streamable HTTP). Client and server negotiate revisions 2025-11-25 (preferred), 2025-06-18 and 2025-03-26; the stateless 2026-07-28 revision is not supported yet, so a 2026-07-28-only peer cannot connect.","title":"MCP Client"},{"category":"section","location":"llm/#Types","page":"LLM Reference","text":"MCPSession            # live connection — manages transport + cached tools/resources/prompts\nMCPToolInfo           # tool definition from tools/list\nMCPToolResult         # typed tools/call result: content, structured, is_error, parts\nMCPResourceInfo       # resource definition from resources/list\nMCPPromptInfo         # prompt definition from prompts/list\nMCPServerCapabilities # capabilities from initialize\nMCPTransport          # abstract (subtypes: StdioTransport, HTTPTransport)\nMCPError <: Exception # JSON-RPC error (code, message, data — any JSON value)\nMCPCrashError <: Exception # stdio server process exited / pipe broke (exitcode, termsignal, cause)\nMCPSessionClosedError <: Exception # call on a closed / not-connected session (cause :disconnected|:timeout|:crash, msg)","title":"Types"},{"category":"section","location":"llm/#Lifecycle","page":"LLM Reference","text":"mcp_connect(command::Cmd; stderr=nothing, client_name=\"UniLM.jl\", client_version=<package version>,\n            protocol_version=\"2025-11-25\", config=nothing, auto_respawn=false) -> MCPSession  # stdio subprocess\nmcp_connect(url::String; headers=Pair{String,String}[], kwargs...) -> MCPSession            # Streamable HTTP\nmcp_connect(transport::MCPTransport; kwargs...) -> MCPSession      # a custom transport must bound its own IO\nmcp_connect(f::Function, args...; kwargs...)                       # do-block, auto-disconnect\nmcp_disconnect!(session)\n\nstderr is where a stdio server's stderr goes — an IO (e.g. devnull) or a file path (appended to); nothing inherits this process's. A stdio server runs in its own process group: disconnecting or a fatal timeout tears it down (stdin EOF, SIGTERM, then SIGKILL of the group), the group is killed when its leader exits, and servers still running at exit are torn down by an exit hook.","title":"Lifecycle"},{"category":"section","location":"llm/#Discovery","page":"LLM Reference","text":"list_tools!(session; timeout=nothing) -> Vector{MCPToolInfo}\nlist_resources!(session) -> Vector{MCPResourceInfo}\nlist_prompts!(session) -> Vector{MCPPromptInfo}\n\nWhen the client reads a notifications/tools/list_changed, session.tools_stale is set to true to flag the cached tool list as out of date; call list_tools!(session) to refresh it (which clears the flag). Server frames are read only during an exchange: a stdio notification sent between calls is seen on the next call, and over HTTP (no standing listener) only notifications inside a response body are seen.","title":"Discovery"},{"category":"section","location":"llm/#Operations","page":"LLM Reference","text":"call_tool(session, name, arguments; timeout=nothing) -> MCPToolResult\nread_resource(session, uri) -> String\nget_prompt(session, name, arguments) -> Vector{Dict}\nping(session)\n\ncall_tool and get_prompt accept any AbstractDict for arguments, so the natural Dict(\"path\" => \"/tmp/x\") form works without conversion.\n\nTimeouts surface as the exported MCPTimeoutError (phase :connect, :queue or :request). A stdio request timeout closes the session — killing the server is the only way to release a read blocked on it; the error surfaces at about the bound and a reply that raced the teardown is discarded. An HTTP request timeout does not close the session, and the timed-out request gets a best-effort notifications/cancelled. A stdio server that exits or whose pipe breaks surfaces the exported MCPCrashError (the session closes with cause :crash). With auto_respawn=true the next call on a session closed by a timeout or a crash respawns the server; otherwise, and after mcp_disconnect!, every call throws MCPSessionClosedError.\n\nA CancelToken (ambient with_cancel) reaches MCP calls over HTTP: a cancel throws UniLMCancelled at once, sends a best-effort notifications/cancelled, and leaves the session open. A stdio exchange does not observe the token (bounded by mcp_request_timeout). Teardown requests (the disconnect DELETE, a cancellation notice) are sent even inside a cancelled scope.","title":"Operations"},{"category":"section","location":"llm/#Session-Concurrency","page":"LLM Reference","text":"An MCPSession runs one call at a time: each call's liveness check, id allocation and request/response exchange holds the session, and waiting callers are served in arrival order (FIFO).\n\nA call's per-call bound (timeout, default mcp_request_timeout) also bounds its wait for the session: a call that cannot acquire it in time throws MCPTimeoutError(:queue, …) without touching it. Once held, the exchange gets the full bound, measured from acquisition, so a waiter never cuts it short.\nmcp_disconnect! waits its turn too, so a disconnect racing a call in flight waits for that exchange to finish instead of tearing the transport down under its reader.\nInterleaved server → client frames are handled in place: notifications are skipped, server ping requests answered, other server requests answered -32601, and a stdio line that is not JSON skipped with a warning.\n\nFor genuine parallelism, open one session per concurrent worker.","title":"Session Concurrency"},{"category":"section","location":"llm/#Tool-Result","page":"LLM Reference","text":"call_tool returns a typed result — a tool-execution error (isError: true) is carried on is_error, not thrown; only JSON-RPC protocol errors throw (MCPError).\n\nstruct MCPToolResult\n    content::String                              # text parts joined by \"\\n\"; non-text parts JSON-encoded\n    structured::Union{Nothing,Dict{String,Any}}  # server's structuredContent verbatim, or nothing\n    is_error::Bool                               # true when the tool reported an execution error (isError)\n    parts::Vector{Any}                           # raw content array verbatim\nend\n\nThe mcp_tools / mcp_tools_respond bridges surface content to the model on success (falling back to JSON.json(structured) when content is empty), and raise content as an error when is_error is set.","title":"Tool Result"},{"category":"section","location":"llm/#Tool-Bridge","page":"LLM Reference","text":"mcp_tools(session) -> Vector{CallableTool{Tool}}         # for tool_loop!\nmcp_tools_respond(session) -> Vector{CallableTool{FunctionTool}}  # for tool_loop\n\nBridged tool names are provider-safe: any character outside [A-Za-z0-9_-] becomes _ and names are cut to 128 characters (the callable still calls the MCP name); two tools mapping to one name raise ArgumentError.","title":"Tool Bridge"},{"category":"section","location":"llm/#Client-Example","page":"LLM Reference","text":"session = mcp_connect(`npx -y @modelcontextprotocol/server-filesystem /tmp`)\ntools = mcp_tools(session)\nchat = Chat(model=\"gpt-5.2\", tools=map(t -> t.tool, tools))\npush!(chat, Message(Val(:system), \"You are a helpful assistant with filesystem access.\"))\npush!(chat, Message(Val(:user), \"List files\"))\nresult = tool_loop!(chat; tools)\nmcp_disconnect!(session)\n\n","title":"Client Example"},{"category":"section","location":"llm/#MCP-Server","page":"LLM Reference","text":"Build MCP servers that expose tools, resources, and prompts.","title":"MCP Server"},{"category":"section","location":"llm/#Types-2","page":"LLM Reference","text":"MCPServer(name, version; description=nothing)\nMCPServerPrimitive    # abstract (MCPServerTool, MCPServerResource, MCPServerResourceTemplate, MCPServerPrompt)","title":"Types"},{"category":"section","location":"llm/#Registration","page":"LLM Reference","text":"register_tool!(server, name, description, schema, handler)\nregister_tool!(server, name, description, handler)           # auto-schema from signature\nregister_tool!(server, ct::CallableTool{Tool})            # bridge from Chat API\nregister_tool!(server, ct::CallableTool{FunctionTool})       # bridge from Responses API\nregister_resource!(server, uri, name, handler; mime_type=\"text/plain\", description=nothing)\nregister_resource_template!(server, uri_template, name, handler; ...)\nregister_prompt!(server, name, handler; description=nothing, arguments=Dict{String,Any}[])\n\nThe inferred-schema register_tool!(server, name, description, handler) binds each tools/call arguments object to the handler's positional parameters by name, as @mcp_tool does: a parameter whose type admits nothing is optional, every other one required, and a value must have its parameter's JSON type (a Symbol binds from a string; typed Vector/Dict parameters convert element-wise). A violation is answered with an isError: true tool result naming the argument. A handler taking one Dict or varargs throws ArgumentError — pass an explicit schema for those. Registration is synchronized: it may run while serve is dispatching. @mcp_tool registers the tool without a description.","title":"Registration"},{"category":"section","location":"llm/#Macros","page":"LLM Reference","text":"@mcp_tool server function name(args...) body end\n@mcp_resource server uri function(args...) body end\n@mcp_prompt server name function(args...) body end","title":"Macros"},{"category":"section","location":"llm/#Serving","page":"LLM Reference","text":"serve(server; transport=:stdio)                          # default — stdio\nserve(server; transport=:http, host=\"127.0.0.1\", port=8080)  # HTTP — blocks until closed\n\nThe HTTP transport blocks until the server is closed (like HTTP.serve); pass block=false to get the running server handle back and close it yourself. It also validates the Origin header (DNS-rebinding defense): requests with no Origin header and localhost origins pass, any other origin gets 403 unless listed in allowed_origins. A request other than initialize whose MCP-Protocol-Version header names an unsupported revision gets 400; initialize answers the requested revision when supported, otherwise 2025-11-25.\n\nConcurrency: over HTTP, tools/call, resources/read and prompts/get handlers run concurrently, one task per request on the default thread pool — handlers must be thread-safe; protocol requests (initialize, lists, ping) are answered inline. Over stdio handlers run one at a time, and the process stdout points at stderr while serving on it, so a printing handler cannot corrupt the frame stream.\n\nhandle = serve(server; transport=:http, port=8080, block=false,\n               allowed_origins=[\"https://app.example.com\"])\nclose(handle)","title":"Serving"},{"category":"section","location":"llm/#Error-and-Limit-Contracts","page":"LLM Reference","text":"A throwing tool handler is a tool result, not a protocol error. The client receives isError: true with the text Error: <showerror text>, which is exactly what lets a model see and correct its own mistake. Write handlers accordingly: raise with a message you are willing to show the model and the client.\nResource, prompt and dispatch-layer errors are generic. An exception from a resource or prompt handler, or any unhandled error below the handler, answers JSON-RPC -32603 with the message \"Internal error\" — the exception and backtrace go to the server's own logs, not to the peer, because an exception string can carry file paths and argument values a remote client has no business reading. A single bad frame never takes the transport down. An InterruptException is never converted: it propagates.\nFrames and request bodies are capped at 16 MiB. stdio answers an oversized frame with -32600, HTTP an oversized body with 413 Payload Too Large, rather than parsing it, since parsing an attacker-sized payload allocates a multiple of it — an out-of-memory kill rather than a protocol error.\nThe server tolerates spec-legal parameter shapes: params: null and positional params do not crash it, and a JSON-RPC response sent to it is accepted without an answer (HTTP 202). Non-object arguments, a missing tool name and an unknown tool answer -32602.","title":"Error and Limit Contracts"},{"category":"section","location":"llm/#Server-Example","page":"LLM Reference","text":"server = MCPServer(\"calc\", \"1.0.0\")\n@mcp_tool server function add(a::Float64, b::Float64)::String\n    string(a + b)\nend\nserve(server)\n\n","title":"Server Example"},{"category":"section","location":"llm/#FIM-Completion","page":"LLM Reference","text":"Fill-in-the-Middle: generate text between a prompt (prefix) and suffix. Supported by DeepSeek (beta), Mistral (routed to /v1/fim/completions), and Ollama; vLLM's completions endpoint rejects suffix.\n\n@kwdef struct FIMCompletion\n    service::ServiceEndpointSpec\n    model::String = \"\"    # sentinel: resolves at serialization to \"deepseek-flash\" for DeepSeek\n    prompt::String\n    suffix::Union{String,Nothing} = nothing\n    max_tokens::Union{Int,Nothing} = 128\n    temperature::Union{Float64,Nothing} = nothing\n    top_p::Union{Float64,Nothing} = nothing\n    stream::Union{Bool,Nothing} = nothing          # stream=true throws ArgumentError (no streaming path)\n    stop::Union{Vector{String},String,Nothing} = nothing\n    echo::Union{Bool,Nothing} = nothing\n    logprobs::Union{Int,Nothing} = nothing\n    frequency_penalty::Union{Float64,Nothing} = nothing\n    presence_penalty::Union{Float64,Nothing} = nothing\nend\n\n@kwdef struct FIMChoice\n    text::String\n    index::Int = 0\n    finish_reason::Union{String,Nothing} = nothing\nend\n\n@kwdef struct FIMResponse\n    choices::Vector{FIMChoice}\n    usage::Union{TokenUsage,Nothing} = nothing\n    model::String = \"\"\n    raw::Dict{String,Any} = Dict{String,Any}()\nend\n\n@kwdef struct FIMSuccess <: LLMRequestResponse\n    response::FIMResponse\nend\n\n@kwdef struct FIMFailure <: LLMRequestResponse\n    response::String\n    status::Int\n    request_id::Union{String,Nothing} = nothing\nend\n\n@kwdef struct FIMCallError <: LLMRequestResponse\n    error::String\n    status::Union{Int,Nothing} = nothing\n    request_id::Union{String,Nothing} = nothing\n    cause::Union{Nothing,Exception} = nothing\nend\n\nfim_complete(fim::FIMCompletion; config=nothing, cancel=nothing) -> LLMRequestResponse\nfim_complete(prompt; suffix=nothing, config=nothing, cancel=nothing, kwargs...) -> LLMRequestResponse  # convenience\nfim_text(result) -> String  # generated text; throws LLMResultError on a FIMFailure/FIMCallError\n\nUnlike Chat, FIMCompletion does not resolve its model at construction — the field stays \"\" until serialization, where it becomes \"deepseek-flash\" for DeepSeekEndpoint. Endpoints without a default FIM model must set model= explicitly, or fim_complete throws ArgumentError. Local validation (capability — strict, so an endpoint that declares nothing throws MethodError — model resolution, routing) throws before any request; every later failure, including a 200 body that is not a completions response, is a FIMCallError whose cause holds the exception. Retries follow the shared policy; FIMSuccess results report usage and cost.","title":"FIM Completion"},{"category":"section","location":"llm/#FIM-Example","page":"LLM Reference","text":"result = fim_complete(\"def fib(a):\",\n    service=DeepSeekEndpoint(), suffix=\"    return fib(a-1) + fib(a-2)\",\n    max_tokens=128, stop=[\"\\n\\n\"])\nprintln(fim_text(result))\n\n","title":"FIM Example"},{"category":"section","location":"llm/#Chat-Prefix-Completion","page":"LLM Reference","text":"Continue from a partial assistant message. The model generates text continuing from the assistant's prefix. DeepSeek beta feature.\n\nprefix_complete(chat::Chat; config=nothing, cancel=nothing) -> LLMRequestResponse\n# Last message must be role=assistant with the prefix text\n\nThe result's message is the continuation the API returns; with chat.history the conversation keeps the whole assistant turn, prefix followed by continuation. A cancelled call returns LLMCallError (cause::UniLMCancelled) and leaves chat untouched.","title":"Chat Prefix Completion"},{"category":"section","location":"llm/#Prefix-Example","page":"LLM Reference","text":"chat = Chat(service=DeepSeekEndpoint())   # deepseek-flash\npush!(chat, Message(Val(:system), \"You are a coding assistant.\"))\npush!(chat, Message(Val(:user), \"Write quicksort in Python\"))\npush!(chat, Message(role=RoleAssistant, content=\"```python\\n\"))\nresult = prefix_complete(chat)\n\n","title":"Prefix Example"},{"category":"section","location":"llm/#TypeSafe-System-One-API-(Jev)","page":"LLM Reference","text":"A System One model answers enumerated questions about a piece of state with a probability distribution over the outcomes you named — no generated text, no parsing. TYPESAFE_API_KEY is required; the default model is jev-latest and pinning \"jev-1.13.0\" keeps tuned confidence thresholds meaningful.\n\n# What a piece of guidance may be on the wire: string | object | array\nconst SystemOneEntry = Union{AbstractString, AbstractDict, AbstractVector, NamedTuple}\n# What `state` may be. No `nothing`: the server reports a null state as missing.\nconst SystemOneState = Union{AbstractString, AbstractDict, AbstractVector, Tuple, NamedTuple}\n\nabstract type SystemOneQuestion end\n\nstruct ChoiceQuestion <: SystemOneQuestion          # 1..255 options, insertion order kept\n    instructions::Union{Nothing,SystemOneEntry}\n    criteria::JSON.Object{String,Any}               # option name => description (or nothing)\nend\n\nstruct ScoreQuestion <: SystemOneQuestion           # 1..10 levels, criteria[1] is level 0\n    instructions::Union{Nothing,SystemOneEntry}\n    criteria::Vector{Any}\nend\n\nstruct NoulCriteria                                 # wire keys \"true\" / \"false\"\n    yes::Union{Nothing,SystemOneEntry}\n    no::Union{Nothing,SystemOneEntry}\nend\n\nstruct NoulQuestion <: SystemOneQuestion            # needs instructions or one criterion\n    instructions::Union{Nothing,SystemOneEntry}\n    criteria::Union{Nothing,NoulCriteria}\nend\n\nstruct SystemOneRequest\n    state::SystemOneState\n    questions::Vector{Pair{String,SystemOneQuestion}}   # ordered, unique non-empty names\n    model::String\nend\n\nabstract type SystemOneAnswer end\n\nstruct NoulAnswer <: SystemOneAnswer                # no confidence: the value is the belief\n    noul::Float64\n    raw::Dict{String,Any}\nend\n\nstruct ChoiceAnswer <: SystemOneAnswer\n    choice::String\n    confidence::Float64\n    probabilities::Dict{String,Float64}             # keyed by option name, unordered\n    raw::Dict{String,Any}\nend\n\nstruct ScoreAnswer <: SystemOneAnswer\n    score::Float64                                  # expectation; may fall between levels\n    confidence::Float64\n    legend::Dict{Int,Any}                           # 0-based level number => description\n    probabilities::Dict{Int,Float64}                # same keys as legend\n    raw::Dict{String,Any}\nend\n\nstruct UnknownAnswer <: SystemOneAnswer             # forward-compatible, never dropped\n    type::String\n    raw::Dict{String,Any}\nend\n\nstruct SystemOneResponse\n    model::String                                   # the versioned id that answered\n    answers::Dict{String,SystemOneAnswer}           # keyed by question name\n    usage::TokenUsage                               # prompt = input, completion = output\n    request_id::Union{Nothing,String}               # x-typesafe-request-id header\n    raw::Dict{String,Any}\nend\n\nstruct SystemOneSuccess <: LLMRequestResponse\n    response::SystemOneResponse\nend\n\n@kwdef struct SystemOneFailure <: LLMRequestResponse   # HTTP non-2xx\n    response::String\n    status::Int\n    request_id::Union{Nothing,String} = nothing\n    error_type::Union{Nothing,String} = nothing     # \"authentication_error\", \"api_usage_error\"\n    message::String = \"\"\n    retry_after::Union{Nothing,Float64} = nothing   # seconds; retry-after-ms, else Retry-After\nend\n\n@kwdef struct SystemOneCallError <: LLMRequestResponse # no usable response\n    error::String\n    status::Union{Int,Nothing} = nothing\n    request_id::Union{Nothing,String} = nothing\n    cause::Union{Nothing,Exception} = nothing       # e.g. a UniLMTimeout or UniLMCancelled\nend\n\nstruct SystemOneError <: Exception                  # thrown by accessors on a non-success\n    result::Union{SystemOneFailure,SystemOneCallError}\nend\n\nstruct TypeSafeModelCard\n    name::String\n    description::String\n    release_date::String                            # opaque string (live returns RFC 3339)\n    raw::Dict{String,Any}\nend\n\nstruct TypeSafeModelsSuccess <: LLMRequestResponse\n    models::Vector{TypeSafeModelCard}\n    raw::Dict{String,Any}\nend","title":"TypeSafe System One API (Jev)"},{"category":"section","location":"llm/#Constructors,-verbs-and-accessors","page":"LLM Reference","text":"choice(instructions, criteria) -> ChoiceQuestion    # criteria: NamedTuple | Dict |\nchoice(criteria) -> ChoiceQuestion                  #   Vector{Pair} | Vector{String}\nscore(instructions, levels::AbstractVector) -> ScoreQuestion\nscore(levels::AbstractVector) -> ScoreQuestion\nnoul(instructions; yes=nothing, no=nothing) -> NoulQuestion\nnoul(; yes=nothing, no=nothing) -> NoulQuestion\n\nSystemOneRequest(state, questions; model=UniLM.default_typesafe_model())\n\nask(request::SystemOneRequest; service=TYPESAFEServiceEndpoint, config=nothing, cancel=nothing)\nask(state, questions...; model=UniLM.default_typesafe_model(), service=TYPESAFEServiceEndpoint,\n    config=nothing, cancel=nothing)\n    # -> SystemOneSuccess | SystemOneFailure | SystemOneCallError\n\nlist_models(; service=TYPESAFEServiceEndpoint, config=nothing, cancel=nothing)\n    # -> TypeSafeModelsSuccess | SystemOneFailure | SystemOneCallError\n\nanswers(result) -> Dict{String,SystemOneAnswer}     # throws SystemOneError on a non-success\nanswer(result, name)                                # name::Union{AbstractString,Symbol}\nresult[name]; haskey(result, name); keys(result)    # same, on Success or Response\n\nquestions accepts name => question pairs (vector, tuple, NamedTuple, or AbstractDict), or bare questions auto-named \"q1\", \"q2\", … in order. Mixing the two, repeating a name, or passing none is an ArgumentError. Both verbs ride the shared retry seam, so RequestConfig.max_attempts applies to 408, 429, 500, 502, 503, 504 and 529, and the final SystemOneFailure carries retry_after — the wait the service asked for in seconds, or nothing when it sent no hint.","title":"Constructors, verbs and accessors"},{"category":"section","location":"llm/#Example","page":"LLM Reference","text":"r = ask(\"Help! My payouts have been failing for 3 days.\",\n        \"department\" => choice(\"Which team should handle this ticket?\",\n            (billing=\"Payments, invoicing, payouts, refunds\",\n             technical=\"Bugs, outages, integrations\",\n             sales=\"Pricing, upgrades, new accounts\")),\n        \"urgency\" => score(\"How urgent is this ticket?\",\n            [\"Can wait\", \"Needs attention this week\", \"Needs attention today\"]),\n        \"is_frustrated\" => noul(\"Is the customer frustrated?\"; yes=\"Frustrated\", no=\"Neutral\"))\nissuccess(r) && println(r[\"department\"].choice, \" \", r[\"urgency\"].score, \" \", r[\"is_frustrated\"].noul)","title":"Example"},{"category":"section","location":"llm/#Natural-language-control-flow","page":"LLM Reference","text":"A Choice answer is already a branch decision, so two constructs compile ordinary control flow into ONE System One request. @branch is a natural-language switch that evaluates only the selected body; nl_dispatch sends one Choice per Meaning slot and then hands the resolved meanings to Julia's own dispatch, so the remaining arguments still dispatch on their types.\n\nstruct Meaning{S} end                     # S::Symbol IS the description\nMeaning(text::AbstractString)             # the instance; nl\"...\" is the TYPE\n\nstruct LowConfidenceError <: Exception    # raised when confidence < min_confidence\n    question::String\n    answer::ChoiceAnswer                  # the full distribution, for diagnostics\n    min_confidence::Float64\nend\n\n@branch state [key = value ...] begin\n    \"option name\"                  => expression   # the name is what the model reads\n    (\"option name\", \"description\") => expression   # the only way to add a description\n    _                              => expression   # requires min_confidence\nend\n# keys: model, min_confidence, instructions, service, config, cancel\n# -> the selected body's value; LowConfidenceError or SystemOneError otherwise\n\nnl_dispatch(f, args...; model=nothing, service=TYPESAFEServiceEndpoint, config=nothing,\n            cancel=nothing, min_confidence=0.0, fallback=nothing, instructions=nothing, state=nothing)\n    # -> f(resolved meanings spliced into their positions, args...)\nmeanings(f) -> Dict{Int,Vector{String}}   # slot position => options, in send order\n\nMethods without a concrete Meaning argument (including wildcard ::Meaning ones) are ordinary methods and are not part of the natural-language interface. Every natural-language method of f must agree on arity and on which positions are slots. Without state, the state is a Dict{String,Any} keyed by the ordinary argument names of the first such method. A resolved combination no method covers raises Julia's own MethodError.\n\nticket = \"My package arrived crushed and the screen is cracked. I want my money back.\"\n\naction = @branch ticket min_confidence=0.6 begin\n    \"the customer wants a refund\"           => :refund\n    \"the customer reports a bug in the app\" => :bug\n    _                                       => :escalate\nend                                         # => :refund\n\nroute(::nl\"the customer wants a refund\", t)           = (:refund, t)\nroute(::nl\"the customer reports a bug in the app\", t) = (:bug, t)\nnl_dispatch(route, ticket)                            # => (:refund, ticket)\nroute(nl\"the customer wants a refund\"(), ticket)      # direct call, no request\n\n","title":"Natural-language control flow"},{"category":"section","location":"llm/#Provider-Capabilities","page":"LLM Reference","text":"Each endpoint declares supported features. Request functions validate before dispatch, so an unsupported feature is an ArgumentError naming the endpoint and what it does support, not an HTTP 404.\n\nprovider_capabilities(service) -> Set{Symbol}\nhas_capability(service, cap::Symbol) -> Bool","title":"Provider Capabilities"},{"category":"section","location":"llm/#Capabilities-by-Provider","page":"LLM Reference","text":"Provider Capabilities\nOpenAI :chat, :responses, :agentic, :embeddings, :images, :image_edits, :tools, :streaming, :json_output, :files, :vector_stores, :conversations, :moderation, :audio, :batch, :fine_tuning, :containers, :uploads, :realtime\nAzure :chat, :tools, :streaming, :json_output\nGemini (native) :chat, :tools, :json_output, :streaming, :agentic\nGemini (OpenAI-compat) :chat, :embeddings, :tools, :streaming, :json_output\nAnthropic (native) :chat, :tools, :json_output, :streaming\nTypeSafe (System One) :system_one, :models\nDeepSeek :chat, :tools, :streaming, :fim, :prefix_completion, :json_output\nGeneric :chat, :embeddings, :fim, :tools, :streaming, :json_output, :responses\n\nValidation comes in two strengths, and the difference matters if you write your own endpoint:\n\nPlatform and lifecycle verbs (files, vector stores, conversations, moderations, audio, batch, fine-tuning, containers, uploads, realtime, FIM, prefix completion, System One) validate strictly: an endpoint with no declaration at all is not dispatched — the call throws MethodError (no provider_capabilities method) — because those surfaces are provider-specific and an undeclared backend would just 404.\nThe four primary verbs — chatrequest! (:chat), embeddingrequest! (:embeddings), respond (:responses or :agentic), and generate_image (:images) — plus edit_image (:image_edits) validate only endpoints that declare their capabilities. A custom endpoint that defines no provider_capabilities method passes through unvalidated: the package has no basis for claiming what someone else's OpenAI-compatible server cannot do, and refusing to dispatch it would be a false negative. Declaring capabilities is therefore opt-in strictness — once you declare, you are held to the declaration.\n\nEither way you never need to check capabilities manually before a request.\n\n","title":"Capabilities by Provider"},{"category":"section","location":"llm/#Request-Configuration-and-Timeouts","page":"LLM Reference","text":"Every network operation is bounded. One struct carries all knobs — all time fields are seconds, Inf disables that bound:\n\nBase.@kwdef struct RequestConfig\n    connect_timeout::Float64     = 10.0\n    request_timeout::Float64     = 600.0\n    stream_idle_timeout::Float64 = 120.0\n    total_deadline::Float64      = 900.0\n    max_attempts::Int            = 3\n    mcp_connect_timeout::Float64 = 120.0\n    mcp_request_timeout::Float64 = 120.0\nend\n\nconnect_timeout — per-attempt connection establishment. request_timeout — per-attempt whole exchange (non-stream); a stream's exchange up to its first byte. stream_idle_timeout — byte-gap between raw stream chunks. total_deadline — across ALL attempts including backoff (streams: until first byte). max_attempts — wire attempts (1 disables retries). mcp_connect_timeout / mcp_request_timeout — MCP handshake / per-exchange bounds.\nValidation: every Float64 field rejects NaN, values ≤ 0 and finite values above 1e9 s with ArgumentError (Inf = disabled); max_attempts ≥ 1.\nCopy-with-overrides: RequestConfig(base::RequestConfig; kwargs...).\nFour channels, struct-wise precedence (a channel supplies a complete struct):\nper-call config::Union{Nothing,RequestConfig} keyword on request verbs;\ndynamic scope: with_request_config(f; kwargs...) — merges kwargs over current_config() at entry; propagates into Threads.@spawn;\nprocess default: set_default_config!(cfg) or set_default_config!(; kwargs...) (merges over the current default) — the channel for REPL/notebook sessions;\nthe built-in defaults above.\ncurrent_config() returns the ambient struct (active scope, else process default).\n\nwith_request_config(request_timeout=30.0, max_attempts=1) do\n    chatrequest!(chat)          # bounded, no retries, inside this scope only\nend\nset_default_config!(total_deadline=120.0)   # process-wide default","title":"Request Configuration & Timeouts"},{"category":"section","location":"llm/#Which-verbs-retry","page":"LLM Reference","text":"max_attempts applies to the inference verbs only: chatrequest!, embeddingrequest!, respond, fim_complete, prefix_complete, generate_image, edit_image, ask, list_models, and the tool_loop family (streams retry only before the first callback fires). Platform and lifecycle verbs — batch, container, conversation, file (upload_file included: a create is never retried), fine-tuning, moderation, upload, vector-store, audio, realtime, and the Responses lifecycle operations — make a single bounded attempt, and max_attempts has no effect on them. poll_batch / poll_file_batch poll through retryable statuses and per-attempt timeouts until their own wall-clock timeout (interval and timeout must be > 0); a poll that times out returns the family's call error with cause = UniLMTimeout(:deadline, …) and the last object seen in last_observed. Both take cancel.","title":"Which verbs retry"},{"category":"section","location":"llm/#Cancellation","page":"LLM Reference","text":"tok = CancelToken()                 # level-triggered: once cancelled, stays cancelled\ncancel!(tok)                        # from any task; idempotent; runs every registered hook\niscancelled(tok)::Bool              # iscancelled(nothing) == false\nwith_cancel(f, tok)                 # tok is the ambient token inside f (and tasks spawned there)\n\nHTTP verbs take cancel::Union{Nothing,CancelToken}=nothing (chatrequest!, respond, embeddingrequest!, generate_image, edit_image, fim_complete, prefix_complete, ask, list_models, poll_batch, poll_file_batch, the tool loops, nl_dispatch, @branch); nothing resolves the ambient token, and every other HTTP verb observes the ambient token. A cancelled call returns its call-error result with status = nothing and cause = UniLMCancelled(source, elapsed) (source is :token, or :callback for a stream stopped with close[] = true), is never retried, commits nothing — unless the provider's completion marker already arrived on a stream: then the turn is committed, the final-message callback runs, and usage may be missing (a Chat stream's marker is the chunk carrying the finish reason, before the usage chunk and [DONE] on the OpenAI wire; a Responses stream's is its terminal event, whose response carries its usage). A pre-cancelled token sends nothing. Limits: a TCP connect / TLS handshake in progress is not interrupted (it completes or hits connect_timeout), a running callback finishes first, and the Realtime WebSocket and MCP stdio exchanges do not observe the token. See the Concurrency, Tasks and Cancellation guide.","title":"Cancellation"},{"category":"section","location":"llm/#Sharp-edges","page":"LLM Reference","text":"Streams, pre-first-byte, are bounded by min(request_timeout, remaining total_deadline) per attempt (a peer that never answers, or never reads the request, ends the attempt with UniLMTimeout(:request, …)) and additionally by stream_idle_timeout (HTTP.jl caps the response-header wait with the read-idle timer), so the effective bound is min(request_timeout, remaining total_deadline, stream_idle_timeout); when the idle bound is the smallest, a breach reports phase :stream_idle even though no byte arrived. A TCP connect / TLS handshake in progress completes, or reaches connect_timeout, first.\nThe idle bound measures wire idleness only. Time a stream spends inside the user's callback / on_tool_call is not counted, so a slow consumer never idle-kills a healthy stream — and no bound covers a callback that never returns.\nTimeouts land on time. A non-streaming attempt's bound fires within a few ms of its limit, and a successful call has no detection latency.\nA completed turn is never discarded or re-sent. Once a stream records its terminal state, teardown noise — idle breach, transport reset, truncated read — finalizes it as a success rather than failing or retrying it, on every provider and both stream drivers. Re-POSTing would bill a second generation.\nRealtime: realtime_connect (OpenAI only: any other service throws ArgumentError before any I/O) bounds the open phase with connect_timeout, surfaces an open that failed for another reason as that error, and closes an upgrade that completes after the timeout without running the handler; realtime_receive bounds the read with stream_idle_timeout and surfaces a breach within [limit, limit + ~5 s] (the WebSocket close waits for the peer's acknowledgement). An open session's lifetime is deliberately unbounded. mint_realtime_secret returns RealtimeCallError for a 200 without a non-empty secret.","title":"Sharp edges"},{"category":"section","location":"llm/#Concurrency","page":"LLM Reference","text":"One Chat per in-flight call. messages and the cumulative-cost Ref are unsynchronized by design; use fork(chat) / fork(chat, n) to fan out. An Embeddings is filled in place (and aliased by its result): one per concurrent call. Respond, ImageGeneration, FIMCompletion and SystemOneRequest are not mutated by their verbs and may be shared.\nAn MCPSession runs one call at a time, FIFO; a call's timeout bounds its wait for the session too (MCPTimeoutError(:queue)), and mcp_disconnect! waits for the in-flight exchange. One session per concurrent worker. serve(:http) runs handlers concurrently (thread-safe handlers required); stdio runs them one at a time.\nStreams use one HTTP/1.1 connection each; non-streaming calls may multiplex over HTTP/2. HTTP.jl sets no per-host connection cap by default. Retry-After is a floor under jitter, so a rate-limited batch does not retry in lockstep.\nThread 1 (the main task, and the libuv event loop) must not be blocked: a task that computes there without yielding delays every timer. Keep callbacks short. tool_concurrency = n runs a turn's tool calls on spawned tasks.\n\n","title":"Concurrency"},{"category":"section","location":"llm/#Result-Type-Hierarchy","page":"LLM Reference","text":"All 65 API call result types inherit from LLMRequestResponse, in 16 per-API families:\n\nLLMRequestResponse   (abstract parent of every result type below)\n│\n├─ Chat Completions   LLMSuccess · LLMFailure · LLMCallError\n├─ Responses API      ResponseSuccess · ResponseFailure · ResponseCallError\n├─ Embeddings         EmbeddingSuccess · EmbeddingFailure · EmbeddingCallError\n├─ Image Generation   ImageSuccess · ImageFailure · ImageCallError\n├─ FIM Completion     FIMSuccess · FIMFailure · FIMCallError\n├─ Files              FileSuccess · FileListSuccess · FileContentSuccess · FileDeleteSuccess · FileFailure · FileCallError\n├─ Vector Stores      VectorStoreSuccess · VectorStoreListSuccess · VectorStoreFileSuccess · VectorStoreBatchSuccess · VectorStoreDeleteSuccess · VectorStoreFailure · VectorStoreCallError\n├─ Conversations      ConversationSuccess · ConversationItemSuccess · ConversationItemListSuccess · ConversationDeleteSuccess · ConversationFailure · ConversationCallError\n├─ Moderations        ModerationSuccess · ModerationFailure · ModerationCallError\n├─ Audio              SpeechSuccess · TranscriptionSuccess · AudioFailure · AudioCallError\n├─ Batch              BatchSuccess · BatchListSuccess · BatchFailure · BatchCallError\n├─ Fine-tuning        FineTuningSuccess · FineTuningListSuccess · FineTuningFailure · FineTuningCallError\n├─ Containers         ContainerSuccess · ContainerListSuccess · ContainerDeleteSuccess · ContainerFailure · ContainerCallError\n├─ Uploads            UploadSuccess · UploadPartSuccess · UploadFailure · UploadCallError\n├─ Realtime           RealtimeSecretSuccess · RealtimeFailure · RealtimeCallError\n└─ System One         SystemOneSuccess · TypeSafeModelsSuccess · SystemOneFailure · SystemOneCallError\n\nEvery *Success wraps a parsed response object, every *Failure carries the HTTP status and body (every family but Embeddings and Realtime also the request_id), and every *CallError carries the rendered error and, in cause, the exception behind it when there is one (a UniLMTimeout for a timeout, a UniLMCancelled for a cancellation). Fields of the five primary families:\n\nLLMSuccess        (.message::Message, .self::Chat, .usage::Union{TokenUsage,Nothing}, .sse_dropped::Int)\nLLMFailure        (.response::String, .status::Int, .self::Chat, .request_id::Union{String,Nothing}, .sse_dropped::Int)\nLLMCallError      (.error::String, .status::Union{Int,Nothing}, .self::Chat, .request_id::Union{String,Nothing}, .cause::Union{Nothing,Exception})\nResponseSuccess   (.response::ResponseObject, .sse_dropped::Int)\nResponseFailure   (.response::String, .status::Int, .request_id::Union{String,Nothing}, .sse_dropped::Int)\nResponseCallError (.error::String, .status::Union{Int,Nothing}, .request_id::Union{String,Nothing}, .cause::Union{Nothing,Exception})\nEmbeddingSuccess  (.embeddings::Embeddings, .usage::Union{TokenUsage,Nothing}, .raw::Dict{String,Any})\nEmbeddingFailure  (.response::String, .status::Int)\nEmbeddingCallError(.error::String, .status::Union{Int,Nothing}, .cause::Union{Nothing,Exception})\nImageSuccess      (.response::ImageResponse)\nImageFailure      (.response::String, .status::Int, .request_id::Union{String,Nothing})\nImageCallError    (.error::String, .status::Union{Int,Nothing}, .request_id::Union{String,Nothing}, .cause::Union{Nothing,Exception})\nFIMSuccess        (.response::FIMResponse)\nFIMFailure        (.response::String, .status::Int, .request_id::Union{String,Nothing})\nFIMCallError      (.error::String, .status::Union{Int,Nothing}, .request_id::Union{String,Nothing}, .cause::Union{Nothing,Exception})\n\nrequest_id carries the provider's request id for support escalation — the x-request-id header, else request-id (Anthropic). It exists on the Chat / Responses / FIM / image failure and call-error types above, on the System One results and on every platform *Failure except RealtimeFailure (a RealtimeCallError carries one); the embedding limbs do not carry it.\n\nsse_dropped (default 0) counts the undecodable SSE data: payloads dropped while assembling that streamed turn. It is 0 for a non-streamed call and for a clean stream; anything higher means the result was built from an incomplete wire — see Streaming.\n\nCredentials never reach a result value. API keys and request bodies are redacted from the error strings these types carry, and a *CallError's cause is named by type rather than dumped when the result is displayed.\n\nStandard pattern-matching idiom:\n\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(result.message.content)\nelseif result isa LLMFailure\n    @error \"HTTP $(result.status): $(result.response)\"\nelseif result isa LLMCallError\n    @error \"Exception: $(result.error)\"\nend\n\nresult = respond(\"Hello\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelseif result isa ResponseFailure\n    @error \"HTTP $(result.status)\"\nelseif result isa ResponseCallError\n    @error result.error\nend\n\nresult = generate_image(\"A cat\")\nif result isa ImageSuccess\n    save_image(image_data(result)[1], \"cat.png\")\nelseif result isa ImageFailure\n    @error \"HTTP $(result.status)\"\nelseif result isa ImageCallError\n    @error result.error\nend\n\n","title":"Result Type Hierarchy"},{"category":"section","location":"llm/#API-Constants","page":"LLM Reference","text":"const OPENAI_BASE_URL = \"https://api.openai.com\"\nconst CHAT_COMPLETIONS_PATH = \"/v1/chat/completions\"\nconst EMBEDDINGS_PATH = \"/v1/embeddings\"\nconst RESPONSES_PATH = \"/v1/responses\"\nconst IMAGES_GENERATIONS_PATH = \"/v1/images/generations\"\nconst COMPLETIONS_PATH = \"/v1/completions\"                     # FIM endpoint\nconst DEEPSEEK_BASE_URL = \"https://api.deepseek.com\"\nconst DEEPSEEK_BETA_BASE_URL = \"https://api.deepseek.com/beta\"\nconst GEMINI_CHAT_URL = \"https://generativelanguage.googleapis.com/v1beta/openai/chat/completions\"","title":"API Constants"},{"category":"section","location":"llm/#Exceptions","page":"LLM Reference","text":"struct InvalidConversationError <: Exception\n    reason::String\nend\n\nThrown by push! (a non-system message onto an empty Chat, a system message after the conversation started, or a same-role repeat other than tool), by pop! on an empty Chat, and by chatrequest! before any request when history=true and the conversation ends with an assistant message. issendvalid never throws — it returns a Bool.\n\nstruct LLMResultError <: Exception\n    result::LLMRequestResponse\nend\n\nThrown by the result accessors text, output_text, embedding_vectors, image_data and fim_text when the result is not a success. showerror prints only the status and a trimmed (≤200-char) excerpt of the response body or error message — never the conversation, endpoint, or API key.\n\nstruct UniLMTimeout <: Exception\n    phase::Symbol        # :connect | :request | :stream_idle | :deadline\n    elapsed::Float64     # seconds, monotonic\n    limit::Float64\nend\n\nstruct MCPTimeoutError <: Exception\n    phase::Symbol        # :connect | :queue (never acquired its session) | :request\n    elapsed::Float64\n    limit::Float64\n    msg::String          # names the applicable timeout override\nend\n\nstruct UniLMCancelled <: Exception\n    source::Symbol       # :token (cancel! on a CancelToken) | :callback (stream close[] = true)\n    elapsed::Float64     # seconds since the operation started\nend\n\nstruct MCPSessionClosedError <: Exception\n    cause::Symbol        # :disconnected | :timeout | :crash\n    msg::String          # recovery guidance\nend\n\nstruct MCPCrashError <: Exception\n    msg::String                          # recovery guidance (embeds exit info)\n    exitcode::Union{Int,Nothing}         # exit code if the server exited and was reaped in time\n    termsignal::Union{Int,Nothing}       # signal number if the server was signal-killed\n    cause::Union{Exception,Nothing}      # underlying transport exception; nothing on clean read-EOF\nend\n\nRaised when a RequestConfig bound is exceeded. Value-returning surfaces (chat, embeddings, responses, images, FIM, System One, platform verbs) deliver UniLMTimeout — and a cancellation's UniLMCancelled — inside their call-error results; the MCP surface throws MCPTimeoutError (and UniLMCancelled for a cancelled HTTP call). A stdio MCP server that exits or whose pipe breaks throws MCPCrashError instead (the session closes with cause :crash), and a call on a closed session throws MCPSessionClosedError.\n\n","title":"Exceptions"},{"category":"section","location":"llm/#Public-Extension-API-(not-exported)","page":"LLM Reference","text":"Declared public, not exported — qualify them (UniLM.get_url). These are the functions a new backend adds methods to and the stream-state types its handlers mutate (see the Custom Backends guide): get_url, auth_header, default_model, encode_request, decode_response, handle_sse_event!, StreamState, encode_agentic, decode_agentic, decode_agentic_stream, AgenticStreamState.\n\n","title":"Public Extension API (not exported)"},{"category":"section","location":"llm/#Complete-Exports-List","page":"LLM Reference","text":"Every exported symbol, grouped by area:\n\nChat Completions: Chat, Message, ProviderContent, RoleSystem, RoleUser, RoleAssistant, Tool, ToolCall, FunctionSignature, FunctionCallResult, ResponseFormat, InvalidConversationError, issendvalid, chatrequest!, update!, fork\n\nLegacy aliases (pre-rename names, exported and non-breaking, retained until 1.0): GPTTool → Tool, GPTToolCall → ToolCall, GPTFunctionSignature → FunctionSignature, GPTFunctionCallResult → FunctionCallResult\n\nResponses API & Agentic: Respond, InputMessage, ResponseObject, ResponseSuccess, ResponseFailure, ResponseCallError, Reasoning, PromptCacheOptions, ModerationConfig, TextConfig, TextFormatSpec, respond, get_response, delete_response, cancel_response, list_input_items, compact_response, count_input_tokens, text_format, json_schema_format, json_object_format\n\nInput builders: input_text, input_image, input_file, configuration_update\nTool types: ResponseTool, FunctionTool, WebSearchTool, FileSearchTool, MCPTool, ComputerUseTool, ComputerTool, ImageGenerationTool, CodeInterpreterTool, LocalShellTool, ShellTool, ApplyPatchTool, CustomTool\nTool constructors: function_tool, web_search, file_search, mcp_tool, computer_use, computer_tool, image_generation_tool, code_interpreter, local_shell, shell, apply_patch_tool, custom_tool, tool_result, mcp_approval_response\nHosted Gemini tools: gemini_google_search, gemini_code_execution, gemini_url_context\ntool_choice builders: tool_choice_function, tool_choice_hosted, tool_choice_mcp, tool_choice_custom, tool_choice_allowed\nResult accessors: output_text, function_calls, refusals, reasoning_summaries, reasoning_items, url_citations, web_search_results, file_search_results, image_generation_results, code_interpreter_outputs, mcp_call_outputs, mcp_approval_requests, response_status, incomplete_details, usage_details\n\nImage Generation & Edits: ImageGeneration, ImageEdit, ImageObject, ImageResponse, ImageSuccess, ImageFailure, ImageCallError, generate_image, edit_image, image_data, save_image\n\nEmbeddings: Embeddings, embeddingrequest!, embedding_vectors, EmbeddingSuccess, EmbeddingFailure, EmbeddingCallError\n\nCost Tracking: TokenUsage, token_usage, estimated_cost, cumulative_cost, DEFAULT_PRICING\n\nService Endpoints: ServiceEndpoint, OpenAIWireEndpoint, ServiceEndpointSpec, OPENAIServiceEndpoint, AZUREServiceEndpoint, GEMINIServiceEndpoint, GEMINIOpenAIServiceEndpoint, ANTHROPICServiceEndpoint, GenericOpenAIEndpoint, OllamaEndpoint, MistralEndpoint, DeepSeekEndpoint, add_azure_deploy_name!\n\nProvider Capabilities: provider_capabilities, has_capability\n\nTool Loop: CallableTool, ToolCallOutcome, ToolLoopResult, tool_loop!, tool_loop, to_tool\n\nForking: fork\n\nMCP Client: MCPSession, MCPToolInfo, MCPToolResult, MCPResourceInfo, MCPPromptInfo, MCPServerCapabilities, MCPTransport, StdioTransport, HTTPTransport, MCPError, MCPCrashError, MCPSessionClosedError, mcp_connect, mcp_disconnect!, mcp_tools, mcp_tools_respond, list_tools!, list_resources!, list_prompts!, call_tool, read_resource, get_prompt, ping\n\nMCP Server: MCPServer, MCPServerTool, MCPServerResource, MCPServerResourceTemplate, MCPServerPrompt, MCPServerPrimitive, register_tool!, register_resource!, register_resource_template!, register_prompt!, serve, @mcp_tool, @mcp_resource, @mcp_prompt\n\nFIM / Completions: FIMCompletion, FIMChoice, FIMResponse, FIMSuccess, FIMFailure, FIMCallError, fim_complete, fim_text, prefix_complete\n\nFiles: FileUpload, FileObject, FileList, FileSuccess, FileListSuccess, FileContentSuccess, FileDeleteSuccess, FileFailure, FileCallError, upload_file, list_files, retrieve_file, delete_file, file_content, save_file_content\n\nVector Stores: VectorStoreObject, VectorStoreFileObject, VectorStoreFileBatch, VectorStoreList, VectorStoreSuccess, VectorStoreListSuccess, VectorStoreFileSuccess, VectorStoreBatchSuccess, VectorStoreDeleteSuccess, VectorStoreFailure, VectorStoreCallError, create_vector_store, retrieve_vector_store, list_vector_stores, delete_vector_store, add_vector_store_file, create_file_batch, retrieve_file_batch, poll_file_batch, vector_store_id\n\nConversations: ConversationObject, ConversationItem, ConversationItemList, ConversationSuccess, ConversationItemSuccess, ConversationItemListSuccess, ConversationDeleteSuccess, ConversationFailure, ConversationCallError, create_conversation, retrieve_conversation, update_conversation, delete_conversation, add_conversation_items, list_conversation_items, delete_conversation_item, conversation_id\n\nModerations: ModerationResponse, ModerationResult, ModerationSuccess, ModerationFailure, ModerationCallError, moderate, is_flagged\n\nTypeSafe System One (Jev): TYPESAFEServiceEndpoint, SystemOneQuestion, ChoiceQuestion, ScoreQuestion, NoulQuestion, NoulCriteria, choice, score, noul, SystemOneRequest, ask, SystemOneAnswer, ChoiceAnswer, ScoreAnswer, NoulAnswer, UnknownAnswer, SystemOneResponse, SystemOneSuccess, SystemOneFailure, SystemOneCallError, SystemOneError, answers, answer, TypeSafeModelCard, TypeSafeModelsSuccess, list_models\n\nNatural-language control flow: Meaning, @nl_str, @branch, nl_dispatch, meanings, LowConfidenceError\n\nAudio: SpeechRequest, TranscriptionRequest, SpeechSuccess, TranscriptionSuccess, AudioFailure, AudioCallError, speak, save_audio, transcribe, translate, transcript_text\n\nBatch: BatchObject, BatchList, BatchSuccess, BatchListSuccess, BatchFailure, BatchCallError, create_batch, retrieve_batch, cancel_batch, list_batches, poll_batch\n\nFine-tuning: FineTuningJob, FineTuningList, FineTuningSuccess, FineTuningListSuccess, FineTuningFailure, FineTuningCallError, create_fine_tuning_job, retrieve_fine_tuning_job, cancel_fine_tuning_job, list_fine_tuning_jobs, list_fine_tuning_events, list_fine_tuning_checkpoints\n\nContainers: ContainerObject, ContainerList, ContainerSuccess, ContainerListSuccess, ContainerDeleteSuccess, ContainerFailure, ContainerCallError, create_container, retrieve_container, list_containers, delete_container, add_container_file\n\nUploads: UploadObject, UploadPartObject, UploadSuccess, UploadPartSuccess, UploadFailure, UploadCallError, create_upload, add_upload_part, complete_upload, cancel_upload\n\nWebhooks: WebhookEvent, WEBHOOK_EVENTS, verify_webhook, parse_webhook\n\nRealtime: RealtimeSession, RealtimeSecretSuccess, RealtimeFailure, RealtimeCallError, mint_realtime_secret, realtime_connect, realtime_send, realtime_receive, realtime_event, session_update, input_audio_append, response_create\n\nResult Types (base): LLMRequestResponse, LLMSuccess, LLMFailure, LLMCallError, LLMResultError, issuccess, isfailure, text\n\nRequest Config, Timeouts & Cancellation: RequestConfig, current_config, with_request_config, set_default_config!, UniLMTimeout, MCPTimeoutError, UniLMCancelled, CancelToken, cancel!, iscancelled, with_cancel","title":"Complete Exports List"},{"category":"section","location":"api/config/#timeouts_api","page":"Request Config & Timeouts","text":"See the Timeouts & Retries guide for usage and rationale.\n\nTimeout, deadline, and retry-budget configuration for UniLM network operations, plus the typed timeout errors.","title":"Request Configuration & Timeouts"},{"category":"section","location":"api/config/#Configuration","page":"Request Config & Timeouts","text":"","title":"Configuration"},{"category":"section","location":"api/config/#Timeout-Errors","page":"Request Config & Timeouts","text":"","title":"Timeout Errors"},{"category":"section","location":"api/config/#Cancellation","page":"Request Config & Timeouts","text":"A cooperative, level-triggered token: cancel it from any task, and every operation that observes it — explicitly through a cancel keyword, or as the ambient token of a with_cancel scope — stops and reports UniLMCancelled in its call-error result. See Concurrency, Tasks and Cancellation.","title":"Cancellation"},{"category":"section","location":"api/config/#Resolution-Precedence","page":"Request Config & Timeouts","text":"A request resolves its RequestConfig struct-wise (whichever channel wins supplies every field):\n\nA per-call config keyword (a complete struct wins outright).\nThe innermost active with_request_config scope.\nThe process default set by set_default_config!.\nThe built-in field defaults.","title":"Resolution Precedence"},{"category":"type","location":"api/config/#UniLM.RequestConfig","page":"Request Config & Timeouts","text":"RequestConfig(; kwargs...)\nRequestConfig(base::RequestConfig; kwargs...)\n\nTimeout and retry budget for every UniLM network operation. All time fields are seconds (Float64); Inf disables that bound.\n\nFields\n\nconnect_timeout::Float64 = 10.0: per-attempt connection-establishment bound.\nrequest_timeout::Float64 = 600.0: per-attempt bound on a whole non-streaming exchange, and on a stream's exchange up to its first byte (its response headers).\nstream_idle_timeout::Float64 = 120.0: maximum byte-gap between raw chunks of a stream; HTTP.jl also applies it to the wait for a stream's response headers.\ntotal_deadline::Float64 = 900.0: bound across ALL attempts including backoff; for streams it applies until the first byte. A stream attempt's headers must therefore arrive within min(request_timeout, remaining total_deadline, stream_idle_timeout).\nmax_attempts::Int = 3: maximum wire attempts (1 disables retries).\nmcp_connect_timeout::Float64 = 120.0: MCP spawn → initialize handshake bound.\nmcp_request_timeout::Float64 = 120.0: per MCP exchange bound.\n\nThe constructor throws ArgumentError for NaN or non-positive time values (NaN is rejected explicitly because it compares false against every bound and would silently disable the timeout), for finite time values above 1e9 seconds (too large for the timers that enforce them; use Inf instead), and for max_attempts < 1.\n\nThe two-argument form copies base with the named fields overridden, under the same validation.\n\nSee also with_request_config, set_default_config!, current_config.\n\n\n\n\n\n","title":"UniLM.RequestConfig"},{"category":"function","location":"api/config/#UniLM.current_config","page":"Request Config & Timeouts","text":"current_config() -> RequestConfig\n\nThe ambient RequestConfig: the innermost active with_request_config scope if any, otherwise the process default set by set_default_config! (initially the field defaults).\n\n\n\n\n\n","title":"UniLM.current_config"},{"category":"function","location":"api/config/#UniLM.with_request_config","page":"Request Config & Timeouts","text":"with_request_config(f; kwargs...)\n\nRun f() with a RequestConfig pinned in dynamic scope and return f()'s value. The given fields are merged over current_config once at entry; the resulting complete struct governs every UniLM call inside f, including tasks spawned inside the scope (scoped values propagate into Threads.@spawn). Mutating the process default while the scope is active does not affect it.\n\nwith_request_config(request_timeout=30.0, max_attempts=1) do\n    chatrequest!(chat)\nend\n\n\n\n\n\n","title":"UniLM.with_request_config"},{"category":"function","location":"api/config/#UniLM.set_default_config!","page":"Request Config & Timeouts","text":"set_default_config!(cfg::RequestConfig) -> RequestConfig\nset_default_config!(; kwargs...) -> RequestConfig\n\nSet the process-wide default RequestConfig and return it. The keyword form merges the given fields over the CURRENT process default (never over an active scope). Intended for REPL/notebook sessions, which cannot hold a dynamic scope across cells; prefer with_request_config in programs.\n\nThe keyword form is a read-modify-write and retries on a compare-and-swap miss: a plain read-then-write loses one of two concurrent calls, because the loser installs a snapshot taken before the winner's write and silently drops its field.\n\n\n\n\n\n","title":"UniLM.set_default_config!"},{"category":"type","location":"api/config/#UniLM.UniLMTimeout","page":"Request Config & Timeouts","text":"UniLMTimeout <: Exception\n\nA configured UniLM time bound was exceeded.\n\nFields\n\nphase::Symbol: which bound fired — :connect, :request, :stream_idle, or :deadline (the total budget across attempts).\nelapsed::Float64: seconds elapsed when the timeout surfaced (monotonic clock).\nlimit::Float64: the configured bound in seconds.\n\nConfigure the bounds via RequestConfig. Value-returning surfaces (chat, embeddings, responses) deliver this inside their error results rather than throwing; see each surface's documentation.\n\n\n\n\n\n","title":"UniLM.UniLMTimeout"},{"category":"type","location":"api/config/#UniLM.MCPTimeoutError","page":"Request Config & Timeouts","text":"MCPTimeoutError <: Exception\n\nAn MCP operation exceeded its configured time bound. The MCP surface is throw-based, so this surfaces as an exception rather than a failure value.\n\nFields\n\nphase::Symbol: :connect (spawn → initialize handshake), :queue (the call could not acquire its session within its per-call bound; the session was not touched) or :request (one MCP exchange).\nelapsed::Float64: seconds elapsed when the timeout fired (monotonic clock).\nlimit::Float64: the configured bound in seconds.\nmsg::String: human-readable message naming the applicable override (the mcp_connect_timeout/mcp_request_timeout field or a per-call timeout keyword).\n\n\n\n\n\n","title":"UniLM.MCPTimeoutError"},{"category":"type","location":"api/config/#UniLM.CancelToken","page":"Request Config & Timeouts","text":"CancelToken()\n\nA cooperative cancellation token: make it ambient with with_cancel (or pass it where an operation takes a cancel keyword), then call cancel! from any task. Cancellation is level-triggered — a cancelled token stays cancelled, and every later request that sees it stops before any network I/O — so use a fresh token per unit of work you may want to abandon.\n\nA token reaches what goes through the package's HTTP layer: every HTTP verb, streams included, and the pauses between retries and polls. The Realtime WebSocket (realtime_connect, realtime_receive) and MCP stdio exchanges do not observe it.\n\nThe Julia 1.14 development branch adds task cancellation built on scoped cancellation tokens (Base.CancellationTokenSource / Base.CANCEL_TOKEN) with a cancel keyword on blocking operations; this API has the same shape so a later version can bridge to it.\n\ntok = CancelToken()\nt = Threads.@spawn with_cancel(() -> chatrequest!(chat), tok)\ncancel!(tok)          # e.g. the user pressed \"stop\"\n\n\n\n\n\n","title":"UniLM.CancelToken"},{"category":"function","location":"api/config/#UniLM.cancel!","page":"Request Config & Timeouts","text":"cancel!(tok::CancelToken) -> CancelToken\n\nCancel tok. The first call flips the flag and then runs every registered hook exactly once, outside the token's lock. A hook that throws is @debug-logged and the rest still run; an InterruptException from a hook is rethrown once every hook has run (the first one, if several). Later calls are no-ops.\n\ntok = CancelToken()\ncancel!(tok); cancel!(tok)   # idempotent\niscancelled(tok)             # true\n\n\n\n\n\n","title":"UniLM.cancel!"},{"category":"function","location":"api/config/#UniLM.iscancelled","page":"Request Config & Timeouts","text":"iscancelled(tok::Union{Nothing,CancelToken}) -> Bool\n\nWhether tok has been cancelled; false for nothing (no token).\n\niscancelled(CancelToken())   # false\niscancelled(nothing)         # false\n\n\n\n\n\n","title":"UniLM.iscancelled"},{"category":"function","location":"api/config/#UniLM.with_cancel","page":"Request Config & Timeouts","text":"with_cancel(f, tok::CancelToken)\n\nRun f() with tok as the ambient cancellation token and return f()'s value. What f sends through the package's HTTP layer observes tok — every HTTP verb, streams included, and the pauses between retries and polls — including requests made in tasks spawned inside f (scoped values propagate into Threads.@spawn); the innermost scope wins. The Realtime WebSocket and MCP stdio exchanges do not observe it. The Julia 1.14 development branch adds task cancellation built on scoped cancellation tokens (Base.CancellationTokenSource / Base.CANCEL_TOKEN) with a cancel keyword on blocking operations; this API has the same shape so a later version can bridge to it.\n\ntok = CancelToken()\nwith_cancel(tok) do\n    chatrequest!(chat)       # cancellable through tok\nend\n\n\n\n\n\n","title":"UniLM.with_cancel"},{"category":"type","location":"api/config/#UniLM.UniLMCancelled","page":"Request Config & Timeouts","text":"UniLMCancelled <: Exception\n\nAn operation was cancelled before it completed.\n\nFields\n\nsource::Symbol: :token (a cancel! on the operation's CancelToken) or :callback (a streaming callback set close[] = true).\nelapsed::Float64: seconds since the operation started (monotonic clock).\n\njulia> sprint(showerror, UniLMCancelled(:token, 0.25))\n\"UniLMCancelled: cancelled by token after 0.25 s\"\n\n\n\n\n\n","title":"UniLM.UniLMCancelled"},{"category":"section","location":"guide/multi_backend/#backend_guide","page":"Multi-Backend","text":"UniLM.jl is built around neutral verbs: the same Chat + chatrequest! — with tools, streaming, and cost accounting — run unchanged across every backend; you only change the service. Cost accounting needs a price row for the model: the built-in table covers the OpenAI, Anthropic, Gemini, DeepSeek and TypeSafe models it lists, and any other model (Ollama, Mistral, custom) is estimated at \\$0 with a one-time warning unless you supply pricing=. The agentic respond verb is neutral the same way across OpenAI (Responses) and Gemini (Interactions); see Agentic Workflows. Native OpenAI, Anthropic, and Gemini are first-class backends with their own wire formats (each exercised by live integration tests), not OpenAI-compatible shims.\n\nSelect the backend with service. Model names and supported generation controls remain provider-specific. A request option the chosen provider (or model) cannot express throws ArgumentError before any request is sent — native Anthropic and Gemini fail closed on unmapped Chat fields rather than dropping them.","title":"Multi-Backend Support"},{"category":"section","location":"guide/multi_backend/#Available-Backends","page":"Multi-Backend","text":"Backend Type Env Variables\nOpenAI (default) OPENAIServiceEndpoint OPENAI_API_KEY\nAzure OpenAI AZUREServiceEndpoint AZURE_OPENAI_BASE_URL, AZURE_OPENAI_API_KEY, AZURE_OPENAI_API_VERSION\nGoogle Gemini GEMINIServiceEndpoint GEMINI_API_KEY\nAnthropic ANTHROPICServiceEndpoint ANTHROPIC_API_KEY\nDeepSeek DeepSeekEndpoint DEEPSEEK_API_KEY\nMistral MistralEndpoint MISTRAL_API_KEY\nOllama (local) OllamaEndpoint (none)\nGeneric GenericOpenAIEndpoint (passed to constructor)","title":"Available Backends"},{"category":"section","location":"guide/multi_backend/#OpenAI-(Default)","page":"Multi-Backend","text":"using UniLM\nusing JSON\n\n# OpenAI is the default — no need to specify service\nchat = Chat(model=\"gpt-5.2\")\nprintln(\"Service: \", chat.service)\nprintln(\"Model: \", chat.model)\nService: OPENAIServiceEndpoint\nModel: gpt-5.2","title":"OpenAI (Default)"},{"category":"section","location":"guide/multi_backend/#Azure-OpenAI","page":"Multi-Backend","text":"# Set environment variables\nENV[\"AZURE_OPENAI_BASE_URL\"] = \"https://your-resource.openai.azure.com\"\nENV[\"AZURE_OPENAI_API_KEY\"] = \"your-key\"\nENV[\"AZURE_OPENAI_API_VERSION\"] = \"2024-10-21\"   # the latest dated GA api-version\nENV[\"AZURE_OPENAI_DEPLOY_NAME_GPT_5_2\"] = \"your-gpt52-deployment\"\n\n# Use Azure (default model: gpt-5.2)\nchat = Chat(service=AZUREServiceEndpoint, model=\"gpt-5.2\")\npush!(chat, Message(Val(:system), \"Hello from Azure!\"))\npush!(chat, Message(Val(:user), \"Hi!\"))\nresult = chatrequest!(chat)\n\nAzure routes each request to a deployment. For every model the deployment is read at call time from AZURE_OPENAI_DEPLOY_NAME_<MODEL>, where <MODEL> is the model id upper-cased with every character other than A-Z and 0-9 mapped to _ (gpt-5.2 → AZURE_OPENAI_DEPLOY_NAME_GPT_5_2, gpt-4o-mini → AZURE_OPENAI_DEPLOY_NAME_GPT_4O_MINI), unless a deployment was registered with add_azure_deploy_name!, which wins. A model with neither makes the call return an LLMCallError whose cause is an ArgumentError naming the variable to set. AZUREServiceEndpoint declares :chat, :tools, :streaming and :json_output.\n\nnote: Azure OpenAI v1 API\nAZUREServiceEndpoint speaks the dated api-version form of the Azure OpenAI API. Azure's newer v1 API, which needs no api-version, has the OpenAI-compatible path shape, so it should be reachable through a generic endpoint — untested: GenericOpenAIEndpoint(\"https://your-resource.openai.azure.com/openai\", key) sends POST …/openai/v1/chat/completions with an Authorization: Bearer <key> header.","title":"Azure OpenAI"},{"category":"section","location":"guide/multi_backend/#Custom-Deployment-Names","page":"Multi-Backend","text":"If your Azure deployment has a custom name:\n\nUniLM.add_azure_deploy_name!(\"my-custom-model\", \"my-deployment-name\")\nprintln(\"Registered deployments: \", collect(keys(UniLM._MODEL_ENDPOINTS_AZURE_OPENAI)))\ndelete!(UniLM._MODEL_ENDPOINTS_AZURE_OPENAI, \"my-custom-model\")  # cleanup\nRegistered deployments: [\"my-custom-model\"]","title":"Custom Deployment Names"},{"category":"section","location":"guide/multi_backend/#Google-Gemini","page":"Multi-Backend","text":"warning: Breaking change since v0.10.3\nGEMINIServiceEndpoint now targets Google's native generateContent API (auth header x-goog-api-key, model in the URL, default model gemini-3.8-flash). The old OpenAI-compatible Gemini path is renamed GEMINIOpenAIServiceEndpoint. Migrate code that relied on the OpenAI-compatible behavior — including Embeddings(...; service=GEMINIServiceEndpoint), which the native endpoint does not support — to GEMINIOpenAIServiceEndpoint.\n\nNative Gemini chat (real call, guarded so a failure never breaks the build):\n\ngemini_chat = Chat(service=GEMINIServiceEndpoint, model=\"gemini-3.8-flash\",\n                   reasoning_effort=\"low\", max_tokens=1024)\npush!(gemini_chat, Message(Val(:system), \"You are a helpful assistant.\"))\npush!(gemini_chat, Message(Val(:user), \"Say hello in one short sentence.\"))\nresult = chatrequest!(gemini_chat)\nif result isa LLMSuccess\n    println(result.message.content)\nelse\n    println(\"Request failed — see result for details\")\nend\nHello, I hope you are having a wonderful day!\n\nTo keep using the OpenAI-compatible endpoint, switch the service type:\n\nchat = Chat(service=GEMINIOpenAIServiceEndpoint, model=\"gemini-3.8-flash\",\n            reasoning_effort=\"low\")","title":"Google Gemini"},{"category":"section","location":"guide/multi_backend/#Current-model-controls","page":"Multi-Backend","text":"As of September 7, 2026, Gemini 3.8 Flash supports reasoning_effort=\"low\", \"medium\", or \"high\". Native Chat maps this to thinkingConfig.thinkingLevel. The completion cap includes thinking tokens, so a very small cap can produce an empty answer. Gemini 3.8 rejects temperature, top_p, and minimal thinking. On the Interactions API (respond), temperature and top_p stay mapped to generation_config for models that accept them (gemini-3.7-flash accepted both on September 22, 2026). Native Gemini tool declarations use parametersJsonSchema, preserving standard JSON Schema constraints such as additionalProperties.\n\nresponse_format is mapped natively: JSON object and JSON Schema output become generationConfig.responseFormat (see Structured Output). safety_identifier is mapped natively to the request label safety_identifier; label values allow at most 63 characters of lowercase letters (international characters allowed), numeric characters, underscores, and dashes, so a value such as an email address or a 64-character SHA-256 hex digest throws ArgumentError at encode time. Native Chat returns one candidate and does not implement OpenAI-specific options such as seed, metadata, and stream_options; those options fail explicitly. Gemini determines parallel tool use; the inherited parallel_tool_calls setting does not constrain native Gemini. See Google's migration guide.\n\nNative Gemini reports finishReason truthfully on both the streamed and the non-streamed path: STOP is \"stop\" (\"tool_calls\" when the turn carries function calls), MAX_TOKENS is \"length\", the safety filters — SAFETY, RECITATION, BLOCKLIST, PROHIBITED_CONTENT, SPII and the image filters IMAGE_SAFETY, IMAGE_PROHIBITED_CONTENT and IMAGE_RECITATION — are \"content_filter\", and any other value passes through lowercased (for example \"malformed_function_call\" or \"no_image\"). A candidate without a finishReason reports nothing, and function calls under a reason other than STOP keep that reason, so a tool loop does not run them. A model turn with neither text nor function calls (a refusal, or a turn spent entirely on thinking) is left out of the next request.\n\nOpenAI defaults to gpt-5.6-sol. GPT-5.6, GPT-6 Sol, and GPT-6 Luna Chat Completions tool calling requires an explicit reasoning_effort=\"none\"; use Responses for reasoning with tools. GPT-6 Sol and Luna also reject temperature, top_p, and log probabilities unless reasoning effort is \"none\" (an omitted effort is the provider default, \"medium\"). On the native OpenAI endpoint, GPT-6 Sol and Luna also refuse \"minimal\" reasoning effort; use \"low\" instead. For inexpensive testing, use gpt-5.6-luna and low or no reasoning. Use Respond(model=\"gpt-6-astra\", ...) for Astra tool workflows: Astra tools require Responses, and Astra rejects sampling controls, log probabilities, and reasoning effort below \"low\" (\"none\", \"minimal\"). The existing service_tier field accepts \"fast\"; OpenAI still accepts \"priority\" as an alias. See OpenAI's migration guide.","title":"Current model controls"},{"category":"section","location":"guide/multi_backend/#Anthropic-(native-Messages-API)","page":"Multi-Backend","text":"ANTHROPICServiceEndpoint calls Anthropic's native /v1/messages API (x-api-key + anthropic-version headers). Default model claude-opus-5-5; max_tokens is required on the wire and defaults to 16000 when you omit it (it caps thinking plus text, and current Claude models think by default — raise it for long generations or xhigh/max effort). The endpoint declares :chat, :tools, :streaming and :json_output.\n\nHow a neutral Chat maps onto the Messages API:\n\nChat field Messages API\nleading system messages the top-level system prompt (joined with blank lines)\nlater system messages kept in place as role: \"system\" on models that accept mid-conversation system messages, where the placement is valid (after a user turn, and last or followed by an assistant turn); hoisted into the top-level system prompt otherwise\ntool messages / assistant tool_calls tool_result blocks (consecutive results in one user turn) / tool_use blocks; an assistant turn with neither text nor tool calls is left out\nmax_completion_tokens, else max_tokens max_tokens (default 16000)\ntools (FunctionSignature.strict) tools with input_schema (and a top-level strict)\ntool_choice \"auto\" → auto, \"none\" → none, \"required\" → any, a GPTToolChoice → tool; any other string throws\nparallel_tool_calls = false (the Chat default when tools is set) tool_choice.disable_parallel_tool_use = true — so a tool request allows one tool call per turn unless you set parallel_tool_calls = true\nresponse_format JSON Schema output_config.format = {type: \"json_schema\", schema} (structured outputs; the schema name, description and strict have no counterpart); json_object throws — there is no schema-less JSON mode\nreasoning_effort output_config.effort plus adaptive thinking, per model family (below)\nsafety_identifier, user, metadata[\"user_id\"] metadata.user_id — set one, or set them equal; metadata may carry only user_id\nservice_tier \"auto\" or \"standard_only\"\nstop, temperature, top_p, stream stop_sequences, temperature (0–1), top_p, stream\nn must be 1\nmoderation, prompt_cache_options (OpenAI-only), seed, logprobs, top_logprobs, presence_penalty, frequency_penalty, logit_bias, verbosity, store, prompt_cache_key, stream_options, prediction, modalities, audio, web_search_options no counterpart: setting any of them throws ArgumentError naming the fields\n\nreasoning_effort per family (the longest matching model-id prefix decides):\n\nModels \"low\" … \"max\" \"none\"\nOpus 5.5, Fable 5.x, Mythos 5.x, Mythos Preview (thinking always on) effort + adaptive thinking; Mythos Preview has no xhigh throws: thinking cannot be disabled\nOpus 5, Sonnet 5 (adaptive thinking on by default) effort + adaptive thinking thinking = {type: \"disabled\"}\nOpus 4.8, Opus 4.7, Opus 4.6, Sonnet 4.6 (thinking off by default) effort + adaptive thinking (so the request reasons); the 4.6 models have no xhigh nothing sent\nOpus 4.5 effort low/medium/high only, no adaptive thinking nothing sent\nSonnet 4.5, Haiku 4.5 and older throws: no effort parameter throws\nan id no row covers (a newer model) effort + adaptive thinking, unvalidated thinking = {type: \"disabled\"}\n\n\"minimal\" throws on every Claude model (use \"low\"). Other requests Claude answers with HTTP 400 are refused locally, before the round trip: temperature outside [0, 1] on every model; any top_p, or a temperature other than the default 1.0, on Opus 4.7 and later (Opus 4.8, Sonnet 5, Opus 5, Opus 5.5, Fable, Mythos); a forced tool_choice (\"required\" or a named function) on Opus 5.5, Fable 5.1 and Mythos 5.1; and a conversation that ends with an assistant turn (response prefill) from the 4.6 generation on.\n\nDecoding: stop_reason maps to finish_reason — end_turn/stop_sequence → \"stop\", tool_use → \"tool_calls\", max_tokens and model_context_window_exceeded → \"length\", refusal → \"content_filter\". A refusal carries no content or tool calls (partial output before it is discarded; deltas already streamed to your callback cannot be recalled), and refusal_message holds stop_details.explanation or a default text. A 200 without a content array or a stop_reason is an LLMCallError. usage.prompt_tokens counts cache reads and writes with the uncached input (cached_tokens is the cache-read share), and reasoning_tokens comes from output_tokens_details.thinking_tokens. The request id is read from the request-id header.\n\nclaude_chat = Chat(service=ANTHROPICServiceEndpoint, model=\"claude-haiku-4-5\")  # native Messages API\npush!(claude_chat, Message(Val(:system), \"You are a helpful assistant.\"))\npush!(claude_chat, Message(Val(:user), \"Say hello in one short sentence.\"))\nresult = chatrequest!(claude_chat)\nif result isa LLMSuccess\n    println(result.message.content)\nelse\n    println(\"Request failed — see result for details\")\nend\nHello! How can I help you today?\n\nnote: Thinking models round-trip automatically\nClaude models that emit thinking blocks (e.g. claude-sonnet-5) require those blocks — signatures intact — to be echoed verbatim on the next request of a tool-calling turn. UniLM captures the provider-native content on Message.provider_content at decode time (non-streaming and streaming) and echoes it automatically when the same provider encodes the conversation again, so multi-turn tool use works out of the box. Moving a conversation to a different provider falls back to the neutral text+tool_calls form (thinking is dropped, as other providers cannot verify another vendor's signatures).","title":"Anthropic (native Messages API)"},{"category":"section","location":"guide/multi_backend/#Responses-API-Backend","page":"Multi-Backend","text":"The Responses API also supports the service parameter:\n\nr = Respond(\n    service=UniLM.OPENAIServiceEndpoint,\n    model=\"gpt-5.2\",\n    input=\"Hello!\",\n)\nprintln(\"Service: \", r.service)\nprintln(\"Model: \", r.model)\nService: OPENAIServiceEndpoint\nModel: gpt-5.2","title":"Responses API Backend"},{"category":"section","location":"guide/multi_backend/#OpenAI-Compatible-Providers-(Generic-Endpoint)","page":"Multi-Backend","text":"Any provider that implements the OpenAI-compatible /v1/chat/completions endpoint can be used with GenericOpenAIEndpoint. This includes Ollama, vLLM, LM Studio, Mistral, and many others.","title":"OpenAI-Compatible Providers (Generic Endpoint)"},{"category":"section","location":"guide/multi_backend/#Ollama-(local)","page":"Multi-Backend","text":"ep = OllamaEndpoint()  # defaults to http://localhost:11434\nchat = Chat(service=ep, model=\"llama3.1\")\nprintln(\"URL: \", UniLM.get_url(chat))\nURL: http://localhost:11434/v1/chat/completions","title":"Ollama (local)"},{"category":"section","location":"guide/multi_backend/#Mistral","page":"Multi-Backend","text":"chat = Chat(service=MistralEndpoint(), model=\"mistral-large-latest\")\nresult = chatrequest!(chat)","title":"Mistral"},{"category":"section","location":"guide/multi_backend/#DeepSeek","page":"Multi-Backend","text":"chat = Chat(service=DeepSeekEndpoint())                        # default: deepseek-flash\nchat = Chat(service=DeepSeekEndpoint(), model=\"deepseek-v4-pro\")\n\nThe default model for chat and FIM is deepseek-flash. DeepSeek models think by default and return their chain of thought as reasoning_content; UniLM keeps it on the assistant Message as ProviderContent(:deepseek, …) (streamed or not) and sends it back on those assistant turns whenever the request carries tools — DeepSeek's thinking mode requires that for multi-turn tool use and ignores it otherwise. Streams request include_usage automatically, and cache hits (prompt_cache_hit_tokens) are priced at the cached rate. The price rows use DeepSeek's peak rates; off-peak hours bill half, so estimates for off-peak calls run high. FIM and prefix completion go to the beta base URL (see FIM & Prefix Completion).","title":"DeepSeek"},{"category":"section","location":"guide/multi_backend/#vLLM-/-LM-Studio","page":"Multi-Backend","text":"# vLLM\nchat = Chat(service=GenericOpenAIEndpoint(\"http://localhost:8000\", \"\"), model=\"meta-llama/Llama-3.1-8B\")\n\n# LM Studio\nchat = Chat(service=GenericOpenAIEndpoint(\"http://localhost:1234\", \"\"), model=\"loaded-model\")","title":"vLLM / LM Studio"},{"category":"section","location":"guide/multi_backend/#Anthropic-(OpenAI-compatible-shim)","page":"Multi-Backend","text":"Prefer the native ANTHROPICServiceEndpoint (the Anthropic (native Messages API) section above). For evaluation only, Anthropic also exposes an OpenAI-compatible endpoint — which Anthropic itself calls \"not a long-term or production-ready solution\" (features like response_format and strict are ignored). GenericOpenAIEndpoint appends /v1/chat/completions to its base URL, so the base is the bare host:\n\nchat = Chat(\n    service=GenericOpenAIEndpoint(\"https://api.anthropic.com\", ENV[\"ANTHROPIC_API_KEY\"]),\n    model=\"claude-sonnet-4-6\"\n)","title":"Anthropic (OpenAI-compatible shim)"},{"category":"section","location":"guide/multi_backend/#Custom-Provider","page":"Multi-Backend","text":"ep = GenericOpenAIEndpoint(\"https://my-llm-server.example.com\", \"sk-my-key\")\nchat = Chat(service=ep, model=\"my-model\")\nprintln(\"URL: \", UniLM.get_url(chat))\nprintln(\"Has auth: \", any(p -> p.first == \"Authorization\", UniLM.auth_header(ep)))\nURL: https://my-llm-server.example.com/v1/chat/completions\nHas auth: true","title":"Custom Provider"},{"category":"section","location":"guide/multi_backend/#Embeddings-with-Generic-Endpoint","page":"Multi-Backend","text":"Embeddings also support the service parameter:\n\nemb = Embeddings(\"test\"; service=OllamaEndpoint(), model=\"nomic-embed-text\")\nprintln(\"URL: \", UniLM.get_url(emb))\nURL: http://localhost:11434/v1/embeddings","title":"Embeddings with Generic Endpoint"},{"category":"section","location":"guide/multi_backend/#API-Compatibility-Tiers","page":"Multi-Backend","text":"API Surface Standard Status Supported Providers\nChat Completions De facto standard OpenAI, Azure, Gemini, Mistral, DeepSeek, Ollama, vLLM, LM Studio, Anthropic*\nEmbeddings Widely adopted OpenAI, Gemini, Mistral, Ollama, vLLM\nResponses API Emerging (Open Responses) OpenAI, Ollama, vLLM, Amazon Bedrock\nFIM Completion Provider-specific DeepSeek (beta), Mistral (/v1/fim/completions), Ollama\nImage Generation Limited OpenAI\n\n*Anthropic compat layer is not production-recommended by Anthropic.","title":"API Compatibility Tiers"},{"category":"section","location":"guide/multi_backend/#Querying-Provider-Capabilities","page":"Multi-Backend","text":"Use has_capability to check what a provider supports before making requests:\n\nfor (name, svc) in [\n    (\"OpenAI\", OPENAIServiceEndpoint),\n    (\"DeepSeek\", DeepSeekEndpoint(\"k\")),\n    (\"Ollama\", OllamaEndpoint())\n]\n    caps = join(sort(collect(provider_capabilities(svc))), \", \")\n    println(\"$name: $caps\")\nend\nOpenAI: agentic, audio, batch, chat, containers, conversations, embeddings, files, fine_tuning, image_edits, images, json_output, moderation, realtime, responses, streaming, tools, uploads, vector_stores\nDeepSeek: chat, fim, json_output, prefix_completion, streaming, tools\nOllama: chat, embeddings, fim, json_output, responses, streaming, tools\n\n# Check specific capabilities\nprintln(\"DeepSeek FIM: \", has_capability(DeepSeekEndpoint(\"k\"), :fim))\nprintln(\"OpenAI FIM: \", has_capability(OPENAIServiceEndpoint, :fim))\nDeepSeek FIM: true\nOpenAI FIM: false","title":"Querying Provider Capabilities"},{"category":"section","location":"guide/multi_backend/#See-Also","page":"Multi-Backend","text":"ServiceEndpoint, GenericOpenAIEndpoint — endpoint types\nOllamaEndpoint, MistralEndpoint — convenience constructors\nOPENAIServiceEndpoint, AZUREServiceEndpoint, GEMINIServiceEndpoint — built-in backends","title":"See Also"},{"category":"section","location":"api/accounting/#accounting_api","page":"Cost Tracking","text":"Token accounting works across providers — Gemini and Anthropic usage is normalized to the shared shape at decode time, so these accessors and the pricing table apply uniformly to Chat, Responses, and the agentic verb.\n\nFor a worked walkthrough — reading usage, the silent-\\$0 footgun on unpriced models, custom pricing, and per-Chat cumulative totals — see the Cost Tracking guide.","title":"Cost Tracking & Token Usage"},{"category":"section","location":"api/accounting/#Token-Usage","page":"Cost Tracking","text":"","title":"Token Usage"},{"category":"section","location":"api/accounting/#Cost-Estimation","page":"Cost Tracking","text":"","title":"Cost Estimation"},{"category":"type","location":"api/accounting/#UniLM.TokenUsage","page":"Cost Tracking","text":"TokenUsage(; prompt_tokens=0, completion_tokens=0, total_tokens=0, cached_tokens=0, reasoning_tokens=0)\n\nToken usage statistics returned by the API.\n\ncached_tokens (a subset of prompt_tokens served from the prompt cache) and reasoning_tokens (a subset of completion_tokens spent on hidden reasoning) are reported by newer models via the *_tokens_details objects; they default to 0 when the provider omits those details. estimated_cost bills cached_tokens at the discounted cached-input rate.\n\n\n\n\n\n","title":"UniLM.TokenUsage"},{"category":"function","location":"api/accounting/#UniLM.token_usage","page":"Cost Tracking","text":"token_usage(result::LLMRequestResponse) -> TokenUsage\n\nExtract token usage from a token-billed API result (chat, Responses, embeddings, image generation and FIM completions); failures of those APIs report zero usage.\n\nResults from APIs that do not report token usage at all — audio, files, batches, moderations, vector stores, … — throw an ArgumentError. A zero would be indistinguishable from a genuinely free call and would quietly under-count spend.\n\n\n\n\n\n","title":"UniLM.token_usage"},{"category":"function","location":"api/accounting/#UniLM.estimated_cost","page":"Cost Tracking","text":"estimated_cost(result::LLMRequestResponse; model=nothing, pricing=DEFAULT_PRICING) -> Float64\n\nEstimate the cost in USD for a single API call result. If model is not provided, it is inferred from the result when possible.\n\nReturns 0.0 for results that carry no billable usage (failures) or an unpriced model; an unpriced model id also logs one warning per id. Throws ArgumentError for result types outside the token-billed APIs — see token_usage; their price is not a zero this function can report. pricing is any AbstractDict{String,PriceRow}.\n\nModel lookup: the exact id first, then the id without a dated snapshot suffix (gpt-5.4-mini-2026-03-17 → gpt-5.4-mini, claude-haiku-4-5-20251001 → claude-haiku-4-5). A versioned Jev id with no row of its own (jev-X.Y.Z, e.g. a release newer than this table) is priced at the jev-latest row. That is an assumption: TypeSafe lists one price, for Jev 1.13; a later version is assumed to keep it until the pricing page says otherwise. The same lookup prices SystemOneSuccess results.\n\n\n\n\n\nestimated_cost(result::SystemOneSuccess; model=nothing, pricing=DEFAULT_PRICING) -> Float64\n\nEstimate the USD cost of one System One evaluation. Only input tokens are billed — output tokens are currently free, which the shipped jev-* price rows encode as an output rate of 0.0. The model is taken from the response (the versioned id that answered) unless model overrides it. An id with a dated snapshot suffix uses its base id's row; a versioned jev-X.Y.Z id without its own row is priced at the jev-latest row (assuming a later Jev keeps the price TypeSafe lists for Jev 1.13); any other unpriced name returns 0.0.\n\n\n\n\n\n","title":"UniLM.estimated_cost"},{"category":"function","location":"api/accounting/#UniLM.cumulative_cost","page":"Cost Tracking","text":"cumulative_cost(chat::Chat) -> Float64\n\nReturn the cumulative estimated cost accumulated by chatrequest! calls on this Chat.\n\n\n\n\n\n","title":"UniLM.cumulative_cost"},{"category":"constant","location":"api/accounting/#UniLM.DEFAULT_PRICING","page":"Cost Tracking","text":"Default per-token pricing; current OpenAI, Gemini, and TypeSafe rows verified on 2026-09-22, Anthropic and DeepSeek rows on 2026-09-24 (prices drift — re-verify before relying on them). Cached input is billed at the discounted cached_input rate; reasoning tokens are already counted within output tokens. These are standard short-context text rates: cache-write premiums, long-context surcharges, service-tier and off-peak adjustments, multimodal rates, and hosted-tool fees are not included.\n\nA lock-guarded AbstractDict{String,PriceRow}: add or replace rows with DEFAULT_PRICING[model] = row from any task while requests are running.\n\n\n\n\n\n","title":"UniLM.DEFAULT_PRICING"},{"category":"section","location":"api/chat/#chat_api","page":"Chat Types","text":"Types and functions for the Chat Completions API.","title":"Chat Completions API"},{"category":"section","location":"api/chat/#Chat-Object","page":"Chat Types","text":"","title":"Chat Object"},{"category":"section","location":"api/chat/#Messages","page":"Chat Types","text":"","title":"Messages"},{"category":"section","location":"api/chat/#Convenience-Constructors","page":"Chat Types","text":"using UniLM\n\nsys = Message(Val(:system), \"You are a helpful assistant\")\nusr = Message(Val(:user), \"Hello!\")\nprintln(\"System role: \", sys.role)\nprintln(\"User role: \", usr.role)\nprintln(\"User content: \", usr.content)\nSystem role: system\nUser role: user\nUser content: Hello!","title":"Convenience Constructors"},{"category":"section","location":"api/chat/#Conversation-Management","page":"Chat Types","text":"","title":"Conversation Management"},{"category":"section","location":"api/chat/#Building-a-Conversation","page":"Chat Types","text":"chat = Chat(model=\"gpt-5.2\")\npush!(chat, Message(Val(:system), \"You are a helpful assistant\"))\npush!(chat, Message(Val(:user), \"What is Julia?\"))\nprintln(\"Messages: \", length(chat))\nprintln(\"Valid for sending: \", issendvalid(chat))\nMessages: 2\nValid for sending: true","title":"Building a Conversation"},{"category":"section","location":"api/chat/#Tools","page":"Chat Types","text":"","title":"Tools"},{"category":"section","location":"api/chat/#Legacy-aliases","page":"Chat Types","text":"The provider-neutral names above are canonical. These GPT* consts are exported aliases that keep pre-rename code working unchanged (silent, no deprecation warning; retained until the 1.0 stability boundary).","title":"Legacy aliases"},{"category":"section","location":"api/chat/#Output-Format","page":"Chat Types","text":"using JSON\n\n# JSON object format\nfmt = ResponseFormat()\nprintln(\"Type: \", fmt.type)\nprintln(\"JSON: \", JSON.json(fmt))\nType: json_object\nJSON: {\"type\":\"json_object\"}","title":"Output Format"},{"category":"section","location":"api/chat/#Request-Function","page":"Chat Types","text":"","title":"Request Function"},{"category":"section","location":"api/chat/#Role-Constants","page":"Chat Types","text":"","title":"Role Constants"},{"category":"section","location":"api/chat/#Tool-Loop","page":"Chat Types","text":"","title":"Tool Loop"},{"category":"section","location":"api/chat/#Model-Constants","page":"Chat Types","text":"println(\"GPT5_2: \", UniLM.GPT5_2)\nGPT5_2: gpt-5.2","title":"Model Constants"},{"category":"type","location":"api/chat/#UniLM.Chat","page":"Chat Types","text":"chat = Chat()\n\nCreates a new Chat object with default settings:\n\nmodel is set to gpt-5.6-sol\nmessages is set to an empty Vector{Message}\nhistory is set to true\n\nOpenAI options, omitted from the request when unset:\n\nprompt_cache_options::Union{PromptCacheOptions,Nothing}: prompt-cache mode and ttl (PromptCacheOptions); supported for gpt-5.6 and later (gpt-5.4-mini answered HTTP 400 \"promptcacheoptions is not supported on this model\" on 2026-09-22). Chat messages cannot carry cache breakpoints, so mode=\"explicit\" means the request does not use prompt caching.\nmoderation::Union{ModerationConfig,Nothing}: moderated completions (ModerationConfig).\nstream_options::Union{AbstractDict,Nothing}: streaming options; when unset, an OPENAIServiceEndpoint or DeepSeekEndpoint stream requests {\"include_usage\": true}, so the streamed result carries token usage.\n\nThe constructor validates ranges and throws ArgumentError on a violation; among them, n must be 1 when set — a result carries a single choice — top_logprobs must be in [0, 20], every logit_bias value (any Real) in [-100, 100], and reasoning_effort one of \"none\", \"minimal\", \"low\", \"medium\", \"high\", \"xhigh\", \"max\". An empty tools vector is stored as nothing.\n\n\n\n\n\n","title":"UniLM.Chat"},{"category":"type","location":"api/chat/#UniLM.Message","page":"Chat Types","text":"Message(; role, content=nothing, name=nothing, finish_reason=nothing, refusal_message=nothing, tool_calls=nothing, tool_call_id=nothing, provider_content=nothing)\n\nRepresents a single message in a Chat Completions conversation.\n\nFields\n\nrole::String: One of RoleSystem, RoleUser, RoleAssistant, or RoleTool.\ncontent::Union{String,Nothing}: The text content of the message.\nname::Union{String,Nothing}: Optional name for the participant.\nfinish_reason::Union{String,Nothing}: Why the model stopped generating (e.g. \"stop\", \"tool_calls\"); nothing when the provider reported none. Response-only: never sent in a request.\nrefusal_message::Union{String,Nothing}: Refusal text when content is filtered; sent on the wire as refusal.\ntool_calls::Union{Nothing,Vector{ToolCall}}: Tool calls requested by the assistant.\ntool_call_id::Union{String,Nothing}: Required when role is \"tool\" — the ID of the tool call being responded to.\nprovider_content::Union{Nothing,ProviderContent}: Provider-native content blocks captured for verbatim round-trip (see ProviderContent); set by the Anthropic, Gemini and DeepSeek decoders (tags :anthropic, :gemini, :deepseek), nothing otherwise. Never serialized on the OpenAI wire.\n\nValidation\n\nrole must be one of \"system\", \"user\", \"assistant\", \"tool\".\nAt least one of content, tool_calls, or refusal_message must be non-nothing.\ntool_call_id is required when role == \"tool\".\n\nConvenience Constructors\n\nMessage(Val(:system), \"You are a helpful assistant\")\nMessage(Val(:user), \"Hello!\")\n\n\n\n\n\n","title":"UniLM.Message"},{"category":"type","location":"api/chat/#UniLM.ProviderContent","page":"Chat Types","text":"ProviderContent(provider::Symbol, blocks::Vector{Any})\n\nProvider-native assistant content captured verbatim at decode time and echoed verbatim at encode time when the SAME provider encodes the turn again.\n\nThe neutral Message carries content::String + tool_calls, but some providers attach blocks that must round-trip byte-faithfully for multi-turn flows to work: Anthropic thinking/redacted_thinking blocks (their signature must be echoed unmodified or tool round-trips on thinking models are rejected with HTTP 400), Gemini text-part thoughtSignatures, and DeepSeek reasoning_content. provider tags the wire dialect (:anthropic, :gemini or :deepseek); blocks is the provider's content/parts array exactly as decoded (String-keyed JSON) — for :deepseek, [Dict(\"reasoning_content\" => text)], echoed only on requests that carry tools.\n\nEncoders ignore a ProviderContent tagged for a different provider — a conversation moved across providers falls back to the neutral reconstruction (the standard \"other models drop thinking\" semantics). The field never serializes on the OpenAI wire (excluded from JSON.lower(::Message)).\n\n\n\n\n\n","title":"UniLM.ProviderContent"},{"category":"method","location":"api/chat/#Base.push!-Tuple{Chat, Message}","page":"Chat Types","text":"push!(chat::Chat, msg::Message)\n\nAdd a message to the conversation, refusing any mutation that would make it invalid.\n\nThrows InvalidConversationError when the mutation would produce an invalid conversation: a non-system message pushed onto an empty conversation (it must start with a system message), a system message pushed once the conversation has already started, or a message whose role repeats the last message's role (consecutive tool-result messages are the one exception). chat[i] = msg and direct edits to chat.messages are not validated.\n\n\n\n\n\n","title":"Base.push!"},{"category":"method","location":"api/chat/#Base.pop!-Tuple{Chat}","page":"Chat Types","text":"pop!(chat::Chat)\n\nRemove the last message from the conversation.\n\nThrows InvalidConversationError when the conversation is empty.\n\n\n\n\n\n","title":"Base.pop!"},{"category":"function","location":"api/chat/#UniLM.issendvalid","page":"Chat Types","text":"issendvalid(chat::Chat)::Bool\n\nCheck if the conversation is valid for sending to the API.\n\nReturns true only when the conversation has at least two messages, the first is a system message, the LAST is a user message, and no two adjacent messages share a role. Note the last two clauses are stricter than push!, which permits consecutive tool messages — this check has no such exemption, and a conversation ending in a tool result is false here.\n\nThis is a heuristic, not a proof: it cannot catch every malformed shape (a second system message in the middle passes the adjacency test). It never throws — a false is a verdict, not an error.\n\n\n\n\n\n","title":"UniLM.issendvalid"},{"category":"function","location":"api/chat/#UniLM.update!","page":"Chat Types","text":"update!(chat::Chat, msg::Message) -> Chat\n\nAppend msg to the conversation when chat.history is on; with history=false the chat is left unchanged, as documented for that setting (logged at debug level only).\n\n\n\n\n\n","title":"UniLM.update!"},{"category":"function","location":"api/chat/#UniLM.fork","page":"Chat Types","text":"fork(chat::Chat) -> Chat\n\nCreate an independent copy of a Chat: every field except service is deep-copied in one pass, so mutating a fork's messages, tools, metadata, stop or any other field never reaches its source or its siblings; service is shared (the endpoint a chat talks to, not conversation state); the cumulative-cost Ref is fresh (copied by value). The copy runs over fieldnames(Chat), so new Chat fields fork automatically. fork itself applies no normalization or rewrite — a fork is configuration-identical to its source.\n\n\n\n\n\nfork(chat::Chat, n::Int) -> Vector{Chat}\n\nCreate n independent forks of a Chat.\n\n\n\n\n\n","title":"UniLM.fork"},{"category":"type","location":"api/chat/#UniLM.InvalidConversationError","page":"Chat Types","text":"InvalidConversationError <: Exception\n\nThrown when a conversation violates structural rules (e.g., missing system message, consecutive messages from the same role).\n\nFields\n\nreason::String: Human-readable explanation of why the conversation is invalid.\n\n\n\n\n\n","title":"UniLM.InvalidConversationError"},{"category":"type","location":"api/chat/#UniLM.Tool","page":"Chat Types","text":"Tool(; type=\"function\", func)\n\nWraps a FunctionSignature for use in the tools parameter of a Chat.\n\nExample\n\ntool = Tool(func=FunctionSignature(\n    name=\"get_weather\",\n    description=\"Get the current weather\",\n    parameters=Dict(\"type\" => \"object\", \"properties\" => Dict())\n))\nchat = Chat(tools=[tool], reasoning_effort=\"none\")   # GPT-5.6 Chat tools need effort \"none\"\n\n\n\n\n\n","title":"UniLM.Tool"},{"category":"type","location":"api/chat/#UniLM.FunctionSignature","page":"Chat Types","text":"FunctionSignature(; name, description=nothing, parameters=nothing, strict=nothing)\n\nDescribes a function that can be called by the model in the Chat Completions API.\n\nFields\n\nname::String: The name of the function.\ndescription::Union{String,Nothing}: A description of what the function does.\nparameters::Union{AbstractDict,Nothing}: JSON Schema object describing the function parameters.\nstrict::Union{Bool,Nothing}: Enable strict schema adherence for function arguments (structured outputs). nothing (default) omits the field from the request — the API default, non-strict. true requires parameters to be a strict-valid schema (additionalProperties: false on every object, all properties required); UniLM does not validate this — the API rejects strict-invalid schemas with a 400.\n\nExample\n\nsig = FunctionSignature(\n    name=\"get_weather\",\n    description=\"Get the current weather in a given location\",\n    parameters=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"location\" => Dict(\"type\" => \"string\", \"description\" => \"The city\")\n        ),\n        \"required\" => [\"location\"],\n        \"additionalProperties\" => false\n    ),\n    strict=true\n)\n\n\n\n\n\n","title":"UniLM.FunctionSignature"},{"category":"type","location":"api/chat/#UniLM.ToolCall","page":"Chat Types","text":"ToolCall(; id, type=\"function\", func)\n\nRepresents a tool call returned by the model. Contains the call id (used to match results back), the tool type, and the [GPTFunction] with name and parsed arguments.\n\nAlso carries an optional thought_signature::Union{Nothing,String} field holding Gemini-3's opaque tool-call signature, which must be echoed verbatim on the next turn; it is set by the Gemini decoder, ignored (nothing) by every other provider, and deliberately excluded from JSON.lower so OpenAI-wire serialization is unaffected.\n\n\n\n\n\n","title":"UniLM.ToolCall"},{"category":"type","location":"api/chat/#UniLM.FunctionCallResult","page":"Chat Types","text":"FunctionCallResult{T}\n\nHolds the result of executing a function that was requested by the model via a tool call.\n\nFields\n\nname::Union{String,Symbol}: The function name.\norigincall::GPTFunction: The original [GPTFunction] call from the model.\nresult::T: The result of executing the function.\n\n\n\n\n\n","title":"UniLM.FunctionCallResult"},{"category":"type","location":"api/chat/#UniLM.GPTTool","page":"Chat Types","text":"GPTTool\n\nLegacy alias for Tool.\n\n\n\n\n\n","title":"UniLM.GPTTool"},{"category":"type","location":"api/chat/#UniLM.GPTToolCall","page":"Chat Types","text":"GPTToolCall\n\nLegacy alias for ToolCall.\n\n\n\n\n\n","title":"UniLM.GPTToolCall"},{"category":"type","location":"api/chat/#UniLM.GPTFunctionSignature","page":"Chat Types","text":"GPTFunctionSignature\n\nLegacy alias for FunctionSignature.\n\n\n\n\n\n","title":"UniLM.GPTFunctionSignature"},{"category":"type","location":"api/chat/#UniLM.GPTFunctionCallResult","page":"Chat Types","text":"GPTFunctionCallResult\n\nLegacy alias for FunctionCallResult.\n\n\n\n\n\n","title":"UniLM.GPTFunctionCallResult"},{"category":"type","location":"api/chat/#UniLM.ResponseFormat","page":"Chat Types","text":"ResponseFormat(; type=\"json_object\", json_schema=nothing)\nResponseFormat(json_schema)\n\nSpecifies the output format for Chat Completions.\n\nFields\n\ntype::String: \"json_object\" or \"json_schema\".\njson_schema::Union{JsonSchemaAPI,AbstractDict,Nothing}: Schema definition when type is \"json_schema\".\n\nExamples\n\n# Free-form JSON\nfmt = ResponseFormat()\n\n# Structured JSON via schema\nfmt = ResponseFormat(UniLM.JsonSchemaAPI(\n    name=\"result\",\n    description=\"A structured result\",\n    schema=Dict(\"type\" => \"object\", \"properties\" => Dict())\n))\n\nnote: Note\nJsonSchemaAPI and the convenience constructors UniLM.json_object() and UniLM.json_schema(name, description, schema; strict=nothing) are not exported: qualify them with UniLM..\n\n\n\n\n\n","title":"UniLM.ResponseFormat"},{"category":"function","location":"api/chat/#UniLM.chatrequest!","page":"Chat Types","text":"chatrequest!(chat::Chat; config=nothing, callback=nothing, on_tool_call=nothing, cancel=nothing)\n\nSend chat to its provider and return a typed result.\n\nNon-streaming (chat.stream !== true): returns LLMSuccess, LLMFailure, or LLMCallError. Transient statuses (408/429/500/502/503/504/529) are retried with backoff and jitter under the resolved RequestConfig (max_attempts, total_deadline; Retry-After honored). Timeouts surface as LLMCallError with status = nothing and the UniLMTimeout in cause — no fabricated HTTP statuses.\n\nStreaming (chat.stream === true): returns a Task whose fetch yields the same typed results. callback(chunk::Union{String,Message}, close::Ref{Bool}) receives text deltas then the final assembled Message; on_tool_call(tc::ToolCall) fires once per completed streamed tool call. Setting close[] = true — in the callback or from any other task — stops the stream at once: the call ends with LLMCallError(status=nothing, cause=UniLMCancelled(:callback, …)), unless the provider's completion marker already arrived — then the turn is committed, the final-message callback runs, and usage may be missing. The marker is the chunk that carries the finish reason; on the OpenAI wire it precedes the usage chunk and [DONE]. The turn is final at the end-of-stream sentinel ([DONE], Anthropic message_stop): the final-message callback runs then, and the result follows when the HTTP body ends, at most 0.5 s later (a body still open then has its connection closed rather than reused). An exception thrown by callback or on_tool_call ends the call with that exception in cause: never retried, nothing committed, and no callback runs after it. Time spent in these callbacks does not count toward stream_idle_timeout, which bounds only the gap between bytes off the socket. A user InterruptException is never converted into a result value: it propagates, so fetch on the streaming task throws a TaskFailedException whose task.exception is the InterruptException.\n\nconfig::Union{Nothing,RequestConfig}: per-call timeout/retry budget; nothing resolves the ambient configuration (with_request_config scope, else the process default set via set_default_config!).\n\ncancel::Union{Nothing,CancelToken}: a CancelToken; nothing resolves the ambient token of with_cancel, at call entry. A cancel at any point — before connecting, during the response-header wait, mid-stream, or during a retry backoff — ends the call with LLMCallError(status=nothing, cause=UniLMCancelled(:token, …)): never retried, nothing committed to chat, no terminal callback — unless the provider's completion marker already arrived (as for close[] = true above): then the turn is committed, the final-message callback runs, and usage may be missing. A pre-cancelled token sends nothing. A TCP connect or TLS handshake already in progress cannot be interrupted (HTTP.jl 2.7.1), so a cancel during one takes effect when it completes or reaches connect_timeout.\n\nLocal validation throws before any network I/O, streaming or not: ArgumentError when callback or on_tool_call is passed without chat.stream === true (they run only on a stream, so they would be ignored), when chat.service is an endpoint type that declares its capabilities and does not list :chat (a custom endpoint declares none and is dispatched unvalidated), or when the provider's encoder rejects the request (e.g. an option the provider or model does not support); InvalidConversationError when chat.history is on and the conversation ends with an assistant message, so the reply could not be appended.\n\n\n\n\n\nchatrequest!(; messages, config=nothing, cancel=nothing, kwargs...)\nchatrequest!(; systemprompt, userprompt, config=nothing, cancel=nothing, kwargs...)\n\nBuild a Chat from keyword arguments, send it with chatrequest!(chat; config, cancel), and return that result (the reply is appended to result.self).\n\nThe conversation is EITHER messages — copied, so the caller's vector is never mutated — OR systemprompt and userprompt, each a String or a Message. Passing neither, only one prompt, or messages together with a prompt throws ArgumentError before any network I/O. Every other keyword is a Chat field, for example:\n\nservice::ServiceEndpointSpec = OPENAIServiceEndpoint: The provider endpoint type or instance.\nmodel::String: The model; the service's default when omitted.\nhistory::Bool = true: Whether the reply is appended to the conversation. It does not change what is sent.\ntools::Union{Vector{Tool},Nothing} = nothing: A list of tools the model may call.\ntool_choice::Union{String,GPTToolChoice,Nothing} = nothing: Controls which (if any) function is called by the model. e.g. \"auto\", \"none\", GPTToolChoice.\nparallel_tool_calls::Union{Bool,Nothing} = false: Whether to enable parallel function calling.\ntemperature::Union{Float64,Nothing} = nothing: Sampling temperature (0.0-2.0). Higher values make output more random. Mutually exclusive with top_p.\ntop_p::Union{Float64,Nothing} = nothing: Nucleus sampling parameter (0.0-1.0). Mutually exclusive with temperature.\nn::Union{Int64,Nothing} = nothing: Number of choices; must be 1 — a result carries a single choice.\nstream::Union{Bool,Nothing} = nothing: If true, the call returns a Task and streams deltas to callback (see chatrequest!(chat)).\nstop::Union{Vector{String},String,Nothing} = nothing: Up to 4 sequences where the API will stop generating further tokens.\nmax_tokens::Union{Int64,Nothing} = nothing: The maximum number of tokens to generate in the chat completion.\npresence_penalty::Union{Float64,Nothing} = nothing: Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they appear in the text so far.\nresponse_format::Union{ResponseFormat,Nothing} = nothing: An object specifying the format that the model must output. e.g., ResponseFormat(type=\"json_object\").\nfrequency_penalty::Union{Float64,Nothing} = nothing: Number between -2.0 and 2.0. Positive values penalize new tokens based on their existing frequency in the text so far.\nlogit_bias::Union{AbstractDict{String,<:Real},Nothing} = nothing: Modify the likelihood of specified tokens appearing in the completion.\nuser::Union{String,Nothing} = nothing: A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse.\nseed::Union{Int64,Nothing} = nothing: This feature is in Beta. If specified, the system will make a best effort to sample deterministically.\nconfig::Union{Nothing,RequestConfig} = nothing: Per-call timeout/retry budget; nothing resolves the ambient configuration (scoped, else process default).\ncancel::Union{Nothing,CancelToken} = nothing: Cancellation token; nothing resolves the ambient token (see chatrequest!(chat)).\n\n\n\n\n\n","title":"UniLM.chatrequest!"},{"category":"constant","location":"api/chat/#UniLM.RoleSystem","page":"Chat Types","text":"RoleSystem\n\nRole constant \"system\" — used for system-level instructions.\n\n\n\n\n\n","title":"UniLM.RoleSystem"},{"category":"constant","location":"api/chat/#UniLM.RoleUser","page":"Chat Types","text":"RoleUser\n\nRole constant \"user\" — used for user messages.\n\n\n\n\n\n","title":"UniLM.RoleUser"},{"category":"constant","location":"api/chat/#UniLM.RoleAssistant","page":"Chat Types","text":"RoleAssistant\n\nRole constant \"assistant\" — used for model-generated messages.\n\n\n\n\n\n","title":"UniLM.RoleAssistant"},{"category":"type","location":"api/chat/#UniLM.CallableTool","page":"Chat Types","text":"CallableTool{T}(tool, callable)\n\nWraps a tool schema T (Tool or FunctionTool) with a callable. JSON serialization delegates to the inner tool, preserving backward compatibility.\n\nFields\n\ntool::T: The tool schema.\ncallable::Function: (name::String, args::Dict{String,Any}) -> String\n\nExample\n\ntool = Tool(func=FunctionSignature(name=\"add\", description=\"Add two numbers\",\n    parameters=Dict(\"type\"=>\"object\",\"properties\"=>Dict(\"a\"=>Dict(\"type\"=>\"number\"),\"b\"=>Dict(\"type\"=>\"number\")))))\nct = CallableTool(tool, (name, args) -> string(args[\"a\"] + args[\"b\"]))\n\n\n\n\n\n","title":"UniLM.CallableTool"},{"category":"type","location":"api/chat/#UniLM.ToolCallOutcome","page":"Chat Types","text":"ToolCallOutcome\n\nPer-call record from a tool dispatch.\n\nFields\n\ntool_name::String: Name of the tool that was called.\narguments::Dict{String,Any}: Arguments passed to the tool.\nresult::Union{FunctionCallResult,Nothing}: The result wrapper, or nothing on failure.\nsuccess::Bool: Whether the dispatch succeeded.\nerror::Union{String,Nothing}: Error message on failure.\n\n\n\n\n\n","title":"UniLM.ToolCallOutcome"},{"category":"type","location":"api/chat/#UniLM.ToolLoopResult","page":"Chat Types","text":"ToolLoopResult\n\nResult of a tool dispatch loop.\n\nFields\n\nresponse::LLMRequestResponse: The last response the loop received: the final text turn, the failure that ended the loop, the turn it stopped on, or — when max_turns ran out — the last tool-call turn (whose calls were dispatched). A loop cancelled between tool dispatches holds a call error whose cause is UniLMCancelled.\ntool_calls::Vector{ToolCallOutcome}: History of all tool dispatches.\nturns_used::Int: Number of API round-trips.\ncompleted::Bool: Whether the loop terminated normally (a text response that finished normally). Truncated or filtered output, a turn stopped before its calls ran, a pending server action, or max_turns exhaustion leaves this false.\nllm_error::Union{String,Nothing}: Why the loop stopped when not completed (e.g. \"max turns (3) exhausted\").\n\n\n\n\n\n","title":"UniLM.ToolLoopResult"},{"category":"function","location":"api/chat/#UniLM.tool_loop!","page":"Chat Types","text":"tool_loop!(chat::Chat, dispatcher::Function; max_turns=10, config=nothing, callback=nothing, on_tool_call=nothing, cancel=nothing, tool_concurrency=1) -> ToolLoopResult\n\nRun a tool-calling loop on a Chat. Repeatedly calls chatrequest!, dispatches tool calls via dispatcher(name, args), pushes tool-role messages back, and repeats until a text response, API error, or max_turns.\n\nTool calls run only on a turn whose finish_reason is \"tool_calls\". A turn that carries tool calls but finished for any other reason (\"length\", \"content_filter\", a provider-specific value) may hold partial calls: none runs, the loop stops with completed=false and an llm_error naming the reason, and that unanswered assistant turn is removed from chat, so the conversation stays sendable. A text turn completes the loop only when it finished with \"stop\" or reported no reason (or finished with \"tool_calls\" but carried no calls); \"length\" stops it with completed=false and llm_error = \"Model output was truncated by the token limit\", and any other reason (\"content_filter\", a provider-specific value) with an llm_error naming it — the turn stays in chat.\n\nchat.history must be true (else ArgumentError before any request): each follow-up request carries the tool results together with the assistant turn that requested them.\n\nArguments\n\ndispatcher: (name::String, args::Dict{String,Any}) -> String; any other return value is sent to the model JSON-encoded.\nmax_turns: Maximum API round-trips (default 10; < 1 throws ArgumentError). When they run out, the result keeps the last response, with completed=false and llm_error = \"max turns (N) exhausted\".\nconfig: Per-request RequestConfig passed to chatrequest! — each turn gets its own attempt/deadline budget.\ncallback: Streaming callback passed to chatrequest!.\non_tool_call: Tool call notification callback passed to chatrequest!. Both are passed through unchanged and need chat.stream == true: otherwise the first request throws ArgumentError before any network I/O.\ncancel: A CancelToken; nothing (default) uses the ambient token of with_cancel. Every turn — its request and its tool dispatches — runs inside with_cancel(cancel), and the token is checked before each dispatch. A cancelled loop returns promptly with completed=false and dispatches nothing further; response is the turn's typed cancellation result: the cancelled request's LLMCallError, or, when the cancel lands between dispatches, an LLMCallError whose cause is UniLMCancelled. A turn cancelled between dispatches is removed from chat (the calls it did run stay in tool_calls), so the conversation stays sendable.\ntool_concurrency: How many of one turn's tool calls may run at once (default 1: one at a time, in the calling task; < 1 throws ArgumentError). Above 1, up to that many run concurrently on spawned tasks (Threads.@spawn), so dispatcher must be thread-safe; their results are appended in call order once all have finished, so the next request does not depend on completion order. An InterruptException from any dispatch propagates, after the interrupted turn is removed from chat (with any results already appended), so the conversation stays sendable.\n\nExample\n\nchat = Chat(model=\"gpt-5.4-mini\", tools=[tool])\npush!(chat, Message(Val(:system), \"You are a calculator\"))\npush!(chat, Message(Val(:user), \"What is 3+5?\"))\nresult = tool_loop!(chat, (name, args) -> string(args[\"a\"] + args[\"b\"]))\n\n\n\n\n\ntool_loop!(chat::Chat; tools::Vector{<:CallableTool}, kwargs...) -> ToolLoopResult\n\nNo-dispatcher variant: builds a dispatcher from CallableTool entries.\n\n\n\n\n\n","title":"UniLM.tool_loop!"},{"category":"function","location":"api/chat/#UniLM.tool_loop","page":"Chat Types","text":"tool_loop(r::Respond, dispatcher::Function; max_turns=10, config=nothing, cancel=nothing, tool_concurrency=1) -> ToolLoopResult\n\nRun a tool-calling loop on a Respond request. Dispatches function calls via dispatcher(name, args) (a non-String return is sent JSON-encoded), builds function_call_output input items, and chains via previous_response_id — or, when r.conversation is set, through the conversation alone (the API rejects the two together). A call whose arguments are not a JSON object is answered with an \"Error: invalid arguments: …\" output, like a dispatcher error, and the loop goes on.\n\nFunction calls run only on a completed or requires_action turn. Any other status (e.g. incomplete, whose calls may be partial) stops the loop with completed=false and an llm_error naming the status and the incomplete_details reason. A turn that requests a client-side call this loop cannot execute (custom_tool_call, apply_patch_call, local_shell_call, computer_call, a shell_call the platform did not run itself, or an mcp_approval_request) stops it the same way, with the pending call type in llm_error; none of that turn's calls run.\n\nmax_turns (default 10; < 1 throws ArgumentError) bounds the round-trips; when they run out, the result keeps the last response, with completed=false and llm_error = \"max turns (N) exhausted\".\n\ncancel works as in tool_loop!: every turn runs inside with_cancel(cancel) (nothing uses the ambient token), the token is checked before each dispatch, and a cancelled loop returns promptly with completed=false and the turn's typed cancellation result — the cancelled request's ResponseCallError, or, between dispatches, a ResponseCallError whose cause is UniLMCancelled. tool_concurrency (default 1) works as in tool_loop!: above 1, up to that many of a turn's calls run at once and their outputs are sent in call order.\n\nPer-call config::RequestConfig overrides timeouts/retry budget.\n\n\n\n\n\ntool_loop(r::Respond; max_turns=10, config=nothing, cancel=nothing, tool_concurrency=1) -> ToolLoopResult\n\nNo-dispatcher variant: extracts callables from CallableTool entries in r.tools.\n\nPer-call config::RequestConfig overrides timeouts/retry budget.\n\n\n\n\n\ntool_loop(input, dispatcher::Function; tools, kwargs...) -> ToolLoopResult\n\nConvenience form: creates a Respond and runs the tool loop.\n\nPer-call config::RequestConfig overrides timeouts/retry budget; max_turns, cancel and tool_concurrency drive the loop as in tool_loop(::Respond, ::Function); every other keyword goes to the Respond constructor.\n\n\n\n\n\ntool_loop(input::String; tools, max_turns=10, config=nothing, cancel=nothing, tool_concurrency=1, kwargs...) -> ToolLoopResult\n\nNo-dispatcher convenience form of the Responses-API tool loop for a plain-string prompt. Wraps input and tools in a Respond and delegates to tool_loop(::Respond), which dispatches each model-requested function call to the matching CallableTool callable.\n\nKeyword routing is explicit: max_turns, config, cancel and tool_concurrency drive the loop (config::RequestConfig overrides timeouts/retry budget), while every other keyword is forwarded verbatim to the Respond constructor — an unknown keyword raises there rather than being silently dropped. tools is required and must hold CallableTool entries (e.g. from mcp_tools_respond).\n\nExample\n\nsession = mcp_connect(\"https://mcp.example.com/mcp\")\ntools = mcp_tools_respond(session)\nresult = tool_loop(\"List files in /tmp\"; tools=tools)\n\n\n\n\n\n","title":"UniLM.tool_loop"},{"category":"function","location":"api/chat/#UniLM.to_tool","page":"Chat Types","text":"to_tool(x)\n\nOverloadable conversion protocol. Identity for Tool, FunctionTool, CallableTool. Converts AbstractDict to Tool. Package extensions can add methods for other types.\n\n\n\n\n\nConvert an MCPServerTool to a FunctionTool for use with the Responses API.\n\n\n\n\n\n","title":"UniLM.to_tool"},{"category":"section","location":"guide/agentic/#agentic_guide","page":"Agentic Workflows","text":"respond is the unified agentic verb. The same call targets OpenAI's Responses API by default, or Google's Gemini Interactions API by setting service=GEMINIServiceEndpoint — identical inputs, tools, lifecycle, and usage/cost accounting.","title":"Agentic Workflows"},{"category":"section","location":"guide/agentic/#One-call,-two-providers","page":"Agentic Workflows","text":"result = respond(\"Explain multiple dispatch in one sentence.\", model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nMultiple dispatch is a programming paradigm where a function’s behavior is chosen based on the runtime types of all its arguments, not just the first one.\n\nSwap the provider with a single keyword:\n\nresult = respond(\"Explain multiple dispatch in one sentence.\"; service=GEMINIServiceEndpoint, model=\"gemini-3.8-flash\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\n**Multiple dispatch** is a programming language feature where the specific function implementation to execute is determined at runtime based on the dynamic types of more than one of its arguments, rather than just the caller.\n\nGoogle shuts down gemini-3.1-flash-lite on May 7, 2027 and names gemini-3.5-flash-lite as its replacement.","title":"One call, two providers"},{"category":"section","location":"guide/agentic/#Hosted-tools-(Gemini)","page":"Agentic Workflows","text":"Control Gemini thinking with the same typed configuration used by OpenAI:\n\nr = Respond(service=GEMINIServiceEndpoint, model=\"gemini-3.8-flash\", input=\"Explain recursion briefly.\",\n            reasoning=Reasoning(effort=\"low\", summary=\"auto\"), max_output_tokens=1024)\n\nThis maps to Interactions generation_config.thinking_level and thinking_summaries. Gemini 3.8 supports low, medium, and high effort and rejects temperature and top_p. Continue with previous_response_id to preserve server state and signatures. Google thinking documentation.\n\nGemini Interactions exposes server-side hosted tools via gemini_google_search, gemini_code_execution, and gemini_url_context — pass them in tools=:\n\nresult = respond(\"What are the latest stable Julia releases?\";\n                 service=GEMINIServiceEndpoint, tools=[gemini_google_search()], model=\"gemini-3.8-flash\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nThe latest supported releases of the **Julia** programming language fall into two main tracks:\n\n### 1. Current Stable Release\n* **Julia 1.13.0**\n  * This is the recommended version for most users and general development.\n  * It includes significant latency/TTFX (time-to-first-execution) and package precompilation improvements, updated REPL features (such as syntax highlighting and fuzzy history search), and other performance optimizations.\n\n### 2. Long-Term Support (LTS) Release\n* **Julia 1.10.12** (from the **v1.10** LTS series)\n  * Recommended for enterprise environments or deployments where stability and certification requirements make frequent upgrades difficult.\n  * It continues to receive critical bug fixes and security backports.\n\n---\n\n### Recent Preceding Releases\n* **v1.12.7**: The final patch in the 1.12 series, superseded by the release of 1.13.\n* **v1.11.9**: The final patch in the 1.11 series.\n\n---\n\n### Installation & Management\nThe official and recommended way to install and manage Julia versions across platforms is via **[`juliaup`](https://github.com/JuliaLang/juliaup)**:\n\n* **macOS / Linux:**\n  ```bash\n  curl -fsSL https://install.julialang.org | sh\n  ```\n* **Windows:** Install via the Microsoft Store (`Julia`) or `winget install julia -s msstore`.\n\n`juliaup` automatically keeps your installation up to date with the latest stable release while making it easy to switch to LTS or specific versions when needed. Manual binaries and checksums are available on the [official Julia Downloads page](https://julialang.org/downloads/).","title":"Hosted tools (Gemini)"},{"category":"section","location":"guide/agentic/#Constraining-tool-choice","page":"Agentic Workflows","text":"Force a specific function with tool_choice_function (works on both providers). The other builders — tool_choice_hosted, tool_choice_mcp, tool_choice_custom, tool_choice_allowed — are OpenAI-Responses selectors: on Gemini, respond throws ArgumentError for them before any request.\n\nrespond(\"What's the weather in Paris?\";\n        tools=[my_function_tool],\n        tool_choice=tool_choice_function(\"get_weather\"))","title":"Constraining tool choice"},{"category":"section","location":"guide/agentic/#Automated-tool-loop","page":"Agentic Workflows","text":"The tool_loop driver (see the Tool Calling guide) runs the call/execute/respond cycle automatically, and works across providers — add service= to target Gemini:\n\nct = CallableTool(function_tool(\"get_weather\", \"Get weather\",\n        parameters=Dict(\"type\" => \"object\",\n                        \"properties\" => Dict(\"location\" => Dict(\"type\" => \"string\")))),\n    (name, args) -> \"22C, sunny\")\nresult = tool_loop(\"What's the weather in Paris?\"; service=GEMINIServiceEndpoint, tools=[ct])\n# result.completed == true when the model returns a final text answer\n\nThe loop takes the same cancel and tool_concurrency keywords on both providers; see Tool Calling for its stop rules.","title":"Automated tool loop"},{"category":"section","location":"guide/agentic/#Feeding-tool-output-back-manually","page":"Agentic Workflows","text":"Use tool_result to return a function's output on the next turn:\n\nr2 = respond(Respond(; service=GEMINIServiceEndpoint,\n                     previous_response_id=r1.response.id,\n                     input=[tool_result(\"call_abc\", \"get_weather\", \"72F and sunny\")]))","title":"Feeding tool output back manually"},{"category":"section","location":"guide/agentic/#Lifecycle-(background-requests)","page":"Agentic Workflows","text":"get_response and cancel_response take a service= keyword, so background Gemini Interactions are managed the same way as OpenAI:\n\nstatus = get_response(\"<interaction_id>\"; service=GEMINIServiceEndpoint)\ncancel_response(\"<interaction_id>\"; service=GEMINIServiceEndpoint)\n\nNote: a freshly created background interaction id may briefly return a 403 permission_denied ResponseFailure before the resource becomes retrievable. Poll with a short delay before treating that as a real permission error. The automatic retry policy deliberately does not retry 403 (permission errors are normally permanent), so this is the caller's poll loop to handle.","title":"Lifecycle (background requests)"},{"category":"section","location":"guide/agentic/#Usage-and-cost-(cross-provider)","page":"Agentic Workflows","text":"token_usage and estimated_cost work for Gemini too — the Interactions decoder normalizes usage into the shared shape and DEFAULT_PRICING includes gemini-3.8-flash (hosted-tool per-call fees are not modeled).\n\nr = respond(\"What is 2+2?\"; service=GEMINIServiceEndpoint, model=\"gemini-3.8-flash\")\nif r isa ResponseSuccess\n    println(\"usage: \", token_usage(r))\n    println(\"est. cost: \\$\", round(estimated_cost(r); digits=6))\nelse\n    println(\"Request failed — \", r)\nend\nusage: TokenUsage(8, 101, 109, 0, 94)\nest. cost: $0.000385","title":"Usage & cost (cross-provider)"},{"category":"section","location":"guide/agentic/#Streaming","page":"Agentic Workflows","text":"Streamed Gemini Interactions assemble each step from the wire's incremental events and rebuild the output exactly as the non-streamed decode does: a streamed respond(...; tools=…, stream=true) finishes with status requires_action and its function_calls populated, like the non-streamed form. Thought steps carry their signature and their summary (from the streamed thought summaries), the text parts of one step stay separate parts (so output_text joins them with a newline), and a function call whose arguments are null reads as \"{}\". Initial text in step.start and subsequent deltas are both included, in step order. Thought summaries stay separate from answer text (reasoning_summaries reads them), while signatures remain available in the provider's raw steps.\n\nAn interaction body — streamed or not — without a non-empty id or status is not an interaction: respond, get_response and cancel_response return a ResponseCallError for it instead of an empty success.","title":"Streaming"},{"category":"section","location":"guide/agentic/#See-Also","page":"Agentic Workflows","text":"Responses API — OpenAI-specific respond/Respond details\nTool Calling — defining tools and the tool loop\nMulti-Backend — provider setup and capabilities","title":"See Also"},{"category":"section","location":"guide/natural_language_dispatch/#nl_dispatch_guide","page":"Multiple Dispatch on Natural Language","text":"Julia selects a method from the types of all the arguments, not just the first. With Jev one of those positions can hold a natural-language meaning instead of a data type: nl\"the customer wants a refund\" is an ordinary Julia type, so route(::nl\"the customer wants a refund\", ticket) is an ordinary method with an ordinary signature. nl_dispatch asks Jev which of the meanings declared across the method table fits the actual input, turns the answer back into a value, and lets Julia's own dispatch pick the method — on that meaning and on the types of every other argument at the same time. However many meaning slots the signature has, one call is one request: each slot rides along as an independent choice question over the same state. The model returns a calibrated choice over the options you declared and never returns text, so the decision arrives already typed, with a probability for every option and a confidence to gate on. It is cheap — only input tokens are billed (Models) — and a System One model is built to judge in one shot rather than to generate (System One). Contrast that with LLM tool calling, where a generative model writes a call into text that you prompt for and then parse, a malformed call costs a retry, and nothing in the reply tells you how close the runner-up was. Here the option list is the method table, the answer is one of its entries by construction, and a combination no method covers surfaces as Julia's own MethodError instead of a silent mis-route.","title":"Multiple Dispatch on Natural Language"},{"category":"section","location":"guide/natural_language_dispatch/#The-sixty-second-version","page":"Multiple Dispatch on Natural Language","text":"Write one method per meaning. meanings shows exactly what will be offered to the model, and needs no network at all:\n\nusing UniLM\n\nroute(::nl\"the customer wants a refund\", ticket)           = :refund\nroute(::nl\"the customer reports a bug in the app\", ticket) = :bug\nroute(::nl\"anything else\", ticket)                         = :other\n\nfor (position, options) in sort(collect(meanings(route)))\n    println(position, \" => \", options)\nend\n1 => [\"the customer wants a refund\", \"the customer reports a bug in the app\", \"anything else\"]\n\nThen dispatch on a real ticket. This is the only line that talks to the service:\n\nnl_dispatch(route, \"My package arrived crushed and the screen is cracked. I want my money back.\")\n# => :refund\n\nOne request went out, it carried one Choice question over the three meanings, and the winning meaning became the first argument of an ordinary Julia call.","title":"The sixty-second version"},{"category":"section","location":"guide/natural_language_dispatch/#How-it-works","page":"Multiple Dispatch on Natural Language","text":"Meaning is a type. Meaning{S} is a zero-field singleton whose only type parameter is a Symbol holding the description. The @nl_str macro writes the type, so it reads as one wherever a type is expected — in a signature, in a const alias, in an isa test. Append () for the instance, or build it from a runtime string with Meaning(text):\n\nconst Refund = nl\"the customer wants a refund\"    # a type alias\n(Refund, Refund(), Meaning(\"the customer wants a refund\") isa Refund)\n(nl\"the customer wants a refund\", nl\"the customer wants a refund\"(), true)\n\nThe text is interned as the type parameter, so two identical descriptions are the same type and === compares them. Nothing about the type reaches the network on its own; only nl_dispatch makes a call, and only to turn a piece of state into the instance to call with.\n\nWhat nl_dispatch collects. It reads methods(f) and keeps the methods that pin at least one positional argument to a concrete meaning. Those methods are sorted by source file and line, so within one file the option order is the order you wrote them in, and meanings previews it faithfully. A method with no concrete meaning anywhere in its signature — a wildcard f(::Meaning, x), or a method with no Meaning at all — is an ordinary method and is skipped. Every method that is collected must agree with the others on positional arity and on which positions are meaning slots, because one set of questions has to serve all of them; a disagreement is an ArgumentError naming both methods, raised before any request goes out. Varargs cannot carry a meaning.\n\nHow the request is built. One choice question per slot, all in a single ask:\n\nThe question name is the slot's argument name, or meaning_<position> when the slot is unnamed (::nl\"...\" with nothing in front of the ::). Question names key the answers and are never sent to the model.\nThe option names are the meanings themselves, with no description attached. There is no other text for the model to read, so the description you write in the signature is the prompt for that option.\nThe state is a JSON object keyed by the names of the remaining, ordinary arguments (arg<position> for an unnamed one). Pass state = ... to send something else instead; with no ordinary arguments at all there is nothing to describe, so state is then required.\nThe instructions default to a generic \"Select the option that best describes the provided state.\" — the options carry the semantics.\nThe model is the client default (jev-latest, or whatever TYPESAFE_DEFAULT_MODEL names) unless model = overrides it for this call.\n\nFor the sixty-second example above, that is exactly this body — captured off the wire by pointing the call at a local server instead of api.typesafe.ai:\n\n{\n  \"state\": {\n    \"ticket\": \"My package arrived crushed and the screen is cracked. I want my money back.\"\n  },\n  \"model\": \"jev-latest\",\n  \"questions\": {\n    \"meaning_1\": {\n      \"type\": \"choice\",\n      \"instructions\": \"Select the option that best describes the provided state.\",\n      \"criteria\": {\n        \"the customer wants a refund\": null,\n        \"the customer reports a bug in the app\": null,\n        \"anything else\": null\n      }\n    }\n  }\n}\n\nThe model sees the state, the instructions and the option names. It does not see the function's name, the method bodies, the return types, the question name meaning_1, or the fact that this is dispatch at all — as far as Jev is concerned this is one Choice question like any other (Choice).\n\nHow the answer comes back. Each answer is a ChoiceAnswer whose choice is one of the option names by construction. nl_dispatch turns it back into Meaning{Symbol(choice)}(), splices it into the position it came from, fills the remaining positions with your arguments in order, and makes a normal Julia call. From there nothing is special: the value it returns is whatever the selected method returns.","title":"How it works"},{"category":"section","location":"guide/natural_language_dispatch/#Several-meaning-slots,-one-request","page":"Multiple Dispatch on Natural Language","text":"A signature may pin more than one position. Four methods over two slots enumerate four combinations:\n\nreply(::nl\"a complaint\", ::nl\"a calm tone\", msg)  = :apologise\nreply(::nl\"a complaint\", ::nl\"an angry tone\", msg) = :escalate\nreply(::nl\"a question\", ::nl\"a calm tone\", msg)   = :answer\nreply(::nl\"a question\", ::nl\"an angry tone\", msg) = :answer_and_apologise\n\nfor (position, options) in sort(collect(meanings(reply)))\n    println(position, \" => \", options)\nend\n1 => [\"a complaint\", \"a question\"]\n2 => [\"a calm tone\", \"an angry tone\"]\n\nnl_dispatch(reply, \"This is the third time I have written about my broken order.\")\n# one request, two questions; the resolved pair selects one of the four methods\n\nBoth slots resolve in one request. The two questions are independent — they are asked about the same state, and neither answer becomes context for the other — so the cost of a second slot is the tokens of its option list, not a second ingestion of the state. That is the same economics as batching questions into one ask (parallel questions).\n\nFour methods cover all four combinations here. When they do not, the resolved pair lands on a gap in the method table and Julia raises its own MethodError — nl_dispatch does not catch it, because a missing method is your bug and not a service failure. Two ways to close the gap:\n\n# 1. A wildcard method. `::Meaning` is not a concrete meaning, so this method is\n#    never collected and never offered to the model — it is a pure Julia\n#    backstop that ordinary dispatch reaches when nothing more specific matches.\nreply(::Meaning, ::Meaning, msg) = :unclassified\n\nreply(nl\"a rant\"(), nl\"a calm tone\"(), \"…\")   # no request; ordinary dispatch\n:unclassified\n\n# 2. An explicit catch-all meaning, which IS offered to the model, so the model\n#    can say \"none of these\" instead of forcing its probability onto an option\n#    that does not fit.\nreply(::nl\"anything else\", ::nl\"a calm tone\", msg)  = :triage\nreply(::nl\"anything else\", ::nl\"an angry tone\", msg) = :triage\n\nfor (position, options) in sort(collect(meanings(reply)))\n    println(position, \" => \", options)\nend\n1 => [\"a complaint\", \"a question\", \"anything else\"]\n2 => [\"a calm tone\", \"an angry tone\"]\n\nPrefer the second: a wildcard tells you nothing about why it fired, while a catch-all meaning comes back with a name and a probability. Note what the wildcard is not allowed to be — a method that pins some slots and leaves others wild, such as reply(::nl\"a complaint\", ::Meaning, msg), disagrees with the other methods about which positions are slots, and meanings and nl_dispatch both reject it with an ArgumentError rather than send a question list that does not match the method table. Wildcard every slot or none of them.","title":"Several meaning slots, one request"},{"category":"section","location":"guide/natural_language_dispatch/#Composing-with-ordinary-dispatch","page":"Multiple Dispatch on Natural Language","text":"A resolved meaning is an ordinary argument, so the remaining positions keep dispatching on their types exactly as they always did:\n\nhandle(::nl\"a greeting\", who::String) = \"hello, \" * who\nhandle(::nl\"a greeting\", n::Int)      = \"hello, customer #\" * string(n)\nhandle(::nl\"a complaint\", who::String) = \"sorry, \" * who\n\nprintln(meanings(handle))\nprintln(handle(nl\"a greeting\"(), \"ada\"))\nprintln(handle(nl\"a greeting\"(), 41))\nDict(1 => [\"a greeting\", \"a complaint\"])\nhello, ada\nhello, customer #41\n\nmeanings(handle) lists \"a greeting\" once even though two methods declare it: the options are the distinct descriptions per slot. The model picks the meaning; Julia picks between the String and the Int method afterwards, with no extra request.\n\nThis also makes the interface open. The option list is the method table, and a method table is extensible from anywhere — a downstream module or a package that does not own handle can add handle(::nl\"a refund request\", who::String), and from the next call on it is one more option the model may choose. Nothing central enumerates the meanings, so adding a case never means editing a list in two places.","title":"Composing with ordinary dispatch"},{"category":"section","location":"guide/natural_language_dispatch/#Confidence-and-control","page":"Multiple Dispatch on Natural Language","text":"A Choice answer always names a winner, even when the distribution is nearly flat, so the gate is confidence rather than choice. confidence measures how concentrated the distribution is — all the mass on one option gives 1.0, a flat spread gives a low number — and says nothing about whether the winner is correct (Confidence).\n\n# Below the threshold, `fallback` is called with the caller's own arguments.\nnl_dispatch(route, ticket; min_confidence = 0.7, fallback = t -> :needs_a_human)\n\n# With no fallback, the same answer is a loud failure instead of a route.\nnl_dispatch(route, ticket; min_confidence = 0.7)\n# => throws LowConfidenceError, whose `.answer` carries the full probabilities\n\nLowConfidenceError holds the whole ChoiceAnswer, so a handler can read probabilities for a diagnostic or a second-best policy. The other keywords:\n\nKeyword Meaning\nmin_confidence threshold in 0 … 1; below it, fallback runs or LowConfidenceError is thrown\nfallback called with the caller's args... when the gate trips\ninstructions a String for every slot, or a Vector with one entry per slot; default is the generic instruction above\nstate send this instead of the object built from the ordinary arguments\nmodel pin a version, e.g. \"jev-1.13.0\", instead of the jev-latest alias\nservice endpoint type; defaults to TYPESAFEServiceEndpoint\nconfig a RequestConfig governing timeouts and the retry budget for this call\ncancel a CancelToken that can cancel the call from another task; nothing (the default) uses the ambient with_cancel token. A cancelled call throws SystemOneError like any failed call\n\nPin the model before you tune a threshold. A threshold is a claim about one version, and jev-latest moves when a new one ships (Models); SystemOneResponse.model reports the version that actually answered. And read meanings(f) first — a threshold that keeps tripping is usually an option list with two overlapping meanings in it, not a number that needs lowering.\n\nA call that failed outright throws SystemOneError rather than resolving to a method, so a timeout or a 500 can never be mistaken for a routing decision.","title":"Confidence and control"},{"category":"section","location":"guide/natural_language_dispatch/#@branch:-the-inline-form","page":"Multiple Dispatch on Natural Language","text":"When the decision is local and nobody needs to extend it, @branch is the same request without the method table. It is a switch whose cases are written in plain language:\n\nticket = \"My package arrived crushed and the screen is cracked. I want my money back.\"\n\naction = @branch ticket min_confidence=0.6 begin\n    \"the customer wants a refund\"                              => :refund\n    (\"the customer reports a bug\", \"A defect in the software\") => :bug\n    \"the customer asks a pricing question\"                     => :pricing\n    _                                                          => :escalate\nend\n# => :refund\n\nEvery line is option => expression:\n\nA bare left-hand side is the option name — the text the model reads and the text the answer returns. Left-hand sides are ordinary expressions, evaluated once in source order, so a name may be computed.\nThe 2-tuple form (\"name\", \"description\") attaches a longer description to that name. It is the only way to do so.\nA final _ => expression is the fallback taken when the winner's confidence is below min_confidence. It requires min_confidence: without a threshold nothing is ever low-confidence and the line could never run, so that spelling is rejected.\n\nKeywords go between the state and the block, written key = value: model, min_confidence, instructions, service, config and cancel, each meaning what it means for nl_dispatch.\n\nThe whole block compiles to a single Choice request whose question is named branch and whose criteria are the option names, in source order. Only the selected body is evaluated — the others are not merely discarded, they never run — and the macro's value is that body's value. Everything that can be checked statically is checked while the macro expands, so it surfaces when the surrounding code is loaded rather than the first time the branch is reached: an unknown keyword, a block that is not begin ... end, a line that is not option => expression, no options at all, two _ lines, or a _ that is not last. A non-success call throws SystemOneError; the branch is never guessed.\n\nWhich to reach for. @branch when the decision belongs to one call site and the arms are three lines of code — it keeps the options and their handling in one place, and it needs no function at all. nl_dispatch when the decision is an interface: the arms are real methods that can be tested and called directly, other modules can add cases, the ordinary arguments keep dispatching on their types, and meanings gives you the option list as data.","title":"@branch: the inline form"},{"category":"section","location":"guide/natural_language_dispatch/#Testing-without-the-API","page":"Multiple Dispatch on Natural Language","text":"The point of putting the meanings in the signature is that the methods stay ordinary. A unit test calls them directly, resolving nothing and spending nothing:\n\nt = \"My package arrived crushed and the screen is cracked. I want my money back.\"\n\n(route(nl\"the customer wants a refund\"(), t),\n route(nl\"the customer reports a bug in the app\"(), t),\n route(Meaning(\"anything else\"), t))\n(:refund, :bug, :other)\n\nThat covers the method bodies. To cover the resolution — question names, option order, the state that goes on the wire, the confidence gate — point TYPESAFE_BASE_URL at a local server and answer the Choice yourself:\n\nusing UniLM, HTTP, JSON\n\ncanned = \"\"\"\n{\"model\": \"jev-1.13.0\",\n \"answers\": {\"meaning_1\": {\"type\": \"choice\",\n                           \"choice\": \"the customer wants a refund\",\n                           \"confidence\": 0.97,\n                           \"probabilities\": {\"the customer wants a refund\": 0.97,\n                                             \"the customer reports a bug in the app\": 0.02,\n                                             \"anything else\": 0.01}}},\n \"usage\": {\"input_tokens\": 96, \"output_tokens\": 3}}\n\"\"\"\n\nseen = String[]\nserver = HTTP.serve!(\"127.0.0.1\", 8123; verbose=false) do request\n    push!(seen, String(copy(request.body)))          # assert on the request you sent\n    HTTP.Response(200, [\"Content-Type\" => \"application/json\"], Vector{UInt8}(canned))\nend\n\nwithenv(\"TYPESAFE_BASE_URL\" => \"http://127.0.0.1:8123\", \"TYPESAFE_API_KEY\" => \"test\") do\n    @assert nl_dispatch(route, t) === :refund\nend\nclose(server)\n\nThe response shape is the whole contract: a model, an answers object keyed by the question names the client sent (meaning_1 here, or the slot's argument name when it has one), each answer a Choice with choice, confidence and probabilities, plus a usage object. The numbers are yours to pick — that is the point of a canned answer. Make confidence low to exercise min_confidence, fallback and LowConfidenceError; return a non-200 to exercise SystemOneError; answer with a meaning whose combination no method covers to exercise the MethodError.","title":"Testing without the API"},{"category":"section","location":"guide/natural_language_dispatch/#Designing-meanings","page":"Multiple Dispatch on Natural Language","text":"Write the meaning as a description, not a label. It is the option name and it is the only text the model gets for that branch. nl\"the customer wants a refund\" works; nl\"refund\" asks the model to guess what you meant by the word.\nKeep them mutually exclusive. Two meanings that overlap split the probability between them, which shows up as low confidence rather than as a wrong answer — check meanings(f) and read the list as the model will.\nAlways include an \"anything else\". Without one, an input outside the enumeration has nowhere to put its probability except onto a meaning that does not fit.\nKeep the state to what matters. Accuracy falls as unrelated material grows around the decision, so filter and retrieve in code and pass only the fields the decision needs — or pass state = ... explicitly.\nThe model reads literally and does no arithmetic. Negations, scoping words and implied conditions are taken at face value, and counting, numeric magnitude and date ordering are unreliable — keep those in Julia and let the meanings carry only the semantic judgment (Jev 1.13 jaggedness).\nMethods are free; requests are not. Ten methods and three methods cost the same single request, and the extra cost of a meaning is the tokens of its own description. Only input tokens are billed (Models).\n255 meanings per slot is the ceiling. That is the server's Choice limit, and choice checks it locally before the round trip (Choice).","title":"Designing meanings"},{"category":"section","location":"guide/natural_language_dispatch/#See-also","page":"Multiple Dispatch on Natural Language","text":"Typed Judgments with Jev (TypeSafe System One) — the ask verb, the three primitives, and the setup this page assumes\nTypeSafe System One API (Jev) — every type and verb, including Meaning, @branch and nl_dispatch\nTypeSafe documentation — the service's own reference for Choice, confidence and models","title":"See also"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"","title":"Getting Started"},{"category":"section","location":"getting_started/#Prerequisites","page":"Getting Started","text":"Julia 1.13+ (as specified in Project.toml)\nAn API key for your chosen provider (OpenAI, DeepSeek, Gemini, Mistral, etc.) — or none at all for local providers like Ollama","title":"Prerequisites"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"UniLM is registered in Julia's General registry:\n\nusing Pkg\nPkg.add(\"UniLM\")\n\nOr from the Pkg REPL:\n\npkg> add UniLM\n\nTo track the latest unreleased changes, install from GitHub instead:\n\nPkg.add(url=\"https://github.com/algunion/UniLM.jl\")","title":"Installation"},{"category":"section","location":"getting_started/#Configuration","page":"Getting Started","text":"UniLM.jl reads API credentials from environment variables. Set them before making any requests:","title":"Configuration"},{"category":"section","location":"getting_started/#OpenAI-(default)","page":"Getting Started","text":"ENV[\"OPENAI_API_KEY\"] = \"sk-...\"\n\nOr via your shell:\n\nexport OPENAI_API_KEY=\"sk-...\"","title":"OpenAI (default)"},{"category":"section","location":"getting_started/#Azure-OpenAI","page":"Getting Started","text":"export AZURE_OPENAI_BASE_URL=\"https://your-resource.openai.azure.com\"\nexport AZURE_OPENAI_API_KEY=\"your-key\"\nexport AZURE_OPENAI_API_VERSION=\"2024-10-21\"\nexport AZURE_OPENAI_DEPLOY_NAME_GPT_5_2=\"your-gpt52-deployment\"\n\nEach model needs its deployment: AZURE_OPENAI_DEPLOY_NAME_<MODEL>, the model id upper-cased with every other character mapped to _ (gpt-5.2 → AZURE_OPENAI_DEPLOY_NAME_GPT_5_2), read when a request is built — or register one with add_azure_deploy_name!.","title":"Azure OpenAI"},{"category":"section","location":"getting_started/#Google-Gemini","page":"Getting Started","text":"Native generateContent API (default model gemini-3.8-flash):\n\nexport GEMINI_API_KEY=\"your-gemini-key\"","title":"Google Gemini"},{"category":"section","location":"getting_started/#Anthropic-(Claude)","page":"Getting Started","text":"Native Messages API (default model claude-opus-5-5, default max_tokens 16000):\n\nexport ANTHROPIC_API_KEY=\"sk-ant-...\"","title":"Anthropic (Claude)"},{"category":"section","location":"getting_started/#DeepSeek","page":"Getting Started","text":"Default model deepseek-flash (chat and FIM):\n\nexport DEEPSEEK_API_KEY=\"sk-...\"","title":"DeepSeek"},{"category":"section","location":"getting_started/#TypeSafe-System-One-(Jev)","page":"Getting Started","text":"Typed judgments and calibrated probabilities instead of generated text — routing, screening, ranking and verification that your code can act on directly:\n\nexport TYPESAFE_API_KEY=\"...\"\n\nOptional: TYPESAFE_BASE_URL overrides the API root (default https://api.typesafe.ai) and TYPESAFE_DEFAULT_MODEL the model used when a call names none (default jev-latest). See Typed Judgments with Jev.","title":"TypeSafe System One (Jev)"},{"category":"section","location":"getting_started/#Ollama-(local-—-no-key-needed)","page":"Getting Started","text":"Just have the Ollama server running on localhost:11434. No API key required.\n\ntip: Try it free, locally\nHosted API calls cost money and need a funded key. To experiment with zero cost and no signup, run a local model with Ollama — set service=OllamaEndpoint() (no key required), as configured above.","title":"Ollama (local — no key needed)"},{"category":"section","location":"getting_started/#Your-First-Request","page":"Getting Started","text":"","title":"Your First Request"},{"category":"section","location":"getting_started/#Using-the-Responses-API","page":"Getting Started","text":"The simplest way to get started — one function call:\n\nresult = respond(\"Explain Julia's type system in 3 bullet points\", model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\n- **Types are mostly about values and dispatch, not rigid class hierarchies.** Julia is dynamically typed, but every value has a type, and functions are specialized on argument types at runtime. This enables **multiple dispatch**, where the method chosen depends on the types of all arguments, not just one object.\n\n- **The type system is parametric and compositional.** You can define generic types like `Vector{Int}` or `Dict{String, Float64}` using type parameters, and create your own abstract and concrete types with `abstract type` and `struct`. This lets you build reusable abstractions without losing performance.\n\n- **Types form a hierarchy with unions and subtyping, but they’re not always required for programming.** `Any` is the top type, `Union{...}` can represent several possible types, and the compiler often infers types automatically. In practice, you usually write generic code and only add type annotations when they help clarity or performance.","title":"Using the Responses API"},{"category":"section","location":"getting_started/#Using-Chat-Completions","page":"Getting Started","text":"For stateful, multi-turn conversations:\n\nchat = Chat(model=\"gpt-5.4-mini\")\npush!(chat, Message(Val(:system), \"You are a concise Julia programming tutor.\"))\npush!(chat, Message(Val(:user), \"What is multiple dispatch? Answer in 2-3 sentences.\"))\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(result.message.content)\n    println(\"\\nFinish reason: \", result.message.finish_reason)\n    println(\"Conversation length: \", length(chat))\nelse\n    println(\"Request failed — see result for details\")\nend\nMultiple dispatch is a programming style where a function chooses which method to run based on the types of all its arguments, not just one. In Julia, this lets you define different behaviors for the same function name across combinations of input types, making code flexible and expressive.\n\nFinish reason: stop\nConversation length: 3","title":"Using Chat Completions"},{"category":"section","location":"getting_started/#Generating-Images","page":"Getting Started","text":"result = generate_image(\n    \"A watercolor painting of a friendly robot reading a Julia programming book\",\n    size=\"1024x1024\", quality=\"medium\"\n)\nif result isa ImageSuccess\n    println(\"Success: true\")\n    println(\"Images: \", length(image_data(result)))\nelse\n    println(\"Success: false\")\n    println(\"Images: 0\")\nend","title":"Generating Images"},{"category":"section","location":"getting_started/#Using-Keyword-Arguments","page":"Getting Started","text":"For one-shot requests without managing Chat objects:\n\nresult = chatrequest!(\n    systemprompt=\"You are a calculator. Respond only with the number.\",\n    userprompt=\"What is 42 * 17?\",\n    model=\"gpt-5.4-mini\",\n    temperature=0.0\n)\nif result isa LLMSuccess\n    println(result.message.content)\nelse\n    println(\"Request failed — see result for details\")\nend\n714","title":"Using Keyword Arguments"},{"category":"section","location":"getting_started/#Handling-Results","page":"Getting Started","text":"All API calls return subtypes of LLMRequestResponse. Use Julia's pattern matching:\n\nusing UniLM\nusing InteractiveUtils\n\n# Construct a chat to show the result type hierarchy\nchat = Chat(model=\"gpt-5.4-mini\")\npush!(chat, Message(Val(:system), \"You are helpful.\"))\npush!(chat, Message(Val(:user), \"Hello!\"))\n\n# Show the type hierarchy:\nprintln(\"LLMRequestResponse subtypes:\")\nfor T in subtypes(UniLM.LLMRequestResponse)\n    println(\"  \", T)\nend\nLLMRequestResponse subtypes:\n  AudioCallError\n  AudioFailure\n  BatchCallError\n  BatchFailure\n  BatchListSuccess\n  BatchSuccess\n  ContainerCallError\n  ContainerDeleteSuccess\n  ContainerFailure\n  ContainerListSuccess\n  ContainerSuccess\n  ConversationCallError\n  ConversationDeleteSuccess\n  ConversationFailure\n  ConversationItemListSuccess\n  ConversationItemSuccess\n  ConversationSuccess\n  EmbeddingCallError\n  EmbeddingFailure\n  EmbeddingSuccess\n  FIMCallError\n  FIMFailure\n  FIMSuccess\n  FileCallError\n  FileContentSuccess\n  FileDeleteSuccess\n  FileFailure\n  FileListSuccess\n  FileSuccess\n  FineTuningCallError\n  FineTuningFailure\n  FineTuningListSuccess\n  FineTuningSuccess\n  ImageCallError\n  ImageFailure\n  ImageSuccess\n  LLMCallError\n  LLMFailure\n  LLMSuccess\n  ModerationCallError\n  ModerationFailure\n  ModerationSuccess\n  RealtimeCallError\n  RealtimeFailure\n  RealtimeSecretSuccess\n  ResponseCallError\n  ResponseFailure\n  ResponseSuccess\n  SpeechSuccess\n  SystemOneCallError\n  SystemOneFailure\n  SystemOneSuccess\n  TranscriptionSuccess\n  TypeSafeModelsSuccess\n  UploadCallError\n  UploadFailure\n  UploadPartSuccess\n  UploadSuccess\n  VectorStoreBatchSuccess\n  VectorStoreCallError\n  VectorStoreDeleteSuccess\n  VectorStoreFailure\n  VectorStoreFileSuccess\n  VectorStoreListSuccess\n  VectorStoreSuccess\n\nresult = chatrequest!(chat)\n\nif result isa LLMSuccess\n    println(\"Assistant: \", result.message.content)\n    println(\"Finish reason: \", result.message.finish_reason)\nelseif result isa LLMFailure\n    @warn \"API returned HTTP $(result.status): $(result.response)\"\nelseif result isa LLMCallError\n    @error \"Call failed: $(result.error)\"\nend\nAssistant: Hello! How can I help you today?\nFinish reason: stop\n\nFor the Responses API:\n\nresult = respond(\"Hello!\", model=\"gpt-5.4-mini\")\n\nif result isa ResponseSuccess\n    println(output_text(result))\n    println(\"Status: \", result.response.status)\n    println(\"Model: \", result.response.model)\nelseif result isa ResponseFailure\n    @warn \"HTTP $(result.status)\"\nelseif result isa ResponseCallError\n    @error result.error\nend\nHello! How can I help you today?\nStatus: completed\nModel: gpt-5.4-mini-2026-03-17","title":"Handling Results"},{"category":"section","location":"getting_started/#What's-Next?","page":"Getting Started","text":"Want to... Read...\nBuild multi-turn conversations Chat Completions Guide\nUse the newer Responses API Responses API Guide\nGenerate images from prompts Image Generation Guide\nCall functions from the model Tool Calling Guide\nStream tokens in real-time Streaming Guide\nGet structured JSON output Structured Output Guide\nUse any provider Multi-Backend Guide\nTrack token usage & cost Cost Tracking Guide\nGround answers in your files Retrieval & File Search\nBound timeouts and retries Timeouts & Retries\nFan out, stream to tasks, cancel Concurrency, Tasks and Cancellation","title":"What's Next?"},{"category":"section","location":"api/audio/#audio_api","page":"Audio","text":"Synthesize speech from text, transcribe audio to text, and translate audio into English. Text-to-speech returns raw audio bytes (mp3, wav, and friends); transcription and translation upload an audio file and return text. OpenAI only.","title":"Audio API"},{"category":"section","location":"api/audio/#Request-Types","page":"Audio","text":"","title":"Request Types"},{"category":"section","location":"api/audio/#Result-Types","page":"Audio","text":"","title":"Result Types"},{"category":"section","location":"api/audio/#Functions","page":"Audio","text":"","title":"Functions"},{"category":"section","location":"api/audio/#Accessors","page":"Audio","text":"","title":"Accessors"},{"category":"section","location":"api/audio/#Usage","page":"Audio","text":"# Text-to-speech: synthesize and save to disk\nresult = speak(\"Hello from UniLM.\", voice=\"alloy\")\nresult isa SpeechSuccess && save_audio(result, \"hello.mp3\")\n\n# Transcribe audio to text in its source language\nt = transcribe(\"hello.mp3\"; languages=[\"en\"], keywords=[\"UniLM\", \"Julia\"])\nt isa TranscriptionSuccess && println(transcript_text(t))\n\n# Translate foreign-language audio into English\ntranslate(\"bonjour.mp3\")\n\nTranscription defaults to gpt-transcribe. Its languages and keywords options are vectors of strings sent as repeated multipart fields. A singular language is translated to a one-element languages list for this model; older models keep their singular field. Setting both forms raises ArgumentError.\n\nOpenAI deprecated gpt-4o-transcribe, gpt-4o-mini-transcribe, gpt-4o-transcribe-diarize, and whisper-1 on August 26, 2026, with shutdown scheduled for February 26, 2027. Translation still defaults to whisper-1 because it remains the documented model for /audio/translations; gpt-transcribe is for transcription. OpenAI transcription guide, deprecation schedule.","title":"Usage"},{"category":"type","location":"api/audio/#UniLM.SpeechRequest","page":"Audio","text":"SpeechRequest(; input, voice=\"alloy\", model=\"gpt-4o-mini-tts\", service=OPENAIServiceEndpoint)\n\nA text-to-speech request. input is the text to synthesize; voice selects the speaker; optional response_format (mp3|opus|aac|flac|wav|pcm), speed (0.25 to 4.0; outside that range an ArgumentError), and instructions tune the output. Pass to speak.\n\n\n\n\n\n","title":"UniLM.SpeechRequest"},{"category":"type","location":"api/audio/#UniLM.TranscriptionRequest","page":"Audio","text":"TranscriptionRequest(; file, model=\"gpt-transcribe\", service=OPENAIServiceEndpoint)\n\nAn audio transcription/translation request. file is a path on disk; optional languages, keywords, prompt, response_format, and temperature refine decoding. For gpt-transcribe, a legacy singular language is translated to languages. Other models retain their singular language field. Never set both forms. response_format is one of json, text, srt, verbose_json, vtt, diarized_json, and temperature lies in 0 … 1; anything else is an ArgumentError. Pass to transcribe or translate.\n\n\n\n\n\n","title":"UniLM.TranscriptionRequest"},{"category":"type","location":"api/audio/#UniLM.SpeechSuccess","page":"Audio","text":"Successful speak result; audio holds the raw audio bytes, content_type the MIME type. Save with save_audio.\n\n\n\n\n\n","title":"UniLM.SpeechSuccess"},{"category":"type","location":"api/audio/#UniLM.TranscriptionSuccess","page":"Audio","text":"Successful transcribe/translate result; text holds the transcript (via transcript_text), raw the parsed JSON when the API returns it.\n\n\n\n\n\n","title":"UniLM.TranscriptionSuccess"},{"category":"type","location":"api/audio/#UniLM.AudioFailure","page":"Audio","text":"Audio API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.AudioFailure"},{"category":"type","location":"api/audio/#UniLM.AudioCallError","page":"Audio","text":"Audio API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout.\n\n\n\n\n\n","title":"UniLM.AudioCallError"},{"category":"function","location":"api/audio/#UniLM.speak","page":"Audio","text":"speak(s::SpeechRequest) -> LLMRequestResponse\nspeak(input; voice=\"alloy\", model=\"gpt-4o-mini-tts\", service=OPENAIServiceEndpoint, kwargs...)\n\nSynthesize speech. On success returns SpeechSuccess with raw audio bytes; otherwise AudioFailure/AudioCallError. Use save_audio to write the bytes to disk.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.speak"},{"category":"function","location":"api/audio/#UniLM.transcribe","page":"Audio","text":"transcribe(t::TranscriptionRequest) / transcribe(path; model=\"gpt-transcribe\", kwargs...)\n\nTranscribe audio to text in the source language. Returns TranscriptionSuccess (.text, via transcript_text), AudioFailure, or AudioCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.transcribe"},{"category":"function","location":"api/audio/#UniLM.translate","page":"Audio","text":"translate(t::TranscriptionRequest) / translate(path; model=\"whisper-1\", kwargs...)\n\nTranslate audio into English text. The translations endpoint takes no languages or keywords hints and has no diarized_json output, so a request carrying any of them throws ArgumentError before any network I/O.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.translate"},{"category":"function","location":"api/audio/#UniLM.transcript_text","page":"Audio","text":"transcript_text(r::TranscriptionSuccess) -> String\n\nThe transcript text from a transcribe or translate result.\n\n\n\n\n\n","title":"UniLM.transcript_text"},{"category":"function","location":"api/audio/#UniLM.save_audio","page":"Audio","text":"save_audio(r::SpeechSuccess, path) -> path\n\nWrite the synthesized audio bytes to path, atomically: the bytes go to a temporary file in the same directory, which is then renamed over path, so a failed write leaves any existing file intact. An existing file keeps its permission bits. A symlink is written through — a dangling one creates its target; a directory throws ArgumentError.\n\n\n\n\n\n","title":"UniLM.save_audio"},{"category":"section","location":"api/files/#files_api","page":"Files","text":"Upload, list, retrieve, and delete files, and download their content. Files feed the Responses file_search and code-interpreter tools, the Batch API, and fine-tuning. OpenAI only.","title":"Files API"},{"category":"section","location":"api/files/#Request-Type","page":"Files","text":"","title":"Request Type"},{"category":"section","location":"api/files/#Parsed-Objects","page":"Files","text":"","title":"Parsed Objects"},{"category":"section","location":"api/files/#Result-Types","page":"Files","text":"","title":"Result Types"},{"category":"section","location":"api/files/#Request-Functions","page":"Files","text":"","title":"Request Functions"},{"category":"section","location":"api/files/#Usage","page":"Files","text":"# Upload a file for use with file_search / batch / fine-tuning\nresult = upload_file(\"data.jsonl\", \"batch\")\nresult isa FileSuccess && println(\"Uploaded: \", result.response.id)\n\n# List, download, delete\nfiles = list_files(purpose=\"batch\")\ncontent = file_content(\"file-abc123\")\ncontent isa FileContentSuccess && save_file_content(content, \"out.jsonl\")\ndelete_file(\"file-abc123\")","title":"Usage"},{"category":"type","location":"api/files/#UniLM.FileUpload","page":"Files","text":"FileUpload(; file, purpose, service=OPENAIServiceEndpoint)\n\nA file-upload request. file is a path on disk; purpose is one of \"assistants\", \"batch\", \"fine-tune\", \"vision\", \"user_data\", \"evals\".\n\n\n\n\n\n","title":"UniLM.FileUpload"},{"category":"type","location":"api/files/#UniLM.FileObject","page":"Files","text":"FileObject\n\nA file stored by the Files API: id, bytes, created_at, filename, purpose, and status; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.FileObject"},{"category":"type","location":"api/files/#UniLM.FileList","page":"Files","text":"FileList\n\nA page of FileObjects from list_files; has_more signals that further pages are available.\n\n\n\n\n\n","title":"UniLM.FileList"},{"category":"type","location":"api/files/#UniLM.FileSuccess","page":"Files","text":"Successful upload/retrieve result wrapping a FileObject.\n\n\n\n\n\n","title":"UniLM.FileSuccess"},{"category":"type","location":"api/files/#UniLM.FileListSuccess","page":"Files","text":"Successful list_files result wrapping a FileList.\n\n\n\n\n\n","title":"UniLM.FileListSuccess"},{"category":"type","location":"api/files/#UniLM.FileContentSuccess","page":"Files","text":"Successful file_content result; content holds the raw file bytes.\n\n\n\n\n\n","title":"UniLM.FileContentSuccess"},{"category":"type","location":"api/files/#UniLM.FileDeleteSuccess","page":"Files","text":"Successful delete_file result: the service confirmed (deleted is always true) the removal of id.\n\n\n\n\n\n","title":"UniLM.FileDeleteSuccess"},{"category":"type","location":"api/files/#UniLM.FileFailure","page":"Files","text":"Files API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.FileFailure"},{"category":"type","location":"api/files/#UniLM.FileCallError","page":"Files","text":"Files API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout.\n\n\n\n\n\n","title":"UniLM.FileCallError"},{"category":"function","location":"api/files/#UniLM.upload_file","page":"Files","text":"upload_file(path, purpose; service=OPENAIServiceEndpoint) -> LLMRequestResponse\nupload_file(u::FileUpload) -> LLMRequestResponse\n\nUpload a file (multipart/form-data). Returns FileSuccess, FileFailure, or FileCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply). Like every other create, an upload is never retried: a POST that timed out, or drew a gateway 5xx after the backend stored the file, may still have created it, and a second attempt would store it twice. A FileFailure/FileCallError therefore does not prove that no file was created.\n\n\n\n\n\n","title":"UniLM.upload_file"},{"category":"function","location":"api/files/#UniLM.list_files","page":"Files","text":"list_files(; purpose=nothing, limit=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nList uploaded files. Returns FileListSuccess, FileFailure, or FileCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_files"},{"category":"function","location":"api/files/#UniLM.retrieve_file","page":"Files","text":"retrieve_file(file_id; service=OPENAIServiceEndpoint)\n\nRetrieve a file's metadata. Returns FileSuccess, FileFailure, or FileCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.retrieve_file"},{"category":"function","location":"api/files/#UniLM.delete_file","page":"Files","text":"delete_file(file_id; service=OPENAIServiceEndpoint)\n\nDelete a file. Returns FileDeleteSuccess, FileFailure, or FileCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.delete_file"},{"category":"function","location":"api/files/#UniLM.file_content","page":"Files","text":"file_content(file_id; service=OPENAIServiceEndpoint)\n\nDownload a file's raw bytes. Returns FileContentSuccess (.content::Vector{UInt8}), FileFailure, or FileCallError. Use save_file_content to write to disk.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.file_content"},{"category":"function","location":"api/files/#UniLM.save_file_content","page":"Files","text":"save_file_content(r::FileContentSuccess, path) -> path\n\nWrite downloaded file bytes to path, atomically: the bytes go to a temporary file in the same directory, which is then renamed over path, so a failed write leaves any existing file intact. An existing file keeps its permission bits. A symlink is written through — a dangling one creates its target; a directory throws ArgumentError.\n\n\n\n\n\n","title":"UniLM.save_file_content"},{"category":"section","location":"guide/image_generation/#images_guide","page":"Image Generation","text":"UniLM.jl supports image generation via the OpenAI Images API using models like gpt-image-2.","title":"Image Generation"},{"category":"section","location":"guide/image_generation/#Basic-Usage","page":"Image Generation","text":"result = generate_image(\n    \"A watercolor painting of a friendly robot reading a Julia programming book\",\n    size=\"1024x1024\",\n    quality=\"medium\"\n)\nprintln(\"Success: \", result isa ImageSuccess)\nif result isa ImageSuccess\n    imgs = image_data(result)\n    println(\"Images: \", length(imgs))\n    println(\"Base64 length: \", length(imgs[1]))\n    save_image(imgs[1], joinpath(@__DIR__, \"..\", \"assets\", \"generated_robot.png\"))\n    println(\"Image saved to assets/generated_robot.png\")\nelse\n    println(\"Images: 0\")\n    println(\"Image generation failed — see result for details\")\nend\n\n(Image: Generated image: A watercolor painting of a friendly robot reading a Julia programming book)","title":"Basic Usage"},{"category":"section","location":"guide/image_generation/#The-ImageGeneration-Type","page":"Image Generation","text":"For full control, construct an ImageGeneration object:\n\nig = ImageGeneration(\n    prompt=\"A minimalist logo for a Julia programming package\",\n    model=\"gpt-image-2\",\n    size=\"1024x1024\",\n    quality=\"high\",\n    background=\"transparent\",\n    output_format=\"png\"\n)\nprintln(\"Model: \", ig.model)\nprintln(\"Size: \", ig.size)\nprintln(\"Quality: \", ig.quality)\nprintln(\"\\nRequest JSON:\")\nprintln(JSON.json(ig))\nModel: gpt-image-2\nSize: 1024x1024\nQuality: high\n\nRequest JSON:\n{\"background\":\"transparent\",\"model\":\"gpt-image-2\",\"output_format\":\"png\",\"prompt\":\"A minimalist logo for a Julia programming package\",\"quality\":\"high\",\"size\":\"1024x1024\"}","title":"The ImageGeneration Type"},{"category":"section","location":"guide/image_generation/#Configuration-Options","page":"Image Generation","text":"Parameter Values Default\nmodel \"gpt-image-2\" \"\" (see below)\nsize \"1024x1024\", \"1536x1024\", \"1024x1536\", \"auto\"; on gpt-image-2 and later any WIDTHxHEIGHT with multiples of 16, up to 3840x2160 API default\nquality \"low\", \"medium\", \"high\", \"auto\"; \"xhigh\", \"max\" on gpt-image-2.5-flare / gpt-image-2.5-sunburst API default\nbackground \"transparent\", \"opaque\", \"auto\" API default\noutput_format \"png\", \"webp\", \"jpeg\" API default\noutput_compression 0–100 (for webp/jpeg) API default\nn 1–10 API default (1)\nmoderation \"auto\", \"low\" API default\n\ninput_fidelity is an image-edit parameter and belongs on ImageEdit; ImageGeneration does not accept it.\n\nnote: `model` is a sentinel, not a resolved default\nUnlike Chat, ImageGeneration does not resolve its model at construction. The field holds \"\" and resolves only when the request is serialized — to \"gpt-image-2\" for OpenAI — so ImageGeneration(prompt=\"…\").model reads back as the empty string. Pass model= explicitly if you need to read it, or inspect JSON.json(ig) to see what will go on the wire. A service with no default image model throws ArgumentError at serialization.","title":"Configuration Options"},{"category":"section","location":"guide/image_generation/#Multiple-Images","page":"Image Generation","text":"Generate multiple images in a single request:\n\nresult = generate_image(\"A cute robot learning to program\", n=3, size=\"1024x1024\")\n\nif result isa ImageSuccess\n    imgs = image_data(result)\n    for (i, img) in enumerate(imgs)\n        save_image(img, \"robot_$i.png\")\n    end\nend","title":"Multiple Images"},{"category":"section","location":"guide/image_generation/#Transparent-Backgrounds","page":"Image Generation","text":"Perfect for logos and icons:\n\nresult = generate_image(\n    \"A simple geometric icon of a butterfly\",\n    background=\"transparent\",\n    output_format=\"png\",\n    quality=\"high\"\n)\n\nif result isa ImageSuccess\n    save_image(image_data(result)[1], \"butterfly_icon.png\")\n    # => PNG with transparent background\nend","title":"Transparent Backgrounds"},{"category":"section","location":"guide/image_generation/#Result-Structure","page":"Image Generation","text":"# Show the type hierarchy for image results\nprintln(\"ImageSuccess <: \", supertype(ImageSuccess))\nprintln(\"ImageFailure <: \", supertype(ImageFailure))\nprintln(\"ImageCallError <: \", supertype(ImageCallError))\nImageSuccess <: LLMRequestResponse\nImageFailure <: LLMRequestResponse\nImageCallError <: LLMRequestResponse\n\nresult = generate_image(\"A sunset over mountains\")\n\nif result isa ImageSuccess\n    r = result.response\n\n    r.created                 # Unix timestamp\n    r.data                    # Vector{ImageObject}\n    r.data[1].b64_json        # base64-encoded image data (nothing when delivered by URL)\n    r.data[1].url             # the image URL, when the API delivered it that way\n    r.data[1].revised_prompt  # revised prompt (may be nothing)\n    r.usage                   # token usage Dict (token_usage(result) reads it)\n\n    # Convenience accessors\n    image_data(result)        # Vector{String}: each image's base64 data, else its URL\n    save_image(image_data(result)[1], \"sunset.png\")\nend","title":"Result Structure"},{"category":"section","location":"guide/image_generation/#Saving-Images","page":"Image Generation","text":"The save_image helper decodes base64 and writes to disk:\n\n# Demonstrate save_image with a tiny test payload\ntmpfile = tempname() * \".txt\"\nUniLM.save_image(\"aGVsbG8=\", tmpfile)  # \"hello\" in base64\nprintln(\"File saved to: \", basename(tmpfile))\nprintln(\"Contents: \", read(tmpfile, String))\nrm(tmpfile)\nFile saved to: jl_32Fana3U7C.txt\nContents: hello","title":"Saving Images"},{"category":"section","location":"guide/image_generation/#Error-Handling","page":"Image Generation","text":"image_data throws LLMResultError on an ImageFailure or ImageCallError — a failed call has no images — so branch on the result type first:\n\nresult = generate_image(\"A sunset over mountains\")\n\nif result isa ImageSuccess\n    save_image(image_data(result)[1], \"sunset.png\")\nelseif result isa ImageFailure\n    @warn \"HTTP $(result.status): $(result.response)\"\nelseif result isa ImageCallError\n    @error \"Call failed: $(result.error)\"\nend","title":"Error Handling"},{"category":"section","location":"guide/image_generation/#Editing-Images","page":"Image Generation","text":"edit_image edits an existing image from a text prompt, optionally masked to an inpainting region. The convenience form takes the source image (a file path, or a vector of paths) and the prompt; an optional mask path restricts edits to the mask's transparent area:\n\n# Inpaint: change only the masked region, described by the prompt\nresult = edit_image(\"room.png\", \"Add a large window with a sea view\"; mask=\"room_mask.png\")\n\nif result isa ImageSuccess\n    save_image(image_data(result)[1], \"room_edited.png\")\nelse\n    println(\"Edit failed — see result for details\")\nend\n\nFor full control, build an ImageEdit and call edit_image(e). The model defaults to gpt-image-2; image editing needs the :image_edits capability (OpenAI): an endpoint that declares its capabilities without it throws ArgumentError before any request.","title":"Editing Images"},{"category":"section","location":"guide/image_generation/#Retry-Behaviour","page":"Image Generation","text":"generate_image and edit_image retry transient HTTP statuses (408, 429, 500, 502, 503, 504, 529) within the request budget configured by RequestConfig (max_attempts, default 3), with exponential backoff and jitter; a Retry-After header is a floor under the jittered wait. Override per call with config=RequestConfig(...). Both take cancel= (default: the ambient with_cancel token); a cancelled call returns an ImageCallError whose cause is a UniLMCancelled. Every ImageFailure and ImageCallError carries the provider's request_id when one was sent, and a call error its exception in cause.","title":"Retry Behaviour"},{"category":"section","location":"guide/image_generation/#See-Also","page":"Image Generation","text":"ImageGeneration — request configuration type\nImageResponse — response type\ngenerate_image — request function\nimage_data, save_image — accessor/utility functions","title":"See Also"},{"category":"section","location":"api/vector_stores/#vector_stores_api","page":"Vector Stores","text":"Create and manage vector stores, along with their files and file batches, that power the Responses file_search tool. Upload files via the Files API first. OpenAI only.","title":"Vector Stores API"},{"category":"section","location":"api/vector_stores/#Objects","page":"Vector Stores","text":"","title":"Objects"},{"category":"section","location":"api/vector_stores/#Result-Types","page":"Vector Stores","text":"","title":"Result Types"},{"category":"section","location":"api/vector_stores/#Request-Functions","page":"Vector Stores","text":"","title":"Request Functions"},{"category":"section","location":"api/vector_stores/#Accessors","page":"Vector Stores","text":"","title":"Accessors"},{"category":"section","location":"api/vector_stores/#Usage","page":"Vector Stores","text":"# Create a vector store, then add files already uploaded via the Files API\nstore = create_vector_store(name=\"docs\")\nstore isa VectorStoreSuccess && println(\"Created: \", vector_store_id(store.response))\nvs = vector_store_id(store.response)\n\n# Add a single file, or a batch, then poll the batch to a terminal status\nadd_vector_store_file(vs, \"file-abc123\")\nbatch = create_file_batch(vs, [\"file-abc123\", \"file-def456\"])\nbatch isa VectorStoreBatchSuccess && poll_file_batch(vs, batch.response.id)\n\n# List, retrieve, delete\nstores = list_vector_stores(limit=10)\nretrieve_vector_store(vs)\ndelete_vector_store(vs)","title":"Usage"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreObject","page":"Vector Stores","text":"VectorStoreObject\n\nA vector store from the Vector Stores API: id, name, status, and file_counts; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.VectorStoreObject"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreFileObject","page":"Vector Stores","text":"VectorStoreFileObject\n\nA file attached to a vector store: id and status; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.VectorStoreFileObject"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreFileBatch","page":"Vector Stores","text":"VectorStoreFileBatch\n\nA batch of files added to a vector store: id, status, and file_counts; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.VectorStoreFileBatch"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreList","page":"Vector Stores","text":"VectorStoreList\n\nA page of VectorStoreObjects from list_vector_stores; has_more signals that further pages are available.\n\n\n\n\n\n","title":"UniLM.VectorStoreList"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreSuccess","page":"Vector Stores","text":"Successful create/retrieve result wrapping a VectorStoreObject.\n\n\n\n\n\n","title":"UniLM.VectorStoreSuccess"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreListSuccess","page":"Vector Stores","text":"Successful list_vector_stores result wrapping a VectorStoreList.\n\n\n\n\n\n","title":"UniLM.VectorStoreListSuccess"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreFileSuccess","page":"Vector Stores","text":"Successful add_vector_store_file result wrapping a VectorStoreFileObject.\n\n\n\n\n\n","title":"UniLM.VectorStoreFileSuccess"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreBatchSuccess","page":"Vector Stores","text":"Successful file-batch result wrapping a VectorStoreFileBatch.\n\n\n\n\n\n","title":"UniLM.VectorStoreBatchSuccess"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreDeleteSuccess","page":"Vector Stores","text":"Successful delete_vector_store result: the service confirmed (deleted is always true) the removal of id.\n\n\n\n\n\n","title":"UniLM.VectorStoreDeleteSuccess"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreFailure","page":"Vector Stores","text":"Vector Stores API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.VectorStoreFailure"},{"category":"type","location":"api/vector_stores/#UniLM.VectorStoreCallError","page":"Vector Stores","text":"Vector Stores API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout. last_observed is set only when poll_file_batch runs out of time or is cancelled: the last VectorStoreFileBatch it saw, if any.\n\n\n\n\n\n","title":"UniLM.VectorStoreCallError"},{"category":"function","location":"api/vector_stores/#UniLM.create_vector_store","page":"Vector Stores","text":"create_vector_store(; name=nothing, file_ids=nothing, expires_after=nothing,\n                    chunking_strategy=nothing, metadata=nothing, service=OPENAIServiceEndpoint)\n\nCreate a vector store. Returns VectorStoreSuccess, VectorStoreFailure, or VectorStoreCallError.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.create_vector_store"},{"category":"function","location":"api/vector_stores/#UniLM.retrieve_vector_store","page":"Vector Stores","text":"retrieve_vector_store(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.retrieve_vector_store"},{"category":"function","location":"api/vector_stores/#UniLM.list_vector_stores","page":"Vector Stores","text":"list_vector_stores(; limit=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_vector_stores"},{"category":"function","location":"api/vector_stores/#UniLM.delete_vector_store","page":"Vector Stores","text":"delete_vector_store(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.delete_vector_store"},{"category":"function","location":"api/vector_stores/#UniLM.add_vector_store_file","page":"Vector Stores","text":"add_vector_store_file(vector_store_id, file_id; chunking_strategy=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.add_vector_store_file"},{"category":"function","location":"api/vector_stores/#UniLM.create_file_batch","page":"Vector Stores","text":"create_file_batch(vector_store_id, file_ids; chunking_strategy=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.create_file_batch"},{"category":"function","location":"api/vector_stores/#UniLM.retrieve_file_batch","page":"Vector Stores","text":"retrieve_file_batch(vector_store_id, batch_id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.retrieve_file_batch"},{"category":"function","location":"api/vector_stores/#UniLM.poll_file_batch","page":"Vector Stores","text":"poll_file_batch(vector_store_id, batch_id; interval=2.0, timeout=300.0, service=OPENAIServiceEndpoint)\n\nPoll a file batch until it reaches a terminal status (completed/failed/cancelled) and return that VectorStoreBatchSuccess. timeout bounds the wall-clock time of the whole poll (Inf waits indefinitely); interval is the pause between GETs. Both must be positive, else ArgumentError.\n\nA transient failure — a status in the retryable set (408/429/500/502/503/504/529) or a GET that timed out — is polled through; any other failure is returned as it came. When the time runs out the result is a VectorStoreCallError with cause = UniLMTimeout(:deadline, …) and last_observed set to the last VectorStoreFileBatch seen (nothing if no GET succeeded).\n\nPass cancel::Union{Nothing,CancelToken} (default: the ambient with_cancel token) to make the poll cancellable: a cancel ends it at once, mid-GET or mid-pause, with a VectorStoreCallError whose cause is a UniLMCancelled and last_observed as above; a token cancelled before the poll sends nothing.\n\nPass config::Union{Nothing,RequestConfig} to bound each GET (a single attempt; its total_deadline is capped at the time left).\n\n\n\n\n\n","title":"UniLM.poll_file_batch"},{"category":"function","location":"api/vector_stores/#UniLM.vector_store_id","page":"Vector Stores","text":"vector_store_id(v::VectorStoreObject) -> String\n\nReturn the id of a VectorStoreObject, for use in file and batch calls.\n\n\n\n\n\n","title":"UniLM.vector_store_id"},{"category":"section","location":"api/capabilities/#capabilities_api","page":"Provider Capabilities","text":"Functions for querying and validating provider capabilities.\n\nEach service endpoint declares which API features it supports. Request functions validate capabilities before dispatching, giving clear errors instead of HTTP 404s.","title":"Provider Capabilities"},{"category":"section","location":"api/capabilities/#What-\"validate-before-dispatch\"-means","page":"Provider Capabilities","text":"Validation comes in two strengths, and which one applies depends on the verb:\n\nPlatform and lifecycle verbs — files, vector stores, conversations, moderations, audio, batch, fine-tuning, containers, uploads, realtime, FIM, prefix completion, System One — validate strictly. These surfaces are provider-specific, and a backend that has not declared them would simply 404, so an endpoint that declares no capabilities at all is not dispatched: the call throws MethodError: no method matching provider_capabilities(::YourEndpoint).\nThe four primary verbs — chatrequest! (:chat), embeddingrequest! (:embeddings), respond (:responses or :agentic, since the two agentic wires name the same surface differently), and generate_image (:images) — plus edit_image (:image_edits) validate only endpoints that declare their capabilities. An endpoint with no provider_capabilities method passes through unvalidated.\n\nThat asymmetry is deliberate. Defining a ServiceEndpoint subtype is the documented way to reach an OpenAI-compatible backend this package does not ship, and such a backend cannot declare anything — refusing to dispatch it would be a false negative about a server the package knows nothing about. \"Undeclared\" is therefore no applicable method, never an empty capability set. Declaring capabilities is opt-in strictness: once your endpoint declares, the four primary verbs hold it to that declaration like any built-in.\n\nA rejection of a declared endpoint throws ArgumentError before any request, naming the feature and the endpoint and listing what it does support (for example Files API is not supported by ANTHROPICServiceEndpoint. Supported: chat, json_output, streaming, tools).","title":"What \"validate before dispatch\" means"},{"category":"section","location":"api/capabilities/#Functions","page":"Provider Capabilities","text":"","title":"Functions"},{"category":"section","location":"api/capabilities/#Capability-Symbols","page":"Provider Capabilities","text":"Symbol Description\n:chat Chat Completions API\n:responses Responses API\n:agentic Agentic respond verb (OpenAI Responses / Gemini Interactions)\n:tools Tool / function calling\n:streaming Server-sent-event token streaming (stream=true)\n:json_output JSON / structured output (response_format)\n:embeddings Embeddings API\n:images Image generation\n:image_edits Image editing\n:fim FIM (fill-in-the-middle) completion\n:prefix_completion Prefix completion (continue a partial assistant message)\n:files Files API\n:vector_stores Vector Stores API\n:conversations Conversations API\n:moderation Moderations API\n:audio Audio (TTS / transcription / translation)\n:batch Batch API\n:fine_tuning Fine-tuning API\n:containers Containers API\n:uploads Resumable Uploads API\n:realtime Realtime API\n:system_one TypeSafe System One evaluation (ask)\n:models Model listing (list_models)","title":"Capability Symbols"},{"category":"section","location":"api/capabilities/#Capabilities-by-Provider","page":"Provider Capabilities","text":"using UniLM\n\nfor (name, svc) in [\n    (\"OpenAI\",                 OPENAIServiceEndpoint),\n    (\"Azure\",                  AZUREServiceEndpoint),\n    (\"Gemini (native)\",        GEMINIServiceEndpoint),\n    (\"Gemini (OpenAI-compat)\", GEMINIOpenAIServiceEndpoint),\n    (\"Anthropic\",              ANTHROPICServiceEndpoint),\n    (\"DeepSeek\",               DeepSeekEndpoint(\"k\")),\n    (\"Generic\",                GenericOpenAIEndpoint(\"http://x\", \"\")),\n    (\"TypeSafe (System One)\",  TYPESAFEServiceEndpoint)\n]\n    caps = join(sort(collect(provider_capabilities(svc))), \", \")\n    println(\"$name: $caps\")\nend\nOpenAI: agentic, audio, batch, chat, containers, conversations, embeddings, files, fine_tuning, image_edits, images, json_output, moderation, realtime, responses, streaming, tools, uploads, vector_stores\nAzure: chat, json_output, streaming, tools\nGemini (native): agentic, chat, json_output, streaming, tools\nGemini (OpenAI-compat): chat, embeddings, json_output, streaming, tools\nAnthropic: chat, json_output, streaming, tools\nDeepSeek: chat, fim, json_output, prefix_completion, streaming, tools\nGeneric: chat, embeddings, fim, json_output, responses, streaming, tools\nTypeSafe (System One): models, system_one","title":"Capabilities by Provider"},{"category":"function","location":"api/capabilities/#UniLM.provider_capabilities","page":"Provider Capabilities","text":"provider_capabilities(service) -> Set{Symbol}\n\nReturn the set of capabilities supported by the given service endpoint.\n\nStandard capability symbols include:\n\nCore: :chat, :responses, :agentic, :tools, :streaming, :json_output\nEmbeddings & images: :embeddings, :images, :image_edits\nCompletions: :fim, :prefix_completion\nPlatform APIs: :files, :vector_stores, :conversations, :moderation, :audio, :batch, :fine_tuning, :containers, :uploads, :realtime\nTypeSafe System One: :system_one, :models\n\n\n\n\n\n","title":"UniLM.provider_capabilities"},{"category":"function","location":"api/capabilities/#UniLM.has_capability","page":"Provider Capabilities","text":"has_capability(service, cap::Symbol) -> Bool\n\nCheck whether the service endpoint supports a given capability.\n\n\n\n\n\n","title":"UniLM.has_capability"},{"category":"section","location":"#UniLM.jl","page":"Home","text":"A unified Julia interface for large language models.\n\n(Image: CI) (Image: codecov) (Image: Aqua QA) (Image: ) (Image: Stable) (Image: Dev)","title":"UniLM.jl"},{"category":"section","location":"#What-is-UniLM.jl?","page":"Home","text":"UniLM.jl provides a Julian, type-safe interface to LLM providers with first-class native backends — OpenAI (Chat Completions + Responses), Anthropic (Messages), and Google Gemini (generateContent + agentic Interactions) — plus any OpenAI-compatible provider (Azure, DeepSeek, Mistral, Ollama, vLLM, LM Studio). It covers Chat Completions & Responses, a cross-provider agentic respond verb, Image Generation/Edits, Embeddings, Files/Vector Stores, Conversations, Audio, Batch, Moderations, Fine-tuning, Webhooks, Realtime, and MCP (client & server) — with built-in token/cost accounting. It also speaks TypeSafe's System One model Jev, which answers enumerated questions about a piece of state with a typed, calibrated judgment instead of generated text — and which can select a Julia method by natural-language meaning.","title":"What is UniLM.jl?"},{"category":"section","location":"#Key-Features","page":"Home","text":"🗣️ Chat Completions — stateful conversations with automatic history management\n🔮 Responses API & Agentic Verb — OpenAI's Responses API plus a cross-provider respond verb that also drives Google's Gemini Interactions\n🖼️ Image Generation & Edits — create and edit images with gpt-image-2\n🔧 Tool/Function Calling — first-class function tools in both APIs, with an automated tool_loop\n🔌 MCP (Model Context Protocol) — connect to MCP servers or build your own, with seamless tool-loop integration\n⚖️ System One (TypeSafe Jev) — typed judgments instead of generated text: ask answers choice / score / noul questions about a piece of state with a calibrated distribution, in one request\n🔀 Multiple Dispatch on Natural Language — nl\"...\" is an ordinary Julia type, so nl_dispatch lets a Jev answer select which method runs; @branch is the same single-request decision inline\n📊 Embeddings — text embedding generation\n💰 Cost & Token Accounting — per-call estimated_cost, per-Chat cumulative_cost, and a built-in multi-provider pricing table\n🌊 Streaming — real-time token streaming with do-block syntax\n📐 Structured Output — JSON Schema–constrained generation\n☁️ Multi-Backend — native OpenAI/Anthropic/Gemini plus Azure, DeepSeek, Ollama, Mistral, vLLM, LM Studio, and any OpenAI-compatible provider\n✅ Type Safety & Capability Introspection — invalid states are unrepresentable and unsupported requests fail fast via provider_capabilities; tested with JET.jl and Aqua.jl","title":"Key Features"},{"category":"section","location":"#Chat-Completions-vs-Responses-(OpenAI)","page":"Home","text":"For OpenAI, use either conversational API — Chat Completions (Chat; also the path for the native Anthropic/Gemini and OpenAI-compatible backends) or the newer Responses (respond; basis for the cross-provider agentic verb):\n\nFeature Chat Completions Responses API\nStateful conversations Chat + push! previous_response_id\nSystem prompt Message(Val(:system), ...) instructions kwarg\nTool calling Tool / ToolCall FunctionTool / function_tool\nWeb search web_search_options WebSearchTool\nFile search — FileSearchTool\nStreaming stream=true + callback do-block syntax\nStructured output ResponseFormat TextConfig / json_schema_format\nReasoning reasoning_effort Reasoning\nAutomated tool loop tool_loop! tool_loop\nMCP integration mcp_tools bridge MCPTool / mcp_tool","title":"Chat Completions vs Responses (OpenAI)"},{"category":"section","location":"#Installation","page":"Home","text":"UniLM requires Julia 1.13+ and is registered in Julia's General registry:\n\nusing Pkg\nPkg.add(\"UniLM\")\n\nOr in the Pkg REPL:\n\npkg> add UniLM\n\nFor the latest unreleased changes, install from GitHub instead:\n\nPkg.add(url=\"https://github.com/algunion/UniLM.jl\")","title":"Installation"},{"category":"section","location":"#Quick-Example","page":"Home","text":"Building requests — these construct objects locally without calling the API:\n\nusing UniLM\nusing JSON\n\n# Chat Completions request\nchat = Chat(model=\"gpt-5.2\")\npush!(chat, Message(Val(:system), \"You are a Julia expert.\"))\npush!(chat, Message(Val(:user), \"Explain multiple dispatch in one sentence.\"))\nprintln(\"Chat has \", length(chat), \" messages, model: \", chat.model)\nprintln(\"Request body preview:\")\nprintln(JSON.json(chat))\nChat has 2 messages, model: gpt-5.2\nRequest body preview:\n{\"messages\":[{\"content\":\"You are a Julia expert.\",\"role\":\"system\"},{\"content\":\"Explain multiple dispatch in one sentence.\",\"role\":\"user\"}],\"model\":\"gpt-5.2\"}\n\n# Responses API request\nr = Respond(input=\"What makes Julia special?\")\nprintln(\"Respond model: \", r.model)\nprintln(JSON.json(r))\nRespond model: gpt-5.6-sol\n{\"input\":\"What makes Julia special?\",\"model\":\"gpt-5.6-sol\"}\n\n# Image Generation request. `model` is a sentinel that resolves at serialization\n# time, so the field itself stays \"\" — read the body to see what gets sent.\nig = ImageGeneration(prompt=\"A watercolor Julia logo\", quality=\"high\")\nprintln(\"Image model field: \", repr(ig.model))\nprintln(JSON.json(ig))\nImage model field: \"\"\n{\"model\":\"gpt-image-2\",\"prompt\":\"A watercolor Julia logo\",\"quality\":\"high\"}\n\nWith a valid API key, actual API calls return structured results:\n\nResponses API (recommended for new code):\n\nresult = respond(\"Explain Julia's multiple dispatch in 2-3 sentences.\", model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nJulia’s multiple dispatch means that the method Julia runs is chosen based on the types of **all** arguments to a function, not just the first one or an object’s class. This lets you define behavior cleanly for different combinations of input types, making code more generic, flexible, and often easier to extend.\n\nChat Completions:\n\nchat = Chat(model=\"gpt-5.4-mini\")\npush!(chat, Message(Val(:system), \"You are a concise Julia programming tutor.\"))\npush!(chat, Message(Val(:user), \"What is multiple dispatch? Answer in 2-3 sentences.\"))\nresult = chatrequest!(chat)\nif result isa LLMSuccess\n    println(result.message.content)\nelse\n    println(\"Request failed — see result for details\")\nend\nMultiple dispatch is a programming style where a function’s behavior is chosen based on the types of more than one argument, not just one. In Julia, this lets you define many versions of the same function for different combinations of input types, and Julia picks the best match at runtime.\n\nImage Generation:\n\nresult = generate_image(\n    \"A watercolor painting of a friendly robot reading a Julia programming book\",\n    size=\"1024x1024\", quality=\"medium\"\n)\nprintln(\"Success: \", result isa ImageSuccess)\nif result isa ImageSuccess\n    save_image(image_data(result)[1], joinpath(@__DIR__, \"assets\", \"generated_robot.png\"))\n    println(\"Saved to assets/generated_robot.png\")\nelse\n    println(\"Image generation failed — see result for details\")\nend\n\n(Image: Generated robot reading Julia)","title":"Quick Example"},{"category":"section","location":"#Next-Steps","page":"Home","text":"Getting Started — setup and first requests\nChat Completions Guide — deep dive into Chat and chatrequest!\nResponses API Guide — the newer Responses API\nImage Generation Guide — create images from text prompts\nMCP Guide — connect to MCP servers or build your own\nTimeouts & Retries — RequestConfig, typed timeout failures, retry contracts\nConcurrency, Tasks and Cancellation — sharing, fan-out, streaming into a Channel, CancelToken\nAPI Reference — full type and function reference","title":"Next Steps"},{"category":"section","location":"#Platform-APIs","page":"Home","text":"Beyond chat and generation, UniLM wraps most of the OpenAI platform surface (OpenAI-only) — each has an API-reference page:\n\nStorage & retrieval — Files, Vector Stores, Conversations, Uploads, Containers\nGeneration & media — Audio, Images\nJobs & ops — Batch, Fine-tuning, Moderations, Webhooks, Realtime\nCross-cutting — Cost Tracking, Provider Capabilities, Retrieval & File Search","title":"Platform APIs"},{"category":"section","location":"api/containers/#containers_api","page":"Containers","text":"Create, retrieve, list, and delete code-interpreter containers, and add files to them. Containers provide sandboxed compute for the Responses code-interpreter tool and expire after idle. OpenAI only.","title":"Containers API"},{"category":"section","location":"api/containers/#Objects","page":"Containers","text":"add_container_file returns a ContainerSuccess whose response is the created ContainerFileObject (not exported); create and retrieve return a ContainerObject.","title":"Objects"},{"category":"section","location":"api/containers/#Result-Types","page":"Containers","text":"","title":"Result Types"},{"category":"section","location":"api/containers/#Request-Functions","page":"Containers","text":"","title":"Request Functions"},{"category":"section","location":"api/containers/#Usage","page":"Containers","text":"# Create a container (optionally seeding it with uploaded file ids)\nresult = create_container(name=\"my-sandbox\")\nresult isa ContainerSuccess && println(\"Created: \", result.response.id)\n\n# Retrieve, list, add a file, delete\ncontainer = retrieve_container(\"cntr-abc123\")\ncontainers = list_containers(limit=20)\nadd_container_file(\"cntr-abc123\", \"data.csv\")\ndelete_container(\"cntr-abc123\")","title":"Usage"},{"category":"type","location":"api/containers/#UniLM.ContainerObject","page":"Containers","text":"ContainerObject\n\nA code-interpreter container: id, status, and name; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.ContainerObject"},{"category":"type","location":"api/containers/#UniLM.ContainerList","page":"Containers","text":"ContainerList\n\nA page of container records (raw JSON dicts) from list_containers; has_more signals that further pages are available.\n\n\n\n\n\n","title":"UniLM.ContainerList"},{"category":"type","location":"api/containers/#UniLM.ContainerFileObject","page":"Containers","text":"ContainerFileObject\n\nA file inside a container, as add_container_file returns it: id, container_id, path (where the code interpreter sees the file), bytes, source (e.g. \"user\", \"assistant\") and created_at; raw holds the unparsed JSON response.\n\n\n\n\n\n","title":"UniLM.ContainerFileObject"},{"category":"type","location":"api/containers/#UniLM.ContainerSuccess","page":"Containers","text":"Successful Containers API result: a ContainerObject from create/retrieve, or the ContainerFileObject that add_container_file created.\n\n\n\n\n\n","title":"UniLM.ContainerSuccess"},{"category":"type","location":"api/containers/#UniLM.ContainerListSuccess","page":"Containers","text":"Successful list_containers result wrapping a ContainerList.\n\n\n\n\n\n","title":"UniLM.ContainerListSuccess"},{"category":"type","location":"api/containers/#UniLM.ContainerDeleteSuccess","page":"Containers","text":"Successful delete_container result: the service confirmed (deleted is always true) the removal of id.\n\n\n\n\n\n","title":"UniLM.ContainerDeleteSuccess"},{"category":"type","location":"api/containers/#UniLM.ContainerFailure","page":"Containers","text":"Containers API error result: HTTP status, the raw response body, and the request_id the service sent (x-request-id/request-id header), if any.\n\n\n\n\n\n","title":"UniLM.ContainerFailure"},{"category":"type","location":"api/containers/#UniLM.ContainerCallError","page":"Containers","text":"Containers API call that produced no usable reply (transport failure, timeout, or a 200 that could not be decoded); cause is the underlying exception — a UniLMTimeout for a timeout.\n\n\n\n\n\n","title":"UniLM.ContainerCallError"},{"category":"function","location":"api/containers/#UniLM.create_container","page":"Containers","text":"create_container(; name, file_ids=nothing, expires_after=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.create_container"},{"category":"function","location":"api/containers/#UniLM.retrieve_container","page":"Containers","text":"retrieve_container(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.retrieve_container"},{"category":"function","location":"api/containers/#UniLM.list_containers","page":"Containers","text":"list_containers(; limit=nothing, after=nothing, service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.list_containers"},{"category":"function","location":"api/containers/#UniLM.delete_container","page":"Containers","text":"delete_container(id; service=OPENAIServiceEndpoint)\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.delete_container"},{"category":"function","location":"api/containers/#UniLM.add_container_file","page":"Containers","text":"add_container_file(container_id, path; service=OPENAIServiceEndpoint)  (multipart upload)\n\nUpload the file at path into the container. A success is a ContainerSuccess whose response is the created ContainerFileObject.\n\nPass config::Union{Nothing,RequestConfig} to override the timeout budget for this call (a single bounded attempt; max_attempts does not apply).\n\n\n\n\n\n","title":"UniLM.add_container_file"},{"category":"section","location":"guide/responses_api/#responses_guide","page":"Responses API","text":"The Responses API is OpenAI's newer, more flexible alternative to Chat Completions. Key advantages include built-in tools (web search, file search), stateless multi-turn via previous_response_id, and reasoning support for O-series models.\n\ntip: Tip\nFor the cross-provider view — the same respond verb against Gemini Interactions — see the Agentic Workflows guide.","title":"Responses API"},{"category":"section","location":"guide/responses_api/#Basic-Usage","page":"Responses API","text":"The simplest call — just a string:\n\nresult = respond(\"Explain Julia's multiple dispatch in 2-3 sentences.\", model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nJulia’s multiple dispatch means that the function chosen depends on the types of **all** arguments, not just one “receiver” object. This lets you define multiple methods with the same name and have Julia automatically pick the most specific one at runtime, making code both flexible and efficient.\n\nif result isa ResponseSuccess\n    println(\"ID:     \", result.response.id)\n    println(\"Status: \", result.response.status)\n    println(\"Model:  \", result.response.model)\nelse\n    println(\"No response metadata available\")\nend\nID:     resp_0afa270a3482ff17006ab4dda71fcc87d1bcc7a4965523a770\nStatus: completed\nModel:  gpt-5.4-mini-2026-03-17","title":"Basic Usage"},{"category":"section","location":"guide/responses_api/#The-Respond-Type","page":"Responses API","text":"For full control, construct a Respond object:\n\nr = Respond(\n    model=\"gpt-5.2\",\n    input=\"Explain monads simply\",\n    instructions=\"You are a functional programming expert. Be concise.\",\n    temperature=0.5,\n    max_output_tokens=500,\n)\nprintln(\"Model: \", r.model)\nprintln(\"Request body:\")\nprintln(JSON.json(r))\nModel: gpt-5.2\nRequest body:\n{\"input\":\"Explain monads simply\",\"instructions\":\"You are a functional programming expert. Be concise.\",\"max_output_tokens\":500,\"model\":\"gpt-5.2\",\"temperature\":0.5}","title":"The Respond Type"},{"category":"section","location":"guide/responses_api/#Instructions-(System-Prompt)","page":"Responses API","text":"Unlike Chat Completions where you push a system Message, the Responses API uses the instructions parameter:\n\nresult = respond(\n    \"Translate to French: The quick brown fox jumps over the lazy dog.\",\n    instructions=\"You are a professional translator. Respond only with the translation.\",\n    model=\"gpt-5.4-mini\"\n)\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nLe rapide renard brun saute par-dessus le chien paresseux.","title":"Instructions (System Prompt)"},{"category":"section","location":"guide/responses_api/#Structured-Input","page":"Responses API","text":"For multimodal inputs, use InputMessage with content helpers:\n\n# Text-only structured input\nmsgs = [\n    InputMessage(role=\"system\", content=\"You analyze images.\"),\n    InputMessage(role=\"user\", content=[\n        input_text(\"What do you see in this image?\"),\n        input_image(\"https://example.com/photo.jpg\"),\n    ]),\n]\nr = Respond(input=msgs, model=\"gpt-5.2\")\nprintln(\"Input is structured: \", r.input isa Vector)\nprintln(\"Number of input messages: \", length(r.input))\nInput is structured: true\nNumber of input messages: 2","title":"Structured Input"},{"category":"section","location":"guide/responses_api/#Input-Helpers","page":"Responses API","text":"Function Purpose\ninput_text Text content part\ninput_image Image URL content part\ninput_file File (URL or ID) content part","title":"Input Helpers"},{"category":"section","location":"guide/responses_api/#Multi-Turn-Conversations","page":"Responses API","text":"Chain requests using previous_response_id — no need to re-send the full history:\n\nr1 = respond(\"Tell me a one-liner programming joke.\", instructions=\"Be concise.\", model=\"gpt-5.4-mini\")\nif r1 isa ResponseSuccess\n    println(output_text(r1))\nelse\n    println(\"Request failed — \", r1)\nend\nWhy do programmers prefer dark mode? Because light attracts bugs.\n\nif r1 isa ResponseSuccess\n    r2 = respond(\"Explain why that's funny, in one sentence.\", previous_response_id=r1.response.id, model=\"gpt-5.4-mini\")\n    if r2 isa ResponseSuccess\n        println(output_text(r2))\n    else\n        println(\"Request failed — \", r2)\n    end\nelse\n    println(\"Skipped — first request failed\")\nend\nIt’s funny because it plays on the double meaning of “bugs,” referring both to insects and to software errors.","title":"Multi-Turn Conversations"},{"category":"section","location":"guide/responses_api/#Built-in-Tools","page":"Responses API","text":"","title":"Built-in Tools"},{"category":"section","location":"guide/responses_api/#Web-Search","page":"Responses API","text":"result = respond(\n    \"What is the latest stable release of the Julia programming language?\",\n    tools=[web_search()],\n    model=\"gpt-5.4-mini\"\n)\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nThe latest stable release of Julia is **v1.13.0**, released on **September 9–10, 2026**. The official Julia downloads page lists **v1.13.0** as the **Current Stable Release**. ([julialang.org](https://www.julialang.org/downloads/manual-downloads/?utm_source=openai))\n\nIf you want, I can also tell you the latest **LTS** release or how to install the stable version with `juliaup`.","title":"Web Search"},{"category":"section","location":"guide/responses_api/#File-Search","page":"Responses API","text":"result = respond(\n    \"Find information about error handling\",\n    tools=[file_search([\"vs_abc123\"])]\n)","title":"File Search"},{"category":"section","location":"guide/responses_api/#Function-Tools","page":"Responses API","text":"weather_tool = function_tool(\n    \"get_weather\",\n    \"Get current weather for a location\",\n    parameters=Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"location\" => Dict(\"type\" => \"string\", \"description\" => \"City name\")\n        ),\n        \"required\" => [\"location\"]\n    )\n)\nprintln(\"Tool name: \", weather_tool.name)\nprintln(\"Tool JSON: \", JSON.json(JSON.lower(weather_tool)))\nTool name: get_weather\nTool JSON: {\"description\":\"Get current weather for a location\",\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"City name\",\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"},\"type\":\"function\"}\n\nresult = respond(\"What's the weather in Tokyo? Use celsius.\", tools=[weather_tool], model=\"gpt-5.4-mini\")\ncalls = function_calls(result)\nif !isempty(calls)\n    println(\"Function: \", calls[1][\"name\"])\n    println(\"Arguments: \", JSON.json(JSON.parse(calls[1][\"arguments\"]), 2))\nelse\n    println(\"No function calls — \", result isa ResponseSuccess ? output_text(result) : result)\nend\nFunction: get_weather\nArguments: {\n  \"location\": \"Tokyo\"\n}","title":"Function Tools"},{"category":"section","location":"guide/responses_api/#Reasoning-and-Prompt-Caching","page":"Responses API","text":"For reasoning models such as gpt-5.4-mini:\n\nr = Respond(\n    input=\"Prove that √2 is irrational\",\n    model=\"gpt-5.4-mini\",\n    reasoning=Reasoning(effort=\"high\", summary=\"detailed\")\n)\nprintln(\"Model: \", r.model)\nprintln(\"Reasoning effort: \", r.reasoning.effort)\nprintln(JSON.json(r))\nModel: gpt-5.4-mini\nReasoning effort: high\n{\"input\":\"Prove that √2 is irrational\",\"model\":\"gpt-5.4-mini\",\"reasoning\":{\"effort\":\"high\",\"summary\":\"detailed\"}}\n\nGPT-5.6 also supports persisted reasoning and execution modes. Configure them through typed options; supported effort levels depend on the model:\n\nr = Respond(input=\"Explain Julia multiple dispatch\", model=\"gpt-5.6-luna\",\n    reasoning=Reasoning(effort=\"low\", context=\"current_turn\", mode=\"standard\"),\n    prompt_cache_options=PromptCacheOptions(mode=\"implicit\", ttl=\"30m\"))\nprintln(JSON.json(r))\n{\"input\":\"Explain Julia multiple dispatch\",\"model\":\"gpt-5.6-luna\",\"prompt_cache_options\":{\"mode\":\"implicit\",\"ttl\":\"30m\"},\"reasoning\":{\"context\":\"current_turn\",\"effort\":\"low\",\"mode\":\"standard\"}}\n\nGPT-5.6 and later use prompt_cache_options instead of prompt_cache_retention. Explicit cache mode requires breakpoints in input content blocks; mark one with input_text(text; cache_breakpoint=true). PromptCacheOptions(prewarm=true) prepares the cache without generating output (the response has an empty output), and comparison_response_id requests cache diagnostics, returned in result.response.raw[\"prompt_cache_diagnostics\"]. On GPT-6 models, a configuration_update input item changes reasoning effort between responses without rewriting the cached prefix. See OpenAI's caching guide for breakpoint placement and cache-write billing, and the reasoning guide for persisted reasoning and pro mode.","title":"Reasoning and Prompt Caching"},{"category":"section","location":"guide/responses_api/#Moderation","page":"Responses API","text":"Set moderation to a ModerationConfig to have OpenAI run a moderation model over the request input and the generated output. The moderation model is required. The optional input_mode and output_mode pick the policy for each side, \"score\" or \"block\"; unset policy parts are omitted from the request. The result comes back in result.response.raw[\"moderation\"], with \"input\" and \"output\" moderation results. Chat takes the same field.\n\nr = Respond(input=\"Summarize this support ticket\", model=\"gpt-5.4-mini\",\n    moderation=ModerationConfig(model=\"omni-moderation-latest\", input_mode=\"block\", output_mode=\"score\"))\nprintln(JSON.json(r))\n{\"input\":\"Summarize this support ticket\",\"model\":\"gpt-5.4-mini\",\"moderation\":{\"model\":\"omni-moderation-latest\",\"policy\":{\"input\":{\"mode\":\"block\"},\"output\":{\"mode\":\"score\"}}}}","title":"Moderation"},{"category":"section","location":"guide/responses_api/#Structured-Output","page":"Responses API","text":"Force JSON-conformant output:\n\n# JSON Schema format\nfmt = json_schema_format(\n    \"colors\",\n    \"A list of colors\",\n    Dict(\n        \"type\" => \"object\",\n        \"properties\" => Dict(\n            \"colors\" => Dict(\n                \"type\" => \"array\",\n                \"items\" => Dict(\"type\" => \"string\")\n            )\n        ),\n        \"required\" => [\"colors\"],\n        \"additionalProperties\" => false\n    ),\n    strict=true\n)\nprintln(\"Format type: \", fmt.format.type)\nprintln(\"Schema name: \", fmt.format.name)\nFormat type: json_schema\nSchema name: colors\n\nresult = respond(\"List 5 popular colors\", text=fmt, model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(JSON.json(JSON.parse(output_text(result)), 2))\nelse\n    println(\"Request failed — \", result)\nend\n{\n  \"colors\": [\n    \"Blue\",\n    \"Red\",\n    \"Green\",\n    \"Black\",\n    \"White\"\n  ]\n}","title":"Structured Output"},{"category":"section","location":"guide/responses_api/#Response-Accessors","page":"Responses API","text":"result = respond(\"Hello!\")\n\nif result isa ResponseSuccess\n    r = result.response\n\n    output_text(result)      # full text output (throws LLMResultError on a failed result)\n    function_calls(result)   # Vector of function call Dicts (empty if none, or on a failure)\n\n    r.id                     # \"resp_00e791c8...\"\n    r.status                 # \"completed\"\n    r.model                  # \"gpt-5.2-2025-12-11\"\n    r.output                 # full output array\n    r.usage                  # Dict with token counts\nend","title":"Response Accessors"},{"category":"section","location":"guide/responses_api/#Managing-Stored-Responses","page":"Responses API","text":"When you pass store=true, the response is saved on OpenAI's servers and can be retrieved, inspected, or deleted later:\n\nr = respond(\"Say 'stored response test' and nothing else.\", store=true, model=\"gpt-5.4-mini\")\nif r isa ResponseSuccess\n    rid = r.response.id\n    println(\"Stored response ID: \", rid)\n\n    # Retrieve\n    retrieved = get_response(rid)\n    if retrieved isa ResponseSuccess\n        println(\"Retrieved text: \", output_text(retrieved))\n    end\n\n    # List input items\n    items = list_input_items(rid)\n    if items isa Dict\n        println(\"Input items: \", length(items[\"data\"]))\n    end\n\n    # Delete\n    del = delete_response(rid)\n    if del isa Dict\n        println(\"Deleted: \", del[\"deleted\"])\n    end\nelse\n    println(\"Request failed — \", r)\nend\nStored response ID: resp_00ea7256c2d73cf3006ab4ddb0e89087d180d8119586b7403f\nRetrieved text: stored response test\nInput items: 1\nDeleted: true","title":"Managing Stored Responses"},{"category":"section","location":"guide/responses_api/#Metadata","page":"Responses API","text":"Attach arbitrary key-value metadata to any request for tracking, filtering, or debugging:\n\nresult = respond(\n    \"Say 'metadata test' and nothing else.\",\n    metadata=Dict(\"env\" => \"docs\", \"request_id\" => \"demo_123\"),\n    model=\"gpt-5.4-mini\"\n)\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\nmetadata test","title":"Metadata"},{"category":"section","location":"guide/responses_api/#Service-Tier","page":"Responses API","text":"Control the processing tier for your request (\"auto\", \"default\", \"flex\", \"scale\", \"priority\"/\"fast\", \"ultrafast\"):\n\nresult = respond(\"Say 'tier test' and nothing else.\", service_tier=\"auto\", model=\"gpt-5.4-mini\")\nif result isa ResponseSuccess\n    println(output_text(result))\nelse\n    println(\"Request failed — \", result)\nend\ntier test","title":"Service Tier"},{"category":"section","location":"guide/responses_api/#Counting-Input-Tokens","page":"Responses API","text":"Estimate token usage before making a full request — useful for cost estimation or verifying that input fits within the context window:\n\nresult = count_input_tokens(input=\"Tell me a joke about programming\")\nif result isa Dict\n    println(\"Input tokens: \", result[\"input_tokens\"])\nelse\n    println(\"Request failed — see result for details\")\nend\nInput tokens: 12\n\nWith tools and instructions:\n\ntool = function_tool(\"search\", \"Search for information\",\n    parameters=Dict(\"type\" => \"object\", \"properties\" => Dict(\n        \"query\" => Dict(\"type\" => \"string\")\n    ), \"required\" => [\"query\"], \"additionalProperties\" => false),\n    strict=true\n)\nresult = count_input_tokens(\n    input=\"Search for Julia language news\",\n    instructions=\"You are a helpful assistant.\",\n    tools=[tool]\n)\nif result isa Dict\n    println(\"Tokens with tools: \", result[\"input_tokens\"])\nelse\n    println(\"Request failed — see result for details\")\nend\nTokens with tools: 54","title":"Counting Input Tokens"},{"category":"section","location":"guide/responses_api/#Compacting-Conversations","page":"Responses API","text":"For long conversations, compact_response compresses the history into opaque, encrypted items that reduce token usage while preserving context:\n\nitems = [\n    Dict(\"role\" => \"user\", \"content\" => \"Hello, I want to learn about Julia.\"),\n    Dict(\"type\" => \"message\", \"role\" => \"assistant\", \"status\" => \"completed\",\n         \"content\" => [Dict(\"type\" => \"output_text\",\n            \"text\" => \"Julia is a high-performance programming language for technical computing.\")])\n]\nresult = compact_response(input=items, model=\"gpt-5.4-mini\")\nif result isa Dict\n    println(\"Compact succeeded\")\n    println(\"Output items: \", length(result[\"output\"]))\n    println(\"Usage: \", result[\"usage\"])\nelse\n    println(\"Request failed — see result for details\")\nend\nCompact succeeded\nOutput items: 3\nUsage: Dict{String, Any}(\"input_tokens_details\" => Dict{String, Any}(\"cache_write_tokens\" => 0, \"cached_tokens\" => 0), \"input_tokens\" => 50, \"total_tokens\" => 162, \"output_tokens_details\" => Dict{String, Any}(\"reasoning_tokens\" => 0), \"output_tokens\" => 112)","title":"Compacting Conversations"},{"category":"section","location":"guide/responses_api/#Cancelling-Responses","page":"Responses API","text":"Cancel an in-progress (background) response:\n\n# Start a background response\nresult = respond(\"Write a very long essay about Julia\", background=true)\n\n# Cancel it\nif result isa ResponseSuccess\n    cancel_result = cancel_response(result.response.id)\n    if cancel_result isa ResponseSuccess\n        println(\"Cancelled: \", cancel_result.response.status)\n    end\nend","title":"Cancelling Responses"},{"category":"section","location":"guide/responses_api/#Parameters-Reference","page":"Responses API","text":"Parameter Type Default Description\nmodel String \"gpt-5.6-sol\" Model to use\ninput Union{String, Vector} (required) String or Vector{InputMessage}\ninstructions String — System-level instructions\ntools Vector — Available tools (function, web search, file search)\ntool_choice String — \"auto\", \"none\", \"required\"\nparallel_tool_calls Bool — Allow parallel tool calls\ntemperature Float64 — 0.0–2.0 (mutually exclusive with top_p)\ntop_p Float64 — 0.0–1.0 (mutually exclusive with temperature)\nmax_output_tokens Int64 — Maximum tokens in the response\nstream Bool — Enable streaming\ntext TextConfig — Output format (text, jsonobject, jsonschema)\nreasoning Reasoning — Effort, summary, persisted context, and execution mode\ntruncation String — \"auto\" or \"disabled\"\nstore Bool — Store response for later retrieval\nmetadata Dict — Arbitrary key-value metadata\nprevious_response_id String — Chain to a previous response for multi-turn\nuser String — End-user identifier\nbackground Bool — Run in background (cancellable)\ninclude Vector{String} — Extra data to include (e.g. \"file_search_call.results\")\nmax_tool_calls Int64 — Max number of tool calls per turn\nservice_tier String — \"auto\", \"default\", \"flex\", \"scale\", \"fast\" (\"priority\" alias), \"ultrafast\"\ntop_logprobs Int64 — 0–20, top log probabilities\nprompt Dict — Prompt template reference; the reusable prompts API (v1/prompts) shuts down on November 30, 2026\nprompt_cache_key String — Cache key for prompt caching\nprompt_cache_options PromptCacheOptions — Cache mode, TTL, prewarm, and diagnostics for GPT-5.6 and later\nprompt_cache_retention String — \"in_memory\" or \"24h\", depending on the older model\nsafety_identifier String — Stable end-user identifier; replaces the deprecated user\nconversation Any — Conversation context (String or Dict)\ncontext_management Vector — Context management strategies\nstream_options Dict — Streaming options (e.g. include_usage)\nmoderation ModerationConfig — Moderation model and input/output policy","title":"Parameters Reference"},{"category":"section","location":"guide/responses_api/#Retry-Behaviour","page":"Responses API","text":"respond automatically retries transient HTTP statuses (408, 429, 500, 502, 503, 504, 529) with full-jitter exponential backoff; a Retry-After header is a floor under the jittered wait. Attempts and total time are bounded by the resolved RequestConfig (max_attempts, default 3; total_deadline, default 900 s), and a retry whose wait would exceed the remaining deadline is not attempted — the call fails immediately with the last real response rather than sleeping past it. Pass config=RequestConfig(max_attempts=1) to disable retries for a call, or set scoped/process-wide defaults with with_request_config / set_default_config!. Timeouts surface as ResponseCallError with status = nothing and the UniLMTimeout (phase, elapsed, limit) in .cause; every other exception that ends the call is in .cause too — a UniLMCancelled for a call cancelled through its cancel token (see Concurrency, Tasks and Cancellation). Local validation — an option the provider or model cannot express, or a service that supports neither :responses nor :agentic — throws ArgumentError before any request.\n\nThis applies to respond only. The Responses lifecycle operations — get_response, delete_response, cancel_response, list_input_items, compact_response, count_input_tokens — make a single bounded attempt; max_attempts has no effect on them. See Timeouts & Retries.","title":"Retry Behaviour"},{"category":"section","location":"guide/responses_api/#Parameter-Validation","page":"Responses API","text":"The Respond constructor validates parameter ranges at construction time:\n\nParameter Valid Range\ntemperature 0.0–2.0\ntop_p 0.0–1.0\nmax_output_tokens ≥ 1\ntop_logprobs 0–20\nreasoning effort \"none\", \"minimal\", \"low\", \"medium\", \"high\", \"xhigh\", \"max\" (checked by Reasoning)\n\nOut-of-range values throw ArgumentError. Additionally, temperature and top_p are mutually exclusive, conversation cannot be combined with previous_response_id (each already supplies the prior turns), background=true requires a stored response (store=false throws), and prompt_cache_retention cannot be combined with prompt_cache_options. A Chat Tool in tools is converted to the equivalent FunctionTool.","title":"Parameter Validation"},{"category":"section","location":"guide/responses_api/#See-Also","page":"Responses API","text":"Respond — full type reference\nResponseObject — response structure\nTool Calling — detailed tool calling guide\nStreaming — streaming with do-blocks","title":"See Also"},{"category":"section","location":"api/completions/#fim_api","page":"FIM Types","text":"Types and functions for FIM (Fill-in-the-Middle) Completion and Chat Prefix Completion.","title":"FIM Types"},{"category":"section","location":"api/completions/#Request-Type","page":"FIM Types","text":"","title":"Request Type"},{"category":"section","location":"api/completions/#Response-Types","page":"FIM Types","text":"","title":"Response Types"},{"category":"section","location":"api/completions/#Result-Types","page":"FIM Types","text":"","title":"Result Types"},{"category":"section","location":"api/completions/#Request-Functions","page":"FIM Types","text":"","title":"Request Functions"},{"category":"section","location":"api/completions/#Example","page":"FIM Types","text":"using UniLM\nusing JSON\n\nfim = FIMCompletion(\n    service=DeepSeekEndpoint(\"demo\"),\n    prompt=\"def add(a, b):\",\n    suffix=\"    return result\",\n    max_tokens=64\n)\nprintln(\"JSON body:\")\nprintln(JSON.json(fim, 2))\nJSON body:\n{\n  \"max_tokens\": 64,\n  \"model\": \"deepseek-flash\",\n  \"prompt\": \"def add(a, b):\",\n  \"suffix\": \"    return result\"\n}","title":"Example"},{"category":"type","location":"api/completions/#UniLM.FIMCompletion","page":"FIM Types","text":"FIMCompletion(; service, model, prompt, suffix=nothing, max_tokens=128, ...)\n\nA Fill-in-the-Middle completion request. The model generates text between prompt (prefix) and suffix.\n\nSupported by DeepSeekEndpoint (beta), Mistral and Ollama (vLLM's completions endpoint does not accept suffix). stream=true throws an ArgumentError: fim_complete has no streaming path.\n\nExample\n\nfim = FIMCompletion(service=DeepSeekEndpoint(), prompt=\"def fib(a):\",\n    suffix=\"    return fib(a-1) + fib(a-2)\", max_tokens=128)\nresult = fim_complete(fim)\nprintln(fim_text(result))\n\n\n\n\n\n","title":"UniLM.FIMCompletion"},{"category":"type","location":"api/completions/#UniLM.FIMChoice","page":"FIM Types","text":"FIMChoice\n\nA single completion choice from a FIM response.\n\nFields\n\ntext::String: The generated text\nindex::Int: Choice index (default 0)\nfinish_reason::Union{String,Nothing}: Why generation stopped (e.g. \"stop\", \"length\")\n\n\n\n\n\n","title":"UniLM.FIMChoice"},{"category":"type","location":"api/completions/#UniLM.FIMResponse","page":"FIM Types","text":"FIMResponse\n\nParsed FIM completion response containing choices, usage, and raw data.\n\nFields\n\nchoices::Vector{FIMChoice}: Generated completions\nusage::Union{TokenUsage,Nothing}: Token usage statistics\nmodel::String: Model that generated the response\nraw::Dict{String,Any}: Complete raw JSON response\n\n\n\n\n\n","title":"UniLM.FIMResponse"},{"category":"type","location":"api/completions/#UniLM.FIMSuccess","page":"FIM Types","text":"Successful FIM completion result.\n\n\n\n\n\n","title":"UniLM.FIMSuccess"},{"category":"type","location":"api/completions/#UniLM.FIMFailure","page":"FIM Types","text":"HTTP-level failure from FIM completion.\n\n\n\n\n\n","title":"UniLM.FIMFailure"},{"category":"type","location":"api/completions/#UniLM.FIMCallError","page":"FIM Types","text":"Exception-level error during FIM completion.\n\n\n\n\n\n","title":"UniLM.FIMCallError"},{"category":"function","location":"api/completions/#UniLM.fim_complete","page":"FIM Types","text":"fim_complete(fim::FIMCompletion; config=nothing, cancel=nothing) -> LLMRequestResponse\n\nExecute a FIM (Fill-in-the-Middle) completion request. Returns FIMSuccess, FIMFailure, or FIMCallError.\n\nPer-call config::RequestConfig overrides timeouts and the retry budget; a silent peer fails with a typed timeout inside the FIMCallError result (cause::UniLMTimeout). Local validation (capability, model resolution, routing) throws an ArgumentError before any request; every later failure — transport, a 200 body that is not a completions response — is a FIMCallError whose cause holds the exception.\n\ncancel::Union{Nothing,CancelToken} (default: the ambient with_cancel token) makes the call cancellable: once the token is cancelled it returns a FIMCallError whose cause is a UniLMCancelled — without sending anything when the token was already cancelled, at once when mid-request (never retried).\n\n\n\n\n\nfim_complete(prompt::String; suffix=nothing, kwargs...) -> LLMRequestResponse\n\nConvenience form: creates a FIMCompletion and executes it. config and cancel go to the request; every other keyword to the constructor.\n\n\n\n\n\n","title":"UniLM.fim_complete"},{"category":"function","location":"api/completions/#UniLM.fim_text","page":"FIM Types","text":"fim_text(result) -> String\n\nExtract the generated text from a FIM completion result. On a FIMFailure or FIMCallError it throws an LLMResultError, as text does on a failed Chat result: a failed call has no text, and \"\" would read as an empty completion. Guard with issuccess.\n\n\n\n\n\n","title":"UniLM.fim_text"},{"category":"function","location":"api/completions/#UniLM.prefix_complete","page":"FIM Types","text":"prefix_complete(chat::Chat; config=nothing, cancel=nothing) -> LLMRequestResponse\n\nChat prefix completion: the model continues from a partial assistant message. The last message in chat must be role=assistant containing the text prefix to continue from. The result's message is the continuation the API returns; with chat.history the conversation keeps the whole assistant turn, prefix followed by continuation.\n\nSupported by DeepSeekEndpoint (beta).\n\nPer-call config::RequestConfig overrides timeouts and the retry budget; a silent peer fails with a typed timeout inside the LLMCallError result (cause::UniLMTimeout). Local validation throws an ArgumentError before any request; every later failure is an LLMCallError whose cause holds the exception. cancel::Union{Nothing,CancelToken} makes the call cancellable as for fim_complete: a cancelled call returns an LLMCallError whose cause is a UniLMCancelled, and chat is left untouched.\n\nExample\n\nchat = Chat(service=DeepSeekEndpoint(), model=\"deepseek-flash\")\npush!(chat, Message(Val(:system), \"You are a coding assistant.\"))\npush!(chat, Message(Val(:user), \"Write a quicksort in Python\"))\npush!(chat, Message(role=RoleAssistant, content=\"```python\\n\"))\nresult = prefix_complete(chat)\n\n\n\n\n\n","title":"UniLM.prefix_complete"}]
}
